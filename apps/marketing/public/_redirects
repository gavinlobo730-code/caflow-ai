# The homepage used to live here as a standalone static document
# (public/practicesync-homepage.html) and this file rewrote `/` onto it,
# because it was plain HTML rather than a React page and therefore could not
# be app/(site)/page.tsx.
#
# It is a React page now — see app/(site)/page.tsx and components/home/ — so
# Next.js emits out/index.html for `/` in the ordinary way and there is
# nothing left to rewrite. The old path is kept as a permanent redirect for
# anything that linked to the document directly.
/practicesync-homepage /  301
/practicesync-homepage.html /  301

# `/story` was a page from 16 to 18 September 2026. It was made by copying the
# homepage's own "Our Story" panel and the copy was never re-written, so both
# carried the same heading and the same callout; the owner's instruction was to
# keep one — "our story must contain the homepage only not the existing our
# story page delete that page". The nav item now points at `/` — the hero — on
# their follow-up: "when we click the our story it its starting from below the
# heropage it should gp tp the hero right directly?" So this lands there too,
# rather than on the panel, or the redirect and the nav would disagree about
# where Our Story is. Kept permanently because the page was live and linkable.
/story / 301
/story/ / 301
