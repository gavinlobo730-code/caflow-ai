# The homepage used to live here as a standalone static document
# (public/practicesync-homepage.html) and this file rewrote `/` onto it,
# because it was plain HTML rather than a React page and therefore could not
# be app/(site)/page.tsx.
#
# It is a React page now — see app/(site)/page.tsx and components/home/ — so
# Next.js emits out/index.html for `/` in the ordinary way and there is
# nothing left to rewrite. The old path is kept as a permanent redirect for
# anything that linked to the document directly.
/practicesync-homepage /  301
/practicesync-homepage.html /  301
