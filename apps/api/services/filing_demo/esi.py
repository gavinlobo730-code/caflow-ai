"""Placeholder — this flow is being built. See gstr1.py for the exemplar and
common.py for the rules every flow obeys."""
from __future__ import annotations


def build(db, firm_id: str, client_id: str, ref: dict) -> dict:
    raise ValueError(
        "This filing's walk-through is not built yet. The GSTR-1 demo shows "
        "the pattern; this module is next.")
