Probed all sixteen against HEAD (`99ac94b5`, 38 commits past the rescore commit `46bd46c`). Three have moved since the rescore; the rest are byte-identical.

---

**ACC-13 — VERDICT: open**
EVIDENCE: `/home/user/caflow-ai/apps/web/app/clients/[id]/accounting/page.tsx:660-661` — `const visibleEntries = useMemo(` / `() => entries.filter((e) => e.source_type === "manual"),`. `TABS` at :43-53 has no Day Book. Sole grep hit repo-wide is the unrelated comment at `/home/user/caflow-ai/apps/api/domain/banking/splits.py:7`.
PREMISE: sound, and cheaper than the finding thinks. `loadEntries` (page.tsx:664-698) already fetches **every** entry in the FY range via keyset PostgREST with `lines:journal_lines(...)` embedded, then discards the non-manual ones in the browser. The day book's data is already on the client; only the filter and a screen are missing. The cost the finding implies (a new endpoint, a paging helper) is already paid. Cost centres and bill-wise are structural and genuinely absent.
SHAPE: Day book = one new tab in `TABS` + a `DataTable` over unfiltered `entries` + `entry_type` as the voucher-type column. No backend, no migration. Caveat: the manual filter is currently what keeps the rendered row count small on a 12,836-entry client — removing it renders every voucher, so the screen needs its own date window rather than the FY default. Cost centres = a dimension column on `journal_lines` (migration, and see ACC-16's trigger problem below) + every one of the 26 posting paths. Bill-wise = a reference table keyed to documents.
SIZE: day book alone — hours. Cost centres / bill-wise — multi-day each, separately.

---

**ACC-14 — VERDICT: open**
EVIDENCE: `/home/user/caflow-ai/apps/api/services/opening_balance_service.py:155` — `AR, AP, BANK = "ar", "ap", "bank"`; `:228` `ar_total = _sum_opening(customers)`. `/home/user/caflow-ai/apps/api/services/trial_balance_import_service.py:67` `TB_SOURCE = "TrialBalance"`, and its docstring at :16-22 still says the two services "must not share a journal family".
PREMISE: sound, including the double-count exposure — the separation is documented as deliberate and there is still no cross-check.
SHAPE: The suggested fix has an invariant problem the finding does not name. "Have the master `opening_balance_paise` fields feed party-level opening DOCUMENTS" means opening invoices/bills, which are posted by the sales/purchase paths, not by `_plan_opening`. But `_plan_opening` is a **delta reconciler**: `_current_opening_net` (:209-211) reads the current opening-family net and `_leg_accounts` (:159+) reverses anything in the family the masters no longer justify. Opening documents posted outside the family would be invisible to it, so the AR leg would keep reconciling to a total that is now also represented by documents — booking the receivable twice. Making the TB import the single path means the AR/AP/bank legs stop posting at all, which means `_leg_accounts`, `_current_opening_net` and the whole reversal logic come out, plus the Opening Balances screen. Touches: `opening_balance_service.py` (substantially), `trial_balance_import_service.py`, the customer/vendor/bank master screens, a new reconciliation screen. Migration: none strictly required for the plumbing; a party-level opening document needs `customers.opening_balance_paise` to become a set of documents, which does.
SIZE: multi-day.

---

**ACC-16 — VERDICT: open**
EVIDENCE: `journal_lines` columns in `/home/user/caflow-ai/apps/api/tests/fixtures/production_schema_2026-09-03.json` are account_id, base_currency, created_at, credit_paise, debit_paise, exchange_rate, id, journal_entry_id, narration, rate_date, rate_source, rate_type, txn_credit, txn_currency, txn_debit — no line_no. `/home/user/caflow-ai/apps/api/services/manual_journal_service.py:190` still reads `"lines:journal_lines(id, account_id, debit_paise, credit_paise, narration)")` with no `.order()`. `/home/user/caflow-ai/apps/api/migrations/266_*.sql:414` — `DELETE FROM public.journal_lines WHERE journal_entry_id = p_entry_id;`
PREMISE: sound on the defect. **The suggested fix's backfill is not executable as written.** The finding says "backfill existing rows by created_at then id". Two problems, one already noted by the verifier (`created_at` is `now()` and identical across an entry's lines) and one nobody has: `/home/user/caflow-ai/apps/api/migrations/251_restore_posted_journal_line_immutability.sql:105-109` puts `trg_prevent_posted_journal_line_update` `BEFORE UPDATE OR DELETE` on `journal_lines`, raising on any row whose parent `is_posted IS TRUE`. **A backfill UPDATE is refused for every posted line in the database.** Migration 251's own header states the only legitimate route: "a migration that deliberately disables it — a rare, reviewable act", and adds that such a migration must also repair `account_period_balances`. (It need not here — `line_no` touches no amount — but the disable/re-enable is unavoidable.)
SHAPE: migration adding `line_no INT` with a default (catalog-only in PG11+, no trigger fires), then `ALTER TABLE ... DISABLE TRIGGER trg_prevent_posted_journal_line_update` around a `ctid`- or `id`-ordered backfill, then re-enable; `NOT NULL` only after. Then `phase2_journal_service._create_journal` (line insert ~:1912+) and `edit_posted_journal` in migration 266 must both stamp it from the array index, and every read path must `.order("line_no")` — `manual_journal_service.py:190` and the frontend's own embed at `apps/web/app/clients/[id]/accounting/page.tsx:680`, which is a direct PostgREST read and therefore not covered by any backend change.
SIZE: day (the trigger dance and the two write paths are what make it not hours).

---

**ACC-19 — VERDICT: open**
EVIDENCE: `/home/user/caflow-ai/apps/api/domain/currency/policy.py:49` — `l2 = bool(firm.get("multi_currency_entitled")) if firm else False`. Kernel refusal now at `/home/user/caflow-ai/apps/api/services/phase2_journal_service.py:1852-1856`.
PREMISE: sound. **Your count confirmed and refined.** Non-test reads: 20 occurrences across 9 files — `phase2_journal_service.py:1703-1704`, `receipt_service.py:92-93`, `purchase_payment_service.py:402-403`, `routers/banking.py:184-186`, `routers/currencies.py:63,69`, `routers/purchase_bills.py:607-608`, `routers/sales_invoices.py:718-719`, `routers/purchase_payments.py:619-620`, `domain/currency/policy.py:49-50`. Every one is inside a `.select(...)` or the resolver. Zero `.update()`, zero `.insert()`, zero PATCH. Writes exist only as `db.seed(...)` in tests (`test_multi_currency_phase5.py`, `test_r227_*`, `test_multi_invoice_bank_allocation.py`, `test_r2_12_python_integration.py`). `grep -rn 'multi_currency\|functional_currency' apps/web` → **0 hits**.

What a Partner-gated write path has to touch:

1. **No firm-settings API router exists.** `apps/api/routers/` has `branding.py` (prefix `/api/settings`), `firm_hsn_library.py`, `firm_hsn_rate_history.py` — nothing that writes `firms`. The firm settings screen writes `firms` **directly via PostgREST** (`/home/user/caflow-ai/apps/web/app/settings/page.tsx:345-357`). That path is already Partner-gated, but by **RLS**, not rbac: migration `260_role_aware_write_policies.sql:136` `['firms', 'Partner', 'Partner']`. So the cheapest correct firm-side toggle is a checkbox on that existing direct write, and it is genuinely Partner-only today.
2. **The client flag cannot ride the same trick.** Migration 260:119 gives `clients` `['clients', 'Manager', 'Partner']` — a direct write of `multi_currency_enabled` would be **Manager**-writable, not Partner. RLS is row-level, not column-level, so it cannot be narrowed for one column. The client flag must go through FastAPI with `rbac("firm", "write")` (`core/permissions.py:55`, `_PARTNER_ONLY`) plus `assert_client_access`. It must **not** be added to `ClientUpdate` (`models/client.py:98+`) — `routers/clients.py:248` guards that with `rbac("client","write")` = Manager+, and `client_repo.update` (`repositories/client_repository.py:132`) passes the dict straight through, so adding the field there silently hands the entitlement to Managers.
3. **`routers/currencies.py` is declared read-only in its own module docstring** (:1-8, "Additive and read-only … nothing here can change posting behaviour"). Adding a PATCH there means rewriting that docstring or putting the write elsewhere.
4. **`functional_currency` must not be exposed, or must refuse non-INR.** `policy.py:56` computes `capability_a = functional == BASE_CURRENCY` and `active = l1 and l2 and l3 and capability_a`. A CA who sets a client's functional currency to USD gets `active=False` — and the kernel then refuses every foreign posting with "the feature is dormant" (`phase2_journal_service.py:1854`), which is the wrong explanation. Capability B is not built.
5. **L1 is invisible.** `render.yaml:205-206` declares `MULTI_CURRENCY_ENABLED` with `sync: false` — its value lives in the Render dashboard. A UI toggle that flips L2 and L3 while L1 is off produces two "on" switches and a feature that still does nothing. The screen needs to read `GET /api/currencies/policy` back and say which gate is holding.
6. Also: `tests/test_direct_write_tables_are_role_guarded.py` and `tests/test_frontend_columns_exist_pg.py` both need the new column in their select lists if the frontend reads it directly.
MIGRATION: **none.** Both columns exist since `146_multi_currency_phase1_foundation.sql:122,129`, `NOT NULL DEFAULT FALSE`.
SIZE: day (hours for the backend if you skip the L1 visibility work, which is the part that makes it actually usable).

---

**ACC-23 — VERDICT: open**
EVIDENCE: `/home/user/caflow-ai/apps/api/services/year_end_workflow_service.py:24` `def lock_year_if_completing(` ends at `set_client_lock(db, firm_id, client_id, financial_year, lock=True, ...)` (:62-67) and posts nothing. `/home/user/caflow-ai/apps/api/domain/reporting/builders.py:636` still `"account_id": "__retained__",`. `closing_entry_dates` computed at `services/year_end_financial_service.py:786`, referenced in the web app only at `apps/web/lib/api/yearEnd.ts:149` (a type declaration) — rendered nowhere.
PREMISE: **FALSE in its load-bearing limb.** The suggested fix says "make the Balance Sheet stop synthesising the line once a closing entry exists for the year — otherwise it would double count." It would not. `year_end_financial_service.py:634-645` explains and `tests/test_year_end_financial_service.py:614-647` (`test_a_manually_posted_closing_entry_is_not_double_counted`) asserts that deriving is exactly self-correcting: the close reduces cumulative PAT by P at the moment it credits equity by P. Implementing the "stop synthesising" half of the fix is what would break the balance sheet. Two more limbs are also false: Schedule III's reserves split already exists (`year_end_financial_service.py:761-762`, `:805-806`), and appropriations/partner-capital allocation are postable today as manual journals through the kernel.
SHAPE: what is actually left is one line of UI — render `closing_entry_dates` (already computed, already typed) so a CA who posts their own close sees why that year's P&L reads nil. Touches `apps/web/app/clients/[id]/accounting/page.tsx` or the year-end statements component only. No migration, no backend, and **do not touch `builders.py`**.
SIZE: hours for the real residue. The finding as written (a posting closing-entry engine) is multi-day and would introduce the double-count it claims to prevent.

---

**ACC-24 — VERDICT: partial** (moved since the rescore, commit `6330e19b`, #496)
EVIDENCE: The screen now writes. `/home/user/caflow-ai/apps/web/app/accounting/account-groups/page.tsx:77-78` — `parent_group: form.parent_group.trim(),` / `sub_group: form.sub_group.trim(),`, sent through `api.accounting.updateAccount` / `createAccount`, honoured at `/home/user/caflow-ai/apps/api/routers/accounting.py:142-143` and `:270-273`. But the seed is unchanged: `/home/user/caflow-ai/apps/api/services/coa_seed_service.py:138` still writes `"account_type": atype, "account_subtype": asub, "is_active": True,` and nothing else, over 57 four-tuples with no group element.
PREMISE: half stale. "Grep confirms the only writer of parent_group/sub_group in the whole repo is `coa-import/page.tsx:169`" is **now false** — there are two more writers, both in `routers/accounting.py`. But the finding's *title* claim survives intact: a normally-onboarded firm still opens the screen to `{parentGroups.length} parent groups` = 1, one "Ungrouped → General" block, 57 accounts (`page.tsx:249-252`, `:232-238`). The CA now has a remedy, and it is 57 modal edits.
SHAPE: add a fifth element to each `STANDARD_COA` tuple in `coa_seed_service.py:44-113` and one key to the insert payload at :138. Optionally a backfill migration for firms already seeded — that one **is** a migration, and it is where the real decision sits: overwrite a CA's hand-typed group or only fill NULLs. Fill NULLs only.
SIZE: hours.

---

**ACC-25 — VERDICT: open**
EVIDENCE: `/home/user/caflow-ai/apps/web/components/journal/JournalEditor.tsx:155-161` — `return { entry_date: entryDate, entry_type: entryType, reference_no: referenceNo.trim(), narration: narration.trim(), lines: out, };`. `grep -in attach` on that file → 0 hits. Backend chain intact: `models/accounting.py:277`, `services/manual_journal_service.py:156,169`, `services/phase2_journal_service.py:1915-1916`.
PREMISE: sound on the gap, **and the suggested fix is the exact mistake the banking module documented and refused.** ACC-25 says "uploads to the existing Documents bucket and sends `[{name, url}]`". `/home/user/caflow-ai/apps/api/domain/banking/attachments.py:23-34`: "That store hands back a SIGNED url which expires in an hour, so writing it here would produce an attachment that is a dead link by the time anyone audits the coding … an uploaded attachment stores the DOCUMENT's id and no url at all … A document-backed attachment carrying a url is REFUSED rather than having the url dropped." The working implementation at `apps/web/components/banking/EntryDetailModal.tsx:124` sends `{ name: file.name, document_id: documentId }`. Worse: `journal_entries.attachments` has **no validation at any layer** — `models/accounting.py:277` is a bare `list[dict]` with no field_validator, and `migrations/138_journal_attachments.sql:13` adds plain jsonb with no CHECK, unlike `migrations/259_bank_transaction_attachments.sql:41-43` which at least bounds the array. So shipping the finding's fix as written adds both an expiring-link rot **and** the stored-XSS surface (`javascript:` / `data:` URLs) that `domain/banking/attachments.py:9-17` exists to close.
SHAPE: reuse `domain/banking/attachments.py`'s validators (they are pure and module-agnostic) from a journal-side validator on `JournalEntryIn`/`JournalEntryUpdateIn`; add `document_id` to the element shape; add the array CHECK to `journal_entries.attachments` (migration, mirroring 259); add `attachments` to `JournalEntryUpdateIn` (`models/accounting.py`, currently absent, so edits cannot set them either); a read endpoint that mints a fresh signed URL; then the upload control in `JournalEditor.tsx` and read-only rendering on the posted-entry view.
SIZE: day. Migration: yes, small.

---

**ACC-26 — VERDICT: open**
EVIDENCE: `/home/user/caflow-ai/apps/api/routers/accounting.py:1099-1101` — `from core.supabase_client import get_supabase` / `from services.ledger_span_service import ledger_span` / `return api_response(True, ledger_span(get_supabase(), current_user["firm_id"], scoped))`. No `_prod_db()` check. Reproduced: `env -u SUPABASE_URL -u SUPABASE_SERVICE_ROLE_KEY` → `get_ledger_span RAISED RuntimeError SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY must be set.`
PREMISE: sound on the defect, **FALSE on the consequence** (as the verifier already found and the finding text still asserts): `apps/web/lib/accounting/useLedgerSpan.ts` catches it and the period picker falls back to the FY, so "All Time breaks the whole reports tab" does not happen. Zero production reach — Render sets `SUPABASE_URL`.
SHAPE: three lines in `routers/accounting.py`. No migration, no frontend.
SIZE: hours (call it minutes).

---

**INV-02 — VERDICT: open**
EVIDENCE: `/home/user/caflow-ai/apps/api/domain/inventory_service.py:9-10` — "this is the defining trait of moving-average costing versus FIFO/LIFO, which track cost by batch instead". Repo-wide grep for `fifo|valuation_method|standard cost` over `apps/api/domain`, `routers`, `migrations`, `models` returns that one docstring line and two unrelated hits in `domain/tds/challan_mapping.py`.
PREMISE: sound. Honest disclosure still holds — `routers/year_end_notes.py:402` `data["inventory_valuation_basis"] = "moving average"` and :472 writes the note from the engine, so the software never certifies a policy it does not follow. This is an absent option, not a wrong number.
SHAPE: per-item `valuation_method` column + a FIFO cost-layer table + a branch in `_compute_stock_out`. Two invariants the finding does not mention: (a) `routers/year_end_notes.py:402` hardcodes the string "moving average" and would then be lying for FIFO items — the note has to become per-item or per-client; (b) `public.stock_position_as_at` (migration 363) derives `avg_cost_paise` as `value_paise / qty`, which is meaningless under FIFO layers, and `domain/reporting/stock_position.py` is its pinned mock twin (`tests/test_stock_position_parity_pg.py`), so both move together or the parity test fails.
SIZE: multi-day.

---

**INV-03 — VERDICT: open**
EVIDENCE: `/home/user/caflow-ai/apps/api/migrations/191_nrv_writedown.sql:14-16` — `CHECK (movement_type IN` … `'adjustment', 'sale_return', 'purchase_return', 'nrv_writedown'));` — no transfer, no manufacture/consume. No godown/batch/reorder/BOM/alternate-unit column in any migration.
PREMISE: one limb still **FALSE** — "no group/category" is wrong, `migrations/180_product_service_master.sql:32` adds `category TEXT`. Everything else sound.
SHAPE: the finding's own scoped version (godowns + batches) is right about the ledger being additive but understates two things. `godown_id` on `inventory_stock_ledger` means the running chain must key per `(item, godown)` — which means `_last_ledger_row`, `_compute_stock_in`, `_compute_stock_out`, the `service_catalogue` cache columns (`stock_qty_units`, `avg_cost_paise`) and `check_inventory_cache_drift` (`services/reconciliation_service.py:228`) all change shape, and `public.stock_position_as_at` + `domain/reporting/stock_position.py` + their parity test change with them. A `transfer` movement type must post **no** GL entry (value does not move between accounts), which is a new case in `apply_*_to_inventory`. Migration: yes, several.
SIZE: multi-day, and realistically weeks for both.

---

**INV-04 — VERDICT: partial** (moved since the rescore)
EVIDENCE: `routers/inventory.py` now has **five** routes, not four: `:65 /items`, `:110 /stock-summary`, `:145 /items/{id}/ledger`, `:215 /items/{id}/adjust`, `:279 /items/{id}/writedown`. And the data the finding wants now exists server-side: `/home/user/caflow-ai/apps/api/migrations/363_closing_stock_as_at_a_date.sql:117` — `MAX(l.movement_date)                       AS last_movement_date,` and `:118` `COUNT(*)                                   AS movements`, both emitted per item at :160-161. Mock twin at `/home/user/caflow-ai/apps/api/domain/reporting/stock_position.py:145`.
PREMISE: partly **FALSE now.** "routers/inventory.py exposes four endpoints only" is false, and "days since last movement" no longer needs building — the server already computes and returns it, server-side aggregated, exactly per the reporting rule. What is missing is that the frontend **drops it**: `/home/user/caflow-ai/apps/web/app/clients/[id]/inventory/page.tsx:154-163` maps the stock-summary response into `StockItem` and does not carry `last_movement_date` or `movements` across. There is still no ageing-by-stock-in-layer report, and there cannot be one under moving average (no layers exist — see INV-02).
SHAPE: slow/non-moving is now a frontend-only change — widen the `StockItem` type, keep the two fields in the map at :154-163, add a "Last moved" column and a derived "Days idle", sort by it. Stock **ageing** proper (how long the units on hand have been held) is blocked on INV-02's cost layers and should be split off as a separate finding rather than delivered here. No migration.
SIZE: hours for the slow-moving half. The ageing half is multi-day and dependent on INV-02.

---

**INV-05 — VERDICT: partial** (moved since the rescore — one half of the mechanism was fixed by PUR-04)
EVIDENCE: `/home/user/caflow-ai/apps/api/domain/inventory_service.py:1000` unchanged — `cost_paise = int(line.get("taxable_amount_paise") or 0)`; the line select at :976 pulls no freight, duty or tax field. But `/home/user/caflow-ai/apps/api/services/phase2_journal_service.py:673-674` now reads `-- BLOCKED TAX IS A COST, NOT AN ASSET (PUR-04).` and :705 `if lr.get("itc_eligible", True): continue` — blocked §17(5) tax is now debited to the **line's own expense account**, and only `creditable_cgst/sgst/igst` (:729-734) reach GST Input.
PREMISE: **half FALSE.** The finding's stated mechanism — "because all GST including `itc_eligible=false` tax is debited to GST Input (`phase2_journal_service.py:656-676`), blocked credit never joins stock cost either" — is no longer true of the code. Blocked tax is no longer a phantom asset. The *conclusion* still holds by a different route: `post_inventory_receipt_journal_entry` (`domain/inventory_service.py:715`) reclassifies only `value_paise` (= `movement["value_delta_paise"]` = taxable) out of expense into Inventory, so the blocked tax stays in P&L rather than in stock cost. And freight/insurance/duty remain entirely unaddressed. Fixed assets already do this correctly (`routers/fixed_assets.py:60` — `return int(cost_paise) + (tax if (tax and itc_eligible is False) else 0)`), which is the pattern to copy.
SHAPE: **the invariant the finding does not mention.** CLAUDE.md states it: "Σ `value_delta_paise` to a date is what ties to the Inventory control account, because the inventory journal posts exactly that delta at exactly that date." So apportioned freight cannot just be added to `record_stock_in`'s `total_cost_paise` — the same amount must also flow into `post_inventory_receipt_journal_entry`'s `items[].value_paise` and be **credited to the freight/duty expense account**, not to the line's own expense account. Otherwise `stock_position_as_at` and `check_missing_inventory_receipt_journals` (`services/reconciliation_service.py:178-225`, which compares Σ|value_delta| per bill against the posted `-INV` journal) both start reporting drift. Touches: `models/purchase_bill*` (a new apportionable-costs payload), `routers/purchase_bills.py`, `domain/inventory_service.py` (`apply_purchase_to_inventory`, `post_inventory_receipt_journal_entry`), plus a migration for the additional-cost columns/table.
SIZE: multi-day.

---

**INV-07 — VERDICT: open**
EVIDENCE: `/home/user/caflow-ai/apps/api/routers/inventory.py:42` — `while remaining:`, with breaks only at :52-53 (`if not page: break`) and :59-60 (`if len(page) < page_size: break`). `remaining` is seeded at :95 from every `kind='good'` catalogue row with no `is_active` filter, so one item that never had a ledger row walks the loop to the end of the client's ledger. The query at :44-50 selects no `.in_("service_catalogue_id", ...)` filter, so every page is a full-width read.
PREMISE: sound, and now sharper: the function's own docstring at :36-37 claims it "stops as soon as every item has been seen, rather than scanning the client's entire movement history", which is precisely what it does not do. Also — migration 363 already ships the SQL-function pattern the fix wants; `public.stock_position_as_at` does the whole job server-side in one round trip.
SHAPE: two options, and the cheap one is real. (a) One-line fix: add `.in_("service_catalogue_id", list(remaining))` to the query at :44 and break when a full page yields no new item — bounds the scan to rows that can matter. (b) Better: a `DISTINCT ON (service_catalogue_id) … ORDER BY created_at DESC` SQL function, which per CLAUDE.md needs a mock-mode twin **and** a parity test, since `_USE_MOCK` short-circuits at :81. Note `/items` and `/stock-summary` answer different questions (:117-127) so you cannot just redirect the tab at 363. No migration for (a); one for (b).
SIZE: hours for (a), day for (b).

---

**INV-08 — VERDICT: open**
EVIDENCE: `routers/inventory.py:215` `@router.post("/items/{service_catalogue_id}/adjust")` — one `service_catalogue_id` per call; no bulk route among the five. `apps/web/app/clients/[id]/inventory/page.tsx:369-373` opens the adjust modal only from the drill-down header.
PREMISE: two sub-claims still **FALSE**, both already caught by the verifier and both still true of HEAD: the register exports to CSV (`page.tsx:290` `exportFilename="inventory-stock-register"` via `components/ui/data-table.tsx`), and `reference_no` exists on `models/inventory.py:49` and in the modal as free text. What is genuinely missing is the round trip. Note the count sheet just got better without anyone noticing: the "As at" picker (`page.tsx:143`) means the CA can now export the register **as at 31 March**, which is the sheet a physical count actually needs.
SHAPE: a bulk `POST /items/adjust-batch` taking `[{service_catalogue_id, counted_qty}]`, computing the variance server-side against `stock_position_as_at`, and posting under one `reference_no`. Invariant to hold: `period_validation_service.validate_posting_date` is currently called per adjustment (`routers/inventory.py:~250`) — a batch must validate **before** posting any of it, or a locked FY leaves a half-posted count. Same for the `service_catalogue` optimistic-concurrency version (migration 241): a batch touching N items takes N compare-and-set updates and needs a defined behaviour when one loses the race. Plus a variance-review screen. Migration: none required.
SIZE: multi-day.

---

**INV-09 — VERDICT: open**
EVIDENCE: `/home/user/caflow-ai/apps/api/migrations/188_inventory_costing.sql:56` — `quantity_delta      NUMERIC(10,3) NOT NULL,` and `:59` `running_qty_units   NUMERIC(10,3) NOT NULL,`. `/home/user/caflow-ai/apps/api/models/inventory.py:33` `quantity: float` with only `positive()` at :52-57.
PREMISE: the finding's stated drift is **FALSE** and remains so — `running_qty_units` is the same NUMERIC(10,3) column, re-read from the DB on every movement, so the stored chain does not disagree with itself. The real residue (verifier's, not the finding's) is that `unit_cost_paise` and `value_delta_paise` are computed from the unrounded Decimal at `domain/inventory_service.py:80` while the quantity stored is rounded to 3dp — sub-paise money against a quantity the ledger did not keep. The frontend's `step="0.001"` at `page.tsx:~600` is still inert (no `<form>`, plain `onClick`).
SHAPE: one `field_validator` on `StockAdjustmentIn.quantity` in `models/inventory.py` rejecting >3dp, and quantise before the money computation at `inventory_service.py:80`. Column width belongs with INV-03's alternate-units work, not here. No migration.
SIZE: hours.

---

**INV-10 — VERDICT: open**
EVIDENCE: `/home/user/caflow-ai/apps/web/app/clients/[id]/inventory/page.tsx:295` — `onRowClick={(item) => setDrillDown(item)}`, the register's only row interaction; no action column in `allColumns` (:198-244), no selection prop on `DataTable`. Adjust Stock at :377-380 and Write Down to NRV at :382-385, both inside `StockLedgerDrillDown`'s header.
PREMISE: sound in every limb.
SHAPE: an action column on the register plus lifting `adjustOpen`/`writedownOpen` out of the drill-down — pure frontend, one file. The cross-item NRV review screen is the other half and is a real build: it needs cost per item (have it, from `/stock-summary`) and NRV per item (nobody holds it — it is a CA judgement per item, so the screen is an input grid, and the write side is INV-08's bulk endpoint). No migration.
SIZE: hours for the row actions; multi-day for the NRV review, and it should be merged into INV-08 rather than built separately.

---

**Summary of what is actually stale in this batch:** three, not the ~10% you expected. ACC-24 (the screen now writes groups; the seed still doesn't), INV-04 (`last_movement_date` is already computed and served; the frontend drops it), INV-05 (blocked-ITC half fixed by PUR-04; freight half untouched). Two more findings have a false *fix* rather than a false premise, and those are the dangerous ones: **ACC-23**, whose "stop synthesising" instruction would break a balance sheet that is currently self-correcting and test-pinned, and **ACC-25**, whose `{name, url}` payload is the expiring-signed-URL and stored-XSS failure that `domain/banking/attachments.py` was written to close. **ACC-16** has an unmentioned blocker: its backfill is refused outright by migration 251's immutability trigger.