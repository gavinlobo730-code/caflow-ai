{
 "findings": [
  {
   "id": "SALES-01",
   "kind": "bug",
   "severity": "critical",
   "title": "Sales-invoice PDF names the CA FIRM as the supplier, with the CA firm's GSTIN and PAN, on the client's own sale",
   "evidence": "apps/api/services/invoice_pdf_service.py:355 `firm = _load_firm(firm_id)` reads `public.firms` (the CA practice / tenant) and passes it to `build_invoice_pdf(invoice_dict, firm, customer)`; lines 148-160 render that row as the supplier block \u2014 `firm.get(\"name\")`, `GSTIN: {firm['gstin']}`, `PAN: {firm['pan']}` \u2014 under the heading \"TAX INVOICE ... Section 31, CGST Act 2017\". The builder was written for `fee_invoices` (the practice billing its own clients \u2014 services/invoice_generation_service.py, repositories/invoice_repository.py) and `get_sales_invoice_pdf` reuses it verbatim for `client_sales_invoices`, whose supplier is the CLIENT. That the client is the supplier is settled by routers/sales_invoices.py:680 `client_state_code = _get_state_code_from_gstin(client_rec.get(\"gstin\"))` \u2014 intra/inter-state is decided by the CLIENT's GSTIN. tests/test_invoice_pdf_line_items.py:193 pins the wrong behaviour (`_load_firm` stubbed to `{\"name\": \"Test & Co\", \"gstin\": \"27AAAAA9999A1Z5\"}`). The same PDF is emailed to the customer (routers/sales_invoices.py:1965) and served on the client portal (routers/portal_data.py:54). services/statement_pdf_service.py:104-105 has the identical defect on the customer statement. apps/api/migrations/001_initial_schema.sql:22-36 shows `clients` already carries client_name, pan, gstin, address, state, state_code.",
   "ca_experience": "The CA emails their client's customer a tax invoice headed with the CA practice's name and the practice's GSTIN. The customer's accountant tries to claim ITC against a GSTIN that never made the supply, and it will not match GSTR-2B. If any such PDF is used as the tax invoice of record it is a Rule 46(a)/(b) failure for the client and a misuse of the practice's own registration.",
   "market_standard": "Tally Prime, Zoho Books, Busy and Marg all print the COMPANY whose books are open as the supplier. In Zoho Books Practice the accountant's own branding never appears on the client's outward invoice.",
   "confidence": "high",
   "suggested_fix": "In `get_sales_invoice_pdf`, load `public.clients` by `invoice.client_id` (firm-scoped) and pass THAT as the supplier; keep `_load_firm` only for the `fee_invoices` path in `get_invoice_pdf`. Rename `build_invoice_pdf(invoice, supplier, recipient)` so the two callers cannot be confused again, and change the test stub so it fails on the old behaviour. Same change in `statement_pdf_service.get_customer_statement_pdf`.",
   "effort": "days",
   "verification": {
    "status": "confirmed",
    "by": "hand",
    "probe": "read invoice_pdf_service.py:355 _load_firm(firm_id) rendered as the supplier block at :148-160"
   }
  },
  {
   "id": "SALES-02",
   "kind": "bug",
   "severity": "critical",
   "title": "Invoice PDF prints a hardcoded 18% / 9% / 9% tax rate on every invoice regardless of the actual line rates",
   "evidence": "apps/api/services/invoice_pdf_service.py:30 `GST_RATE_PCT = 18`; lines 223-226 emit `f\"CGST @ {GST_RATE_PCT // 2}%\"`, `f\"SGST @ {GST_RATE_PCT // 2}%\"`, `f\"IGST @ {GST_RATE_PCT}%\"`. The AMOUNTS come from the stored heads (correct), but the printed RATE is the constant. The line table (rows built at 199-214) carries only `#`, `Description`, `SAC`, `Taxable Value` \u2014 no quantity, no unit, no per-line rate, no per-line tax. The frontend by contrast gets this right: components/invoices/InvoiceEditor.tsx:604-612 only shows `@ x%` when `uniformRate != null`, i.e. when every line shares one rate.",
   "ca_experience": "A 5% invoice (or a 12% one, or a mixed-rate one) prints \"CGST @ 9% ... SGST @ 9%\" beside a 5% amount. The customer's team sees a document whose stated rate and stated tax do not reconcile; a GST officer reading it sees a Rule 46(l) defect. On a goods invoice the missing quantity and unit are further Rule 46(g)/(h) defects.",
   "market_standard": "Every tool in the market (Tally Prime, Zoho Books, Busy, Marg) prints a rate-wise tax summary derived from the lines, plus quantity, UQC and per-line rate/tax columns, because Rule 46 requires them.",
   "confidence": "high",
   "suggested_fix": "Delete `GST_RATE_PCT`. Group the invoice's lines by `gst_rate_bps` and emit one CGST/SGST or IGST row per rate band, labelled from the band. Add Qty, Unit, Rate and per-line tax columns to the line table. Add a regression test that renders a 5% invoice and asserts \"9%\" does not appear.",
   "effort": "days",
   "verification": {
    "status": "confirmed",
    "by": "hand",
    "probe": "read invoice_pdf_service.py:30 GST_RATE_PCT = 18 emitted at :223-226"
   }
  },
  {
   "id": "SALES-03",
   "kind": "bug",
   "severity": "high",
   "title": "Invoice PDF always states \"tax payable on reverse charge basis: No\", and never prints the place of supply or the IRN/QR",
   "evidence": "apps/api/services/invoice_pdf_service.py:253-254 `story.append(Paragraph(\"Whether tax is payable on reverse charge basis: No\", ...))` \u2014 an unconditional string. `is_reverse_charge` is a real, CA-settable column (migration 268; models/invoices.py:139; components/invoices/InvoiceEditor.tsx:766-770) and is plumbed correctly into GSTR-1 (`domain/gst/gstr1_builder.py:232 \"rchrg\": \"Y\" if inv.is_reverse_charge else \"N\"`), so the PDF is the only place that contradicts it. `get_sales_invoice_pdf`'s `invoice_dict` (lines 331-345) never carries `supply_state_code`, and `build_invoice_pdf` never prints a place of supply. No IRN/QR is rendered anywhere in the module even though `einvoice_records.irn`/`qr_data` exist (domain/income_tax/einvoice_service.py:154-160).",
   "ca_experience": "An RCM invoice \u2014 the exact document where the declaration matters \u2014 carries a printed statement saying the opposite. Rule 46(p). The recipient reading \"No\" may not self-assess. An inter-state invoice carries no place of supply (Rule 46(n)). Once e-invoicing applies to the client, Rule 48(5) makes an invoice without the IRN QR not an invoice at all, and this PDF can never carry one.",
   "market_standard": "Tally Prime and Zoho Books print the reverse-charge declaration from the voucher's own flag, print Place of Supply on every invoice, and stamp the IRN + signed QR onto the PDF once the IRN is returned.",
   "confidence": "high",
   "suggested_fix": "Read `is_reverse_charge` into `invoice_dict` and render Yes/No from it; add a Place of Supply line (state code + name); when a `generated` einvoice_record exists for the invoice, render the IRN, Ack No/date and the QR from `qr_data`.",
   "effort": "days",
   "verification": {
    "status": "confirmed",
    "by": "agent",
    "corrected_severity": "high",
    "what_is_actually_true": "All three parts hold: apps/api/services/invoice_pdf_service.py:253 prints the reverse-charge declaration as a hardcoded 'No' regardless of client_sales_invoices.is_reverse_charge; build_invoice_pdf never prints a place of supply (Rule 46(n)); and no IRN/Ack/QR is rendered even though einvoice_records holds them. Worse than reported, the same builder hardcodes the RATE in the tax-row labels \u2014 invoice_pdf_service.py:223-226 prints 'CGST @ {GST_RATE_PCT//2}%' / 'IGST @ {GST_RATE_PCT}%' from a module constant, so a 5% or 12% invoice prints a 9%/18% label beside a correct amount.",
    "probe": null,
    "lens_notes": {
     "literal": "Confirmed at apps/api/services/invoice_pdf_service.py:252-254 \u2014 story.append(Paragraph(\"Whether tax is payable on reverse charge basis: No\", ...)) is an unconditional literal; `invoice` is never consulted. A full scan of every Paragraph()/rows.append() call in build_invoice_pdf (lines 153-258) shows the document body is: title, Rule 46 citation, firm block, meta block (invoice no / date / due date / status), Bill To, the line table, taxable + CGST/SGST/IGST + round-off + total, amount in words, the RCM literal, and the signatory. There is no Place of Supply line and no IRN/Ack/QR anywhere in the module. get_sales_invoice_pdf's invoice_dict (invoice_pdf_service.py:337-352) carries only invoice_no, invoice_date, due_date, status, amount_paise, cgst/sgst/igst, round_off, total and lines \u2014 is_reverse_charge and supply_state_code are not read from the row even though `select(\"*\")` fetched them.",
     "guards": "None. is_reverse_charge is a real settable column (migrations/268_sales_invoice_gst_classification.sql:45, models/invoices.py:142/193, apps/web/components/invoices/InvoiceEditor.tsx:766-770 checkbox) and is honoured on the GSTR-1 path (domain/gst/gstr1_builder.py:232 and :463 emit \"rchrg\": \"Y\"/\"N\"), so the PDF is the only consumer that contradicts the stored flag. einvoice_records with irn/ack_number/ack_date/qr_data exists (migrations/156_missing_tables_f5.sql:204; domain/income_tax/einvoice_service.py:149-170 record_irn_generated) and is never joined by the PDF builder. No test asserts the declaration matches the flag (tests/test_invoice_pdf.py only covers _compute_tax_splits; tests/test_invoice_pdf_line_items.py only covers line items).",
     "reach": "Reachable. GET /api/sales-invoices/{invoice_id}/pdf at apps/api/routers/sales_invoices.py:1870-1891, called from apps/web/components/invoices/InvoiceViewDrawer.tsx:44 behind the 'PDF' action at line 274, and from lib/api/index.ts:1802. Also served to the client portal (routers/portal_data.py:52-54) and attached to collection emails (services/collections_service.py:359)."
    }
   }
  },
  {
   "id": "SALES-04",
   "kind": "bug",
   "severity": "high",
   "title": "Deleting a middle draft credit note (or sales debit note) permanently wedges that client's numbering for the rest of the financial year",
   "evidence": "apps/api/routers/credit_notes.py:111-119 `_next_cn_seq` returns `count(credit_notes LIKE 'CN-{fy}-%') + 1` \u2014 a COUNT, not a max. routers/credit_notes.py:652-737 `delete_credit_note` hard-deletes a draft (`db.table(\"credit_notes\").delete()`), and the number was already assigned at create (line 322 `insert_numbered_document_with_lines`). migrations/151_per_client_document_numbering.sql:93-94 puts UNIQUE (firm_id, client_id, credit_note_no) on the table. services/numbering.py:80-97 retries 6 times but re-calls the SAME `next_seq`, which returns the same value every time. Reproduced against the repo's own test double (tests/test_document_numbering_unit._FakeDB): with CN-2627-0001/0002/0003 present, deleting 0002 makes `_next_cn_seq` return 3 forever \u2014 `retry sequence values: [3, 3, 3, 3, 3, 3]`, colliding with the live CN-2627-0003 on every attempt. routers/sales_debit_notes.py:121-128 `_next_sdn_seq` and services/receipt_service.py:70-80 `_next_receipt_seq` are the identical shape.",
   "ca_experience": "The CA raises three credit notes, deletes the middle draft as a mistake, and from then on every attempt to create a credit note for that client fails with a generic \"Unable to complete credit note operation\" until the financial year rolls over. There is no message telling them why and no way out from the UI.",
   "market_standard": "Tally Prime and Zoho Books derive the next voucher number from the highest number in the series (or a dedicated sequence), so a deleted voucher leaves a gap and never blocks the next one.",
   "confidence": "high",
   "suggested_fix": "Derive the sequence from the MAX numeric suffix in the series for (firm, client, FY) rather than a row count \u2014 or move to a Postgres sequence per (firm, client, series, FY) as `next_invoice_number` (migration 124) already does for fee invoices. Add a test that creates three notes, deletes the second, and asserts the fourth is created successfully.",
   "effort": "hours",
   "verification": {
    "status": "confirmed",
    "by": "agent",
    "corrected_severity": "high",
    "what_is_actually_true": "Exactly as described for credit notes and sales debit notes. Deleting any but the last draft in the series permanently blocks new documents in that (firm, client, FY) series, because routers/credit_notes.py:113-127 counts rows instead of taking the max and services/numbering.py:83-97 retries the same value six times against the UNIQUE constraint from migrations/151:93-94. Receipts share the count-based generator (services/receipt_service.py:70-80) but have no delete path, so only credit notes and sales debit notes are wedgeable today.",
    "probe": "cd apps/api && python - (reusing tests/test_document_numbering_unit._FakeDB) with CN-2627-0001/0002/0003 seeded, then popping 0002 \u2014 printed: 'before delete, next seq: 4' / 'after deleting CN-2627-0002, retry sequence values: [3, 3, 3, 3, 3, 3]' / 'existing numbers: [CN-2627-0001, CN-2627-0003]' / 'collision every attempt: True'.",
    "lens_notes": {
     "literal": "Confirmed. apps/api/routers/credit_notes.py:113-127 _next_cn_seq returns (count of credit_notes LIKE 'CN-{fy}-%') + 1 \u2014 a COUNT with no deleted_at filter and no MAX. The number is assigned at create (routers/credit_notes.py:326-332, via insert_numbered_document_with_lines). apps/api/services/numbering.py:83-97 retries 6 times but re-calls the SAME next_seq callable, which is a pure function of the row count, so every attempt produces the identical number. delete_credit_note (routers/credit_notes.py:667-737) hard-deletes a draft row: db.table(\"credit_notes\").delete().eq(\"id\", cn_id) at line 715. migrations/151_per_client_document_numbering.sql:93-94 adds UNIQUE (firm_id, client_id, credit_note_no).",
     "guards": "None that helps. The 6-attempt retry is the only guard and it is the thing that fails \u2014 it recomputes an identical value. The failure surfaces as api_response(False, None, 'Unable to complete credit note operation. Please try again.') at routers/credit_notes.py:737, i.e. a generic message with no diagnosis. No test in apps/api/tests covers create-after-delete: tests/test_document_numbering_unit.py only asserts per-client scoping, and the real-Postgres tests/test_per_client_numbering.py only asserts two clients can share a number.",
     "reach": "Reachable. deleteCreditNote at apps/web/app/clients/[id]/sales/page.tsx:3810 (row menu at :3967, drawer at :3990) plus a bulk draft-delete at :3839-3858. The same shape exists for sales debit notes \u2014 routers/sales_debit_notes.py:121-128 _next_sdn_seq is count-based and routers/sales_debit_notes.py:496 is a DELETE endpoint \u2014 and for receipts (services/receipt_service.py:70-80 _next_receipt_seq), though receipts have no DELETE endpoint (only POST /{receipt_id}/reverse at routers/receipts.py:340), so the receipt series is exposed only if a row is removed out-of-band."
    }
   }
  },
  {
   "id": "SALES-05",
   "kind": "bug",
   "severity": "high",
   "title": "The Sales screen's \"Outstanding\" tile overstates receivables \u2014 it sums the full invoice total for partially-paid invoices and ignores credit notes entirely",
   "evidence": "apps/web/app/clients/[id]/sales/page.tsx:1439-1443: `let outstanding = 0 ... if (inv.status === \"issued\" || inv.status === \"partially_paid\") outstanding += inv.total_paise;` \u2014 `paid_paise` is fetched (line 1372 select list, mapped at 1424) and simply not subtracted, and `credited_paise` / `debit_note_paise` are not even selected. The backend already computes this correctly: routers/sales_invoices.py:344-348 `outstanding = sum(total + debit_note - paid - credited)`, and migration 278 added an `outstanding_paise` generated column for exactly this. Rendered at page.tsx:1856 `<SummaryCard label=\"Outstanding\" value={fmt(stats.outstanding)} />`.",
   "ca_experience": "A client with \u20b910,00,000 invoiced, \u20b98,00,000 collected on account and \u20b950,000 credited sees \"Outstanding \u20b910,00,000\" on the Sales dashboard instead of \u20b91,50,000. Every partial payment and every credit note makes the number worse. The CA reads the tile, reports the receivable to the client, and it is wrong.",
   "market_standard": "Zoho Books' Invoices dashboard shows Total Outstanding as balance due (invoice total less payments and credits applied); Tally's Bills Outstanding report is pending-amount based.",
   "confidence": "high",
   "suggested_fix": "Call `GET /api/sales-invoices/outstanding?client_id=\u2026` for the tile (the endpoint exists and is correct), or select `outstanding_paise` (migration 278) and sum it. Do not recompute the formula in TypeScript \u2014 that is what let it drift.",
   "effort": "hours",
   "verification": {
    "status": "confirmed",
    "by": "agent",
    "corrected_severity": "high",
    "what_is_actually_true": "The tile sums gross invoice totals for issued and partially_paid invoices, subtracting neither cash received nor credit notes (apps/web/app/clients/[id]/sales/page.tsx:1441). A \u20b910,00,000 invoice with \u20b98,00,000 collected shows the full \u20b910,00,000. Two further inaccuracies the finding did not name: the tile is scoped to the selected period's invoice_date range (page.tsx:1376-1377), so it is a period total rather than a receivable balance at all; and the other four outstanding computations on the same page (:146, :1013, :1239, :2303) subtract paid_paise but still ignore credited_paise/debit_note_paise, so the credit-note half of the drift is page-wide, not tile-only.",
    "probe": null,
    "lens_notes": {
     "literal": "Confirmed at apps/web/app/clients/[id]/sales/page.tsx:1438-1445: `let outstanding = 0, issued = 0, paid = 0; for (const inv of mapped) { if (inv.status === \"issued\" || inv.status === \"partially_paid\") outstanding += inv.total_paise; ... }`. paid_paise IS selected (page.tsx:1373) and mapped (page.tsx:1425) and simply never subtracted; credited_paise and debit_note_paise are absent from the select list entirely. Rendered at page.tsx:1856 <SummaryCard label=\"Outstanding\" ...>.",
     "guards": "None on this tile. The correct formula exists twice server-side \u2014 apps/api/routers/sales_invoices.py:344-348 computes total + debit_note \u2212 paid \u2212 credited, and migrations/278_outstanding_generated_column.sql:70-77 adds client_sales_invoices.outstanding_paise as a generated column with the same expression \u2014 and the frontend uses neither for the tile. Elsewhere on the very same page the arithmetic is at least half-right (page.tsx:146, :1013, :1239, :2303 all do total_paise \u2212 paid_paise), which makes the tile the outlier rather than a house convention.",
     "reach": "Reachable and prominent \u2014 it is the first of three summary cards at the top of the Sales tab for every client (apps/web/app/clients/[id]/sales/page.tsx:1854-1858)."
    }
   }
  },
  {
   "id": "SALES-06",
   "kind": "bug",
   "severity": "high",
   "title": "Goa, Puducherry, Ladakh, Dadra & Nagar Haveli and Daman & Diu, Lakshadweep and Andaman & Nicobar cannot be selected as place of supply or as a customer's state",
   "evidence": "apps/web/lib/invoices/gst.ts:217-233 defines a SECOND `INDIAN_STATES` list containing only 01-24, 27, 29, 32, 33, 36, 37 \u2014 Goa (30), DNH&DD (26), Puducherry (34), Ladakh (38), Lakshadweep (31) and A&N (35) are absent, as are 96/97. components/invoices/InvoiceEditor.tsx:41 imports `INDIAN_STATES` from `@/lib/invoices/shared` (which re-exports `./gst` at lib/invoices/shared.ts:12) and passes it explicitly at line 715 `<StateLookup states={INDIAN_STATES} \u2026>`, overriding the CORRECT canonical list that StateLookup defaults to (components/lookups/StateLookup.tsx:4 \u2192 lib/constants/indianStates.ts:10-45, which has Goa, DNH&DD, Puducherry and Ladakh). components/customers/CustomerFormModal.tsx:23,197 does the same for the customer's State Code. The backend accepts all of 01-38 plus 96/97 (apps/api/domain/gst/validator.py:18-25).",
   "ca_experience": "A Goa customer cannot be given a state code. `state_code` stays blank, so on invoice creation `effective_supply_state = supply_state_code or customer.state_code or \"\"` (routers/sales_invoices.py:672) is empty, `is_interstate` evaluates False (line 677), and a Maharashtra client billing a Goa customer is charged CGST+SGST instead of IGST \u2014 a wrong tax on a real invoice. The error only surfaces much later, when the GSTR-1 validator rejects the blank place of supply (domain/gst/validator.py:93-99).",
   "market_standard": "Every Indian accounting product ships the full 38-entry GST state/UT master plus 96/97 (Other Territory / Other Country) and derives the state from the GSTIN prefix as a fallback.",
   "confidence": "high",
   "suggested_fix": "Delete the duplicate list in lib/invoices/gst.ts and re-export `lib/constants/indianStates`; add 31 (Lakshadweep), 35 (Andaman & Nicobar), 97 (Other Territory) and 96 (Other Country) to the canonical list; drop the explicit `states={INDIAN_STATES}` props so StateLookup's default is used. Separately, default `customers.state_code` from `gstin[:2]` server-side when it is blank.",
   "effort": "hours",
   "verification": {
    "status": "confirmed",
    "by": "agent",
    "corrected_severity": "high",
    "what_is_actually_true": "The truncated list at apps/web/lib/invoices/gst.ts:216-234 is real and is forced onto both the customer State Code picker (CustomerFormModal.tsx:197) and the invoice Place of Supply picker (InvoiceEditor.tsx:715), so Goa, Puducherry, Ladakh and DNH&DD cannot be selected in either; Lakshadweep (31), A&N (35), 96 and 97 are missing from the canonical list too, so no picker in the app can select them. The wrong-tax consequence at apps/api/routers/sales_invoices.py:683-687 (blank effective_supply_state \u2192 is_interstate False \u2192 CGST+SGST instead of IGST) is real but narrower than stated: it needs a customer with no GSTIN, because CustomerFormModal.tsx:101-105 auto-fills state_code from the GSTIN prefix for registered customers.",
    "probe": null,
    "lens_notes": {
     "literal": "Confirmed. apps/web/lib/invoices/gst.ts:216-234 defines a second INDIAN_STATES with 30 entries: 01-24, 27, 29, 32, 33, 36, 37. Missing: 26 (DNH&DD), 30 (Goa), 31 (Lakshadweep), 34 (Puducherry), 35 (A&N), 38 (Ladakh), 96, 97. apps/web/lib/invoices/shared.ts:12 `export * from \"./gst\"` re-exports it. components/invoices/InvoiceEditor.tsx:41 imports it from @/lib/invoices/shared and passes it explicitly at :715 <StateLookup states={INDIAN_STATES} ...>, and components/customers/CustomerFormModal.tsx:23,197 does the same. components/lookups/StateLookup.tsx:23 `const { states = INDIAN_STATES, ...rest }` would otherwise default to the canonical lib/constants/indianStates.ts:10-45, which HAS 26/30/34/38 \u2014 but still lacks 31, 35, 96 and 97. Backend accepts all of 01-38 plus 96/97 (apps/api/domain/gst/validator.py:18-25).",
     "guards": "One partial mitigation the finding missed: components/customers/CustomerFormModal.tsx:101-105 handleGstinChange auto-fills stateCode from gstin.slice(0,2), so a REGISTERED Goa customer gets state_code '30' without touching the picker, and EntityLookup (components/lookups/EntityLookup.tsx:54-59) resolves an unknown value to null without calling onChange, so re-editing that customer does not silently clear it. That mitigation does nothing for an UNREGISTERED (B2C) customer, and nothing for the invoice's own place-of-supply override at InvoiceEditor.tsx:715. No other guard: nothing derives state_code from the GSTIN server-side, and apps/api/domain/gst/validator.py:93-99 only rejects a blank place of supply at GSTR-1 build time, long after the invoice was taxed.",
     "reach": "Reachable. CustomerFormModal is mounted from apps/web/app/clients/[id]/sales/page.tsx:1725 and :2879; InvoiceEditor is mounted from app/clients/[id]/sales/invoices/[invoiceId]/edit/_page.tsx:115 behind a real page.tsx route in the same directory (both files present)."
    }
   }
  },
  {
   "id": "SALES-07",
   "kind": "gap",
   "severity": "high",
   "title": "TDS deducted by the customer (\u00a7194J / \u00a7194Q) cannot be recorded on a receipt from any screen",
   "evidence": "`ReceiptIn.tds_paise` exists (apps/api/models/invoices.py:394) and the whole settlement engine honours it \u2014 services/receipt_service.py:533-535 `settlement = amount_paise + tds_paise`, services/phase2_journal_service.py:206-238 posts Dr Bank + Dr TDS Receivable / Cr Trade Receivables. But the receipt form never sends it: apps/web/app/clients/[id]/sales/page.tsx:3470-3480 posts only `client_id, customer_id, receipt_date, amount_paise, payment_mode, reference_no, allocations, currency, exchange_rate`. `grep -rn \"tds_paise\" apps/web/app apps/web/lib apps/web/components` returns hits only in payroll, purchases and the TDS module \u2014 nothing in the sales receipt path. The screen's own \"Unallocated\" line (page.tsx:3629) computes `amountPaise - totalAllocated`, which is the wrong basis even if TDS were entered.",
   "ca_experience": "On a \u20b91,00,000 professional-fee invoice the customer pays \u20b990,000 and deducts \u20b910,000 TDS \u2014 the normal case for almost every B2B service invoice in India. The CA can only record \u20b990,000. The invoice stays `partially_paid` with \u20b910,000 outstanding forever, AR is overstated by the TDS on every invoice, the TDS Receivable asset is never booked, and there is nothing to reconcile against Form 26AS. The only workaround is a manual journal, outside the sub-ledger.",
   "market_standard": "Tally Prime's receipt voucher takes a TDS deduction line against the bill; Zoho Books has an explicit \"TDS deducted\" field on Record Payment that closes the invoice and books TDS receivable. This is table stakes for an Indian receivables module.",
   "confidence": "high",
   "suggested_fix": "Add a TDS Deducted (\u20b9) field to ReceiptForm, parsed with `paiseFromRupeeInput`, sent as `tds_paise`; change the Unallocated display to `amount + tds - allocated` to match `create_receipt_core`; show the settlement value beside the cash amount so the CA can see the invoice will close.",
   "effort": "hours",
   "verification": {
    "status": "refuted",
    "by": "hand",
    "corrected_severity": "medium",
    "what_is_actually_true": "REFUTED AS WRITTEN, narrowed. Customer-deducted TDS IS recordable: apps/web/components/banking/SettleDocumentsModal.tsx:194 sends tds_paise through api.banking.matchMulti -> routers/banking.py -> apps/api/services/bank_posting_service.py:1022 -> create_receipt_core, reachable from /clients/[id]/bank -> Entries. What survives is narrower and still real: the standalone receipt form on the sales page has no TDS field (grep tds_paise in app/clients/[id]/sales/page.tsx returns nothing), so a CA recording a receipt outside the bank-matching flow cannot capture it. Two verifiers disagreed on this one; settled by hand.",
    "probe": "grep tds_paise across SettleDocumentsModal.tsx, bank_posting_service.py and the sales page"
   }
  },
  {
   "id": "SALES-08",
   "kind": "data_integrity",
   "severity": "high",
   "title": "Every receipt debits one firm-wide \"Bank\" ledger \u2014 cash receipts land in Bank, and the selected bank account is discarded",
   "evidence": "apps/api/services/phase2_journal_service.py:210 `bank_id = self._find_account(db, firm_id, client_id, \"%Bank%\", system_key=\"bank\")` \u2014 unconditional; `receipt[\"payment_mode\"]` (bank/cash/cheque/upi/neft/rtgs/online) is read at services/receipt_service.py:632 into the row but never consulted when building the journal. `_find_account`'s system_key branch (phase2_journal_service.py:1423-1435) filters on firm_id + system_account_key only, with no client or account filter, so it returns one ledger for the whole firm. `ReceiptIn.bank_account_id` (models/invoices.py:400) is accepted by the API and then dropped \u2014 it is absent from the `receipt_payload` dict built at services/receipt_service.py:623-637, so it is never even stored. The frontend never sends it either (app/clients/[id]/sales/page.tsx:3470-3480).",
   "ca_experience": "A cash sale receipt is posted to the Bank ledger, so the client's cash book is understated and the bank ledger overstated by every cash collection. A client with two bank accounts has all receipts posted to one of them, so the bank reconciliation for the other account can never tie and the first shows deposits that never hit the statement.",
   "market_standard": "Tally Prime's Receipt voucher requires the cash/bank ledger to be selected and posts to it. Zoho Books requires \"Deposit To\" on every payment received.",
   "confidence": "high",
   "suggested_fix": "Store `bank_account_id` on the receipt, resolve the debit ledger from it, and fall back to the Cash-in-Hand ledger when `payment_mode == \"cash\"`. Make the Deposit To / Cash account a required field on the receipt form.",
   "effort": "days",
   "verification": {
    "status": "confirmed",
    "by": "agent",
    "corrected_severity": "high",
    "what_is_actually_true": "Confirmed on the live path: apps/api/services/phase2_journal_service.py:210 resolves the debit ledger to a '%Bank%'/system_key='bank' account with no reference to payment_mode, and receipt_service.py:397 is the production caller, so a cash receipt is debited to Bank. bank_account_id is accepted by ReceiptIn (models/invoices.py:399), dropped from the payload (receipt_service.py:624-639), and has no column on receipts in any migration. The account resolution is worse than described: migrations/092:53-56 tags both bank AND cash accounts with system_account_key='bank' under a non-unique index, and the lookup at phase2_journal_service.py:1424-1436 does not filter by client, so the chosen ledger can be the client's cash account or another client's bank account.",
    "probe": null,
    "lens_notes": {
     "literal": "Confirmed, and the docstring that looks like a refutation does not apply. apps/api/services/phase2_journal_service.py:210 `bank_id = self._find_account(db, firm_id, client_id, \"%Bank%\", system_key=\"bank\")` is unconditional inside receipt_journal_lines; receipt[\"payment_mode\"] is never read in that function. The docstring above it (phase2_journal_service.py:198-204) says the helper is 'never in production' \u2014 that claim is about journal_for_receipt, not about receipt_journal_lines, which the PRODUCTION atomic path calls directly at services/receipt_service.py:397 (`lines = phase2_journal_service.receipt_journal_lines(db, receipt_payload, firm_id, client_id)`) before handing them to settle_receipt_atomic. payment_mode is stored (receipt_service.py:633) and never consulted. ReceiptIn.bank_account_id (models/invoices.py:399) is absent from receipt_payload (receipt_service.py:624-639) and from the foreign-receipt payload (receipt_service.py:213-223); `grep -rn bank_account_id apps/api/migrations/*.sql` shows no such column on receipts at all, so it could not be stored even if it were passed. The frontend never sends it (app/clients/[id]/sales/page.tsx:3468-3481).",
     "guards": "None, and the account resolution is looser than the finding says. _find_account's system_key branch (phase2_journal_service.py:1424-1436) filters firm_id + system_account_key + is_active with .limit(1) and no client filter and no ordering. chart_of_accounts IS client-scoped (migrations/003_phase3a_foundation.sql:101-115, client_id nullable), and migrations/092_coa_system_account_key.sql:53-56 backfills system_account_key='bank' onto every Asset account whose name or subtype matches '%bank%' OR '%cash%', under a non-unique index (092:59-61). So the 'bank' key can be on several rows \u2014 including a different client's ledger and the client's own Cash-in-Hand \u2014 and limit(1) picks arbitrarily. There is no 'cash' system key anywhere in migrations/. Rows created after migration 092 have a NULL key and fall through to the name fallback (phase2_journal_service.py:1443-1450), which IS client-scoped but matches '%Bank%' only.",
     "reach": "Production path. Every INR receipt goes through _settle_receipt_via_atomic_rpc (receipt_service.py:651), which builds its lines from receipt_journal_lines at :397."
    }
   }
  },
  {
   "id": "SALES-09",
   "kind": "gap",
   "severity": "high",
   "title": "GSTR-1 Table 11A/11B (tax on advances, \u00a713(2)) is unreachable \u2014 the three columns it needs are never written by any code path",
   "evidence": "migrations/286_advance_receipt_gst.sql:48-53 adds `receipts.gst_rate_bps`, `receipts.place_of_supply`, `receipts.is_interstate` and `clients.gst_advance_tax_applicable`. services/gst_advance_service.py:143-155 reads them and does `if rate is None or not pos: continue` for every receipt. But `ReceiptIn` (models/invoices.py:380-410) has no such fields, `create_receipt_core`'s `receipt_payload` (services/receipt_service.py:623-637) and `create_foreign_receipt`'s (line 236) omit them, and `grep -rn \"gst_rate_bps\" apps/api/services apps/api/routers` finds no writer for the receipts table. The frontend never sends them either. The only place they are ever set is the test fixture itself \u2014 tests/test_gstr1_advances_are_surfaced.py:163 seeds them straight into the DB, so the feature is green in CI and dead in production.",
   "ca_experience": "A client who supplies services and takes advances turns on \"tax liability on advance receipts\", and GSTR-1 Table 11A still comes out empty. The advance is visible only in the informational advances report (which itself reports `table_11_computed: false`, gst_advance_service.py:230), so tax on advances under \u00a713(2) is simply never declared \u2014 an under-declaration on a filed return.",
   "market_standard": "Tally Prime's \"Enable tax liability on advance receipts\" prompts for the rate and place of supply on the receipt voucher and fills Table 11A/11B; ClearTax GST picks it up from the advance ledger.",
   "confidence": "high",
   "suggested_fix": "Add `gst_rate_bps`, `place_of_supply`, `is_interstate` to `ReceiptIn` and to the receipt payload, defaulting place of supply from the customer's state; show the three fields on the receipt form only when the client's `gst_advance_tax_applicable` is true (the migration's own stated design). Then make `advances_report` report `table_11_computed: true` for those clients.",
   "effort": "days",
   "verification": {
    "status": "confirmed",
    "by": "agent",
    "corrected_severity": "high",
    "what_is_actually_true": "The Table 11A/11B path is dead end-to-end: no code writes receipts.gst_rate_bps / place_of_supply / is_interstate (only tests/test_gstr1_advances_are_surfaced.py:155,163 seeds them), AND clients.gst_advance_tax_applicable cannot be turned on from any screen or endpoint, so table_11_sections (services/gst_advance_service.py:126-139) always short-circuits to {'at': [], 'txpd': [], 'applicable': False} before it reaches the receipt loop. gst_return_service.py:1096 therefore always merges nothing into the GSTR-1 payload, silently, and the honest 'table_11_computed: false' warning at gst_advance_service.py:230 sits behind GET /api/gst-workspace/gstr1/advances (routers/gst_workspace.py:1261), which no frontend screen calls.",
    "probe": null,
    "lens_notes": {
     "literal": "Confirmed. migrations/286_advance_receipt_gst.sql:48-53 adds receipts.gst_rate_bps, receipts.place_of_supply, receipts.is_interstate (and clients.gst_advance_tax_applicable at :39-46). services/gst_advance_service.py:142-158 selects those three and `if rate is None or not pos: continue`. Nothing writes them: ReceiptIn (models/invoices.py:387-410) has no such fields, receipt_payload (services/receipt_service.py:624-639) and the foreign-receipt payload (:213-223) omit them, and the only receipt mutation endpoints are POST /, PATCH /{id}/allocate and POST /{id}/reverse (routers/receipts.py:105,146,178,203,340) \u2014 none touches them. The only assignment anywhere is the test fixture, tests/test_gstr1_advances_are_surfaced.py:155,163.",
     "guards": "The refusal exists but never reaches a CA. services/gst_advance_service.py:230 returns table_11_computed: False with an explicit 'why' string, exactly the CLAUDE.md refuse-and-name-the-gap posture \u2014 but its only endpoint, GET /api/gst-workspace/gstr1/advances (routers/gst_workspace.py:1261), is called from nowhere in apps/web (grep for '/advances' across apps/web returns no API call). table_11_sections IS wired into the real return: services/gst_return_service.py:1096 merges its 'at'/'txpd' into the GSTR-1 payload, so the section is silently omitted rather than flagged.",
     "reach": "Worse than the finding states. clients.gst_advance_tax_applicable is not settable from anywhere \u2014 `grep -rn gst_advance_tax_applicable` across apps/web returns nothing, and across apps/api it appears only in gst_advance_service.py:129-138 and the test fixture. There is no API field, no router, no UI toggle. So the CA cannot even perform the first step the ca_experience describes."
    }
   }
  },
  {
   "id": "SALES-10",
   "kind": "gap",
   "severity": "high",
   "title": "Export/SEZ invoices produce a GSTR-1 the portal will reject \u2014 shipping bill number, date and port code are hardcoded empty, and SEZ/deemed exports are filed as ordinary exports",
   "evidence": "apps/api/domain/gst/gstr1_builder.py:538-549 builds Table 6A with `\"sbpcode\": \"\"`, `\"sbnum\": \"\"`, `\"sbdt\": \"\"` \u2014 literals, because `client_sales_invoices` has no column for them (no shipping-bill columns anywhere in migrations/, and none in models/invoices.py `SalesInvoiceIn`). domain/gst/classifier.py:110-124 maps `SEZ_with_payment`/`SEZ_without_payment`/`Deemed_export` onto `EXP_WP`/`EXP_WOP`, and gstr1_builder's table map (lines 6-14) has only `6A \u2192 EXP` \u2014 there is no Table 6B (SEZ) or 6C (Deemed export) builder at all. The classifier's own comment even says `Deemed_export` \"defaults to WOPAY (conservative)\".",
   "ca_experience": "An exporter client's GSTR-1 is offered with blank shipping-bill details, so the return cannot be matched to ICEGATE for the IGST refund under \u00a716(3) and the portal flags the rows. An SEZ supply \u2014 with or without payment \u2014 is filed in Table 6A as an ordinary export, so the SEZ unit's own refund/ITC claim has nothing to match against, and a deemed export is filed as an export, which it is not.",
   "market_standard": "Tally Prime captures shipping bill no/date/port code on the export invoice and fills 6A; ClearTax and Zoho Books both separate 6A (Exports), 6B (SEZ) and 6C (Deemed exports).",
   "confidence": "high",
   "suggested_fix": "Add `shipping_bill_no`, `shipping_bill_date`, `port_code` columns and editor fields (shown when supply_type is zero_rated), feed them into `_build_exp`, and add `_build_sez` / `_build_deemed_export` with their own GSTInvoiceCategory values so 6B and 6C are emitted instead of being folded into 6A.",
   "effort": "weeks",
   "verification": {
    "status": "confirmed",
    "by": "agent",
    "corrected_severity": "high",
    "what_is_actually_true": "Both halves hold. Export rows in Table 6A carry hardcoded empty sbpcode/sbnum/sbdt (domain/gst/gstr1_builder.py:544-548; the amendment path repeats it at domain/gst/amendments.py:141) because no shipping-bill column exists anywhere in apps/api/migrations or models/invoices.py; and SEZ with/without payment and deemed exports are all mapped onto EXP_WP/EXP_WOP by domain/gst/classifier.py:102-115 and filed in 6A, since gstr1_builder has no 6B or 6C builder. The two existing tests (tests/e2e_gst_verification.py:674, tests/test_gstr1_amendments.py:146-151) assert only key presence, so they pin the blanks in place rather than catching them.",
    "probe": null,
    "lens_notes": {
     "literal": "Confirmed. apps/api/domain/gst/gstr1_builder.py:544-548 emits \"sbpcode\": \"\", \"sbnum\": \"\", \"sbdt\": \"\" as literals in _build_exp, and domain/gst/amendments.py:141 does the same for an export amendment. `grep -rn 'shipping_bill|port_code' apps/api` returns only those two emitters plus tests/e2e_gst_verification.py:674 and tests/test_gstr1_amendments.py:146-151, which assert only that the KEYS are present \u2014 there is no column on client_sales_invoices in any migration and no field on SalesInvoiceIn (models/invoices.py:130-200). domain/gst/classifier.py:102-115 folds SEZ_with_payment \u2192 EXP_WP and SEZ_without_payment / Deemed_export \u2192 EXP_WOP, and the builder's table map (gstr1_builder.py:6-14) lists only '6A \u2192 EXP' \u2014 there is no 6B or 6C builder in the module.",
     "guards": "None \u2014 the two tests that touch these fields lock in the empty strings rather than catching them. domain/gst/validator.py has no shipping-bill rule (VALID_STATE_CODES at :18-25 and the place-of-supply check at :93-99 are the only export-adjacent validations), so a 6A block with three blank shipping-bill fields passes the product's own validator.",
     "reach": "Reachable. The CA can classify an invoice as zero_rated / SEZ_with_payment / SEZ_without_payment / Deemed_export from the editor \u2014 apps/web/lib/invoices/classification.ts:51-60 defines the options and InvoiceEditor.tsx:760 renders them \u2014 and the GSTR-1 is generated from books via POST /api/gst/gstr1/from-books, called from apps/web/lib/data/gst.ts:500 and app/clients/[id]/compliance/gst/page.tsx:473."
    }
   }
  },
  {
   "id": "SALES-11",
   "kind": "missing_feature",
   "severity": "high",
   "title": "No discount field anywhere on a sales invoice \u2014 not per line, not at document level",
   "evidence": "`InvoiceLineIn` (apps/api/models/invoices.py:33-58) has description, hsn_sac, quantity, unit, rate_paise, gst_rate_percent, is_service, service_catalogue_id \u2014 and no discount. `client_sales_invoice_lines` (migrations/050_sales_purchase_cycle.sql:58-74) has no discount column, and no later migration adds one (`grep -rn \"discount\" apps/api/routers apps/api/models apps/api/services apps/api/domain/gst` returns nothing but an unrelated comment in models/inventory.py:12). The editor's line table (components/invoices/InvoiceEditor.tsx:843-851) has Product/Service, Description, HSN/SAC, Qty, Unit, Rate, GST %, Amount \u2014 no discount column.",
   "ca_experience": "A trading client who gives a 5% line discount, or a 2% cash discount on the footer, cannot record the invoice as it was actually raised. The CA has to net the discount into the rate, which loses the disclosure the customer's own copy shows and makes the invoice un-reconcilable to the client's price list. CGST \u00a715(3)(a) allows a pre-supply discount shown on the invoice to be excluded from the value of supply \u2014 with no field, the arithmetic has to be done by hand off-system.",
   "market_standard": "Tally Prime has a Discount column (percentage or amount) on every invoice line plus ledger-level discounts; Zoho Books has line discounts, document-level discount and \"discount before/after tax\"; Busy and Marg both have scheme/trade/cash discount slabs.",
   "confidence": "high",
   "suggested_fix": "Add `discount_percent_bps` and `discount_paise` to the line (with an invoice-level discount that allocates pro-rata to lines), make `taxable_amount_paise = qty \u00d7 rate \u2212 discount` in `_create_invoice_core` and in `lib/money/gstLine.ts`, regenerate `shared/gst-parity-vectors.json`, and print the discount column on the PDF.",
   "effort": "weeks",
   "verification": {
    "status": "confirmed",
    "by": "agent",
    "corrected_severity": "high",
    "what_is_actually_true": "There is no discount concept anywhere on the sales-invoice path: apps/api/models/invoices.py:33-58 (no field), apps/api/migrations/050_sales_purchase_cycle.sql:57-74 (no column), apps/api/routers/sales_invoices.py:721 (taxable = qty x rate, nothing subtracted), apps/web/components/invoices/InvoiceEditor.tsx:840-847 (no column). A trade or cash discount can only be netted into the rate by hand.",
    "probe": "cd apps/api && python -c \"from models.invoices import InvoiceLineIn; print(list(InvoiceLineIn.model_fields))\" -> ['description','hsn_sac','quantity','unit','rate_paise','gst_rate_percent','is_service','service_catalogue_id']; discount present? False",
    "lens_notes": {
     "literal": "Confirmed at every layer. apps/api/models/invoices.py:33-58 InvoiceLineIn has exactly description, hsn_sac, quantity, unit, rate_paise, gst_rate_percent, is_service, service_catalogue_id \u2014 a runtime probe of model_fields returns that list and nothing containing 'discount'. apps/api/migrations/050_sales_purchase_cycle.sql:57-74 client_sales_invoice_lines has no discount column, and `grep -rni discount apps/api/migrations/*.sql` returns exactly one hit, a prose comment at 050:155 about post-sale discounts on credit notes. `grep -rni discount` over apps/api/models, routers, services, domain returns one hit, an unrelated comment at models/inventory.py:12. The editor's line header (apps/web/components/invoices/InvoiceEditor.tsx:840-847) is Product/Service, Description, HSN/SAC, Qty, Unit, Rate, GST %, Amount \u2014 no discount column. Taxable value is computed as quantity x rate_paise with nothing subtracted (routers/sales_invoices.py:721).",
     "guards": "None. There is no DB column, no Pydantic field, no CHECK constraint and no frontend control \u2014 there is nothing to guard. The only place the word appears in the domain is CGST \u00a734 credit-note prose, which is the post-supply remedy, not the \u00a715(3)(a) pre-supply discount the finding is about.",
     "reach": "Fully reachable: the sales invoice editor is the primary create path on apps/web/app/clients/[id]/sales/page.tsx (Invoices tab), and every client sales invoice goes through _create_invoice_core."
    }
   }
  },
  {
   "id": "SALES-12",
   "kind": "missing_feature",
   "severity": "high",
   "title": "Sales invoice numbering is entirely manual; the Invoice Settings numbering series a CA configures in the UI is read by nothing on this path",
   "evidence": "apps/api/models/invoices.py:11-16 and routers/sales_invoices.py:127-134 state the decision: \"numbering is fully manual (the CA types it \u2014 no Caflow-generated scheme)\". Meanwhile routers/branding.py:43-56 exposes `PUT /api/settings/invoice-settings` with `prefix`, `include_financial_year`, `sequence_length`, `starting_number`, `manual_override_allowed`, surfaced at apps/web/app/settings/page.tsx:663 and wired at apps/web/lib/api/index.ts:2018-2020. The only reader is repositories/invoice_repository.py:149-152 \u2014 and that serves `fee_invoices` (the practice's own billing), never `client_sales_invoices`. Unattended generation is worse: services/numbering.py:18-28 `draft_placeholder_invoice_no()` returns `DRAFT-<10 hex>` and services/recurring_invoice_service.py:477 stamps that onto every generated recurring invoice.",
   "ca_experience": "For every invoice on every client the CA types the number by hand and hopes it is consecutive \u2014 Rule 46(b) requires a consecutive serial. Gaps and repeats are the CA's problem to notice; the app only rejects an exact duplicate, after the fact. Recurring invoicing, the one feature that should remove this work, hands back drafts numbered `DRAFT-A1B2C3D4E5` that must each be renumbered by hand before issue. And a CA who configures a numbering series in Settings gets no effect at all, with nothing saying so.",
   "market_standard": "Tally Prime has automatic voucher numbering with per-FY reset, prefix/suffix and a duplicate/gap warning; Zoho Books, Busy and Marg all auto-number with a configurable series per document type and per branch.",
   "confidence": "high",
   "suggested_fix": "Give `client_sales_invoices` a per-(firm, client, series, FY) sequence (the pattern `next_invoice_number`, migration 124, already uses for fee invoices), pre-fill the editor's Invoice Number from it while leaving it editable when `manual_override_allowed`, and use it for recurring generation instead of `DRAFT-\u2026`. Until then, either wire Invoice Settings to this path or remove the Settings page so it does not promise something it cannot do.",
   "effort": "weeks",
   "verification": {
    "status": "confirmed",
    "by": "agent",
    "corrected_severity": "medium",
    "what_is_actually_true": "Client sales-invoice numbering is manual by an explicit, documented decision (apps/api/routers/sales_invoices.py:131-134), and duplicates are rejected by _assert_invoice_no_available plus a partial unique index \u2014 so this is a market-standard convenience absent, not a correctness hole. What is genuinely broken is the Settings promise: /settings/invoice-settings offers prefix / FY / sequence length / starting number / manual-override, and only the first three are read, by repositories/invoice_repository.py:149-152, which serves fee_invoices; nothing on the client_sales_invoices path reads any of it, and the page says nothing about that.",
    "probe": "grep -rn \"get_invoice_settings\" apps/api --include=*.py -> routers/branding.py:188,195 and repositories/invoice_repository.py:151 only; repositories/invoice_repository.py:25 shows the table is fee_invoices",
    "lens_notes": {
     "literal": "The three factual claims hold. (1) Manual numbering: apps/api/models/invoices.py:13-16 and apps/api/routers/sales_invoices.py:131-134 both state the decision verbatim; apps/web/components/invoices/InvoiceEditor.tsx:142 initialises invoiceNo to `existing?.invoice_no ?? \"\"` and there is no suggestion endpoint on the router (grep for 'suggest' in routers/sales_invoices.py returns only the HSN/SAC suggestion routes at :356). (2) The Settings series is unread on this path: `grep -rn get_invoice_settings apps/api --include=*.py` returns routers/branding.py:188/195 and repositories/invoice_repository.py:151 only, and invoice_repository reads fee_invoices (repositories/invoice_repository.py:25), the practice's own billing \u2014 never client_sales_invoices. Only prefix / include_financial_year / sequence_length are consumed there; starting_number and manual_override_allowed are read by nothing at all. (3) services/numbering.py:28 returns DRAFT-<10 hex> and services/recurring_invoice_service.py:478 and services/billing_service.py:232 stamp it on every unattended invoice.",
     "guards": "Duplicates ARE caught, not merely 'after the fact': routers/sales_invoices.py:129-140 _assert_invoice_no_available rejects a repeat per (firm, client) backed by a partial unique index (migrations 151/209), and models/invoices.py:19 enforces the Rule 46(b) 16-character / [A-Za-z0-9-/] shape. Gaps are not detected. The DRAFT- placeholder is a documented, deliberate design (services/numbering.py:18-28): it is made obviously fake precisely so a CA must replace it, so it is not the accident the finding's tone implies.",
     "reach": "Both halves reach a user. /settings/invoice-settings is linked from apps/web/app/settings/page.tsx:663 and saves through apps/web/lib/api/index.ts:2018-2020. Recurring invoices are a live tab (apps/web/app/clients/[id]/sales/page.tsx:66, :359 POST /api/recurring-invoices/{id}/run) and the router is mounted at apps/api/main.py:315."
    }
   }
  },
  {
   "id": "SALES-13",
   "kind": "gap",
   "severity": "high",
   "title": "Firm branding, invoice templates, bank/UPI details and footer text are stored by a full Settings UI that nothing ever reads",
   "evidence": "`grep -rn \"branding\" apps/api/services apps/api/routers apps/api/domain` returns only routers/branding.py itself \u2014 no consumer. The router persists logo_url, colours, font (lines 33-41), five template types (line 25 `_VALID_TEMPLATE_TYPES`), logo position/header/footer/signature placement (lines 58-66), and bank_name / account_number / ifsc_code / upi_id / upi_qr_url / footer_text (lines 48-56). `services/invoice_pdf_service.build_invoice_pdf` imports none of it \u2014 the PDF is a fixed reportlab layout with a `#1f2937` header and no logo. The screens exist: apps/web/app/settings/page.tsx:663 and :679 link to `/settings/invoice-settings` and `/settings/invoice-templates`, with a full API client at apps/web/lib/api/index.ts:2000-2031. The email templates (`/api/settings/email-templates`) are likewise unread \u2014 services/email_service.py is not among the grep hits.",
   "ca_experience": "The CA uploads the client's logo, picks the \"Professional CA\" template, enters the client's bank account, IFSC and UPI QR for the customer to pay against, and writes footer terms \u2014 then downloads the invoice and none of it is there. The customer receives an unbranded document with no payment instructions, which is the single most common reason an SME invoice does not get paid on time.",
   "market_standard": "Zoho Books ships 16 invoice templates with logo, colour and font control plus bank details and a UPI QR block; Tally Prime has configurable print with company logo and bank details; Marg and Busy both have designer templates.",
   "confidence": "high",
   "suggested_fix": "Either wire it up \u2014 load `firm_branding` + `invoice_settings` in `get_sales_invoice_pdf`, draw the logo and accent colour, and add a Bank Details / UPI QR / footer block \u2014 or take the Settings pages down until it is. Add a test asserting the configured footer text appears in the rendered PDF, so it cannot silently detach again.",
   "effort": "weeks",
   "verification": {
    "status": "confirmed",
    "by": "agent",
    "corrected_severity": "high",
    "what_is_actually_true": "Firm branding, the five invoice templates, bank/UPI details, footer text and the email templates are persisted by routers/branding.py and read by nothing that produces output. services/invoice_pdf_service.py:140 renders a fixed reportlab layout from `firms` + invoice + customer only, so the PDF the customer receives and the email body they get are both unbranded and carry no payment instructions.",
    "probe": "grep -rn \"branding_repo\\|firm_branding\" apps/api --include=*.py | grep -v tests -> only routers/branding.py and repositories/invoice_repository.py:150-151 (fee-invoice numbering prefix/FY/length)",
    "lens_notes": {
     "literal": "Confirmed. `grep -rn branding_repo apps/api --include=*.py` (excluding tests) returns routers/branding.py:16,120-384 and repositories/invoice_repository.py:150-151 and nothing else. That single non-router consumer reads only prefix / include_financial_year / sequence_length for fee-invoice numbering. logo_url, secondary_logo_url, tagline, primary/secondary/accent colour, font_family (routers/branding.py:33-41), the five template types (:25), and bank_name / account_number / account_holder / ifsc_code / upi_id / upi_qr_url / footer_text (:43-56) are written and never read. services/invoice_pdf_service.py:140-262 build_invoice_pdf takes (invoice, firm, client, engagement) only; its firm dict comes from _load_firm (:297-307), a plain `firms` row select \u2014 it never touches firm_branding or invoice_settings. The PDF has no logo, no accent colour, no bank block, no UPI QR and no footer text. Email templates likewise: services/email_service.py hard-codes every subject and HTML body (:82-183) and is not among the branding_repo consumers.",
     "guards": "None. There is no test asserting any configured value appears in output, which is exactly why it can be detached silently.",
     "reach": "All four screens are live and linked: apps/web/app/settings/page.tsx:663 (invoice-settings), :679 (invoice-templates), :743 (email-templates), plus /settings/branding; API client at apps/web/lib/api/index.ts:2000-2041; router mounted at apps/api/main.py:398-400. The output side is equally live: GET /api/sales-invoices/{id}/pdf (routers/sales_invoices.py:1870) is called from apps/web/components/invoices/InvoiceViewDrawer.tsx:44, and the same bytes are emailed to the customer (routers/sales_invoices.py:1965-1989)."
    }
   }
  },
  {
   "id": "SALES-14",
   "kind": "gap",
   "severity": "high",
   "title": "An unallocated advance receipt can never be applied to a later invoice from any screen, and it never becomes a party credit",
   "evidence": "`PUT /api/receipts/{receipt_id}/allocations` exists and is correct \u2014 routers/receipts.py:250-330 reverses prior allocations, re-validates each against live outstanding and re-applies. `grep -rn \"/allocations\" apps/web/app apps/web/lib apps/web/components` returns nothing; the only receipt call besides create is `POST /api/receipts/{id}/reverse` (app/clients/[id]/sales/page.tsx:3204). Separately, party credit is granted ONLY from the bank-matching path \u2014 `grep -rn \"grant_credit\" apps/api` finds one caller, services/bank_posting_service.py:669 \u2014 so a manually entered over-payment sits in `receipts.unallocated_paise` and never reaches `party_credit_balances`, which is the balance the Statements tab's \"Apply credit\" flow reads (app/clients/[id]/sales/page.tsx:1000, 1029).",
   "ca_experience": "A customer pays \u20b95,00,000 on account in March against invoices not yet raised. The CA records the receipt with no allocation. In April the invoices go out and there is no way to apply that advance to them \u2014 the receipt shows Unallocated \u20b95,00,000 forever, the new invoices show fully outstanding, and the customer statement double-counts. The only route is to reverse the receipt and re-enter it, which changes the receipt number and the posting date.",
   "market_standard": "Tally's bill-wise details let you settle a new bill against an existing On-Account/Advance reference; Zoho Books shows \"Use this advance payment\" / \"Apply credits\" directly on the invoice.",
   "confidence": "high",
   "suggested_fix": "Add an \"Allocate\" action on a receipt row that opens the open-invoice list and calls `PUT /api/receipts/{id}/allocations`, and surface `unallocated_paise` as an advance on the customer statement beside the party-credit balance (or unify the two by granting a party credit when a receipt is left unallocated).",
   "effort": "days",
   "verification": {
    "status": "confirmed",
    "by": "agent",
    "corrected_severity": "high",
    "what_is_actually_true": "Correct in substance, with one part of the stated consequence wrong. PUT /api/receipts/{id}/allocations (apps/api/routers/receipts.py:250) is called from nowhere in apps/web, and no path grants a party credit outside bank matching (services/bank_posting_service.py:669), so an advance receipt can only be applied by reversing and re-keying it. But the customer STATEMENT does not double-count: services/customer_statement_service.py:295 sets ar_relief_paise = allocations + unallocated_paise, so the running balance and closing outstanding are right. What is actually wrong is per-invoice: the later invoices stay fully open, so invoice status and the AR ageing (which reads the generated outstanding_paise) overstate what is owed.",
    "probe": "grep -rn \"/allocations\" apps/web/app apps/web/lib apps/web/components -> no matches (exit 1); grep -rn grant_credit apps/api --include=*.py -> services/bank_posting_service.py:669 only",
    "lens_notes": {
     "literal": "The endpoint exists and is correct (apps/api/routers/receipts.py:250-330: caps at cash+TDS, revalidates against live outstanding, reverses priors before re-applying). No frontend call: `grep -rn \"/allocations\" apps/web/app apps/web/lib apps/web/components` exits 1 \u2014 zero hits. Allocation is only ever set at CREATE time: apps/web/app/clients/[id]/sales/page.tsx:3350-3478 (the new-receipt modal) and apps/web/components/invoices/InvoiceViewDrawer.tsx:469-477 (Record Payment, which POSTs a NEW receipt allocated to that one invoice). Party credit: `grep -rn grant_credit apps/api --include=*.py` gives one caller, services/bank_posting_service.py:669; routers/party_credits.py exposes only GET '' (:37), GET /{party_type}/{party_id} (:57) and POST /apply (:80) \u2014 there is no grant endpoint, so a manually keyed over-payment never reaches party_credit_balances.",
     "guards": "None that closes it. The receipts grid actually makes the trap visible without offering a way out: apps/web/app/clients/[id]/sales/page.tsx:3246 renders an 'Unallocated' column and :3256 an 'Unallocated only' filter, and the only row actions are reverse (:3204) and PDF.",
     "reach": "Live: the Receipts tab is one of the sales tabs (apps/web/app/clients/[id]/sales/page.tsx:63) and routers/receipts.py is mounted. This is a backend capability reachable from no screen \u2014 which per the instructions CONFIRMS the gap rather than refuting it."
    }
   }
  },
  {
   "id": "SALES-15",
   "kind": "bug",
   "severity": "medium",
   "title": "Credit notes, sales debit notes and receipts can be posted into a period whose GSTR-1 has already been filed \u2014 only invoices are guarded",
   "evidence": "`grep -rn \"period_lock_service\" apps/api/routers apps/api/services` returns only routers/sales_invoices.py:825 (create), :1252-1254 (update) and routers/purchase_bills.py:1345-1347. routers/credit_notes.py imports `period_validation_service` only (line 19) and calls `validate_posting_date` at :271 (create), :434 (update) and :506 (issue) \u2014 the FY lock, never `assert_open`. routers/sales_debit_notes.py:203, :318, :378 likewise. services/receipt_service.py:548 likewise. routers/sales_invoices.py:1478 (`issue_invoice`) also calls only `validate_posting_date`, so a draft created before a return was filed can still be issued into that filed period.",
   "ca_experience": "The CA files GSTR-1 for July, then in September posts a July-dated credit note against a July invoice. Nothing objects. July's outward supply figures in the books no longer agree with the July return that was filed, and the correction that should have gone in a later period's amendment tables (CGST \u00a737) has silently rewritten a closed one. The invoice screen refuses the same thing, so the behaviour is inconsistent and the CA has no reason to expect it.",
   "market_standard": "ClearTax and Zoho Books both lock a GST period once the return is marked filed and force corrections into an amendment. Tally Prime warns on altering a voucher in a period marked as returns-filed.",
   "confidence": "high",
   "suggested_fix": "Call `period_lock_service.assert_open(db, firm_id, client_id, date)` on credit-note create/update/issue, sales-debit-note create/update/issue, receipt create, and in `issue_invoice` (re-checking at issue, not just at create).",
   "effort": "hours",
   "verification": {
    "status": "confirmed",
    "by": "agent",
    "corrected_severity": "medium",
    "what_is_actually_true": "The filed-return lock covers exactly two document types. Everything else on the sales cycle \u2014 credit notes (routers/credit_notes.py:274,434,508), sales debit notes (routers/sales_debit_notes.py:203,318,378), receipts (services/receipt_service.py:106,548) and invoice ISSUE (routers/sales_invoices.py:1478) \u2014 checks only the financial-year lock and can post into a period whose GSTR-1 has been filed. The invoice create/update path refuses the same thing, so the behaviour is inconsistent within one screen.",
    "probe": "grep -rn \"period_lock_service|assert_open\" apps/api/routers apps/api/services -> sales_invoices.py:22,825,1252,1254 and purchase_bills.py:25,1345,1347 only",
    "lens_notes": {
     "literal": "Confirmed exactly. `grep -rn \"period_lock_service\\|assert_open\" apps/api/routers apps/api/services` returns only routers/sales_invoices.py:22,825,1252,1254 and routers/purchase_bills.py:25,1345,1347. routers/credit_notes.py imports period_validation_service alone (:19) and calls validate_posting_date at :274 (create), :434 (update), :508 (issue). routers/sales_debit_notes.py: :42, :203, :318, :378. services/receipt_service.py: :22, :106 (create), :548. routers/sales_invoices.py:1478 (issue_invoice) also calls only validate_posting_date, so a draft raised before the return was filed can still be issued into the filed period. validate_posting_date takes (firm_id, date) and knows nothing about filings \u2014 services/period_lock_service.py:1-30 says so in its own words.",
     "guards": "The GL kernel does not close it. journal_period_lock_reason (migrations 266/267) is enforced only inside edit_posted_journal / the discard and pair-delete RPCs (migrations 275:104, 276:238) \u2014 i.e. on rewriting an existing entry, not on INSERTING a new one. So a fresh credit-note journal into a filed month posts cleanly. The existing test file tests/test_documents_locked_by_filed_return.py covers only sales invoices and purchase bills (its 13 tests name si and pb; wire_e2e at :42 wires exactly those two routers).",
     "reach": "Live: Credit Notes, Debit Notes and Receipts are all tabs on apps/web/app/clients/[id]/sales/page.tsx:63 and their routers are mounted."
    }
   }
  },
  {
   "id": "SALES-16",
   "kind": "bug",
   "severity": "medium",
   "title": "Nothing checks that a reverse-charge or nil/exempt invoice actually carries zero tax, so the books and GSTR-1 disagree",
   "evidence": "`_create_invoice_core` computes GST purely from `gst_rate_bps` and `is_interstate` (routers/sales_invoices.py:719-724); `supply_type`, `invoice_type` and `is_reverse_charge` are stored (lines 887-892) and never influence the tax. On the return side, `domain/gst/gstr1_builder.py:415-430` (`_build_nil_exempt`) reports only `taxable_amount_paise` into nil_amt/expt_amt/ngsup_amt \u2014 the CGST/SGST/IGST heads on such an invoice are silently dropped. `domain/gst/validator.py:82-145` checks CGST=SGST, IGST-vs-interstate and tax arithmetic, but never supply_type-vs-tax or reverse-charge-vs-tax. The editor warns that the classification is non-standard (InvoiceEditor.tsx:777-785) but does not check the rates.",
   "ca_experience": "The CA ticks \"Exempt\" (or \"Reverse charge\") on an invoice and leaves the lines at 18%. The invoice posts CGST/SGST Output to the ledger and the customer is charged tax. GSTR-1 then declares the supply as exempt with no tax at all (or as rchrg=Y, meaning the recipient owes it). The GST payable in the books and the GST declared on the return differ by the whole amount, and nothing anywhere says so until a reconciliation is attempted.",
   "market_standard": "Tally Prime refuses to save a voucher whose GST classification and tax ledgers conflict and shows it under \"Incomplete/Mismatch in information\"; ClearTax's GSTR-1 validation flags a nil/exempt row carrying tax.",
   "confidence": "high",
   "suggested_fix": "In `_create_invoice_core` and the credit/debit-note equivalents, reject (or require an explicit override on) an invoice where `supply_type in (nil_rated, exempt, non_gst)` or `is_reverse_charge` is true and any line's `gst_rate_bps > 0`. Add the same check to `GSTValidator.validate_invoice` so a legacy row is caught before the return is built.",
   "effort": "hours",
   "verification": {
    "status": "confirmed",
    "by": "agent",
    "corrected_severity": "medium",
    "what_is_actually_true": "Tax is computed from gst_rate_bps and is_interstate alone; supply_type and is_reverse_charge are stored and never consulted (apps/api/routers/sales_invoices.py:719-727, 884-886). A nil/exempt/non-GST invoice carrying an 18% line posts CGST/SGST output to the ledger, and GSTR-1 table 8 (_build_nil, domain/gst/gstr1_builder.py:396-440) reports only the taxable value \u2014 the tax heads silently disappear. Nothing in domain/gst/validator.py or the DB constraints catches the contradiction.",
    "probe": "cd apps/api && python -c \"import inspect,routers.sales_invoices as si,domain.gst.gstr1_builder as b; print(inspect.signature(si._compute_line_gst)); print(any(k in inspect.getsource(b._build_nil) for k in ('cgst','sgst','igst')))\" -> (taxable_paise:int, gst_rate_bps:int, is_interstate:bool); False",
    "lens_notes": {
     "literal": "Confirmed by probe. _compute_line_gst has signature (taxable_paise, gst_rate_bps, is_interstate) \u2014 supply_type and reverse charge are not parameters (apps/api/routers/sales_invoices.py:725-727). Inside _create_invoice_core the strings 'supply_type' and 'is_reverse_charge' appear only as payload keys being stored (:884,:886), never in a condition. The return side: the function is _build_nil (apps/api/domain/gst/gstr1_builder.py:396, not '_build_nil_exempt' as cited \u2014 the line range 415-430 is inside it) and its source contains no reference to cgst/sgst/igst at all, so tax on a nil/exempt/non-GST invoice is dropped from the payload. apps/api/domain/gst/validator.py:82-145 checks CGST=SGST, IGST-vs-interstate and the rate arithmetic, and never compares supply_type or reverse charge against the tax heads.",
     "guards": "Migration 268:37-42 constrains the ENUM of supply_type and invoice_type but adds no relationship to the tax columns. The editor's only defence is presentational and says so in its own comment: apps/web/components/invoices/InvoiceEditor.tsx:777-786 warns that the classification is non-standard, 'Presentational only', and never inspects the line rates. No test asserts the invariant.",
     "reach": "Live: the Supply type dropdown (InvoiceEditor.tsx:746) and the Reverse charge checkbox (:768) sit on the create/edit form and are sent as classification payload at :480/:497."
    }
   }
  },
  {
   "id": "SALES-17",
   "kind": "bug",
   "severity": "medium",
   "title": "The e-way bill threshold is tested against taxable value instead of consignment value including tax, so invoices over \u20b950,000 are advised as \"not required\"",
   "evidence": "apps/web/lib/invoices/compliance.ts:22 `export const EWAY_THRESHOLD_PAISE = 5_000_000;` and lines 168-170: `if (inv.taxable_amount_paise < EWAY_THRESHOLD_PAISE) warnings.push(\"Taxable value is below \u20b950,000 \u2014 an E-Way Bill is usually not required.\")`. `compInv.taxable_amount_paise` is fed from `invoice.taxable_amount_paise` (components/invoices/CompliancePanel.tsx:52), i.e. the value BEFORE GST. Rule 138(1) Explanation 2 defines consignment value as the \u00a715 value declared in the invoice INCLUDING central/State/integrated tax and cess. Confirmed by tests/compliance.test.ts:54-55, which asserts the below-threshold behaviour against the taxable value.",
   "ca_experience": "A goods invoice with \u20b948,000 taxable and \u20b98,640 IGST is a \u20b956,640 consignment and needs an e-way bill. The Compliance panel tells the CA it is \"below \u20b950,000 \u2014 usually not required\". If the CA relies on it, the goods move without an e-way bill; \u00a7129 detention and a penalty of the tax plus an equal amount follow.",
   "market_standard": "Tally Prime and Zoho Books both test the invoice total (value including tax) against \u20b950,000 and prompt for e-way bill details at that point.",
   "confidence": "high",
   "suggested_fix": "Compare `total_paise` (or taxable + all tax heads, excluding exempt lines) against the threshold, and cite Rule 138(1) Explanation 2 in the message. Move the rule to the backend \u2014 see SALES-18.",
   "effort": "hours",
   "verification": {
    "status": "confirmed",
    "by": "agent",
    "corrected_severity": "medium",
    "what_is_actually_true": "The e-way threshold is tested against the pre-tax taxable value (apps/web/lib/invoices/compliance.ts:167-169, fed from CompliancePanel.tsx:52), not the tax-inclusive consignment value Rule 138(1) Explanation 2 defines. For a goods invoice between roughly \u20b942,373 and \u20b950,000 taxable at 18%, the panel advises 'usually not required' on a consignment that is over \u20b950,000. It is advisory wording rather than a hard block, which is why this is medium and not critical.",
    "probe": null,
    "lens_notes": {
     "literal": "Confirmed verbatim. apps/web/lib/invoices/compliance.ts:22 `export const EWAY_THRESHOLD_PAISE = 5_000_000;` and :167-169 `if (inv.taxable_amount_paise < EWAY_THRESHOLD_PAISE) warnings.push(\"Taxable value is below \u20b950,000 \u2014 an E-Way Bill is usually not required.\")`. The ComplianceInvoice interface (:33-43) carries taxable_amount_paise and no total, and apps/web/components/invoices/CompliancePanel.tsx:52 feeds it straight from invoice.taxable_amount_paise, the pre-GST figure stored by routers/sales_invoices.py:888. Rule 138(1) Explanation 2 defines consignment value as the \u00a715 value declared in the invoice INCLUDING central/State/integrated tax and cess, so the comparison is against the wrong base. apps/web/lib/invoices/compliance.test.ts:54-56 pins the behaviour to the taxable value.",
     "guards": "None anywhere. The backend never re-checks: apps/api/routers/eway_bill.py has no threshold logic at all (its only raises are a 404 scope check at :34 and 500s), and CreateEWayBillRequest (:38-52) simply accepts a caller-supplied taxable_value_paise >= 0. domain/income_tax/eway_service.py records; it does not validate.",
     "reach": "Live: CompliancePanel is rendered inside InvoiceViewDrawer (apps/web/components/invoices/InvoiceViewDrawer.tsx:35,361), which the sales page opens at apps/web/app/clients/[id]/sales/page.tsx:1839. The message is an advisory warning, not a blocker (the finding does not claim otherwise) \u2014 the CA can still generate."
    }
   }
  },
  {
   "id": "SALES-18",
   "kind": "security",
   "severity": "medium",
   "title": "Statutory rules live in TypeScript with no backend counterpart, against the house rule \"zero business logic in the frontend\"",
   "evidence": "apps/web/lib/invoices/compliance.ts is a 296-line statutory module: the Rule 138 \u20b950,000 threshold (line 22), Rule 48(4) IRN eligibility including the B2B/export/SEZ scope test (lines 141-160), e-way eligibility (163-178), place-of-supply validation with its own valid-state-code range (lines 13-18, 97-118) and a THIRD GSTIN regex (line 20) \u2014 distinct both from apps/web/lib/gst/gstin.ts (which is pinned to the backend by apps/api/tests/fixtures/gstin.json) and from apps/api/domain/gst/gstin.py. `grep -rn \"irn_eligib|eway_eligib|EWAY_THRESHOLD|Rule 138\" apps/api` finds nothing but a docstring in routers/eway_bill.py:2. So these rules exist in exactly one place and it is the browser bundle, with no parity test and no server-side enforcement. SALES-17 is what that costs.",
   "ca_experience": "Not visible directly, but it is why SALES-17 exists and stayed: a wrong threshold in a TypeScript file has no Python twin to disagree with it and no parity vector to fail. Every future change to an IRN or e-way rule has to be made by someone who thinks to look in apps/web.",
   "market_standard": "Not a market comparison \u2014 this is the repo's own stated rule (CLAUDE.md, \"Code rules\": computation, validation and statutory rules live in apps/api), which the money layer (lib/money/gstLine.ts pinned by shared/gst-parity-vectors.json) already honours properly.",
   "confidence": "high",
   "suggested_fix": "Move `irnEligibility` / `ewayEligibility` / `validatePlaceOfSupply` into `apps/api/domain/gst/` behind an endpoint the panel calls, or \u2014 if a live preview is wanted \u2014 keep the mirror but pin it with a generated fixture the way `shared/gst-parity-vectors.json` pins the GST arithmetic. Collapse the third GSTIN regex onto lib/gst/gstin.ts.",
   "effort": "days",
   "verification": {
    "status": "confirmed",
    "by": "agent",
    "corrected_severity": "low",
    "what_is_actually_true": "The Rule 138 threshold and the Rule 48(4) IRN eligibility test do live only in apps/web/lib/invoices/compliance.ts with no Python counterpart and no parity vector, which is a real breach of the CLAUDE.md 'statutory rules live in apps/api' rule and is why SALES-17 went unnoticed. But the 'security' framing does not hold \u2014 nothing on the server is gated on these rules (apps/api/routers/eway_bill.py is record-only), so there is no bypass \u2014 and two of the cited duplications are false alarms: the state-code range is identical to domain/gst/validator.VALID_STATE_CODES, and the GSTIN regex is byte-equivalent to the two it is said to differ from.",
    "probe": "cd apps/api && python -c \"from domain.gst.validator import VALID_STATE_CODES; print(sorted(VALID_STATE_CODES))\" -> ['01'..'38','96','97'] (40 codes), matching compliance.ts:13-18 exactly",
    "lens_notes": {
     "literal": "Partly true, and two sub-claims are overstated. TRUE: apps/web/lib/invoices/compliance.ts is 296 lines of statutory derivation, and `grep -rn \"irn_eligib|eway_eligib|EWAY_THRESHOLD|Rule 138\" apps/api` finds only prose \u2014 routers/eway_bill.py:2 and domain/income_tax/eway_service.py:2 \u2014 so the Rule 138 threshold (:22) and the Rule 48(4) IRN scope test (:141-160) genuinely have no Python twin and no parity fixture. OVERSTATED: the 'own valid-state-code range' at :13-18 is (n 1-38) or 96 or 97, which is EXACTLY domain/gst/validator.VALID_STATE_CODES \u2014 a runtime probe prints the same 40 codes \u2014 so it is a duplicate that agrees, not a divergence. And the 'THIRD GSTIN regex' at :20 is byte-equivalent to lib/gst/gstin.ts:28 and domain/gst/gstin.py:60 modulo redundant {1} quantifiers; the repo in fact carries six copies of that same shape (also lib/imports/mappers.ts:18, domain/gst/validator.py:14, models/client.py:33). Shape-only is also the CLAUDE.md-recorded position everywhere a GSTIN is not being typed by a human, and compliance.ts uses it to decide B2B-vs-B2C, not to validate input.",
     "guards": "No parity test and no server-side twin \u2014 that part stands. But there is nothing server-side to bypass either: apps/api/routers/eway_bill.py and routers/einvoice.py only RECORD what a human generated on the NIC/IRP portal, so no backend action is gated on these rules.",
     "reach": "The module is reachable (CompliancePanel inside InvoiceViewDrawer), but everything it produces is advisory text \u2014 blockers only disable buttons in a prepare-only workflow."
    }
   }
  },
  {
   "id": "SALES-19",
   "kind": "glitch",
   "severity": "medium",
   "title": "The export/SEZ treatment is captured twice in two different vocabularies that can disagree, and the Compliance panel reads only the wrong one",
   "evidence": "The invoice carries `supply_type` + `invoice_type` (migration 268; set in components/invoices/InvoiceEditor.tsx:745-775 and read by domain/gst/classifier.py). The e-invoice record independently carries `gst_treatment` with a SEPARATE vocabulary \u2014 `regular | export_with_payment | export_without_payment | sez_with_payment | sez_without_payment | deemed_export` (apps/web/lib/invoices/compliance.ts:26-33; stored by domain/income_tax/einvoice_service.py:76-83). CompliancePanel.tsx:51-53 builds its view from `gst_treatment: irn.record?.gst_treatment ?? null` and hardcodes `is_reverse_charge: null`, ignoring `invoice.is_reverse_charge`, which is present on the same `InvoiceDetail` object (apps/web/lib/invoices/gst.ts:115). Nothing reconciles the two vocabularies.",
   "ca_experience": "The CA classifies an invoice as SEZ without payment in the editor. Opening the same invoice's Compliance panel, the GST treatment reads \"Regular \u00b7 B2B \u00b7 Inter-state (IGST)\" until they pick the treatment a second time while preparing the IRN \u2014 and even then the two records can permanently disagree, with GSTR-1 following one and the e-invoice record the other. Reverse charge never shows on the panel at all.",
   "market_standard": "Tally Prime holds one \"Nature of transaction\" per voucher that drives the return, the e-invoice JSON and the print alike; there is no second, independent classification.",
   "confidence": "high",
   "suggested_fix": "Derive `gst_treatment` from the invoice's `supply_type` + `invoice_type` when preparing an IRN record (pre-fill and disable the picker), and pass `invoice.is_reverse_charge` into `compInv` instead of `null`. Longer term, drop `einvoice_records.gst_treatment` as an independent field.",
   "effort": "days",
   "verification": {
    "status": "confirmed",
    "by": "agent",
    "corrected_severity": "medium",
    "what_is_actually_true": "Two independent classifications exist and the invoice's own one is never consulted by the Compliance panel: CompliancePanel.tsx:50-51 hardcodes is_reverse_charge to null and takes gst_treatment solely from the e-invoice record, so a SEZ/export invoice reads \"Regular \u00b7 B2B \u00b7 Inter-state (IGST)\" (CompliancePanel.tsx:142) and the Prepare-IRN picker defaults to \"regular\" (CompliancePanel.tsx:289). Contained to display and to e-invoice record metadata \u2014 einvoice_service.py:84-86 keeps gst_treatment out of all tax/journal math and no payload builder reads it \u2014 so it is a wrong statutory label shown to a CA, not a wrong figure in a return.",
    "probe": "grep -n 'reverseCharge' apps/web/components/invoices/CompliancePanel.tsx -> no matches (the field is computed at compliance.ts:86 and never rendered); grep -n 'supply_type|invoice_type' apps/web/components/invoices/InvoiceViewDrawer.tsx -> no matches.",
    "lens_notes": {
     "literal": "Every cited line checks out. apps/web/components/invoices/CompliancePanel.tsx:46-53 builds compInv with `is_reverse_charge: null` (line 50) and `gst_treatment: irn.record?.gst_treatment ?? null` (line 51) \u2014 it never reads invoice.supply_type / invoice.invoice_type, which ARE on the same object (apps/web/lib/invoices/gst.ts:109-115). compliance.ts:75 does `inv.gst_treatment ?? \"regular\"`, so with no e-invoice record the panel's \"GST treatment\" row (CompliancePanel.tsx:142) renders \"Regular \u00b7 B2B \u00b7 Inter-state (IGST)\" for an invoice the CA classified SEZ_without_payment. PrepareIrnModal seeds its picker `useState<GstTreatment>(\"regular\")` (CompliancePanel.tsx:289) with no pre-fill from the invoice. The two vocabularies are genuinely separate: migration 268 adds supply_type/invoice_type/is_reverse_charge to client_sales_invoices; migration 177 adds the independent gst_treatment CHECK to einvoice_records; nothing reconciles them (grep gst_treatment across apps/api returns only routers/einvoice.py, migration 177, domain/income_tax/einvoice_service.py and its tests).",
     "guards": "None found. domain/gst/classifier.py:91-110 branches only on supply_type/invoice_type (the migration-268 pair), so GSTR-1 is driven by the editor's values and is unaffected by gst_treatment \u2014 but that is the divergence, not a guard against it. einvoice_service.py:84-86 states gst_treatment is 'metadata on this record only \u2014 never fed into tax/journal math', which BOUNDS the blast radius: it reaches no return, no journal and no generated payload (there is no e-invoice JSON builder reading it). One correction to the finding: `reverseCharge` is computed at compliance.ts:86 but is never rendered anywhere in CompliancePanel.tsx, so the hardcoded null has no display consequence today \u2014 the honest statement is that reverse charge is not shown, not that the null corrupts a shown value.",
     "reach": "Reaches a real user. CompliancePanel is rendered by InvoiceViewDrawer.tsx:361, the drawer opened from the sales invoice list; routers/einvoice.py is mounted at apps/api/main.py:297. Worse than the finding says: InvoiceViewDrawer.tsx contains no reference to supply_type/invoice_type at all (grep is empty), so the panel's derived row is the ONLY place the invoice's GST character is shown after issue \u2014 and it shows the wrong one."
    }
   }
  },
  {
   "id": "SALES-20",
   "kind": "missing_feature",
   "severity": "medium",
   "title": "No GST compensation cess on a sales line",
   "evidence": "`client_sales_invoice_lines` (migrations/050_sales_purchase_cycle.sql:58-74, plus every later ALTER \u2014 none adds one) has no cess column, and `InvoiceLineIn` (models/invoices.py:33-58) has no cess field. The GSTR-1 builder already expects one and says so: domain/gst/gstr1_builder.py:634-636 \u2014 \"Latent today \u2014 client_sales_invoice_lines has no cess column, so line cess is always 0 \u2014 but wrong the moment one is added.\" `_compute_line_gst` (routers/sales_invoices.py:167-193) returns only CGST/SGST/IGST.",
   "ca_experience": "A client dealing in aerated drinks, tobacco, coal, or motor vehicles cannot raise a compliant invoice at all \u2014 the cess simply cannot be charged. Their GSTR-1 `csamt` is zero on every row and GSTR-3B under-declares the cess liability. The CA has to keep those clients on Tally.",
   "market_standard": "Tally Prime, Zoho Books, Busy and Marg all support compensation cess (ad valorem and per-unit) as a separate head on the line.",
   "confidence": "high",
   "suggested_fix": "Add `cess_rate_bps` and `cess_paise` to the line and `cess_paise` to the header, include it in `_compute_line_gst`, in `journal_for_sales_invoice` (a Cess Output ledger), in the mirror `lib/money/gstLine.ts` and in the parity vectors. The GSTR-1 builder already reads `inv.cess_paise` / `line.cess_paise`.",
   "effort": "weeks",
   "verification": {
    "status": "confirmed",
    "by": "agent",
    "corrected_severity": "medium",
    "what_is_actually_true": "There is no compensation-cess head anywhere in the sales cycle \u2014 no column (only migrations 050 and 184 ever touch client_sales_invoice_lines), no model field (models/invoices.py:33-58), no computation (routers/sales_invoices.py:167-193), no UI. GSTR-1 csamt and GSTR-3B cess are therefore structurally zero for outward supplies (gst_return_service.py:301, :632). Affected population is narrow but real (pan masala/tobacco, aerated waters, coal, motor vehicles), so medium is right.",
    "probe": "grep -rn 'client_sales_invoice_lines' apps/api/migrations/*.sql | grep -iE 'alter|create table' -> only 050:58 (CREATE), 050:78 (RLS), 184:25 (service_catalogue_id). No cess column.",
    "lens_notes": {
     "literal": "Confirmed. The only DDL ever executed against the sales line table is migrations/050_sales_purchase_cycle.sql:58 (CREATE) and migrations/184_sales_invoice_line_service_catalogue_link.sql:25 (ALTER, adds service_catalogue_id) \u2014 grep of every ALTER/CREATE naming client_sales_invoice_lines returns exactly those two, so no cess column exists; nor does one exist on the header (no migration adds cess_paise to client_sales_invoices). models/invoices.py:33-58 InvoiceLineIn has no cess field, and routers/sales_invoices.py:167-193 _compute_line_gst returns only (cgst, sgst, igst). The GSTR-1 builder's own comment at domain/gst/gstr1_builder.py:634-636 says exactly what the finding quotes, and services/gst_return_service.py:275 repeats it ('None of the three line tables has a cess column') then hardcodes cess_paise=0 at line 301.",
     "guards": "None \u2014 the reverse. gst_return_service.py:632 reads `inv.get(\"cess_paise\") or 0` from a column that does not exist on client_sales_invoices, so it silently resolves to 0 and GSTR-1/3B declare csamt = 0 for every outward supply. migrations/181_firm_hsn_rate_history.sql:31 already stores an optional `cess_rate_pct` per HSN, so the rate can be recorded in the firm library and then has nowhere to be applied. The transactions table (migration 036:15,24) has cess_paise on both header and line \u2014 but that is the legacy GST-engine table, not the live sales cycle.",
     "reach": "The sales invoice path is the live one (routers/sales_invoices.py mounted, the Sales tab is the product's invoicing screen), so a CA billing cess goods has no way to charge cess. Frontend confirms: grep 'cess' over apps/web/components/invoices/InvoiceEditor.tsx and lib/money/gstLine.ts returns nothing."
    }
   }
  },
  {
   "id": "SALES-21",
   "kind": "missing_feature",
   "severity": "medium",
   "title": "No quotation, proforma invoice, sales order or delivery challan \u2014 the sales cycle starts at the tax invoice",
   "evidence": "`grep -rln \"quotation|proforma|delivery_challan|sales_order|estimate\" apps/api/routers apps/api/services apps/api/domain apps/api/models apps/api/migrations` returns only unrelated hits (task templates, payroll, income tax, workflow, inventory). The Sales page's tabs are Invoices, Recurring, Customers, Receipts, Credit Notes, Debit Notes, Statements (apps/web/app/clients/[id]/sales/page.tsx:235-275). Nothing precedes the invoice.",
   "ca_experience": "A trading or manufacturing client who sends goods on a delivery challan and invoices later, or who quotes before selling, cannot record either step. The CA either keeps a parallel book in Excel or keeps the client on Tally, which defeats the one-platform promise for exactly the SME segment being targeted.",
   "market_standard": "Tally Prime: Quotation, Sales Order, Delivery Note, Rejections In. Zoho Books: Estimates, Retainer Invoices, Sales Orders, Delivery Challans (with the GST delivery-challan format under Rule 55). Busy and Marg both have the full order-to-invoice chain.",
   "confidence": "high",
   "suggested_fix": "Start with the delivery challan (Rule 55 \u2014 it is a statutory document with a prescribed format and it feeds e-way bills for movement without an invoice) and the quotation/estimate with a one-click convert-to-invoice. Sales orders can follow once inventory commitment is wanted.",
   "effort": "weeks",
   "verification": {
    "status": "confirmed",
    "by": "agent",
    "corrected_severity": "medium",
    "what_is_actually_true": "No quotation, proforma, sales order or delivery challan exists in schema, API or UI \u2014 zero migration hits for any of those document names, and the Sales tab list at apps/web/app/clients/[id]/sales/page.tsx:64-71 starts at the tax invoice. The e-way bill record additionally hard-requires an invoice number (CompliancePanel.tsx:99-106), so Rule 55 movement-without-invoice has no path either.",
    "probe": "grep -rniE '\\\\b(quotation|proforma|pro forma|delivery challan|delivery note|sales order)\\\\b' apps/api/migrations/*.sql -> no output (0 matches).",
    "lens_notes": {
     "literal": "Confirmed. `grep -rniE '\\b(quotation|proforma|pro forma|delivery challan|delivery note|sales order)\\b' apps/api/migrations/*.sql` returns ZERO rows \u2014 there is no table for any pre-invoice document. The earlier auditor's broader grep hits are all unrelated (payroll 'estimate', workflow 'quotation' as a task-template word). apps/web/app/clients/[id]/sales/page.tsx:64-71 defines TABS as exactly Sales Invoices, Recurring, Customers, Receipts, Credit Notes, Debit Notes, Statements, rendered at :237-275. Nothing precedes the tax invoice.",
     "guards": "None. A draft invoice is the nearest substitute but it is a tax invoice in draft, not a Rule 55 delivery challan, and it consumes an invoice number the CA types. The e-way bill record path requires an invoice_number (CompliancePanel.tsx:99-106 posts invoice_number: invoice.invoice_no), so movement of goods without an invoice \u2014 the exact case Rule 55 exists for \u2014 cannot be recorded at all.",
     "reach": "Sales is a primary client workspace tab and is the only sales surface, so the absence is what a CA meets."
    }
   }
  },
  {
   "id": "SALES-22",
   "kind": "missing_feature",
   "severity": "medium",
   "title": "No HSN/SAC master is exposed \u2014 every firm builds its code library from zero before it can raise its first invoice",
   "evidence": "routers/firm_hsn_library.py:1-13: \"Caflow does not ship or expose a shared HSN/SAC master to users. Every code a firm bills against comes from THIS table\". routers/hsn.py:3-11 confirms the search endpoint \"used to merge in public.hsn_master ... It no longer does\". migrations/175_hsn_master_official_expansion.sql:15-23 says the loaded backbone is 2-digit HSN chapters and 4-digit SAC groups plus some 6-digit SAC leaves \u2014 \"The full 8-digit HSN leaf catalogue (~12k rows) is a large dataset best refreshed out-of-band\". The dependency chain is enforced: every invoice line must reference a service_catalogue item (models/invoices.py:106-112, a hard 422), and the catalogue item's HSN comes from the firm library.",
   "ca_experience": "On a new firm, the first invoice fails three times in a row \u2014 no catalogue item, then no HSN in the library, then the code has to be typed with its rate from memory or from the CBIC site. For a practice with 50 SME clients across trading, manufacturing and services, that is hundreds of codes entered by hand before any invoicing happens. It is the single biggest friction at first use and it lands before the product has proved anything.",
   "market_standard": "Tally Prime, Zoho Books, Busy and Marg all ship the full HSN/SAC master with GST rates and let you search it while typing a line; ClearTax has an HSN/rate finder. None asks the user to build the list.",
   "confidence": "high",
   "suggested_fix": "Load the full CBIC HSN leaf catalogue and the SAC annexure into `hsn_master` (both are Government of India publications in the public domain, as migration 175 itself notes) and let the firm library's search offer them as \"add from master\" \u2014 keeping the firm's own curated list as the billing source, but seeding it in one click instead of one code at a time.",
   "effort": "weeks",
   "verification": {
    "status": "confirmed",
    "by": "agent",
    "corrected_severity": "medium",
    "what_is_actually_true": "No shared HSN/SAC master is exposed \u2014 hsn_master holds only the chapter/group backbone (migration 175:13-23) and neither routers/hsn.py nor routers/firm_hsn_library.py reads it; every billable code comes from the firm's own library. The dependency chain is real (models/invoices.py:104-111 hard-422 on service_catalogue_id; routers/service_catalogue.py:330 requires the HSN be in the firm library). Correction to the ca_experience: HSN itself is optional (routers/service_catalogue.py:55-58), so only the catalogue item is an unconditional blocker, and bulk CSV seeding already exists (firm_hsn_library.py:185; onboarding step 3 at apps/web/app/onboarding/page.tsx:105-133) \u2014 the friction is sourcing the data, not entering it one row at a time.",
    "probe": "grep -n '@router.' apps/api/routers/firm_hsn_library.py -> GET /, POST /, POST /bulk, PATCH, DELETE, DELETE /purge, POST /bulk-delete (a bulk import path exists).",
    "lens_notes": {
     "literal": "The core claim is exactly right. routers/firm_hsn_library.py:1-15 and routers/hsn.py:3-11 both state the shared master is not exposed and that hsn.py reads only firm_hsn_library; migrations/175_hsn_master_official_expansion.sql:13-23 confirms only 2-digit HSN chapters and 4-digit SAC groups (plus some 6-digit SAC leaves) are loaded, and that the ~12k 8-digit leaf catalogue is not. The catalogue dependency is real and is a hard 422: models/invoices.py:104-111 `require_service_catalogue_id` rejects any line without a service_catalogue_id, with a message whose own comment (models/invoices.py:95-102) admits 'Walking a full year through the API found three such prerequisites stacked (catalogue, then the firm HSN library behind it), each discovered by failing the next.' routers/service_catalogue.py:330 and :649 enforce that a catalogue item's hsn_sac must be an active row in firm_hsn_library.",
     "guards": "Two the finding omits, both of which soften the 'fails three times' story without removing the gap. (1) HSN is OPTIONAL at every level: routers/service_catalogue.py:55-58 '_hsn_in_library ... A blank hsn_code is always allowed', and models/invoices.py:35 hsn_sac is Optional \u2014 so only the CATALOGUE ITEM is a hard blocker; the HSN library blocks only once the CA supplies a code (which Rule 46(g) requires above the turnover thresholds, so it bites in practice). (2) Bulk seeding exists: routers/firm_hsn_library.py:185 POST /bulk, a CSV import screen at apps/web/app/settings/firm-hsn-library/page.tsx:204, and onboarding step 3 of 3 is 'HSN/SAC Library' (apps/web/app/onboarding/page.tsx:105-133) with a template and an outbound link to the GST portal's own search page. So the library can be loaded in one CSV rather than one code at a time \u2014 the CA just has to source the CSV.",
     "reach": "Reaches every new firm: the onboarding wizard's third step is the library import, and the invoice line 422 is the first wall otherwise. The omission is a recorded product decision (apps/web/app/onboarding/page.tsx:111-124 gives the licensing rationale and cites docs/HSN_SAC_REDESIGN.md), but it is recorded in code comments/docs, not as a statutory decision in CLAUDE.md, so it is a legitimate product-gap finding."
    }
   }
  },
  {
   "id": "SALES-23",
   "kind": "gap",
   "severity": "medium",
   "title": "No automated payment-reminder cadence to customers \u2014 the automatic run was removed and only a manual, one-invoice-at-a-time reminder remains",
   "evidence": "services/collections_service.py:297-304 records the removal: \"enabled/interval_days/max_reminders governed the automatic bulk cadence run (run_due_reminders), removed entirely: it looped every open invoice unbounded ... The manual, CA-initiated single-invoice send (send_invoice_reminder, below) is the only way a reminder goes out now\". `send_overdue_reminders` (line 247) still runs in the nightly sweep but only writes a timeline entry (lines 276-283) \u2014 it sends the customer nothing. The manual path is `POST /api/sales-invoices/{id}/remind` (routers/sales_invoices.py:2093), driven one row at a time from the UI (app/clients/[id]/sales/page.tsx:1655-1662). Delivery is email only (services/email_service.send_invoice_to_customer); nothing in the sales path touches WhatsApp.",
   "ca_experience": "The owner's stated goal is to replace WhatsApp follow-ups. Today the CA still has to open each overdue invoice and click Remind, one by one, on a schedule they keep in their own head \u2014 which is the WhatsApp workflow with more clicks. There is no cadence (7/15/30 days), no escalation, no bulk send, and no channel the SME's customers actually read.",
   "market_standard": "Zoho Books has configurable automated payment reminders (before due, on due, and repeating after due) with per-customer opt-out; Zoho and Refrens both offer WhatsApp reminder delivery in India.",
   "confidence": "high",
   "suggested_fix": "Rebuild the cadence bounded and resumable \u2014 a per-firm daily batch with a row cap and a cursor (the pattern `_paginate_all` and the bank \"Pass N ready\" chunking already use), keyed on `last_reminded_at` + `REMINDER_INTERVAL_DAYS`, sending the customer-facing reminder rather than a timeline entry. Add bulk Remind over the DataTable selection as an interim step (bulk Issue already exists at page.tsx:1607).",
   "effort": "days",
   "verification": {
    "status": "confirmed",
    "by": "agent",
    "corrected_severity": "medium",
    "what_is_actually_true": "There is no automated customer-facing payment-reminder cadence: the bulk run was deleted (services/collections_service.py:296-304) and the only surviving customer reminder is the manual one-invoice POST /api/sales-invoices/{id}/remind (routers/sales_invoices.py:2103), clicked per row at apps/web/app/clients/[id]/sales/page.tsx:1643-1666, email only, with no bulk action (bulk Issue exists at :1609 but there is no bulk Remind). The nightly send_overdue_reminders is the practice's own fee sweep, not a customer reminder (collections_service.py:247-256, :294-296).",
    "probe": "grep -rn 'send_overdue_reminders|run_due_reminders' apps/api --include=*.py -> scheduler.py:213-215 and billing.py:174 call the practice-fee sweep; run_due_reminders has no definition left anywhere.",
    "lens_notes": {
     "literal": "Confirmed, with one mischaracterisation to correct. services/collections_service.py:296-304 records the removal of run_due_reminders verbatim as quoted. The manual path is real: routers/sales_invoices.py:2103 POST /{invoice_id}/remind, driven one row at a time from apps/web/app/clients/[id]/sales/page.tsx:1643-1666 (openRemind/remindInvoice). CORRECTION: send_overdue_reminders (collections_service.py:247) is NOT the customer-facing sweep at all \u2014 it reads `_open_invoices(firm_id, get_internal_client_id(firm_id))`, i.e. the PRACTICE's own fee invoices, and the file says so at :294-296 ('the practice's internal fee-collections sweep (timeline-only, Partner dashboard)'). So it is not a degraded customer reminder; there is simply no automatic customer reminder of any kind.",
     "guards": "None that restores a cadence. jobs/scheduler.py:213-215 runs only send_overdue_reminders (the practice-fees sweep). collections_service.py:305 REMINDER_DEFAULTS is down to {'attach_pdf': True} \u2014 the enabled/interval_days/max_reminders knobs are gone. The removal itself was deliberate and reasoned (unbounded loop starving the nightly sweep), which is why the fix is 'rebuild it bounded', not 'revert'.",
     "reach": "The manual reminder reaches users (sales page Remind action, email only via services/email_service.send_invoice_to_customer). WhatsApp: there IS a composer at apps/web/app/notifications/whatsapp/page.tsx with a 'payment_reminder' template (line 85) \u2014 but it builds a wa.me deep link (line 149) for the CA's own CLIENTS via ClientLookup, not for a client's customers, and it is not wired to any sales invoice. So the finding's 'nothing in the sales path touches WhatsApp' holds."
    }
   }
  },
  {
   "id": "SALES-24",
   "kind": "glitch",
   "severity": "medium",
   "title": "Receipt and credit-note numbers take their financial year from today's date, not the document date",
   "evidence": "services/receipt_service.py:56-61 `_current_fy()` reads `datetime.now(timezone.utc)` and is used at line 549 (`fy = _current_fy()`) to build `receipt_no = f\"RCPT-{fy}-{seq:04d}\"` (line 621) \u2014 `data[\"receipt_date\"]` is never consulted. routers/credit_notes.py:104-109 and :270 are identical for `CN-{fy}-\u2026`, as is routers/sales_debit_notes.py:81-86 for `SDN-{fy}-\u2026`. The FY-lock and period checks correctly use the document date (services/receipt_service.py:548), so only the NUMBER is wrong.",
   "ca_experience": "Year-end data entry is when a CA touches the most receipts. Every March-dated receipt keyed in April is numbered `RCPT-2627-\u2026` and sits in next year's series, out of order in the FY it belongs to. The same for credit notes. The CA discovers it when printing a year's receipt register and the numbers do not run.",
   "market_standard": "Tally Prime numbers a voucher from the voucher date's financial year, not the entry date; so do Zoho Books and Busy.",
   "confidence": "high",
   "suggested_fix": "Derive the FY label from the document's own date (`core.ist_clock` already has the helpers) in all three `_current_fy()` call sites. Note this changes numbering for back-dated documents going forward only \u2014 existing numbers stay as issued.",
   "effort": "hours",
   "verification": {
    "status": "confirmed",
    "by": "agent",
    "corrected_severity": "medium",
    "what_is_actually_true": "All three auto-numbered sales documents take their FY prefix from the wall clock rather than the document date, and a probe proves it: a receipt dated 2026-03-31 created today (2026-09-07) is numbered RCPT-2627-0001. Only the NUMBER is affected \u2014 the posting-date lock at receipt_service.py:548 correctly uses receipt_date. The pattern is wider than the finding says: purchase_payment_service.py:171/:459, purchase_credit_notes.py:212 and debit_notes.py:206 do the same, and all six use a UTC clock rather than core.ist_clock, adding a 00:00-05:30 IST off-by-one at the 1 April boundary.",
    "probe": "cd apps/api && python -c \"import services.receipt_service as rs; r=rs.create_receipt_core('firm-1',{'client_id':'client-1','customer_id':'cust-1','receipt_date':'2026-03-31','amount_paise':100000,'payment_mode':'bank','allocations':[]},{'id':'u1','firm_id':'firm-1','role':'Partner'},None); print(r['receipt_no'], r['receipt_date'])\" -> 'RCPT-2627-0001 | 2026-03-31'.",
    "lens_notes": {
     "literal": "Confirmed by execution. services/receipt_service.py:56-61 _current_fy() reads datetime.now(timezone.utc); it is called at :550 immediately after the period check at :548 which correctly uses data['receipt_date'], and the number is built at :621 as RCPT-{fy}-{seq:04d}. routers/credit_notes.py:106-111 / :276 and routers/sales_debit_notes.py:81-86 are byte-identical in shape.",
     "guards": "None. The FY-lock (period_validation_service.validate_posting_date) uses the document date, so it neither catches nor prevents this \u2014 it only proves the two facts are available side by side. Note also that _current_fy uses UTC, not IST: between 00:00 and 05:30 IST the clock is still on the previous day, so on 1 April the very first receipts of a new FY get last year's series. CLAUDE.md names core.ist_clock as the authority for FY labels; ist_fy_label(date(2026,3,31)) correctly returns '2025-26'.",
     "reach": "Every receipt, credit note and sales debit note the product numbers. The SAME pattern is in the purchase cycle: services/purchase_payment_service.py:30 used at :171 and :459, routers/purchase_credit_notes.py:89 used at :212, routers/debit_notes.py:74 used at :206 \u2014 six auto-numbered document series in total, not three."
    }
   }
  },
  {
   "id": "SALES-25",
   "kind": "gap",
   "severity": "medium",
   "title": "No warning when a credit note is issued outside the \u00a734(2) window, and no customer credit limit at all",
   "evidence": "Two absences in the same area. (a) `issue_credit_note` (routers/credit_notes.py:464-660) checks the FY lock and the invoice's outstanding but never the \u00a734(2) time limit; `compliance_engine.correction_window_closes()` exists and is the stated authority (CLAUDE.md; services/compliance_engine.py:196) but `grep -rn \"correction_window\" apps/api/routers` shows it is used only by the filing-demo flows, never by credit notes. (b) `customers` (migrations/049 \u2192 the CREATE TABLE at lines 4-27) has `credit_days` but no `credit_limit_paise`; migration 030 gave suppliers one (`030_supplier_tds_and_credit.sql:11,25`) and customers never got the equivalent. Nothing in `_create_invoice_core` checks a customer's exposure.",
   "ca_experience": "(a) The CA issues a credit note in February 2027 against an FY 2025-26 invoice and the app accepts it silently. The receivable and the books reduce, but the GST liability cannot be reduced \u2014 the window closed on 30 November 2026 or on the date GSTR-9 was furnished, whichever was earlier \u2014 so the client pays tax on a supply that was credited. (b) A customer already \u20b915,00,000 overdue can be invoiced again with nothing said.",
   "market_standard": "ClearTax and Zoho Books both warn when a credit note falls outside the amendment window. Tally Prime enforces a per-ledger Credit Limit and blocks or warns on a sales voucher that breaches it; Zoho Books shows a credit-limit warning on invoice creation.",
   "confidence": "high",
   "suggested_fix": "(a) On credit-note issue, compare the note date with `correction_window_closes(fy_end_of_original_invoice)` and warn (not block \u2014 the commercial credit is still valid, only the tax adjustment is not), recording that the GST reduction is unavailable. (b) Add `customers.credit_limit_paise` and check it in `_create_invoice_core` against the customer's live outstanding, as a warning by default and a block when the firm turns that on.",
   "effort": "days",
   "verification": {
    "status": "confirmed",
    "by": "agent",
    "corrected_severity": "medium",
    "what_is_actually_true": "(a) Neither creating nor issuing a credit note tests the \u00a734(2) window \u2014 routers/credit_notes.py:464-560 checks only status, the FY lock and the invoice outstanding \u2014 even though compliance_engine.correction_window_closes (services/compliance_engine.py:196) and domain/gst/correction_window.py already exist and are used by gst_amendment_service.py:101,110. (b) customers has no credit_limit_paise (migrations/049:4-27, never ALTERed); the only credit_limit_paise columns are on suppliers and clients (migration 030:11,25) and neither is read by any backend code \u2014 the supplier one is display-only in apps/web/app/accounting/suppliers/page.tsx.",
    "probe": "grep -rn 'credit_limit' apps/api --include=*.py -> no output (no backend code reads any credit limit); grep -rn 'correction_window' apps/api --include=*.py -> filing_demo/*, compliance_engine.py, correction_window.py, gst_amendment_service.py only \u2014 never credit_notes.py.",
    "lens_notes": {
     "literal": "Both halves confirmed. (a) routers/credit_notes.py:464-560 issue_credit_note checks status, the FY lock (:507 validate_posting_date) and the invoice's net outstanding (:544-551) \u2014 there is no \u00a734(2)/\u00a737(3) date test, and the CREATE path (:275-277) likewise has only validate_posting_date. (b) grep 'credit_limit' across apps/api/migrations returns only 030_supplier_tds_and_credit.sql:11 (suppliers.credit_limit_paise) and :25 (clients.credit_limit_paise \u2014 the CA's client, not a customer); the customers CREATE TABLE at migrations/049:4-27 has credit_days and no limit, and no later ALTER adds one. Nothing in the invoice create path reads any exposure figure.",
     "guards": "The window machinery exists and is applied elsewhere, which sharpens rather than refutes: services/compliance_engine.py:196 correction_window_closes and domain/gst/correction_window.py are wired into services/gst_amendment_service.py:101,110 (2A/2B amendment proposals) and the filing-demo flows \u2014 but never into credit notes, so the function a fix would call is already written and tested. On (b) there is no guard at all: even the supplier limit that does exist is stored-only \u2014 grep credit_limit over apps/api *.py returns nothing, so no backend ever enforces it; only apps/web/app/accounting/suppliers/page.tsx reads it, for display.",
     "reach": "Credit notes are a live Sales tab (apps/web/app/clients/[id]/sales/page.tsx:69) and issue posts a real journal via phase2_journal_service.journal_for_credit_note, so a late note reduces the books and lands in the CDNR table of whatever period it is dated in. Customer creation/invoice creation are the primary sales paths."
    }
   }
  },
  {
   "id": "SALES-26",
   "kind": "ux",
   "severity": "medium",
   "title": "Nowhere in the sales screens can a CA see what a single invoice still owes",
   "evidence": "The invoice list's columns are Invoice No, Date, Customer, Taxable, GST, Total, Due, Status (apps/web/app/clients/[id]/sales/page.tsx:1752-1778) \u2014 there is no balance/outstanding column, and the list query (line 1372) does not even select `credited_paise` or `debit_note_paise`. The receipt allocation screen shows the invoice TOTAL, not the outstanding: page.tsx:3606 `{inv.invoice_no} \u2014 {inv.invoice_date} \u2014 {fmtAmt(invoiceDisplayTotal(inv))}`, with the helper's own comment at :3418 admitting \"(invoice total, not outstanding)\". There is no auto-allocate-oldest-first control. Migration 278 added an `outstanding_paise` generated column precisely so this would be cheap, and no sales screen reads it.",
   "ca_experience": "Collections is the daily job and the CA cannot do it from this screen. To answer \"how much does this invoice still owe?\" they open the invoice detail. To allocate a \u20b92,00,000 receipt across four partly-paid invoices they must open each one first to find the balance, then type four numbers, with the screen showing them the full totals the whole time \u2014 which is how an over-allocation gets attempted and 422s.",
   "market_standard": "Tally's Bills Outstanding shows pending amount per bill and auto-adjusts against the oldest; Zoho Books' invoice list has a Balance Due column and Record Payment pre-fills the balance per invoice with an \"apply to oldest\" option.",
   "confidence": "high",
   "suggested_fix": "Select `outstanding_paise` (migration 278) and add a Balance column to the list, sortable. In the receipt allocator show the outstanding per invoice, pre-fill down to the receipt amount oldest-first with a one-click \"Auto-allocate\", and base the Unallocated line on amount + TDS \u2212 allocated.",
   "effort": "days",
   "verification": {
    "status": "confirmed",
    "by": "agent",
    "corrected_severity": "medium",
    "what_is_actually_true": "The title is wrong \u2014 Outstanding IS shown on the invoice detail drawer (InvoiceViewDrawer.tsx:245, pre-filled into Record Payment at :451) and per-invoice in reports/ageing (page.tsx:637-643). What is genuinely missing is a Balance column on the invoice list (columns at sales/page.tsx:1752-1778; query at :1372 omits credited_paise/outstanding_paise) and any outstanding figure or oldest-first fill in the multi-invoice receipt allocator (:3397-3400 selects only total_paise; :3606 displays the total; helper comment at :3417). The fix has a working template in the same codebase: components/banking/SettleDocumentsModal.tsx:108-126,172.",
    "probe": "grep -rn 'outstanding_paise' apps/web --include=*.tsx -> hits in banking/SettleDocumentsModal, banking/FindMatchModal, lookups/CustomerLookup, reports/ageing \u2014 and none in app/clients/[id]/sales/page.tsx.",
    "lens_notes": {
     "literal": "The body is right, the title is overstated. Columns at apps/web/app/clients/[id]/sales/page.tsx:1752-1778 are Invoice No, Date, Customer, Taxable, GST, Total, Due, Status \u2014 no balance column; the list query at :1372 selects paid_paise but not credited_paise, debit_note_paise or outstanding_paise. The receipt allocator's per-invoice query (:3397-3400) selects total_paise and no settlement fields at all, so it cannot show a balance, and :3606 renders invoiceDisplayTotal(inv) with the helper's own comment at :3417 admitting '(invoice total, not outstanding)'. There is no auto-allocate control. Migration 278:70-78 does add outstanding_paise as a generated column on client_sales_invoices.",
     "guards": "Three that narrow the impact. (1) The invoice DETAIL drawer \u2014 opened from this same list \u2014 computes and shows Outstanding (components/invoices/InvoiceViewDrawer.tsx:182, :245) and RecordPaymentModal pre-fills it (:451) and refuses to exceed it (:464). (2) Per-document outstanding already has a screen: apps/web/app/clients/[id]/reports/ageing/page.tsx:637-643 lists invoice_no with outstanding_paise, linked from reports/page.tsx:66, backed by /api/customers/ar-aging. (3) The oldest-first pattern already exists in the product \u2014 components/banking/SettleDocumentsModal.tsx:108-126 shows per-document outstanding and :172 fills `Math.min(doc.outstanding_paise, remainingBefore)` down the list. Server-side, receipt_service.py:612-620 raises a 422 'allocation would exceed invoice outstanding', so an over-allocation is caught, just without telling the CA the number.",
     "reach": "Both screens are live Sales tabs. So the true gap is not 'nowhere' \u2014 it is that the invoice LIST and the receipt ALLOCATOR, the two multi-invoice screens where collections actually happens, are the two that omit it, while the single-invoice drawer and a separate reports page have it."
    }
   }
  },
  {
   "id": "SALES-27",
   "kind": "bug",
   "severity": "medium",
   "title": "A line with no HSN/SAC silently prints 998211 \u2014 the CA-services SAC \u2014 on any invoice, including a goods invoice",
   "evidence": "apps/api/services/invoice_pdf_service.py:29 `SAC_CODE = \"998211\"` (\"professional/CA services\", per the module docstring at line 11) and line 206 `line.get(\"hsn_sac\") or SAC_CODE` \u2014 the fallback applies to every line of every sales invoice, not just the legacy fee-invoice path. `client_sales_invoice_lines.hsn_sac` is nullable (migrations/050_sales_purchase_cycle.sql:63) and `InvoiceLineIn.hsn_sac` is `Optional[str] = None` (models/invoices.py:35), so a blank is easy to produce; the editor treats HSN as an optional cell (components/invoices/InvoiceEditor.tsx:888-899) and `isValidLine` does not require it (lib/invoices/gst.ts:333-339).",
   "ca_experience": "A trading client's invoice for steel pipes prints SAC 998211 \u2014 \"Financial auditing services\" \u2014 as the classification, because a line was saved without a code. The customer's GSTR-2B match fails on HSN, and the printed document misclassifies the supply under Rule 46(g). Nothing on screen ever showed the CA a code they did not enter.",
   "market_standard": "Tally Prime blocks the voucher (\"HSN/SAC not specified\") and lists it under Incomplete/Mismatch; Zoho Books warns before saving. None substitutes a default code.",
   "confidence": "high",
   "suggested_fix": "Delete the fallback in the sales path \u2014 print the cell blank so the omission is visible \u2014 and require `hsn_sac` on a line at issue time (a draft may still be incomplete). Keep `SAC_CODE` only for the legacy engagement-based fee invoice, which genuinely has no lines.",
   "effort": "hours",
   "verification": {
    "status": "confirmed",
    "by": "agent",
    "corrected_severity": "medium",
    "what_is_actually_true": "A sales-invoice line saved without an HSN/SAC prints \"998211\" \u2014 the CA-services SAC \u2014 in the SAC column of the customer-facing tax invoice PDF, on the goods path as well as the services one (apps/api/services/invoice_pdf_service.py:210, SAC_CODE at :29). Nothing in the model, the DB, the editor or issue_invoice requires the code, and the only warning about it lives in the invoice view drawer's IRN-eligibility block (apps/web/lib/invoices/compliance.ts:126-129).",
    "probe": "cd apps/api && venv/bin/python -c \"from reportlab import rl_config; rl_config.pageCompression=0; from services.invoice_pdf_service import build_invoice_pdf; ...two lines, one with hsn_sac=None ('MS Steel Pipes 50mm'), one with '998311'...\" printed: \"prints 998211 for the blank-HSN goods line: True / prints 998311 for the coded line: True\". Also `venv/bin/python -c \"from models.invoices import SalesInvoiceIn; ...\"` printed \"hsn_sac on the line: None\" \u2014 the model accepts the omission.",
    "lens_notes": {
     "literal": "Confirmed, and settled by running it. apps/api/services/invoice_pdf_service.py:29 defines SAC_CODE = \"998211\" (docstring line 8 calls it \"professional/CA services\"), and line 210 renders `line.get(\"hsn_sac\") or SAC_CODE` for EVERY line of the line-item table, not only the legacy no-lines branch at line 219. A blank or NULL hsn_sac is falsy, so it takes the fallback. Schema and model both allow blank: apps/api/migrations/050_sales_purchase_cycle.sql:62 `hsn_sac TEXT` (nullable), apps/api/models/invoices.py:36 `hsn_sac: Optional[str] = None` with no validator (the model's only model_validator, models/invoices.py:104-111, requires service_catalogue_id, not hsn_sac).",
     "guards": "No guard blocks it. service_catalogue.hsn_sac is itself optional (apps/api/migrations/176_service_catalogue.sql:26 '-- default SAC/HSN (optional; free-text on the line)'), so the mandatory Product/Service link (migration 206 / models/invoices.py:104) does not imply an HSN. The editor cell is optional free text with placeholder \"Set HSN/SAC\" (apps/web/components/invoices/InvoiceEditor.tsx:892-899) and isValidLine does not require it (apps/web/lib/invoices/gst.ts:331-337). The one soft guard the finding missed: apps/web/lib/invoices/compliance.ts:126-129 raises a severity-'warning' issue \"One or more lines are missing an HSN/SAC code\" \u2014 but it is only surfaced through irnEligibility's warnings list (compliance.ts:157-160), i.e. inside the 'Prepare IRN' eligibility block of the view drawer, not in the editor and not on the PDF path. Nothing blocks issuing, sending or printing.",
     "reach": "Real. get_sales_invoice_pdf (invoice_pdf_service.py:309-357) feeds every client_sales_invoice line into build_invoice_pdf, and it is reached by the mounted download route (apps/api/routers/sales_invoices.py:1870-1893 GET /{invoice_id}/pdf), the email-to-customer path (_do_send_invoice, routers/sales_invoices.py:1896-1903), the collections chaser (services/collections_service.py:347) and the client portal (routers/portal_data.py:52). So the wrong code goes out on the document the customer receives."
    }
   }
  },
  {
   "id": "SALES-28",
   "kind": "gap",
   "severity": "medium",
   "title": "E-invoice and e-way bill are record-keeping ledgers only \u2014 no JSON is produced, no applicability is determined, no validity or expiry is tracked",
   "evidence": "domain/income_tax/einvoice_service.py:41-53 `ManualProvider.generate_irn` returns all-None; the module stores irn/ack_number/ack_date/qr_data typed in after the fact (lines 145-170). There is no e-invoice schema builder anywhere \u2014 `grep` finds no IRN JSON assembly. Nothing computes applicability: no AATO threshold check exists for e-invoicing, and the UI only advises \"E-invoicing applies only above your firm's turnover threshold \u2014 confirm before generating\" (apps/web/lib/invoices/compliance.ts:158). For e-way bills, `ewb_valid_upto` is typed in by the CA (routers/eway_bill.py:59 `ewb_valid_upto: str`) \u2014 Rule 138(10)'s one-day-per-200-km validity is never computed even though `distance_km` is captured (line 50), and nothing alerts before expiry. There is no /eway page at all; `grep -rn \"eway\" apps/web/app` finds only the audit-log label.",
   "ca_experience": "The CA still generates every IRN and every e-way bill by re-keying the whole invoice into the IRP/NIC portal, then re-keys the result back here. The platform saves no data entry on the two highest-volume GST compliance actions, and it will not tell the CA which clients have crossed the e-invoicing threshold or which e-way bills expire tomorrow.",
   "market_standard": "Tally Prime and Zoho Books generate the e-invoice JSON and the EWB JSON from the voucher for offline bulk upload even without a GSP, compute EWB validity from distance, and flag e-invoice applicability from the client's AATO. The bulk-generation tools on the government portals need no registration.",
   "confidence": "high",
   "suggested_fix": "Two steps that need no GSP and break no rule in CLAUDE.md: (1) build the IRP e-invoice JSON and the EWB JSON from the invoice for download and manual upload, which is a pure prepare-only path exactly like the existing GSTR-1 JSON; (2) compute `ewb_valid_upto` from `distance_km` per Rule 138(10) and surface expiring bills on the deadlines view. Applicability from AATO is a separate, larger piece.",
   "effort": "weeks",
   "verification": {
    "status": "confirmed",
    "by": "agent",
    "corrected_severity": "medium",
    "what_is_actually_true": "Confirmed as a gap, with one evidence correction: e-invoice and e-way are reachable from the invoice drawer's Compliance panel (apps/web/components/invoices/CompliancePanel.tsx:104-124), not absent from the UI \u2014 but they are record-keeping only. No IRP or EWB JSON is generated anywhere, no AATO applicability is computed, ewb_valid_upto is typed by the CA (apps/api/routers/eway_bill.py:59), distance_km is stored and never read (domain/income_tax/eway_service.py:70,89,115), and nothing sweeps for expiring bills.",
    "probe": "grep -rn 'SellerDtls|BuyerDtls|ItemList|einvoice_json|ewb_json' over apps/ \u2192 no matches outside node_modules; grep -rn 'distance_km' --include=*.py apps/api \u2192 5 hits, all in the request model, the create call and the stored dict (routers/eway_bill.py:50,94; domain/income_tax/eway_service.py:70,89,115), zero readers.",
    "lens_notes": {
     "literal": "The substantive claims hold. apps/api/domain/income_tax/einvoice_service.py:45-46 ManualProvider.generate_irn returns {irn: None, ack_number: None, ack_date: None, qr_data: None}; the module only stores what a human types back. A repo-wide grep for IRP schema assembly (SellerDtls / BuyerDtls / ItemList / einvoice_json / ewb_json) returns nothing outside node_modules \u2014 no e-invoice or EWB JSON is built anywhere. No AATO applicability exists: the only turnover figure in the codebase is aggregate_turnover_paise, passed per-request into the GSTR-1 builder for the HSN-digit rule (apps/api/domain/gst/gstr1_builder.py:102-111), never an e-invoicing threshold; the UI's only statement is the advisory string at apps/web/lib/invoices/compliance.ts:160. For e-way, ewb_valid_upto is a typed-in string on the request model (apps/api/routers/eway_bill.py:59) stored verbatim (domain/income_tax/eway_service.py:137,147), and distance_km is captured (routers/eway_bill.py:50) and then only stored (eway_service.py:70,89,115) \u2014 grep shows zero other readers, so Rule 138(10) validity is never computed.",
     "guards": "None, and none is claimed to be. The docs record the registration gates honestly (docs/compliance/02-gst.md:175-205, and the scoped TODO(compliance) markers at routers/einvoice.py:7-15 and routers/eway_bill.py:4-10) but nothing there says offline JSON generation or distance-based validity was considered and declined \u2014 so this is not a decision CLAUDE.md or docs/compliance already records as deliberate. No scheduler job or deadline sweep touches e-way expiry (no ewb/eway reader in apps/api/jobs/).",
     "reach": "Reachable, and one piece of the finding's evidence is wrong. There is no /eway page, but the e-way and e-invoice workflows ARE reachable from the invoice itself: apps/web/components/invoices/CompliancePanel.tsx (prepareEway at :104-115, recordEway at :116-124, IRN actions at :78-102), rendered by InvoiceViewDrawer.tsx:361, which is opened from app/clients/[id]/sales/page.tsx:1839. So the workflow is not unreachable \u2014 it is present and purely clerical. Note the panel's prepareEway does not even send distance_km, so the one field Rule 138(10) needs is never collected in the UI."
    }
   }
  },
  {
   "id": "SALES-29",
   "kind": "gap",
   "severity": "medium",
   "title": "An invoice can be created and issued with no place of supply, and only the GSTR-1 build discovers it months later",
   "evidence": "routers/sales_invoices.py:672 `effective_supply_state = supply_state_code or customer.get(\"state_code\") or \"\"` \u2014 an empty string is written to `client_sales_invoices.supply_state_code` (line 878) with no validation, and `issue_invoice` (line 1433) does not check it either. `SalesInvoiceIn.supply_state_code` is `Optional[str] = None` (models/invoices.py:136) with no state-code validator, and the editor's Supply State picker has no required marker (components/invoices/InvoiceEditor.tsx:713-716). It becomes an error only when the return is built: domain/gst/validator.py:93-99 rejects a place of supply not in `VALID_STATE_CODES`. The frontend has a `validatePlaceOfSupply` (lib/invoices/compliance.ts:97-118) but it runs only in the Compliance panel, never in the editor's `validateInvoiceEditor` (lib/invoices/gst.ts:341-364).",
   "ca_experience": "Invoices go out all quarter with a blank place of supply \u2014 easy, since it is only auto-filled when the customer has a state code, and SALES-06 means several states cannot have one. On the 10th of the following month the CA builds GSTR-1 and gets a list of errors for invoices that are already issued, posted and sent to customers, each of which now needs a correction that cannot be a silent edit (CGST \u00a734).",
   "market_standard": "Tally Prime will not accept a GST sales voucher without Place of Supply; Zoho Books requires it on every invoice for a GST-registered organisation.",
   "confidence": "high",
   "suggested_fix": "Require a valid 2-digit place of supply at issue time (not at draft), server-side in `issue_invoice` and mirrored in `validateInvoiceEditor` so the CA is stopped at the point of entry; default it from `customer.state_code` and, failing that, from `customer.gstin[:2]`.",
   "effort": "hours",
   "verification": {
    "status": "confirmed",
    "by": "agent",
    "corrected_severity": "medium",
    "what_is_actually_true": "An invoice can be created, issued and posted with supply_state_code = '' \u2014 no Pydantic validator (models/invoices.py:136), no DB CHECK (migrations/050_sales_purchase_cycle.sql:18), no check in issue_invoice (routers/sales_invoices.py:1432ff) \u2014 and it first becomes a hard error at GSTR-1 build (domain/gst/validator.py:92-99). Correction to the finding: the editor already falls back to customer.gstin[:2] (InvoiceEditor.tsx:48-51), and the invoice drawer's Compliance panel does show a red 'Place of supply is missing' after issue (CompliancePanel.tsx:57,148-152), so the blind spot is narrower than 'discovered months later' \u2014 it is B2C/unregistered customers with no state_code, plus any CA who never opens that panel.",
    "probe": "cd apps/api && venv/bin/python -c \"from models.invoices import SalesInvoiceIn; m=SalesInvoiceIn(client_id='c',customer_id='cu',invoice_no='INV-1',invoice_date='2026-05-01',place_of_supply='ZZ',supply_state_code='',lines=[{'description':'x','rate_paise':100,'service_catalogue_id':'s1'}]); print(repr(m.supply_state_code))\" printed \"''\" \u2014 the empty string is accepted unvalidated.",
    "lens_notes": {
     "literal": "Confirmed, with the cited line numbers shifted. The resolution is apps/api/routers/sales_invoices.py:683 `effective_supply_state = supply_state_code or customer.get(\"state_code\") or \"\"` (the finding says 672), read from data at :627, and written to client_sales_invoices.supply_state_code at :879 (and :839 on the mock branch). issue_invoice (routers/sales_invoices.py:1432-1500) validates the FY lock and posts the journal but never looks at supply_state_code. models/invoices.py:136 `supply_state_code: Optional[str] = None` has no state-code validator, and migrations/050_sales_purchase_cycle.sql:18 declares the column bare TEXT with no CHECK. It becomes an error only in domain/gst/validator.py:92-99, where a place of supply outside VALID_STATE_CODES is a GSTR-1 error.",
     "guards": "Two partial guards, neither blocking. (1) The editor auto-fills from the customer INCLUDING the gstin[:2] fallback the finding proposes \u2014 apps/web/components/invoices/InvoiceEditor.tsx:48-51 customerStateCode() and :322-327 \u2014 so a GST-registered customer is covered. It is a B2C/unregistered customer with no state_code that leaves it blank, and the picker at :713-716 carries no required marker. (2) The invoice view drawer DOES surface it after the fact: CompliancePanel.tsx:57 runs validatePlaceOfSupply and renders its severity-'error' messages at :148-152, so 'only the GSTR-1 build discovers it' overstates \u2014 the drawer shows a red 'Place of supply is missing.' if the CA opens it. Nothing stops entry, issue, posting or sending.",
     "reach": "Real. The create path is the mounted POST on the sales invoice router used by the editor (InvoiceEditor.tsx:472,493 send supply_state_code) and by the CSV importer (lib/invoices/importMapping.ts), and the invoice is posted to the GL at issue with a blank place of supply, after which the document is frozen (\u00a734 correction, not a silent edit)."
    }
   }
  },
  {
   "id": "SALES-30",
   "kind": "data_integrity",
   "severity": "low",
   "title": "A credit note's is_interstate can be inherited from another client's invoice in the same firm",
   "evidence": "routers/credit_notes.py:251-262: the lookup for the linked invoice filters on `.eq(\"id\", data[\"sales_invoice_id\"]).eq(\"firm_id\", firm_id)` \u2014 client_id is not in the filter, unlike the equivalent read at issue time (line 528, which does filter `.eq(\"client_id\", client_id)` and 422s). So at CREATE the note can silently take `is_interstate` from an invoice belonging to a different client of the same firm; the mismatch only surfaces when the CA tries to issue it.",
   "ca_experience": "Rare, and it fails safe at issue. But the draft's computed CGST/SGST-vs-IGST split can be wrong from the moment it is created, so a CA reviewing the draft sees the wrong tax heads and a wrong total before the eventual refusal.",
   "market_standard": "Not a market-comparison item \u2014 it is an internal scoping inconsistency against the same router's own later check.",
   "confidence": "high",
   "suggested_fix": "Add `.eq(\"client_id\", client_id)` to the create-path invoice lookup and 422 when it misses, matching the issue path.",
   "effort": "hours",
   "verification": {
    "status": "confirmed",
    "by": "agent",
    "corrected_severity": "low",
    "what_is_actually_true": "True and correctly rated low: the credit-note CREATE path inherits is_interstate from an invoice matched on firm_id alone (routers/credit_notes.py:256-263), while the ISSUE path for the same link demands client_id and 422s (:528-532). It fails safe at issue but can produce a draft with the wrong tax heads, and it is inconsistent with the create-time party-ownership checks the same codebase applies to receipts and payments (tests/test_r227_party_ownership_guard.py:83-202).",
    "probe": null,
    "lens_notes": {
     "literal": "Confirmed; line numbers are 256-263, not 251-262. apps/api/routers/credit_notes.py:256-263 selects client_sales_invoices filtered on `.eq(\"id\", data[\"sales_invoice_id\"]).eq(\"firm_id\", firm_id)` only, and :264-265 copies is_interstate off whatever it finds. The issue path at :528-532 filters `.eq(\"firm_id\", firm_id).eq(\"client_id\", client_id)` and raises 422 'Linked invoice is not part of this client's books.'",
     "guards": "No create-path guard. The pattern exists elsewhere in the same codebase \u2014 receipts and purchase payments validate party ownership at CREATE and are pinned by tests (apps/api/tests/test_r227_party_ownership_guard.py:83-202) \u2014 but that file has no credit-note case, and no test covers a cross-client sales_invoice_id at credit-note creation. The DB has no cross-column constraint tying credit_notes.client_id to the linked invoice's client_id.",
     "reach": "Requires a hand-made API call or a client switch mid-session: the UI picks the invoice from a client-scoped list. Same-firm only, so it is not a tenancy hole \u2014 the firm filter is intact. Impact is a draft whose CGST/SGST-vs-IGST split may be computed from the wrong invoice and which then cannot be issued (422 at :532)."
    }
   }
  },
  {
   "id": "SALES-31",
   "kind": "glitch",
   "severity": "low",
   "title": "SalesInvoiceIn.place_of_supply is accepted by the API and silently ignored in production",
   "evidence": "models/invoices.py:134 declares `place_of_supply: Optional[str] = None  # 2-digit state code`. In `_create_invoice_core` it is read ONLY on the mock branch (routers/sales_invoices.py:660-663, alongside a hardcoded `client_state_code = \"27\"`); the real branch at line 672 uses `supply_state_code or customer.state_code` and never looks at it. So the two branches decide intra- vs inter-state by different rules. The frontend and the CSV importer both send `supply_state_code` (InvoiceEditor.tsx:494; lib/invoices/importMapping.ts:237), so nothing in-product is broken today \u2014 but any integration reading the model would be.",
   "ca_experience": "Not visible today. It matters to whoever writes the next integration or bulk-loader: they set the documented place of supply, the field is dropped, the customer master's state is used instead, and an invoice is taxed as intra-state that should be inter-state, with no error.",
   "market_standard": "n/a \u2014 API hygiene.",
   "confidence": "high",
   "suggested_fix": "Either fold `place_of_supply` into the real-branch resolution (`supply_state_code or place_of_supply or customer.state_code`) or delete the field from the model so it cannot be relied on. Aligning the mock branch's rule with the real one would also stop the two paths testing different logic.",
   "effort": "hours",
   "verification": {
    "status": "confirmed",
    "by": "agent",
    "corrected_severity": "low",
    "what_is_actually_true": "Confirmed as API hygiene: SalesInvoiceIn.place_of_supply (models/invoices.py:134) is honoured only on the mock branch (routers/sales_invoices.py:632) and silently dropped on the real branch (:683), so the two branches decide intra- vs inter-state by different rules and any future integration setting the documented field would be taxed off the customer master instead. Nothing in the product sends it today.",
    "probe": "cd apps/api && grep -n 'supply_state_code|place_of_supply' routers/sales_invoices.py \u2192 627, 631, 632, 683, 839, 879 \u2014 place_of_supply appears only at 631 (comment) and 632 (mock branch). venv/bin/python -c \"...SalesInvoiceIn(place_of_supply='ZZ'...)\" printed \"place_of_supply accepted as: 'ZZ'\" \u2014 not validated either.",
    "lens_notes": {
     "literal": "Confirmed exactly, with corrected line numbers. apps/api/models/invoices.py:134 declares `place_of_supply: Optional[str] = None  # 2-digit state code` (the finding's cite of :134 is right). In routers/sales_invoices.py the mock branch at :632 reads `place = data.get(\"place_of_supply\") or data.get(\"supply_state_code\") or \"\"` and derives interstate from it against a hardcoded client_state_code = \"27\" (:633), while the real branch derives from `effective_supply_state = supply_state_code or customer.get(\"state_code\") or \"\"` at :683 \u2014 grep for place_of_supply in that router returns exactly lines 631, 632 and nothing else, so the real branch never reads it.",
     "guards": "None. No validator on the field (it accepts arbitrary text), and no test asserts the two branches agree \u2014 indeed the mock branch is the one the ~9,900-test mock suite exercises, so the divergence is untested by construction.",
     "reach": "No in-product caller today: a repo-wide grep of apps/web for place_of_supply finds only read-side type declarations (lib/api/index.ts:155, lib/data/transactions.ts:41, lib/data/gst.ts:47) and the compliance validator's issue field names (lib/invoices/compliance.ts:102-111); the editor and the CSV importer both send supply_state_code. So the risk is entirely forward-looking, which is what 'low' means here."
    }
   }
  },
  {
   "id": "SALES-32",
   "kind": "missing_feature",
   "severity": "low",
   "title": "No TCS on sales, and the rate registry's note about \u00a7206C(1H) looks stale",
   "evidence": "domain/tds/section_rates.py:142-149 carries a `\"206C\"` entry commented \"TCS on sale of goods, Section 206C(1H) \u2014 unchanged, 0.1%\" and immediately states \"NOT wired to any computation anywhere in this codebase \u2014 confirmed zero readers ... do not assume TCS is an implemented feature because a rate exists here.\" Nothing in the sales path collects TCS: `_create_invoice_core` (routers/sales_invoices.py:640-960) has no TCS branch, `client_sales_invoices` has no TCS column, and `journal_for_sales_invoice` (services/phase2_journal_service.py:59-150) posts no TCS payable line. `_SECTIONS_2025_26` is used unchanged for FY 2026-27 (line 156).",
   "ca_experience": "For a client dealing in scrap (\u00a7206C(1)), motor vehicles over \u20b910 lakh (\u00a7206C(1F)) or overseas remittances (\u00a7206C(1G)), the invoice cannot collect TCS and there is no 27EQ data to file from. The CA keeps those clients elsewhere. Separately, a future implementer reading the \"unchanged, 0.1%\" note would build \u00a7206C(1H) collection for a provision I believe was omitted by the Finance (No. 2) Act 2024 with effect from 1 April 2025 \u2014 I could not verify that against a primary source in this session, so treat the statutory point as needing confirmation, not the code reading.",
   "market_standard": "Tally Prime, Busy and Marg all support TCS on sales with the per-buyer threshold tracking and 27EQ data; Zoho Books supports TCS on invoices.",
   "confidence": "medium",
   "suggested_fix": "Leave TCS unbuilt until it is scoped (the comment is right that it is a feature, not a data gap). Do verify the \u00a7206C(1H) status against the Finance (No. 2) Act 2024 and correct the comment either way \u2014 a wrong \"unchanged\" note in a rate registry is exactly the trap CLAUDE.md's annual-update section is written to prevent.",
   "effort": "hours",
   "verification": {
    "status": "confirmed",
    "by": "agent",
    "corrected_severity": "low",
    "what_is_actually_true": "Confirmed at low, and the statutory point the auditor could not verify IS verifiable inside this repo: docs/audits/2026-09-07-market-research/income-tax-tds-primary.md:566-582 records that \u00a7206C(1H) TCS on sale of goods ceased to operate from 01-04-2025 (with the sources disagreeing on omission vs an inapplicability proviso, and advising 'cite the effect, not the mechanism'). So section_rates.py:142's 'unchanged, 0.1%' is stale for FY 2025-26 onwards \u2014 harmless today only because the entry has zero readers.",
    "probe": "cd apps/api && grep -n '_FY_2026_27 = ' domain/tds/section_rates.py \u2192 156 (matches the finding exactly); grep -rni 'tcs' migrations/*.sql \u2192 only enum labels and 037_tds_engine.sql:140, i.e. no TCS column on client_sales_invoices; grep -n 'tcs' services/phase2_journal_service.py \u2192 no matches.",
    "lens_notes": {
     "literal": "Every code claim checks out. apps/api/domain/tds/section_rates.py:142-149 carries the '206C' entry with the comment 'TCS on sale of goods, Section 206C(1H) \u2014 unchanged, 0.1%' and the R3.1 note that it has zero readers; :156 is exactly `_FY_2026_27 = FYTDSRates(fy=\"2026-27\", verified=False, sections=_SECTIONS_2025_26)`. A case-insensitive grep for 'tcs' across apps/api/migrations returns only enum labels and the migration 037 seed row (037_tds_engine.sql:140) \u2014 client_sales_invoices has no TCS column in any migration. services/phase2_journal_service.py:59-150 posts Dr AR / Cr Sales / Cr GST output only; grep for 'tcs' in that file returns nothing. No TCS branch exists in _create_invoice_core.",
     "guards": "The absence is guarded in the honest sense \u2014 the comment itself warns not to assume TCS is implemented, and apps/web/app/risks/page.tsx:110-120 excludes 27EQ from the TDS statement list with the note '27EQ is TCS under \u00a7206C and is deliberately absent: nothing in this codebase generates it'. Counter-note: apps/api/routers/tds.py:469-472 still accepts return_type='27EQ' and migrations 014/037 allow it in the CHECK, so a 27EQ row can be created with nothing behind it \u2014 a smaller, separate hygiene point.",
     "reach": "No wrong number reaches a CA: the '206C' rate entry has no readers, so the stale comment misleads a future implementer rather than a user. The missing feature reaches a CA only for a client with \u00a7206C(1)/(1F)/(1G) collections, who cannot be served here at all."
    }
   }
  }
 ],
 "maturity": {
  "grade": "C",
  "rationale": "The transactional engine is genuinely strong and, in places, better than the market: every issue posts through the one kernel, money is integer paise end to end, receipt settlement is a single atomic RPC with CAS fallback, the frontend GST preview is pinned to the Python arithmetic by a generated fixture, tenant and client-assignment scoping is enforced on every route, and the GSTR-1 classification (supply type / invoice type / reverse charge / B2CL threshold by date) is more careful than most competitors. What is weak is everything the CA and their customer actually TOUCH. The tax invoice PDF \u2014 the deliverable, the legal document \u2014 names the CA practice as the supplier with the practice's own GSTIN, prints a hardcoded 18%/9%/9% rate regardless of the lines, states \"reverse charge: No\" unconditionally, omits quantity, unit, place of supply and the IRN/QR, and ignores every branding, template, bank-detail and footer setting the product lets a CA configure. Three market-standard essentials are simply absent: line discounts, automatic invoice numbering, and compensation cess. Several backend capabilities have no screen at all \u2014 receipt TDS, re-allocating an advance, GSTR-1 Table 11A. And one numbering defect (delete a middle draft credit note) permanently wedges a client's credit notes for the year. The gap is not depth of thinking \u2014 the code is unusually well-reasoned \u2014 it is that the last mile between a correct ledger entry and a correct document was never closed.",
  "would_a_ca_buy_this_module_alone": "Not yet \u2014 a CA would try to send the first invoice, see their own practice's GSTIN printed as the supplier on their client's sale with a hardcoded 18% tax rate and no logo, and stop there."
 },
 "open_questions_for_owner": [
  "The invoice PDF builder was written for the practice's own fee invoices (`fee_invoices`) and reused for clients' sales invoices without re-pointing the supplier block. Was the sales-invoice PDF ever printed and looked at by a CA against a real client's invoice, or has it only ever been exercised through the API and the tests? The answer changes whether SALES-01/02/03 are one afternoon's fix plus a proof-read, or a sign that the whole print path needs an owner.",
  "Sales invoice numbering was decided as 'fully manual, no generated scheme' and that decision is recorded in three places. Was it a deliberate product judgement about CAs owning their clients' series, or a deferral? The Settings \u2192 Invoice Settings screen (prefix / FY / sequence / starting number) exists, is reachable, and is read by nothing on this path \u2014 so today the product asks for a decision it then ignores. Which way do you want it resolved: build auto-numbering, or take the screen down?",
  "Who is the invoicing user you are optimising for \u2014 the CA keying a client's back-book from vouchers, or the CA's SME client raising invoices themselves through the portal? The current editor (Product/Service mandatory on every line, HSN from a library the firm must build, invoice number typed by hand) is priced for the first and would defeat the second. Discounts, auto-numbering, branding and the delivery challan are all far more urgent for the second.",
  "The firm HSN/SAC library starts empty by design, and `hsn_master` is populated but deliberately not exposed. What was the reasoning against shipping the CBIC master (a public-domain dataset) as a seedable source? If it was rate accuracy, note that the library's stored rate is already a pre-fill hint that never touches tax math \u2014 so seeding descriptions and codes alone would remove most of the onboarding friction without asserting a rate.",
  "Automatic customer dunning was removed because the unbounded loop could starve the nightly sweep. Do you want it rebuilt bounded, and if so on what channel? Email-only reminders will not replace WhatsApp follow-ups, which is the stated goal \u2014 and a WhatsApp channel is a commercial integration (BSP, template approval), not a coding task, so it needs deciding before the cadence is designed around it.",
  "Compensation cess, TCS, discounts, delivery challans and quotations each unlock a specific client type (FMCG/coal/auto, scrap dealers, traders, manufacturers). Which of those client types are actually in the target book? Migration 286 and the AA analysis suggest the live book is seven clients, all companies/LLPs/firms on current accounts \u2014 if the real segment is professional-services SMEs, discounts and auto-numbering matter far more than cess and TCS, and the ranking above should change.",
  "Two independent classifications of the same export/SEZ fact now exist (`supply_type`+`invoice_type` on the invoice, `gst_treatment` on the e-invoice record). Which one do you want to be the single truth? Collapsing them is cheap now and expensive after either has real data behind it.",
  "The e-invoice and e-way modules today only record what a human did on a portal. Building the IRP/EWB JSON for offline bulk upload needs no GSP, no registration and breaks none of the never-auto-submit rules \u2014 is that deliberately out of scope, or simply not reached yet?"
 ],
 "strengths": [
  "One posting kernel, honoured without exception. Issue, cancel, credit note, sales debit note and receipt all route through `services/phase2_journal_service` \u2014 routers/sales_invoices.py:1489 posts the journal FIRST and only then flips the status, so a Chart-of-Accounts failure leaves a re-tryable draft rather than an issued-but-unposted invoice; cancellation reverses through the kernel (routers/sales_invoices.py:1622) and never rewrites the original.",
  "Receipt settlement is genuinely atomic in production. `settle_receipt_atomic` (migration 160, called at services/receipt_service.py:497) posts the journal, the receipt row and every allocation's invoice update in one database transaction; the pre-atomic path is preserved for test doubles with a compare-and-set retry loop (lines 690-720) and a compensating reversal that refuses to reverse a journal another receipt already claims (lines 340-355). The reasoning behind each of those guards is written down beside it.",
  "Frontend/backend money parity is real, not aspirational. `apps/web/lib/money/gstLine.ts` mirrors the Python arithmetic operation for operation \u2014 floor-then-split, BigInt exact-decimal truncation to match `Decimal(str(qty))` \u2014 and `shared/gst-parity-vectors.json` is GENERATED from the backend functions (apps/api/tests/generate_gst_parity_vectors.py) and asserted from both suites (apps/api/tests/test_gst_parity_vectors.py, apps/web/lib/money/gstLine.parity.test.ts). The preview a CA sees cannot disagree with what is saved.",
  "The CGST+SGST split is computed correctly. `_compute_line_gst` (routers/sales_invoices.py:167-193) floors the FULL tax first and then halves it, so CGST+SGST always equals the IGST an identical inter-state supply would attract, with SGST carrying the odd paise \u2014 and the same fix was carried into credit_notes.py:139-149 rather than left to drift.",
  "GSTR-1 classification is plumbed end to end and is more careful than most. Migration 268 gave the invoice supply_type / invoice_type / is_reverse_charge, the editor sets all three (components/invoices/InvoiceEditor.tsx:745-775), recurring templates stamp them onto every generated occurrence (services/recurring_invoice_service.py:485-489), and reverse charge reaches the return's `rchrg` flag (domain/gst/gstr1_builder.py:232). The B2CL rule is date-aware \u2014 Notification 12/2024's \u20b91 lakh from 01-08-2024, tested on INVOICE VALUE not taxable value (domain/gst/classifier.py:29-63).",
  "Immutability after issue is enforced and explained. `_SOFT_UPDATE_FIELDS` (routers/sales_invoices.py:1556) limits a post-issue PATCH to reference/notes/due date/terms plus line units, the editor mirrors it so the CA never fills in a form that will be rejected (InvoiceEditor.tsx:126, 440-460), and cancellation is refused outright when receipts, credit notes or debit notes are attached (routers/sales_invoices.py:1596-1603).",
  "Client-assignment scoping is enforced on every route, with the reasoning for it preserved. routers/sales_invoices.py:40-108 documents exactly what was reachable before and adds `_assert_invoice_scope` (404, never 403, so the status is not an id oracle) plus `_assert_batch_scope`, which checks every distinct client in a bulk payload before ANY row is written.",
  "Reporting-performance discipline is followed here too. The invoice list batch-fetches lines in one query rather than N+1 (routers/sales_invoices.py:474-482); the customer statement asks the database for the opening position via `party_opening_position` (migration 282) and then bounds the document reads to the printed window (services/customer_statement_service.py:243-256), degrading to a full replay rather than to a wrong number.",
  "The credit-note / debit-note sub-ledger is coherent and CAS-guarded. `credited_paise` and `debit_note_paise` are applied under compare-and-set with rollback of both the allocation row and the invoice on a journal failure (routers/credit_notes.py:521-585), and the settlement ceiling `(total + debit notes) \u2212 paid \u2212 credited` is used identically in the receipt path, the re-allocation path and the statement \u2014 one formula, three call sites, no drift.",
  "The multi-currency sales path is thought through. The document currency is frozen at creation and reconstructed from the stored row on edit rather than re-resolved (routers/sales_invoices.py:1278-1280), the base total is defined as the SUM of the converted components so the GL balances with no FX-rounding plug (lines 754-762), and a foreign receipt relieves each invoice at ITS booked rate with the difference posted to realized FX in one balanced journal (services/receipt_service.py:145-175).",
  "Recurring invoicing is correctly conservative: draft only, never auto-issued, never auto-emailed, generated through the SAME `create_invoice` endpoint so GST, FY-lock and validation are identical to a manual invoice (services/recurring_invoice_service.py:1-21, 499-505), with a unique (template, occurrence) constraint and a self-cleaning duplicate path (lines 400-416).",
  "The GST advance service refuses rather than guessing, and says so in its payload. `advances_report` returns `table_11_computed: false` with a plain-English `why` (services/gst_advance_service.py:228-243) explaining Notification 66/2017 and the goods/services split \u2014 a CA reading an empty Table 11 learns whether it is empty because there were no advances or because nothing computes it. (The refusal is right; that the computing path is unreachable is SALES-09.)"
 ],
 "subsystem": "Sales cycle: customers, sales invoices, credit/debit notes, receipts and allocation, recurring invoices, customer statements, party credits, invoice PDF/templates/branding, e-invoice and e-way bill records, service catalogue and HSN library",
 "test_coverage_notes": "Volume is high and the quality of individual tests is good, but the coverage has a consistent shape: the ledger is tested hard, the document is not, and several tests pin behaviour that no user action can reach.\\n\\nWhat is well covered. `tests/test_e2e_sales_cycle.py` drives the real endpoints against a FakeDB through create \u2192 issue \u2192 receipt and asserts the trial balance ties, AR nets to zero, and Bank is debited \u2014 and it exercises the REAL (non-mock) branch, so `test_sales_cycle_interstate_uses_igst` (line 124) genuinely covers the client-GSTIN-vs-customer-state derivation of `is_interstate`. `tests/test_invoice_round_off.py` proves CGST+SGST equals the IGST equivalent for odd tax, that the round-off delta stays in [-49,+50], and that the journal balances in all four rounding cases. `shared/gst-parity-vectors.json` is generated from the backend and asserted from both suites. Client-scope isolation has a dedicated file per router (test_invoice_bill_client_scope, test_receipts_client_scope, test_oos2_sales_invoice_scope, test_customers_client_scope, test_recurring_client_scope, test_eway_bill_client_scope). Concurrency and atomicity have their own real-Postgres files (test_r2_12_atomic_receipt_settlement_pg, test_r227_settle_receipt_atomic_status_guard_pg, test_r2_9_document_numbering_pg). I ran tests/test_phase2_sales_cycle.py, test_credit_notes.py, test_sales_debit_notes.py, test_invoice_round_off.py and test_gstr1_classification_from_invoice.py: 115 passed in 0.99s.\\n\\nTests that pin the wrong thing. `tests/test_invoice_pdf_line_items.py:193` stubs `_load_firm` to return `{\\\"name\\\": \\\"Test & Co\\\", \\\"gstin\\\": \\\"27AAAAA9999A1Z5\\\"}` and then asserts the rendered PDF is correct \u2014 it locks in SALES-01, the CA firm appearing as the supplier on a client's sale. Nothing in test_invoice_pdf.py or test_invoice_pdf_line_items.py asserts the printed TAX RATE, so the hardcoded 18%/9%/9% (SALES-02) and the unconditional \\\"reverse charge: No\\\" (SALES-03) are invisible to CI.\\n\\nTests that cover unreachable code. `tests/test_gstr1_advances_are_surfaced.py:163` seeds `gst_rate_bps` and `place_of_supply` directly into the receipts table to exercise Table 11A/11B \u2014 a state no API call or screen can produce (SALES-09). The feature is green and dead.\\n\\nGaps. Nothing tests numbering after a delete: `tests/test_document_numbering_unit.py` covers only that `_next_cn_seq` is per-client, and no test creates three notes, deletes the middle draft and creates a fourth \u2014 the exact sequence that wedges numbering permanently (SALES-04, which I reproduced against that file's own FakeDB). No test asserts what the Sales page's Outstanding tile computes (SALES-05). No test covers a supply_type / reverse-charge classification against a non-zero tax rate (SALES-16), an empty place of supply reaching issue (SALES-29), or `period_lock_service` on the credit-note / debit-note / receipt paths (SALES-15). The frontend statutory module `lib/invoices/compliance.ts` has a test (compliance.test.ts) but it ASSERTS the taxable-value threshold at line 54-55, so it pins SALES-17 rather than catching it. `lib/invoices/gst.ts`'s duplicate INDIAN_STATES list has no test at all, which is why six live state codes can go missing (SALES-06).",
 "what_exists": "Backend. `routers/sales_invoices.py` (2,144 lines) is the centre: create (single and `/bulk` for CSV import), draft PATCH with full GST recompute, `/issue` (posts through `phase2_journal_service.journal_for_sales_invoice` FIRST, then flips status), `/cancel` (Partner-only; refuses when receipts/credit/debit notes are attached; reverses through the kernel), draft-only DELETE, `/pdf`, `/send`, `/resend`, `/deliveries`, `/remind`, `/reminders`, `/hsn-suggestions`, `/outstanding`, and a `maintenance/unposted` + `/repost-journal` remediation pair. GST is computed per line in integer paise (`_compute_line_gst`: floor the full tax, then split, SGST carrying the odd paise), intra/inter decided by the CLIENT's GSTIN prefix vs the effective supply state. Invoice-level round-off to \u20b91 is opt-in (migration 247) and posts to a Round Off ledger. Multi-currency freezes the rate at creation. Sibling routers: `credit_notes.py`, `sales_debit_notes.py` (both draft \u2192 issue with CAS-guarded application of `credited_paise` / `debit_note_paise` to the linked invoice and rollback on posting failure), `receipts.py` + `services/receipt_service.py` (allocation against invoices, TDS-inclusive settlement, `settle_receipt_atomic` RPC in production, realized-FX path for foreign receipts, reversal, re-allocation), `recurring_invoices.py` + service (weekly\u2192yearly cadence, month-end clamped, generates DRAFT invoices only through the same `create_invoice`), `customer_statements.py` (+ PDF + email + deliveries), `customers.py`, `party_credits.py`, `service_catalogue.py`, `firm_hsn_library.py`, `firm_hsn_rate_history.py`, `hsn.py`, `einvoice.py`, `eway_bill.py`, `branding.py`. Domain: `gst/classifier.py` (B2B/B2CS/B2CL/EXP/NIL_EXEMPT with a date-aware Rule 59(4) threshold), `gst/gstr1_builder.py`, `gst/validator.py`, `services/gst_advance_service.py` (Table 11 reporting), `services/collections_service.py` (ageing buckets, overdue sweep, manual reminders), `services/invoice_pdf_service.py`, `services/statement_pdf_service.py`, `services/numbering.py`.\\n\\nTables. `client_sales_invoices` + `_lines` (migration 050, extended by 075/076/077/099/100/107/134/140/148/149/151/174/184/185/209/210/247/268/278/280/303), `credit_notes` + lines + `credit_note_allocations`, `sales_debit_notes` + lines + allocations, `receipts` + `receipt_allocations`, `customers`, `service_catalogue`, `firm_hsn_library`, `hsn_sac_preferences`, `hsn_master` (internal, not exposed), `recurring_invoice_templates`/`_lines`/`_runs`, `invoice_deliveries`, `party_credit_balances`/`_ledger`, `einvoice_records`, `eway_bill_records`, `firm_branding`, `invoice_settings`, `invoice_templates`, `email_templates`.\\n\\nFrontend. `app/clients/[id]/sales/page.tsx` (4,537 lines) with seven tabs \u2014 Invoices, Recurring, Customers, Receipts, Credit Notes, Debit Notes, Statements \u2014 reading the invoice/customer/catalogue lists directly over PostgREST and using the API for every mutation. The editor is `components/invoices/InvoiceEditor.tsx` (1,004 lines, full-page, Product/Service-first line grid with Enter/Tab spreadsheet navigation, live server-mirroring totals preview, GSTR-1 classification controls, opt-in round-off, multi-currency), with `InvoiceViewDrawer`, `CompliancePanel` (IRN / e-way prepare-and-record), `SalesCreditNoteEditor`, `SalesDebitNoteEditor`, `CustomerFormModal`, `ServiceCataloguePicker`, `HsnLookup`, `StateLookup`. Pure domain in `lib/invoices/` (gst, classification, compliance, importMapping, lineItemPayload) and `lib/money/` (rupeeInput, gstLine, lineInput). Separate pages: `/einvoice`, `/settings/firm-hsn-library`, `/settings/invoice-settings`, `/settings/invoice-templates`."
}