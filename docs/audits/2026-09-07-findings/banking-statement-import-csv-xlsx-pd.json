{
 "findings": [
  {
   "id": "BANK-01",
   "kind": "bug",
   "severity": "high",
   "title": "A statement that prints a page subtotal labelled \"Total\" is refused outright, with a message blaming the column mapping",
   "evidence": "domain/banking/tie_out.py:190-206 \u2014 printed_totals iterates rows and RETURNS THE FIRST row whose cell[0] matches _TOTAL_LABEL (`^\\s*(grand\\s+)?totals?\\s*:?\\s*$`, normalizer.py:75) and carries exactly two numeric cells. A per-page or per-month subtotal labelled \"Total\" satisfies both. statement_check (tie_out.py:302-319) then treats the mismatch as `tot_bad` and routers/banking.py:851-853 raises HTTPException(422) BEFORE anything is written. I reproduced it end to end in this session: a 6-column cheque-layout CSV with a page \"Total\" row before the real \"Grand Total\" parsed all 3 rows correctly and came back `verified: False`, refusal = 'This statement does not match its own \"Total\" row: deposits came to \u20b9500.00 against the statement's \u20b90.00. Either some lines were not read, or a column is mapped to the wrong thing. Nothing has been imported.' There is no server-side override: `overrideBalance` in apps/web/components/banking/AccountsPanel.tsx:583,992 gates only the client-side balance_check, and no form field skips the totals check.",
   "ca_experience": "The CA uploads a perfectly good statement and cannot import it at all. The refusal tells them to check the debit/credit column mapping \u2014 when no mapping was involved and nothing was misread. The only way past it is to edit the bank's file by hand. Per-page and per-month subtotals labelled \"Total\" are common in Indian bank PDF and Excel exports, so this can block a client's whole month.",
   "market_standard": "Tally Prime cannot import a PDF statement at all and its Excel import simply skips rows it cannot parse. Zoho Books and QuickBooks skip non-transaction rows silently and never block an import on a totals row. No competitor refuses an import because it read a subtotal.",
   "confidence": "high",
   "suggested_fix": "Take the LAST matching totals row rather than the first, and prefer a row whose label literally contains \"grand\". Better: collect every candidate totals row and only use one if exactly one of them equals the parsed sums, or if it is the last row of the file. Also add a `skip_totals_check` acknowledgement path so a CA who has compared the figures by eye is never stuck \u2014 the refusal should be a stop, not a wall.",
   "effort": "hours",
   "verification": {
    "status": "confirmed",
    "by": "agent",
    "corrected_severity": "high",
    "what_is_actually_true": "Exactly as claimed: the first row labelled Total/Totals/Grand Total with two numeric cells wins, and if it is a page or period subtotal the import is refused outright with a message that blames the column mapping (domain/banking/tie_out.py:188-206, tie_out.py:302-319, routers/banking.py:853-855). There is no override anywhere in the request surface.",
    "probe": "PYTHONPATH=apps/api python - (a 6-column generic_cheque CSV with a page 'Total,,,0.00,500.00,' row before the real 'Grand Total,,,200.00,1200.00,'): printed txns: 3 (all three parsed correctly, debits 20000 paise, credits 120000 paise); printed_totals -> {'label': 'Total', 'total_debits_paise': 0, 'total_credits_paise': 50000}; statement_check -> verified: False, refusal: 'This statement does not match its own \"Total\" row: withdrawals came to \u20b9200.00 against the statement's \u20b90.00 and deposits came to \u20b91,200.00 against the statement's \u20b9500.00. Either some lines were not read, or a column is mapped to the wrong thing. Nothing has been imported.'",
    "lens_notes": {
     "literal": "Verified line by line and reproduced. domain/banking/tie_out.py:188-206 loops `for row in rows` and RETURNS on the FIRST row whose cells[0] matches `_TOTAL_LABEL` (normalizer.py:75, `^\\s*(grand\\s+)?totals?\\s*:?\\s*$`) and that carries exactly two numeric cells after column 0 \u2014 there is no 'keep looking for a later/grand one' branch. domain/banking/normalizer.py:762-773 `_finish` passes the WHOLE row list (header rows included) to printed_totals. tie_out.py:302-319 `statement_check` sets refusal from tot_bad FIRST, and routers/banking.py:853-855 raises HTTPException(422) with that refusal before any write (the comment at :850-852 says so explicitly).",
     "guards": "None. tests/test_statement_printed_totals.py has 20 tests and not one puts two totals-shaped rows in a file (test names cover the label variants, three-numbers refusal, adapter ordering, a date not being mistaken for a total \u2014 never a second Total row). The only relaxations in the module are the opposite direction: three numbers in the row => None, no debit/credit columns => None. No DB constraint, no migration, no scheduler, no flag bears on this.",
     "reach": "Fully reachable. routers/banking.py:761 POST /banking/statements/upload, router mounted at main.py:341. The frontend calls it via apps/web/lib/api/index.ts:917-928 `uploadStatement` from components/banking/AccountsPanel.tsx. There is no server-side bypass: the upload form fields are file/client_id/bank_name/account_number/bank_account_id/column_mapping/save_mapping/opening_balance_paise/closing_balance_paise/allow_vision (routers/banking.py:762-773) \u2014 nothing skips the totals check. `overrideBalance` (AccountsPanel.tsx:583, 910, 992) only relaxes the CLIENT-side preview gate on balance_check; it is never sent to the server."
    }
   },
   "rescore_2026_09_08": {
    "state": "still_open",
    "what_is_true_now": "Unchanged since 7 September. The first row labelled Total/Totals/Grand Total with exactly two numeric cells still wins, a page or period subtotal still refuses the whole import, and the refusal still blames the column mapping. There is still no server-side acknowledgement path.",
    "remaining": null,
    "probe": "Ran a probe at HEAD (46bd46c): parse_statement_detailed() on a 6-col cheque-layout CSV carrying a page 'Total' row before the real 'Grand Total'. Printed: \"printed: {'label': 'Total', 'total_debits_paise': 0, 'total_credits_paise': 50000}\" / \"verified: False\" / \"refusal: This statement does not match its own \\\"Total\\\" row: withdrawals came to Rs100.00 against the statement's Rs0.00 and deposits came to Rs800.00 against the statement's Rs500.00. Either some lines were not read, or a column is mapped to the wrong thing. Nothing has been imported.\" The code is unchanged: domain/banking/tie_out.py printed_totals() still `return`s from inside `for row in rows:` on the FIRST label match, and routers/banking.py:855 still does `raise HTTPException(status_code=422, detail=check[\"refusal\"])` before anything is written. `grep -n 'skip_totals\\|override' routers/banking.py` -> no match.",
    "against_commit": "46bd46c"
   }
  },
  {
   "id": "BANK-02",
   "kind": "data_integrity",
   "severity": "high",
   "title": "Overdraft and Cash Credit bank accounts are given an Asset ledger, and the ledger picker will not offer a Liability one",
   "evidence": "migrations/054_v13_payroll_assets_banking.sql:100-101 \u2014 bank_accounts.account_type CHECK allows 'Current','Savings','Cash Credit','Overdraft'. apps/web/components/banking/AccountsPanel.tsx:415-417 offers all four in the form. But routers/banking.py:222-253 `_ensure_bank_ledger` hardcodes `\"account_type\": \"Asset\", \"account_subtype\": \"Bank\"` for the auto-created ledger, and AccountsPanel.tsx:339-342 filters the manual ledger picker with `.eq(\"account_type\", \"Asset\")`. I grepped the whole banking backend for `account_type` (services/bank_*.py, services/banking_service.py, routers/banking.py, domain/banking/*.py): the bank account's own account_type is read NOWHERE \u2014 only the chart account's is.",
   "ca_experience": "Almost every Indian SME runs a Cash Credit or Overdraft limit. Its statement imports and posts fine, but the utilised limit sits as a NEGATIVE ASSET on the balance sheet instead of under Schedule III \"Short-term borrowings\". Current assets are overstated (understated by a negative), current liabilities are understated, and the current ratio in the ratio report is wrong. The CA cannot fix it from the UI: the ledger picker refuses to show a Liability account.",
   "market_standard": "Tally requires a Bank OD/CC account to be created under the \"Bank OD A/c\" group, which is a Loans (Liability) group, and reports it under short-term borrowings. Zoho Books and Busy both classify an OD/CC ledger as a liability.",
   "confidence": "high",
   "suggested_fix": "Make `_ensure_bank_ledger` read the account_type: create a Liability / 'Bank OD' subtype ledger for 'Cash Credit' and 'Overdraft', an Asset / 'Bank' one otherwise. Widen the ledger picker's filter to Asset OR Liability-with-a-bank-subtype. Add a Schedule III mapping so the balance lands under short-term borrowings, and a migration to re-classify existing OD/CC ledgers with a report of what moved.",
   "effort": "days",
   "verification": {
    "status": "confirmed",
    "by": "agent",
    "corrected_severity": "high",
    "what_is_actually_true": "Confirmed. A 'Cash Credit' or 'Overdraft' bank account gets an Asset/Bank ledger (routers/banking.py:250) and the picker will not offer anything else (AccountsPanel.tsx:339-341), so the utilised limit sits as a negative Cash & Cash Equivalents figure (domain/reporting/schedule_iii.py:74-75) instead of Schedule III Short Term Borrowings (:53). Nothing in the banking backend ever reads bank_accounts.account_type except to display it.",
    "probe": null,
    "lens_notes": {
     "literal": "Confirmed at every cited line. migrations/054_v13_payroll_assets_banking.sql:100-101 \u2014 `account_type TEXT NOT NULL DEFAULT 'Current' CHECK (account_type IN ('Current','Savings','Cash Credit','Overdraft'))`. apps/web/components/banking/AccountsPanel.tsx:416 offers all four. routers/banking.py:250 hardcodes `\"account_type\": \"Asset\", \"account_subtype\": \"Bank\"` in `_ensure_bank_ledger`, with no reference to the bank account's own type (the function signature at :222 does not even receive it). AccountsPanel.tsx:339-341 filters the manual picker `.eq(\"account_type\", \"Asset\")` with the comment 'Only Asset accounts can be a bank's GL account'.",
     "guards": "None, and the downstream consequence is real. domain/reporting/schedule_iii.py:53 routes 'overdraft'/'cc limit' to 'Short Term Borrowings' only when account_type is LIABILITY; the asset branch (:74-75) sends any subtype containing 'bank' to 'Cash & Cash Equivalents', and there is no negative-balance reclassification anywhere in the module (the only sign handling is at :498, a display helper). domain/banking/account_category.py:161 additionally hard-requires account_type == 'Asset' for a ledger to count as a bank/cash account, so even a hand-linked Liability ledger would break Transfer derivation. The only banking-backend read of `account_type` is services/bank_register_service.py:221, which echoes the bank account's type into the register header for display.",
     "reach": "Fully reachable \u2014 the account-type dropdown is on the live Add/Edit Bank Account form (AccountsPanel.tsx:414-417) and the ledger is auto-created on POST /banking/accounts (routers/banking.py:445-447). The API would accept a Liability coa_account_id (create_bank_account does no type check, :448-457 only checks for a conflicting bank), but no screen can supply one because the picker query filters to Asset."
    }
   },
   "rescore_2026_09_08": {
    "state": "still_open",
    "what_is_true_now": "Unchanged. An Overdraft or Cash Credit account still gets an Asset/Bank ledger and the picker still offers nothing else, so a utilised limit reads as negative Cash & Cash Equivalents rather than Schedule III Short Term Borrowings.",
    "remaining": null,
    "probe": "routers/banking.py:250 still reads `\"account_type\": \"Asset\", \"account_subtype\": \"Bank\", \"is_active\": True,` inside _ensure_bank_ledger, with no reference to the bank account's own account_type. apps/web/components/banking/AccountsPanel.tsx:342 still `.eq(\"account_type\", \"Asset\")` on the manual ledger picker, and :416 still offers `[\"Current\",\"Savings\",\"Cash Credit\",\"Overdraft\"]`.",
    "against_commit": "46bd46c"
   }
  },
  {
   "id": "BANK-03",
   "kind": "data_integrity",
   "severity": "high",
   "title": "A foreign-currency bank account can be created, and its statement imports and posts to the ledger at 1:1 as if the amounts were rupees",
   "evidence": "routers/banking.py:166-180 `_guard_foreign_bank_currency` explicitly PERMITS a non-INR bank account when multi-currency is active for the client, and routers/banking.py:441-447 stores it. routers/banking.py:615-646 `bank_account_balance` even reports a `foreign_balance_minor`. But the import path never reads the currency: services/banking_service.py:126-138 `import_normalized` stores `debit_paise`/`credit_paise`/`balance_paise` verbatim from the parser, and services/bank_posting_service.py:287-420 `_plan` builds lines with no currency or rate and calls `phase2_journal_service._create_journal` (bank_posting_service.py:558-565) without `txn_currency` \u2014 while that kernel does accept `txn_currency`/`exchange_rate` (phase2_journal_service.py:1523, 1589, 1688). I grepped services/bank_posting_service.py, services/banking_service.py and domain/banking/*.py for currency/fx/exchange handling in the import-and-post path: there is none.",
   "ca_experience": "A client with a USD EEFC account: a $10,000 receipt imports as `credit_paise = 1000000` and posts as \u20b910,000 instead of roughly \u20b98.5 lakh. The bank ledger, the trial balance, the P&L and the cash-flow statement are all wrong by the exchange rate, and nothing on any screen says so. There is no FX revaluation of the bank balance at year end either.",
   "market_standard": "Zoho Books holds a base and a foreign amount per bank line and revalues foreign bank accounts at period close. Tally Prime records a forex bank ledger with rate-wise entries and produces a Forex Gain/Loss adjustment at year end.",
   "confidence": "high",
   "suggested_fix": "Either (a) refuse a statement import for a non-INR bank account until the FX path exists \u2014 one line in `upload_statement`, and honest; or (b) carry the account's currency onto the parsed rows, ask for the rate per statement or per line, and pass `txn_currency`/`exchange_rate` into `_create_journal` the way the sales/purchase FX paths already do. (a) today, (b) when a client needs it.",
   "effort": "days",
   "verification": {
    "status": "confirmed",
    "by": "agent",
    "corrected_severity": "medium",
    "what_is_actually_true": "The bank statement import and posting path genuinely ignores bank_accounts.currency and books minor units as paise at 1:1 (services/banking_service.py:126-138; services/bank_posting_service.py:555-563 passes no txn_currency to phase2_journal_service._create_journal, which then defaults rate=1 at phase2_journal_service.py:1589-1590). But it is latent, not live: no UI can create a foreign-currency bank account (AccountsPanel.tsx has no currency field) and the feature needs three flags that all default off (domain/currency/policy.py:48-56, core/feature_flags.py:15-21). The claim that year-end FX revaluation of a bank balance does not exist is wrong \u2014 domain/currency/fx_revaluation_service.py:178 does it, tested at tests/test_multi_currency_phase5.py:442.",
    "probe": "grep -rn \"currency\" apps/web/components/banking/AccountsPanel.tsx -> no matches (no currency field on the bank account form). grep -n \"currency|fx|exchange\" apps/api/services/bank_posting_service.py services/banking_service.py domain/banking/*.py -> hits only in match_and_settle_multi (:926, :1026-1043); the _plan/post path has none.",
    "lens_notes": {
     "literal": "The core claim is true. routers/banking.py:166-179 `_guard_foreign_bank_currency` permits a non-INR bank account when the policy is active and the code is in the ISO master, and :440-446 stores it. The import path never reads it: services/banking_service.py:126-138 `import_normalized` copies debit_paise/credit_paise/balance_paise verbatim, and services/bank_posting_service.py:555-563 calls `phase2_journal_service._create_journal` with no `txn_currency` and no `exchange_rate`, so the kernel defaults `entry_ccy = base` and `rate = Decimal(1)` (phase2_journal_service.py:1589-1590). Grepping services/bank_posting_service.py, services/banking_service.py and domain/banking/*.py for currency/fx/exchange returns hits only inside `match_and_settle_multi` (:926, 1026-1043), which is a different path and takes the currency from the CALLER, not from the bank account.",
     "guards": "Two the finding missed. (1) FX revaluation of a foreign bank balance DOES exist \u2014 domain/currency/fx_revaluation_service.py:178 `revalue` emits item_type 'bank' adjustments keyed on the bank's GL account, proven by tests/test_multi_currency_phase5.py:442-464 and :468-476 (idempotent) and :478-485 (currency_exposure includes the bank). So the sub-claim 'There is no FX revaluation of the bank balance at year end either' is FALSE. (2) The whole surface is triple-gated: domain/currency/policy.py:48-56 needs MULTI_CURRENCY_ENABLED (core/feature_flags.py:15-21, default OFF; declared `sync: false` in render.yaml:205, i.e. unset in the manifest) AND firms.multi_currency_entitled AND clients.multi_currency_enabled.",
     "reach": "Much narrower than claimed. apps/web/components/banking/AccountsPanel.tsx contains no 'currency' string at all \u2014 the Add/Edit Bank Account form has no currency field, so no screen can create a non-INR bank account. The only frontend mention of a bank account's currency is a read-only FX exposure row (apps/web/app/clients/[id]/accounting/page.tsx:1313). Reaching this bug needs the platform flag on, the firm entitled, the client enabled, and a hand-crafted POST to /api/banking/accounts."
    }
   },
   "rescore_2026_09_08": {
    "state": "still_open",
    "what_is_true_now": "Unchanged, and still latent for the same reason: the import/post path books minor units as paise at 1:1, but no screen can create a foreign-currency bank account and the feature needs three default-off flags.",
    "remaining": null,
    "probe": "`grep -rn currency apps/web/components/banking/AccountsPanel.tsx` -> no match (still no currency field on the form). routers/banking.py:166-180 _guard_foreign_bank_currency still PERMITS a non-INR account when multi-currency is active. services/bank_posting_service.py:556-563 still calls `phase2_journal_service._create_journal(db, firm_id=..., entry_date=..., reference_no=f\"BANK-{txn_id}\", ... lines=lines, is_posted=True, source_type=\"bank_transaction\", ...)` with no txn_currency/exchange_rate argument. `grep -n currency services/banking_service.py` -> no match.",
    "against_commit": "46bd46c"
   }
  },
  {
   "id": "BANK-04",
   "kind": "gap",
   "severity": "high",
   "title": "The \"Bank Reconciliation Statement\" is a statement-line tick-off; it has no unpresented cheques and no deposits in transit, which is what a BRS is",
   "evidence": "domain/banking/reconciliation.py:19-50 \u2014 the whole tie-out is `opening + reconciled credits \u2212 reconciled debits \u00b1 adjustments == statement closing`. Every row in the report comes from services/bank_reconciliation_service.py:60-72 `_posted_account_txns`, which reads bank_transactions for the account's statements and keeps only those with a `posted_journal_id`. `_classify` (:84-100) buckets them as reconciled / unreconciled / exceptions \u2014 all three are STATEMENT lines. Nothing in the module reads journal_entries or journal_lines, so a cheque issued and entered in the books but not yet presented at the bank has no row anywhere in it, and neither does a deposit banked but not yet credited. services/bank_reconciliation_pdf_service.py:83-90 prints exactly those five tie-out rows under the title \"Bank Reconciliation Statement\".",
   "ca_experience": "The CA opens Reconcile expecting the document they have prepared every month of their career \u2014 \"Balance as per Cash Book \u2026 add cheques issued not presented \u2026 less cheques deposited not credited \u2026 Balance as per Pass Book\" \u2014 and gets a tick-list plus a difference. At year end, the auditor's BRS working paper cannot be produced from this product at all, and the PDF it does produce is titled as if it were one. This is the single most recognisable accountant-facing artefact in the module and it is the wrong document.",
   "market_standard": "Tally Prime's BRS is the reference implementation: it lists \"Amounts not reflected in Bank\" from the COMPANY BOOKS with instrument number and date, plus \"Amounts not reflected in Company Books\", and reconciles Balance as per Company Books to Balance as per Bank. Zoho Books, Busy and Marg all produce the two-sided statement.",
   "confidence": "high",
   "suggested_fix": "Add the book side. Read the bank ledger's journal_lines for the period, subtract the lines that came from a posted bank_transaction, and what remains is the reconciling set: cheques issued not presented (credits to bank with no statement line) and deposits in transit (debits to bank with no statement line). Render the classic two-sided statement in the PDF, with the instrument number from `reference_no`. The existing tie-out stays as the internal check.",
   "effort": "weeks",
   "verification": {
    "status": "confirmed",
    "by": "agent",
    "corrected_severity": "high",
    "what_is_actually_true": "Confirmed as a gap, not a bug. Every row the reconciliation and its PDF know about is a bank_transactions statement line (services/bank_reconciliation_service.py:60-72, 84-100); neither the service, the domain module nor the PDF reads journal_entries/journal_lines at all, so cheques issued but not presented and deposits in transit have no representation, and the PDF titled 'Bank Reconciliation Statement' (services/bank_reconciliation_pdf_service.py:81-92) is a one-sided tie-out.",
    "probe": "grep -n 'journal_entries\\|journal_lines' apps/api/services/bank_reconciliation_service.py apps/api/services/bank_reconciliation_pdf_service.py apps/api/domain/banking/reconciliation.py -> no output. grep -rn -i 'unpresented|not presented|in transit|cheques issued' apps/ -> only unrelated hits (a payroll test string, the register's 'uncleared' filter).",
    "lens_notes": {
     "literal": "Confirmed. domain/banking/reconciliation.py:19-50 `tie_out` is exactly `opening + deposits - withdrawals + adjustments` vs statement closing. Every input comes from services/bank_reconciliation_service.py:60-72 `_posted_account_txns`, which reads bank_statements for the account and then bank_transactions on those statement ids, keeping rows with a posted_journal_id. `_classify` (:84-100) buckets those same statement rows into reconciled/unreconciled/exceptions. `grep -n 'journal_entries|journal_lines'` over services/bank_reconciliation_service.py, services/bank_reconciliation_pdf_service.py and domain/banking/reconciliation.py returns NOTHING. services/bank_reconciliation_pdf_service.py:81-92 prints exactly the seven tie-out rows under the report title.",
     "guards": "None, and the near-miss does not help: the 'uncleared' status in the Bank Book (services/bank_register_service.py:41, 107) is also a STATEMENT-line concept \u2014 services/bank_register_service.py:59-85 `_txns` reads bank_transactions only \u2014 so nothing anywhere in the module lists a book entry with no statement line. A repo-wide grep for 'unpresented', 'not presented', 'in transit', 'cheques issued' across apps/ finds no such feature.",
     "reach": "Fully reachable and this IS the accountant-facing artefact: routers/banking.py:1962 GET /reconciliations/{id}/report.pdf, plus :2007 /report and :2021 /report.csv; the Reconcile tab is apps/web/components/banking/ReconcileTab.tsx and 'Bank reconciliation' is linked from apps/web/app/clients/[id]/reports/page.tsx:122-123. Book-side entries with no statement line genuinely exist \u2014 a Receipt or Payment voucher posts to the bank ledger independently of any statement import."
    }
   },
   "rescore_2026_09_08": {
    "state": "still_open",
    "what_is_true_now": "Unchanged. The BRS is still a one-sided tie-off over statement lines only; nothing in the module reads the book side, so cheques issued but not presented and deposits in transit have no representation anywhere.",
    "remaining": null,
    "probe": "`grep -n 'journal_entries\\|journal_lines' apps/api/services/bank_reconciliation_service.py apps/api/services/bank_reconciliation_pdf_service.py apps/api/domain/banking/reconciliation.py` -> exit 1, no output. `grep -rniE 'unpresented|not presented|in transit|cheques issued' apps/api apps/web` -> only an unrelated comment in test_sales_invoice_bulk_create.py and a payroll test string. services/bank_reconciliation_service.py:71 still ends _posted_account_txns with `return [t for t in rows if t.get(\"posted_journal_id\")]`.",
    "against_commit": "46bd46c"
   }
  },
  {
   "id": "BANK-05",
   "kind": "data_integrity",
   "severity": "high",
   "title": "\"Adjustments\" is an unexplained plug number that can force a reconciliation to tie out and produce a signed BRS PDF",
   "evidence": "migrations/102_bank_reconciliation_b4.sql:20 adds `adjustments_paise bigint NOT NULL DEFAULT 0` with no companion reason, narration or itemisation column. models/banking.py:353-358 `ReconciliationUpdateIn` accepts it as a bare Optional[int]. services/bank_reconciliation_service.py:294-304 `update_session` writes it with no validation and no audit call. domain/banking/reconciliation.py:39 folds it straight into `book_balance`, so `complete()` (bank_reconciliation_service.py:401-405) passes its `summary[\"reconciles\"]` gate. The UI exposes it as a free text box: apps/web/components/banking/ReconcileTab.tsx:422-428 renders \"Adjustment (\u20b9)\" and PATCHes `adjustments_paise`. The completion is then frozen into a snapshot and rendered by services/bank_reconciliation_pdf_service.py as a certified \"Bank Reconciliation Statement\". Contrast the reopen path, which DOES demand a \u226510-character reason enforced by a DB CHECK (models/banking.py:312-329).",
   "ca_experience": "An Executive with `banking.write` who cannot find a \u20b947,300 difference types 47300 into Adjustment, the tie-out goes green, Complete is enabled, and the firm now holds a certified reconciliation PDF whose \"Adjustments\" line means nothing. Nobody \u2014 including the partner reviewing it \u2014 can tell from the document what the \u20b947,300 was.",
   "market_standard": "Tally's BRS has no plug field at all; every difference must be an actual voucher or a bank date. Zoho Books requires you to either match a transaction or create one for the difference, and QuickBooks forces an explicit \"reconciliation discrepancy\" journal that posts to the ledger. None of them lets a number reconcile by assertion.",
   "confidence": "high",
   "suggested_fix": "Require a substantive reason for any non-zero adjustment (mirror `ReconciliationReopenIn.substantive` and back it with a DB CHECK), write an audit_log row when it changes, print the reason on the PDF beside the figure, and gate a non-zero adjustment on `banking.approve` (Manager+). Better still, make an adjustment an actual posted journal so the books and the reconciliation agree.",
   "effort": "days",
   "verification": {
    "status": "confirmed",
    "by": "agent",
    "corrected_severity": "high",
    "what_is_actually_true": "Confirmed, and slightly worse than described: the adjustment is an unvalidated, unaudited, un-narrated plug written by any Executive (services/bank_reconciliation_service.py:294-304) that satisfies the completion gate (domain/banking/reconciliation.py:39 with bank_reconciliation_service.py:403-405), and the `banking.approve` Manager tier that core/permissions.py:183 defines for signing off a reconciliation is referenced by no router at all.",
    "probe": "grep -rn 'rbac(\"banking\", \"approve\")' apps/api/routers/ -> no output. grep -rn 'adjustments_paise|adjustment_reason' apps/api/migrations/*.sql -> only migration 102 lines 20 and 33; no reason column, no CHECK.",
    "lens_notes": {
     "literal": "Confirmed at every line. migrations/102_bank_reconciliation_b4.sql:20 adds `adjustments_paise bigint NOT NULL DEFAULT 0` and :33-34 comments it 'documented adjustments' \u2014 with no reason/narration/itemisation column anywhere (grep of migrations/*.sql for adjustment_reason returns nothing). models/banking.py:352-358 `ReconciliationUpdateIn` takes it as a bare Optional[int] with no validator. services/bank_reconciliation_service.py:294-304 `update_session` int()s it into the row with no validation, no audit_service call and no `_log` activity entry. domain/banking/reconciliation.py:39 folds it into book_balance, so `complete` (:403-405) passes its `summary['reconciles']` gate.",
     "guards": "The opposite \u2014 the module shows it knows how to demand a reason and does not here. models/banking.py:312-329 `ReconciliationReopenIn.substantive` requires >=10 characters and says a DB CHECK (migration 253) backs it. Nothing equivalent exists for the adjustment. Stronger still: core/permissions.py:181-185 defines `banking.approve` = Manager+ with the comment 'sign off a reconciliation', and `grep -rn 'rbac(\"banking\", \"approve\")' routers/` returns NOTHING \u2014 POST /reconciliations/{id}/complete uses rbac('banking','write') (routers/banking.py:1909-1912), which is _AT_LEAST_EXECUTIVE. So the Manager gate the permission table was written for is not wired up.",
     "reach": "Fully reachable. PATCH /banking/reconciliations/{recon_id} (routers/banking.py:1846-1859, rbac banking.write) is called from apps/web/components/banking/ReconcileTab.tsx:420-431 \u2014 a free 'Adjustment (\u20b9)' number box with an Apply button that PATCHes adjustments_paise. Complete then freezes a snapshot (bank_reconciliation_service.py:407-414) which services/bank_reconciliation_pdf_service.py renders as a certified statement, printing 'Adjustments' with the figure and no explanation (:87)."
    }
   },
   "rescore_2026_09_08": {
    "state": "still_open",
    "what_is_true_now": "Unchanged. A non-zero adjustment is still an unvalidated, unaudited, un-narrated plug any Executive can write, and it still satisfies the completion gate that freezes the certified BRS snapshot.",
    "remaining": null,
    "probe": "`grep -rn 'rbac(\"banking\", \"approve\")' apps/api/routers/` -> exit 1, no output (the Manager tier core/permissions.py defines is still referenced by no router). `grep -rn 'adjustments_paise\\|adjustment_reason' apps/api/migrations/*.sql` -> only migration 102 lines 20 and 33; still no reason column and no CHECK. domain/banking/reconciliation.py still folds adjustments straight into book_balance and services/bank_reconciliation_service.py's complete() still gates only on summary[\"reconciles\"].",
    "against_commit": "46bd46c"
   }
  },
  {
   "id": "BANK-06",
   "kind": "gap",
   "severity": "high",
   "title": "There is no way to delete or undo a mis-imported statement \u2014 nothing in the backend or the UI",
   "evidence": "routers/banking.py registers GET /statements (:1097) and POST /statements/import and /upload, but no DELETE. I grepped apps/api/routers/*.py for a bank_statements delete and apps/web for any `from(\"bank_statements\")` write: there are none \u2014 the only DELETE in the module's api client is `deleteColumnMapping` (apps/web/lib/api/index.ts:954). The statements table in apps/web/components/banking/AccountsPanel.tsx:235-252 offers exactly one action per row: \"View\". The import modal defaults the account to `accounts[0]?.id` (AccountsPanel.tsx:565), so picking the wrong bank is one careless click. The dedup hash includes bank_account_id (domain/banking/dedup.py:30-40), so re-importing to the CORRECT account works \u2014 and the wrong-account copy stays forever.",
   "ca_experience": "A CA imports April's HDFC statement against the Cosmos account. 312 lines are now on the wrong bank, appear in the wrong Bank Book, will be offered to the wrong reconciliation, and permanently block that account's deletion (routers/banking.py:_DELETE_BLOCKERS). The only remedy in the product is to open each of the 312 lines and Set aside, and even then the statement row and its totals remain. Same problem for a statement imported under a wrong column mapping that passed the checks.",
   "market_standard": "Zoho Books lets you delete an uploaded statement and all its unreconciled transactions in one action. QuickBooks has \"Undo\" on a bank import batch. Tally's bank statement import can be reverted by deleting the imported vouchers as a set.",
   "confidence": "high",
   "suggested_fix": "Add DELETE /statements/{id}, permitted only while every line on it is unposted and unreconciled, cascading to bank_transactions (the FK is already ON DELETE CASCADE), writing the whole statement header to audit_log first, and refusing with a named reason otherwise. Surface it as a Delete action on the statements table beside View.",
   "effort": "days",
   "verification": {
    "status": "confirmed",
    "by": "agent",
    "corrected_severity": "high",
    "what_is_actually_true": "Confirmed as a gap. No endpoint or UI can delete or undo an imported statement (routers/banking.py has no DELETE /statements; apps/web never writes bank_statements). A statement imported against the wrong bank account is permanent, keeps that account undeletable (routers/banking.py:268-269), and the only partial remedy is per-line batch-exclude (:1385), which leaves the statement row and its totals in place.",
    "probe": "grep -n '@router\\.' apps/api/routers/banking.py -> 64 routes; the four DELETEs are /accounts/{id}, /statements/column-mappings/{id}, /transactions/{id}/transfer-pair, /rules/{rule_id}. grep -rn 'bank_statements' apps/web --include=*.ts --include=*.tsx -> no output.",
    "lens_notes": {
     "literal": "Confirmed by enumerating the router. The only DELETE verbs in routers/banking.py are :548 /accounts/{id}, :1071 /statements/column-mappings/{id}, :1496 /transactions/{id}/transfer-pair and :2166 /rules/{id}. There is no DELETE on /statements and none on /transactions. On the frontend, grep for `from(\"bank_statements\")` across apps/web returns nothing, and the only banking DELETE in the api client is deleteColumnMapping (apps/web/lib/api/index.ts:953-954) plus deleteBankAccount (:911).",
     "guards": "The remedies that exist do not reach it. POST /transactions/{id}/undo (:1286) reverses a POSTING, not the import; batch-exclude (:1385) only marks lines ignored and leaves the statement and its totals; services/bank_erasure.py is a DPDP retention path for deleting a whole bank ACCOUNT and is itself blocked by _DELETE_BLOCKERS (routers/banking.py:268-273, first entry 'bank statements have been imported for it'). The dedup fingerprint at domain/banking/dedup.py:30-40 includes bank_account_id, so a re-import to the correct account is allowed and the wrong-account copy is permanent.",
     "reach": "Fully reachable, and easy to hit. apps/web/components/banking/AccountsPanel.tsx:565 defaults the import modal's account to `accounts[0]?.id`, and the statements table (:233-252) has one action per row: a View/Hide toggle. There is no Delete."
    }
   },
   "rescore_2026_09_08": {
    "state": "still_open",
    "what_is_true_now": "Unchanged. A statement imported against the wrong bank account is still permanent, still blocks deleting that account, and the only remedy is still per-line batch-exclude.",
    "remaining": null,
    "probe": "`grep -n '@router.delete' apps/api/routers/banking.py` -> 548 /accounts/{id}, 1071 /statements/column-mappings/{id}, 1496 /transactions/{id}/transfer-pair, 2166 /rules/{rule_id}. Still no DELETE for a statement. `grep -rn 'bank_statements' apps/web --include=*.ts --include=*.tsx` -> no output. AccountsPanel.tsx:565 still `useState(accounts[0]?.id ?? \"\")` for the import modal's account.",
    "against_commit": "46bd46c"
   }
  },
  {
   "id": "BANK-07",
   "kind": "performance",
   "severity": "high",
   "title": "The Bank Book fetches every transaction on the account and computes the running balance in Python \u2014 the exact pattern CLAUDE.md forbids",
   "evidence": "services/bank_register_service.py:60-84 `_txns` uses `fetch_all` to page EVERY bank_transaction for the account ('ALL of them \u2014 not just posted ones'), then :86-98 reads the reconciliation statuses, then domain/banking/register.py:146-205 `build_register` sorts the whole set and accumulates the balance row by row in Python, and only THEN does `_matches` filter and the service page. CLAUDE.md, \"Reporting performance \u2014 the rule, not a preference\": \"No report may fetch rows proportional to transaction volume\u2026 Fetching raw rows and computing in Python is the third option and it is not available.\" The Bank Book is listed as a report (apps/web/app/clients/[id]/reports/page.tsx:119-121, under Operational). The same shape is in services/bank_reconciliation_service.py:60-72, which is called again by `opening_suggestion` (:164) on every session create.",
   "ca_experience": "On the client CLAUDE.md already measured (12,836 entries), opening the Bank Book is thirteen Singapore\u2192Mumbai round trips of 1,000 rows each before the first row renders. That is the same shape as the cash-flow report that measured 54.34s and could not finish inside lib/api's 45-second abort. A CA who opens Bank Book to look up one balance waits, or sees the generic error card.",
   "market_standard": "Tally computes a bank ledger's running balance in its own engine over a memory-mapped company file \u2014 instant. Zoho Books pages the register server-side with the balance carried in the query.",
   "confidence": "high",
   "suggested_fix": "Move the running balance into SQL, the way public.cash_flow_report (migration 277) and public.schedule_iii_ageing (migration 303) were moved: a SQL function returning finished register rows with a window-function running balance, plus the first-divergence probe. Keep domain/banking/register.py as the mock-mode twin and pin the two with a parity test, exactly as tests/test_cash_flow_sql_parity_pg.py does.",
   "effort": "days",
   "verification": {
    "status": "confirmed",
    "by": "agent",
    "corrected_severity": "high",
    "what_is_actually_true": "Confirmed, and the dominant cost is not the round trips: on 12,836 rows the pure-Python `view_opening` step at services/bank_register_service.py:212-215 alone takes 29.2 seconds of CPU, on top of paging every bank_transaction on the account (services/bank_register_service.py:59-85) and building the whole register in Python (domain/banking/register.py:146-206). That is the exact pattern CLAUDE.md's 'Reporting performance' rule forbids, on a route listed as a report.",
    "probe": "PYTHONPATH=apps/api python - with 12,836 synthetic rows: 'build_register over 12836 rows: 0.071s' then 'min(filtered, key=lines.index) over 12836: 29.185s' \u2014 i.e. the unfiltered Bank Book spends ~29s of CPU inside one request before any network, well past lib/api's 45s abort once the thirteen Singapore->Mumbai pages are added.",
    "lens_notes": {
     "literal": "Confirmed, and the cost is larger than the finding says. services/bank_register_service.py:59-85 `_txns` uses `fetch_all` to page EVERY bank_transaction on the account (its own docstring: 'ALL of them \u2014 not just posted ones'); :87-98 reads the reconciliation statuses; domain/banking/register.py:146-206 `build_register` sorts the whole set and accumulates the balance row by row in Python; the service then filters, sorts and slices in Python (:193-205). On top of that, services/bank_register_service.py:212-215 computes the view opening as `min(filtered, key=lambda l: all_lines.index(l))` \u2014 `all_lines.index` is O(n) over a frozen dataclass with 16 fields (domain/banking/register.py:75-76), so the default unfiltered view is O(n^2) field-wise comparisons.",
     "guards": "None. There is no SQL function for the register (grep of migrations for a register/bank-book function finds nothing \u2014 the SQL-side reports are public.cash_flow_report, migration 277, and public.schedule_iii_ageing, migration 303). The route is not behind a flag. `fetch_all` makes the read CORRECT (the docstring records the PostgREST 1000-row truncation it fixed) but that is exactly what makes it proportional to volume.",
     "reach": "Fully reachable and default-heavy. routers/banking.py:1139-1151 GET /banking/register, mounted at main.py:341; apps/web/app/clients/[id]/reports/bank-book/page.tsx renders components/banking/BankBook.tsx, linked as 'Bank Book' under Operational at apps/web/app/clients/[id]/reports/page.tsx:119-121. BankBook.tsx:92-95 defaults dateFrom/dateTo empty and status 'all', so the first render is the worst case. The same read shape is in services/bank_reconciliation_service.py:60-72 and is re-run by `opening_suggestion` (:165) on every session create."
    }
   },
   "rescore_2026_09_08": {
    "state": "still_open",
    "what_is_true_now": "Unchanged, and reproduced today at 32.4 s of pure CPU inside one request before any network. The Bank Book still fetches rows proportional to transaction volume and computes the running balance in Python, which the reporting rule forbids.",
    "remaining": null,
    "probe": "Ran the timing probe at HEAD against domain/banking/register.py with 12,836 synthetic rows: 'build_register over 12836 rows: 0.071s' then 'min(filtered,key=lines.index) over 12836: 32.407s'. services/bank_register_service.py:59-85 still pages every bank_transaction via fetch_all, and :212-215 still does `earliest = min(filtered, key=lambda l: all_lines.index(l))` \u2014 a linear .index() inside a min() over the whole list.",
    "against_commit": "46bd46c"
   }
  },
  {
   "id": "BANK-08",
   "kind": "performance",
   "severity": "high",
   "title": "The statement upload endpoint is `async def` but does blocking PDF rasterisation and up to twenty vision calls on the event loop",
   "evidence": "routers/banking.py:761-762 `async def upload_statement`, :956 `async def inspect_statement_file`, :994 `async def preview_statement_with_mapping` \u2014 the only three async routes in the router (every other route is a plain `def`, which FastAPI runs in a threadpool). Their bodies are fully synchronous: `_read_statement_file` (routers/banking.py:655-741) calls `parse_statement_detailed` \u2192 `_pdf_rows` \u2192 pdfplumber (domain/banking/normalizer.py:839-874), then `vision.page_images` rasterises up to MAX_PAGES = 20 pages at 150 dpi (domain/banking/vision.py:70-133), then `vision.read_statement` makes one BLOCKING Gemini call per page (vision.py:212-246, services/statement_vision.py:33-42). Nothing is awaited and nothing runs in a thread.",
   "ca_experience": "One CA uploading a 20-page scanned statement freezes the entire API process for every other user of the firm \u2014 the API runs as a single Render service in Singapore. Other screens hang and then show the generic error card. The uploading CA also has no progress indication for a request that can run for minutes; `uploadStatement` in apps/web/lib/api/index.ts:917-928 uses a bare fetch with no AbortController, so the browser waits until the platform proxy gives up.",
   "market_standard": "Zoho Books and QuickBooks both process a statement upload asynchronously and show an import job with progress; neither holds an HTTP request open across an OCR run.",
   "confidence": "high",
   "suggested_fix": "Cheapest correct fix: make the three routes plain `def` so Starlette runs them in the threadpool \u2014 the file read is the only genuinely async part and can be handled with `file.file.read()`. Then chunk the vision path: rasterise and read pages in batches with the client looping, the way Entries already loops redraft and pass-ready, so a long scan reports progress instead of occupying one request.",
   "effort": "hours",
   "verification": {
    "status": "confirmed",
    "by": "agent",
    "corrected_severity": "high",
    "what_is_actually_true": "Confirmed. The three multipart routes are the router's only `async def`s (routers/banking.py:762, 956, 994) and their bodies are entirely synchronous \u2014 pdfplumber, then a 150-dpi rasterisation of up to 20 pages, then one blocking Gemini call per page (domain/banking/vision.py:70-133, 212-246; services/statement_vision.py:32-42) \u2014 so they run on the event loop of a single gunicorn worker (apps/api/Dockerfile:22), stalling every other request, and the 120s worker timeout drops those requests rather than only the upload.",
    "probe": "grep -n 'async def' apps/api/routers/banking.py -> 762, 956, 994 only (64 routes total). grep -rn 'run_in_threadpool|anyio.to_thread|asyncio.to_thread' routers/banking.py domain/banking/*.py services/statement_vision.py -> no output. grep -n 'workers' apps/api/Dockerfile -> `--workers 1 ... --timeout 120`.",
    "lens_notes": {
     "literal": "Confirmed. `grep -n 'async def' apps/api/routers/banking.py` returns exactly three: :762 upload_statement, :956 inspect_statement_file, :994 preview_statement_with_mapping \u2014 out of 64 routes; every other handler is a plain `def` that Starlette runs in the threadpool. Their bodies await only `file.read()`: routers/banking.py:655-741 `_read_statement_file` calls parse_statement_detailed -> _pdf_rows -> pdfplumber synchronously, then vision.page_images (domain/banking/vision.py:70-133, rasterising up to MAX_PAGES = 20 at 150 dpi, vision.py:75), then vision.read_statement (vision.py:212-246), which loops the pages calling services/statement_vision.py:32-42 `call` \u2014 a synchronous `client.models.generate_content`. `grep -rn 'run_in_threadpool|anyio.to_thread|asyncio.to_thread'` over routers/banking.py, domain/banking/*.py and services/statement_vision.py returns nothing.",
     "guards": "Nothing bounds it, and the deployment makes it worse than the finding says. apps/api/Dockerfile:22 runs `gunicorn main:app --workers 1 --worker-class uvicorn.workers.UvicornWorker --timeout 120`: ONE worker, so a blocked event loop is the whole API, and the 120s gunicorn timeout kills the worker (dropping every other in-flight request) rather than just failing the upload. `allow_vision` defaults False (routers/banking.py:772), which limits the 20-call path to opt-in uploads, but the deterministic pdfplumber parse still runs on the loop for every PDF upload, inspect and preview.",
     "reach": "Fully reachable. All three routes are called from the import modal: apps/web/lib/api/index.ts:917-928 uploadStatement and :930-944 postStatementForm (used by inspectStatement :946-947 and previewStatement :949-950). Both use a bare `fetch` with no AbortController, unlike `request` at lib/api/index.ts:358-374, which arms a 45-second abort \u2014 so the browser has no client-side ceiling either."
    }
   },
   "rescore_2026_09_08": {
    "state": "still_open",
    "what_is_true_now": "Unchanged. The router's only three async routes are the three multipart ones, their bodies are still fully synchronous (pdfplumber, 150-dpi rasterisation of up to 20 pages, one blocking Gemini call per page), and nothing is offloaded to a thread.",
    "remaining": null,
    "probe": "`grep -n 'async def' apps/api/routers/banking.py` -> exactly 762, 956, 994 (upload_statement, inspect_statement_file, preview_statement_with_mapping). `grep -rn 'run_in_threadpool|anyio.to_thread|asyncio.to_thread' routers/banking.py domain/banking/*.py services/statement_vision.py` -> no output. `grep -n workers apps/api/Dockerfile` -> `--workers 1 ... --timeout 120`.",
    "against_commit": "46bd46c"
   }
  },
  {
   "id": "BANK-09",
   "kind": "data_integrity",
   "severity": "high",
   "title": "Two genuinely identical transactions on a statement with no balance column are silently merged into one",
   "evidence": "domain/banking/dedup.py:18-40 \u2014 `transaction_hash` is over (client, bank account, date, debit, credit, BALANCE, description, reference). services/banking_service.py:171-187 drops within-file duplicates keeping the first, and reports the loss only as `duplicates_skipped`. When the statement carries no balance column \u2014 most PDF statements, per the module's own note in domain/banking/tie_out.py:13-15 \u2014 every row's balance_paise is 0, so the balance term stops discriminating. I verified in this session: `transaction_hash(\"c1\",\"a1\",\"2026-04-10\",50000,0,0,\"UPI/DR/RAMESH KUMAR\",None)` computed twice gives the same hash. The tie-out cannot catch it either: statement_check runs on `txns` BEFORE dedup (routers/banking.py:849-853), so the totals agree and the import reports itself verified.",
   "ca_experience": "Two \u20b9500 UPI payments to the same payee on the same day, on a PDF statement with no reference column, become one \u20b9500 line. The client's bank balance in the books is \u20b9500 higher than the bank's, the expense is understated by \u20b9500, and the import screen says \"verified \u2014 every line was read\" with \"1 line was already in and was skipped\". The CA finds it at reconciliation, if at all.",
   "market_standard": "Zoho Books and QuickBooks dedupe on the bank's own transaction id from the feed, and on manual import they flag a possible duplicate for the user to accept or reject rather than dropping it. Tally's Excel import does not dedupe at all \u2014 it imports both and lets the user see them.",
   "confidence": "high",
   "suggested_fix": "Add the row's ordinal position within the file to the hash, so two identical rows are two rows. Then move the WITHIN-FILE duplicate drop from silent to reported: if two rows in one file hash identically, keep both and let the cross-file check (which is what idempotent re-import actually needs) do the deduping. At minimum, run statement_check on the deduped set so a merged row breaks the totals.",
   "effort": "hours",
   "verification": {
    "status": "confirmed",
    "by": "agent",
    "corrected_severity": "high",
    "what_is_actually_true": "Exactly as described: with no balance column every row's balance_paise is 0 (domain/banking/normalizer.py:458), so transaction_hash collapses two genuinely identical rows (domain/banking/dedup.py:29-39), services/banking_service.py:171-183 keeps only the first, and the verification that would have caught it ran on the pre-dedup list (routers/banking.py:852-855 vs :896-900). The one correction is that the fix is bigger than 'hours': migration 224's UNIQUE index on (client_id, import_hash) makes keeping both rows a schema change, not just a service change.",
    "probe": "cd apps/api && python -c \"from domain.banking.dedup import transaction_hash; ...\" printed 'identical hashes: True 950030cb41b728ab' for transaction_hash('c1','a1','2026-04-10',50000,0,0,'UPI/DR/RAMESH KUMAR',None) computed twice.",
    "lens_notes": {
     "literal": "Confirmed at the cited lines. domain/banking/dedup.py:18-40 hashes (client, bank_account, date[:10], debit, credit, BALANCE, whitespace/case-normalised description, reference). normalizer.py:458 sets balance_paise=_to_paise(col(\"balance\")), which is 0 when the layout has no balance column, so the balance term stops discriminating exactly as claimed. services/banking_service.py:171-187 builds `seen`/`deduped` and keeps only the first row per hash; the loss is reported only as the aggregate `duplicates_skipped` (:186-190). The order claim also holds: routers/banking.py:852-855 runs statement_check(txns, ...) on the PARSED list before banking_service.import_normalized is called at :896-900, so the tie-out and the printed-totals check both see the un-deduped rows and agree while one row is dropped.",
     "guards": "Every guard I could find either misses this or makes it worse. (1) tie_out/statement_check runs pre-dedup (above), so `verified: true` is returned for an import that stored one row fewer. (2) balance_agreement is also computed on `txns` (routers/banking.py:882) and in any case returns checked:false when there is no balance column \u2014 domain/banking/tie_out.py:13-15 says so in the module's own words. (3) Migration 224 (uq_bank_txns_client_import_hash, a UNIQUE index on (client_id, import_hash)) does not catch it \u2014 it ENFORCES the collapse at the DB level, so the suggested fix of \"keep both rows\" needs a migration, not just a service change. (4) tests/test_bank_feed_import.py:335-344 (test_import_dedups_duplicate_rows_within_file) asserts imported==1 / duplicates_skipped==1 for two identical rows, and :378-385 (test_distinct_same_day_same_amount_kept) shows the design relies on a DIFFERENT RUNNING BALANCE to tell two real transactions apart \u2014 the very column this case lacks. Nothing in CLAUDE.md records the no-balance case as a deliberate decision.",
     "reach": "Real. routers/banking.py's router is mounted at apps/api/main.py:341, the import endpoint is the only way statement data enters the product (CLAUDE.md, 'Bank data'), and the PDF/vision paths that most often lack a balance column go through the same _import_core."
    }
   },
   "rescore_2026_09_08": {
    "state": "still_open",
    "what_is_true_now": "Unchanged. With no balance column every row's balance_paise is 0, so two genuinely identical transactions still collapse to one, silently, and the totals check that would catch it still runs on the list before the merge.",
    "remaining": null,
    "probe": "Ran at HEAD: `transaction_hash('c1','a1','2026-04-10',50000,0,0,'UPI/DR/RAMESH KUMAR',None)` twice -> 'identical hashes: True 950030cb41b728ab'. services/banking_service.py:172-181 still keeps only the first of a within-file hash collision (`if h not in seen: seen.add(h); deduped.append(...)`) and reports the loss only as `duplicates_skipped`. routers/banking.py:853 still runs statement_check on the PRE-dedup `txns`.",
    "against_commit": "46bd46c"
   }
  },
  {
   "id": "BANK-10",
   "kind": "gap",
   "severity": "medium",
   "title": "Auto-match never suggests a part payment against a partially-paid invoice, because the amount band is taken on the document TOTAL",
   "evidence": "services/bank_matching_service.py:300-336 \u2014 `_fetch_invoice_pool` bands the SQL query on `total_paise`, and `_invoices_from` filters again with `self._in_band(int(r.get(\"total_paise\") or 0), amount)`, setting `Candidate.amount_paise = total` and `outstanding_paise = total - paid` only as an extra scoring signal. Bills do the same on `net_payable_paise` (:338-385). domain/banking/matcher.py:167-182 computes `difference = c.amount_paise - txn_amount` and CONTINUES (skips the candidate) when the bank line is short by more than NEAR_MATCH_BAND_BPS = 2500 (25%, matcher.py:46). So a \u20b92,00,000 receipt against a \u20b93,00,000 invoice with \u20b91,00,000 already paid is 33% short of the total and is dropped, even though it settles the outstanding exactly. domain/banking/candidate_search.py does lift the band, but it ranks by `abs(h.difference_paise)` against the total too (:150-157), so the same document is described as \"short by \u20b91,00,000 \u2014 consistent with \u2026 TDS\" (candidate_search.py:171-177).",
   "ca_experience": "Staged payments are the norm in Indian SME practice. The second and third instalments against an invoice show no suggestion at all on the Entries row \u2014 the line reads \"From whom, or which ledger?\" \u2014 so the CA has to open the line, use Find Match, and read a \"short by \u20b91,00,000, consistent with TDS\" label that is nonsense. The settlement itself works correctly (bank_posting_service._settle_doc allocates against true outstanding), so this is purely a suggestion failure, but it is the failure a CA notices on every instalment.",
   "market_standard": "Zoho Books offers an invoice whose BALANCE DUE matches the deposit and lets you apply a partial payment. Tally's bill-wise details are built entirely around Against Ref / On Account allocation across part payments. QuickBooks matches on open balance, not on invoice total.",
   "confidence": "high",
   "suggested_fix": "Band and score on OUTSTANDING, not on the document total: use `total_paise - paid_paise` for invoices and the equivalent for bills in both the pool query and `_in_band`, set `Candidate.amount_paise` to the outstanding figure, and keep the total for the label only. `difference_paise` then means what the row says it means, and the TDS shape is detected against the right base.",
   "effort": "days",
   "verification": {
    "status": "confirmed",
    "by": "agent",
    "corrected_severity": "medium",
    "what_is_actually_true": "A part-payment against a partly-paid invoice or bill is never auto-suggested, because both the SQL band and the in-memory re-filter use the document TOTAL (bank_matching_service.py:299-336) and matcher.py:173-179 drops anything short by more than 25%. Find Match does surface the document but describes and ranks the shortfall against the total (candidate_search.py:142,158,176-186), so the difference and any TDS label are computed on the wrong base. Settlement itself is right (bank_posting_service.py:498-511).",
    "probe": "cd apps/api && python -c \"from domain.banking.matcher import rank_suggestions, Candidate; c=Candidate('sales_invoice','i1','INV-1',30000000,'2026-04-01',outstanding_paise=20000000); print(rank_suggestions(20000000,'2026-04-01','NEFT ACME',[c]))\" printed '[]' \u2014 a \u20b92,00,000 receipt that exactly clears the \u20b92,00,000 outstanding on a \u20b93,00,000 invoice yields no suggestion at all.",
    "lens_notes": {
     "literal": "Confirmed line by line. services/bank_matching_service.py:299-306 bands the invoice query on total_paise; :315-336 (_invoices_from) re-applies _in_band on total_paise, skips fully-paid rows, and sets Candidate.amount_paise = total with outstanding_paise = total - paid as a side field only. _in_band (:214-218) is `amount <= value <= _near_match_ceiling(amount)`, so a document larger than the bank line by more than the near-match band is out. Bills band on net_payable_paise (:338-385), which is TDS/note-net but NOT paid-net, so a part-paid bill fails the same way. domain/banking/matcher.py:167-182 computes difference = c.amount_paise - txn_amount and `continue`s when the line is short by more than NEAR_MATCH_BAND_BPS = 2500 (matcher.py:46).",
     "guards": "Find Match does list the document \u2014 services/bank_candidate_search_service.py:73-94 and :121-144 fetch by DATE with no amount band and carry outstanding_paise \u2014 but domain/banking/candidate_search.py:142 computes difference_paise against c.amount_paise (the TOTAL) and :158-168 ranks on abs(difference_paise), and describe() (:176-186) prints 'Bank line is short by <total shortfall>'. The TDS label at :153 fires whenever total_shortfall/total lands on one of matcher.TDS_RATES_BPS (0.1/1/2/5/10/20%) within \u00b1\u20b91, so a \u20b91,00,000 invoice with \u20b910,000 already paid and a \u20b990,000 second instalment is described as '10% TDS'. The auditor's own \u20b93,00,000/\u20b91,00,000 example is 33%, which is not in the table, so that specific quoted sentence would not appear \u2014 the mechanism is real, the illustration is off. Settlement is correct, as claimed: services/bank_posting_service.py:498-511 (_invoice_outstanding/_bill_outstanding) and _settle_doc (:637) allocate against true outstanding.",
     "reach": "Live. The suggestion path feeds the Entries drafts (services/bank_entry_service.py:371-373 -> suggestions_for_many, then E.from_documents), and the Find Match search is mounted at routers/banking.py:1197-1221."
    }
   },
   "rescore_2026_09_08": {
    "state": "still_open",
    "what_is_true_now": "Unchanged. A Rs 2,00,000 receipt that exactly clears the Rs 2,00,000 outstanding on a Rs 3,00,000 invoice still yields no auto-suggestion, and Find Match still ranks and labels the shortfall against the document total.",
    "remaining": null,
    "probe": "Ran at HEAD: `rank_suggestions(20000000,'2026-04-01','NEFT ACME',[Candidate('sales_invoice','i1','INV-1',30000000,'2026-04-01',outstanding_paise=20000000)])` -> `[]`. services/bank_matching_service.py:305/319/327 still band and re-filter invoices on `total_paise`; :355/369-379 still uses `net_payable_paise` for bills (the gross, not the outstanding).",
    "against_commit": "46bd46c"
   }
  },
  {
   "id": "BANK-11",
   "kind": "missing_feature",
   "severity": "medium",
   "title": "The rule engine is one case-insensitive substring plus an amount range and a direction \u2014 no regex, no multiple conditions, no payee/reference field, no split action, no priority",
   "evidence": "domain/banking/rules.py:52-69 `rule_matches` is the whole engine: `pattern not in narration.lower()`, an amount min/max, and a txn_type of debit/credit/any. `RuleSuggestion` (:31-49) carries only a category, ONE account_id, a narration, a GST rate and an interstate flag \u2014 a rule can never propose a split across ledgers, a party (customer/vendor), or a TDS treatment. Precedence is creation order only (routers/banking.py:2042-2044 and :2065 `.order(\"created_at\")`); there is no priority column and no way to reorder. The UI matches: apps/web/components/banking/RulesTab.tsx:233-245 exposes exactly narration-contains, min, max, direction.",
   "ca_experience": "A CA cannot write \"NEFT from any of these three customers \u2192 Trade Receivables, set the party\", or \"salary batch \u2192 split 60% Salaries / 40% Director's Remuneration\", or \"EMI to HDFC \u2192 principal to Loan, interest to Interest Expense\". Every EMI, every payroll batch and every mixed payment stays a manual split, twelve times a year per client. And because the first matching rule wins with no way to reorder, a broad rule written early permanently shadows a narrow one written later.",
   "market_standard": "Zoho Books bank rules support multiple criteria with AND/OR over description, payee, reference number and amount, with contains / starts with / is operators, and actions that set the category, the payee and the tax. QuickBooks bank rules do the same plus rule priority and split lines by percentage or amount. This is the single biggest gap against both.",
   "confidence": "high",
   "suggested_fix": "Three increments, in value order: (1) add a `priority` integer and order by it \u2014 one column, one migration; (2) let a rule carry several conditions over description/reference/amount with contains/starts-with/equals; (3) let a rule name split legs (percentage or fixed) reusing domain/banking/splits.build_split_lines, which already exists and is tested. Keep the trusted-rule CHECK in step.",
   "effort": "weeks",
   "verification": {
    "status": "confirmed",
    "by": "agent",
    "corrected_severity": "medium",
    "what_is_actually_true": "The rule engine is exactly one substring + amount range + direction (domain/banking/rules.py:52-69), the payload cannot name a party, a TDS treatment or split legs (rules.py:31-49), and precedence is created_at with no priority column and no reorder control (bank_entry_service.py:365-367, RulesTab.tsx:226-252). One nuance the finding omits: a rule can also carry a GST rate and an interstate flag (migration 254), which QuickBooks/Zoho rules do not.",
    "probe": "grep -rn 'priority' apps/api/migrations/*.sql | grep -i rule returned only migration 045 (assignment_rules) \u2014 bank_matching_rules has no priority column.",
    "lens_notes": {
     "literal": "Confirmed. domain/banking/rules.py:52-69 rule_matches is exactly is_active + one case-insensitive substring on description_pattern + amount_min/max + txn_type debit|credit|any \u2014 no regex, no second condition, no reference/payee field. RuleSuggestion (:31-49) carries rule_id, rule_name, category, ONE account_id, narration, gst_rate_bps, is_interstate; there is no party, no TDS and no split leg. Precedence is creation order: services/bank_entry_service.py:365-367 orders active rules by created_at, routers/banking.py:2064-2066 does the same for the list, and match_rule (rules.py:71-100) returns the FIRST non-empty match. No priority column exists \u2014 the only 'priority' in apps/api/migrations is migration 045 on assignment_rules; bank_matching_rules has none (054:150-163, plus 093/096/254/322 additions). The UI matches: apps/web/components/banking/RulesTab.tsx:226-252 offers narration-contains ('Plain text, not case-sensitive. No wildcards.'), min, max, direction only.",
     "guards": "None that closes the gap. domain/banking/splits.py exists and is tested, but splits are a manual per-line action, not a rule action. GST rate and interstate are extra rule payload (migration 254), which is more than the finding credits but is not a second condition. There is no reorder endpoint or drag handle in RulesTab.tsx; the only workaround for shadowing is turning a rule off (RulesTab.tsx:355-358) and recreating it.",
     "reach": "The Rules tab is one of the three live bank tabs (docs/architecture/09-bank-entries.md:210-215), and trusted rules post unattended (services/bank_entry_service.py:536-551), so the expressiveness limit reaches every client."
    }
   },
   "rescore_2026_09_08": {
    "state": "still_open",
    "what_is_true_now": "Unchanged. One substring, one amount range, one direction; no regex, no multiple conditions, no party, no TDS, no split legs, no priority and no reorder.",
    "remaining": null,
    "probe": "domain/banking/rules.py:52-70 rule_matches is still `pattern not in narration.lower()` plus an amount min/max plus a debit/credit/any txn_type, and nothing else. `grep -rn priority apps/api/migrations/*.sql | grep -i bank` -> no match (bank_matching_rules still has no priority column); precedence is still created_at order.",
    "against_commit": "46bd46c"
   }
  },
  {
   "id": "BANK-12",
   "kind": "data_integrity",
   "severity": "medium",
   "title": "Deleting a matching rule silently strips the trusted-rule attribution from every entry it posted, and needs only banking.write",
   "evidence": "routers/banking.py:2166-2181 `delete_rule` hard-DELETEs the row on `rbac(\"banking\", \"write\")` (Executive+), with no audit_log write, and its docstring justifies this as \"A rule is configuration, not a financial record \u2014 it has never written anything to the ledger.\" That is no longer true: migration 322 added `posted_by_rule_id UUID REFERENCES public.bank_matching_rules(id) ON DELETE SET NULL` on bank_transactions, and services/bank_entry_service.py:536-551 stamps it plus an audit row whenever a trusted rule passes a line. Promotion to trusted is deliberately gated on `banking.approve` (Manager+, routers/banking.py:2118-2129), but deletion is not.",
   "ca_experience": "The register sentence the design promises \u2014 \"Passed by rule Bank charges, trusted by Priya on 3 Sep\" (docs/architecture/09-bank-entries.md) \u2014 becomes blank on every entry the rule posted, and the Rules tab's \"what it has passed\" count disappears with it. An Executive can erase a Manager's authorisation trail for hundreds of auto-posted journals with one click, and there is no audit_log row saying the rule ever existed.",
   "market_standard": "Zoho Books keeps the rule name on the transaction's audit trail after the rule is deleted. QuickBooks records \"Added by rule <name>\" in the audit log independently of the rule row.",
   "confidence": "high",
   "suggested_fix": "Refuse to delete a rule that has posted anything (`posted_by_rule_id` references exist) \u2014 deactivate it instead, which the endpoint already supports; require `banking.approve` to delete a rule that is or has been trusted; and write the whole rule row to audit_log in the same transaction, the way the journal deletes do.",
   "effort": "hours",
   "verification": {
    "status": "confirmed",
    "by": "agent",
    "corrected_severity": "low",
    "what_is_actually_true": "Deleting a rule is a hard DELETE on banking.write with no application-level audit write (routers/banking.py:2166-2181), while trusting it needs banking.approve (:2116-2129) \u2014 a real permission asymmetry, and the docstring's 'it has never written anything to the ledger' is stale since migration 322:62. But the authorisation trail is NOT erased: the per-entry audit row carrying rule_name and trusted_by is written at pass time and survives (bank_entry_service.py:540-548), and migration 111's trg_audit_capture logs the DELETE with the full old row under the production USE_USER_JWT posture (render.yaml:174-197). The only real loss is the ' \u00b7 rule' badge on the entry row (EntriesTab.tsx:503) \u2014 the register sentence and the 'what it has passed' count the finding says disappear were never built (docs/architecture/09-bank-entries.md:195 vs RulesTab.tsx:210).",
    "probe": "sed -n '98,143p' apps/api/migrations/111_audit_capture_triggers.sql showed the generic trigger loop over every firm-scoped table with an id, exclusion array printed in full \u2014 bank_matching_rules is not in it; grep -n 'passed|count' apps/web/components/banking/RulesTab.tsx showed line 210 counting rules and trusted rules only.",
    "lens_notes": {
     "literal": "The mechanism is real but the consequences are not. routers/banking.py:2166-2181 delete_rule is rbac('banking','write') (Executive+), hard-DELETEs the row, writes no audit row in application code, and its docstring still says 'it has never written anything to the ledger' \u2014 stale since migration 322:62 added posted_by_rule_id UUID REFERENCES bank_matching_rules(id) ON DELETE SET NULL, stamped by services/bank_entry_service.py:536-539. Promotion to trusted does require banking.approve (routers/banking.py:2116-2129), so the permission asymmetry is genuine.",
     "guards": "Two guards refute the damage claim. (1) The pass-time audit row survives the delete: services/bank_entry_service.py:540-548 calls audit_service.log_event with rule_id, rule_name AND trusted_by in new_data and source='bank_trusted_rule' \u2014 that record is in audit_log independently of the rule row, and the journal's created_by is trusted_by regardless. (2) Migration 111:98-143 attaches trg_audit_capture AFTER INSERT OR UPDATE OR DELETE to every firm-scoped table with an id that is not in its exclusion list; bank_matching_rules qualifies (migration 054:150-163 has firm_id and id) and is not excluded, so the DELETE writes an audit_log row whose old_data is the whole rule. That trigger only fires when auth.uid() is non-null (111:41-45), and in production it is: render.yaml:174-197 records that USE_USER_JWT is true, and routers/banking.py:84-89 -> core/supabase_client.py:134-138 then returns the caller-JWT client. So 'there is no audit_log row saying the rule ever existed' is wrong for the live deployment.",
     "reach": "Two of the three named consequences do not exist to lose. The register sentence 'Passed by rule Bank charges, trusted by Priya on 3 Sep' (docs/architecture/09-bank-entries.md:195) is not implemented anywhere \u2014 the UI only ever renders a flag, ' \u00b7 rule' (apps/web/components/banking/EntriesTab.tsx:502-503) and ' \u00b7 by a trusted rule' (EntryDetailModal.tsx:354). The Rules tab has no 'what it has passed' count: RulesTab.tsx:210 shows the rule count and trusted count only, and there is no rule filter on the Entries list either. So what actually goes blank on delete is a two-word badge."
    }
   },
   "rescore_2026_09_08": {
    "state": "still_open",
    "what_is_true_now": "Unchanged, including the stale docstring. The permission asymmetry (Executive can delete a rule a Manager trusted) and the loss of the on-row rule attribution both stand; the per-entry audit row written at pass time still survives, which is why the verifier corrected this to low.",
    "remaining": null,
    "probe": "routers/banking.py:2166-2181 delete_rule is still `Depends(rbac(\"banking\", \"write\"))` doing a hard `.delete()` with no audit call, and its docstring still says \"it has never written anything to the ledger\" \u2014 while migrations/322:62 still declares `posted_by_rule_id UUID REFERENCES public.bank_matching_rules(id) ON DELETE SET NULL` and services/bank_entry_service.py:538 still stamps it. Trusting a rule is still gated on banking.approve at :2116-2129.",
    "against_commit": "46bd46c"
   }
  },
  {
   "id": "BANK-13",
   "kind": "gap",
   "severity": "medium",
   "title": "The exception-rules engine \u2014 materiality, duplicates, cash withdrawals, new payees \u2014 is complete, tested, and wired to nothing",
   "evidence": "domain/banking/exceptions.py is 315 lines implementing `_above_materiality`, `_short_settlement`, `_weak_match`, `_new_payee`, `_unusual_account`, `_cash_withdrawal`, `_duplicate_suspect` with per-client policy thresholds. Its docstring (:20-22) says the context-gathering lives in `services/bank_exception_service.py` \u2014 that file DOES NOT EXIST (`ls apps/api/services/bank_exception_service.py` \u2192 no such file). A repo-wide grep for `banking.exceptions` / `bank_exception` returns exactly two hits: the module itself and tests/test_bank_exception_rules.py (25 tests). Nothing else imports it. The related column tells the same story: `needs_review` is only ever written FALSE (services/bank_matching_service.py:816, services/bank_posting_service.py:1060) \u2014 nothing sets it True \u2014 yet services/bank_register_service.py:41,115 exposes a `needs_review` status filter and apps/web/components/banking/BankBook.tsx:80 renders it as a \"Needs review\" option in the Bank Book dropdown.",
   "ca_experience": "There is no partner review list. A partner has no screen that says \"these eleven lines are worth looking at this month\" \u2014 an amount over materiality, a cash withdrawal over \u20b950,000, a payee never seen before, a possible duplicate a week apart. And the Bank Book's \"Needs review\" filter always returns an empty table, which reads as \"nothing needs review\" rather than \"this filter does nothing\".",
   "market_standard": "This is where the product could beat everyone \u2014 Tally, Zoho and QuickBooks all lack a risk-based partner review over the bank feed; it is exactly the kind of analytics the owner wants CAs addicted to. The work is already done and unreachable.",
   "confidence": "high",
   "suggested_fix": "Write the missing `bank_exception_service` (gather the payee/account context the pure module wants), store the flags on the row or compute them per page, and surface them as a fourth Entries filter or a partner dashboard card. Until then, remove the \"Needs review\" option from the Bank Book so it does not lie, and mark the module's tests as covering an unwired feature.",
   "effort": "days",
   "verification": {
    "status": "confirmed",
    "by": "agent",
    "corrected_severity": "medium",
    "what_is_actually_true": "domain/banking/exceptions.py and its 25 tests are complete and imported by nothing but the tests; the service its own docstring names (services/bank_exception_service.py:20-23) was never written. needs_review is never set True anywhere, yet the Bank Book's 'Needs review' filter is a live, selectable option (bank_register_service.py:41,115; BankBook.tsx:80) that always returns an empty table.",
    "probe": "ls apps/api/services/bank_exception_service.py -> 'No such file or directory'; grep -rn 'needs_review.*True' across apps/api and apps/web returned only the read filter (bank_matching_service.py:509) and one test fixture (test_bank_matching.py:526).",
    "lens_notes": {
     "literal": "Confirmed. apps/api/domain/banking/exceptions.py is 315 lines of pure rules and its docstring at :20-23 points at services/bank_exception_service.py, which does not exist (`ls` returns 'No such file or directory'). A repo-wide grep for 'domain.banking.exceptions' / 'bank_exception' returns exactly two hits: the module and tests/test_bank_exception_rules.py (295 lines). The module is not re-exported from domain/banking/__init__.py either. needs_review is written only False in application code \u2014 services/bank_matching_service.py:816 and services/bank_posting_service.py:1060 \u2014 and bank_entry_service.py:570 only restores a prior value; the only True in the whole repo is the READ filter at bank_matching_service.py:508-509 and a test fixture at tests/test_bank_matching.py:526.",
     "guards": "None. Migration 096:24 defaults needs_review to false and nothing sets it. There is no endpoint or model field that accepts needs_review (routers/banking.py has it only in the register status pattern at :1145; models/banking.py has none), so a CA cannot even flag a line by hand.",
     "reach": "Both halves reach a user, which is what makes it worth fixing. The dead filter is live: services/bank_register_service.py:41 lists needs_review among STATUS_FILTERS, :115 filters on the column, routers/banking.py:1145 accepts it, and apps/web/components/banking/BankBook.tsx:80 renders 'Needs review' as an option on the Bank Book report page (apps/web/app/clients/[id]/reports/bank-book/page.tsx), which is linked from the reports index (apps/web/app/clients/[id]/reports/page.tsx:119-121). The exception engine reaches nobody. A backend capability reachable from no screen CONFIRMS the gap."
    }
   },
   "rescore_2026_09_08": {
    "state": "still_open",
    "what_is_true_now": "Unchanged. The exception-rules module and its 25 tests are still imported by nothing but the tests, the service its docstring names still does not exist, and the Bank Book's 'Needs review' filter still always returns an empty table.",
    "remaining": null,
    "probe": "`ls apps/api/services/bank_exception_service.py` -> 'No such file or directory'. `grep -rn 'banking.exceptions|bank_exception' apps/api --include=*.py` -> two hits only: tests/test_bank_exception_rules.py:15 and the module's own docstring at domain/banking/exceptions.py:22. `grep -rn needs_review` -> the only writes are `\"needs_review\": False` (bank_matching_service.py:816, bank_posting_service.py:1060); the filter is still live at bank_register_service.py:41,115 and BankBook.tsx:80.",
    "against_commit": "46bd46c"
   }
  },
  {
   "id": "BANK-14",
   "kind": "bug",
   "severity": "medium",
   "title": "A PDF whose first page has ruled table lines and whose later pages do not silently loses the later pages",
   "evidence": "domain/banking/normalizer.py:846-860 `_pdf_rows` loops every page collecting `page.extract_tables()`, then `if rows: return rows` \u2014 the geometric fallback `_rows_by_position` runs ONLY when NO page produced a ruled table anywhere in the document. A statement whose first page is ruled and whose continuation pages are not therefore returns page 1 only. Separately, `_rows_by_position` (:908-951) locates a header line per page and `if header is None: return []`, so a continuation page that does not repeat its column headings contributes nothing. Neither case raises; both come back as a short, well-formed row set.",
   "ca_experience": "A five-page bank PDF imports as one page. If the statement prints its own Grand Total or the CA typed the balances, the tie-out refuses the import with a confusing message; if it does neither, the import succeeds with `verified: false` and an amber \"nothing confirmed that every line was read\" note that is easy to click past \u2014 and four pages of transactions are simply not in the books.",
   "market_standard": "Dedicated PDF-to-Tally converters (the small industry this feature exists to replace) handle mixed-ruling documents by falling back per page. Zoho Books does not accept PDF at all, so the feature itself is ahead of the market \u2014 the per-page fallback is what makes it trustworthy.",
   "confidence": "high",
   "suggested_fix": "Decide the strategy per page, not per document: for each page, take `extract_tables()` if it yields rows, otherwise `_rows_by_position(page)`. Carry the header line found on the first page forward to pages that do not repeat it. Both are small changes inside `_pdf_rows`.",
   "effort": "hours",
   "verification": {
    "status": "confirmed",
    "by": "agent",
    "corrected_severity": "medium",
    "what_is_actually_true": "Exactly as described. normalizer.py:855-856 chooses the extraction strategy per DOCUMENT, not per page, so a mixed-ruling PDF silently keeps only the ruled pages; and _rows_by_position returns [] for any page that does not repeat its column headings (normalizer.py:924-925). The tie-out cannot rescue it because printed_totals reads the same truncated row set (tie_out.py:144-175), so the outcome is a short, well-formed import reported as verified:false rather than refused.",
    "probe": null,
    "lens_notes": {
     "literal": "Confirmed at the cited lines. domain/banking/normalizer.py:791 _pdf_rows opens the PDF, loops EVERY page appending page.extract_tables() rows into one list (:848-854), and then at :855-856 does `if rows: return rows`. The geometric fallback loop at :858-860 is reached only when the whole document produced no ruled table. So a document whose first page is ruled returns page 1's rows and never looks at the unruled continuation pages. Separately _rows_by_position (:908-951) finds a header line per page and returns [] at :924-925 when none is found, and again at :927 when the header has fewer than 3 columns \u2014 a continuation page that does not repeat its headings contributes nothing. Neither path raises.",
     "guards": "tie_out is the intended backstop and it fails in exactly this case. tie_out.printed_totals (domain/banking/tie_out.py:144-175) reads the bank's own 'Grand Total' line out of the SAME rows list, so if the totals row is on a page that was dropped there are no printed totals and totals_agreement reports checked:false. balance_agreement (normalizer.py:631-664) is a consecutive-delta check whose own module docstring (tie_out.py:17-21) records that truncating from either end leaves the chain self-consistent. statement_check (tie_out.py:272-302) then returns verified:false with a gap rather than a refusal unless the CA typed the opening/closing balances or a vision read was used (routers/banking.py:854-866). So the import proceeds with an amber note.",
     "reach": "Live: _pdf_rows is reached from parse_statement's extension dispatch (normalizer.py:979+) on the same POST /banking/statements/import path that is mounted at main.py:341."
    }
   },
   "rescore_2026_09_08": {
    "state": "still_open",
    "what_is_true_now": "Unchanged, both halves. A PDF whose first page is ruled and whose later pages are not still silently imports page 1 only, and a page without repeated headings still yields no rows.",
    "remaining": null,
    "probe": "domain/banking/normalizer.py:850-860 still loops every page collecting `page.extract_tables()` and then `if rows: return rows` before the geometric fallback runs \u2014 the strategy is still chosen per DOCUMENT. _rows_by_position still has `header = next((ln for ln in lines if _looks_like_header(...)), None)` followed by `if header is None: return []` (normalizer.py:922-925), so a continuation page that does not repeat its headings still contributes nothing.",
    "against_commit": "46bd46c"
   }
  },
  {
   "id": "BANK-15",
   "kind": "performance",
   "severity": "medium",
   "title": "Transfer detection scans only the newest 1,000 lines, and is re-run from scratch for every 100-line redraft chunk",
   "evidence": "services/bank_transfer_service.py:29 `SCAN_LIMIT = 1000` and :55-58 `.order(\"transaction_date\", desc=True).limit(SCAN_LIMIT)` \u2014 a hard cap, not `fetch_all`, and the only bank read that is not covered by tests/test_bank_reads_are_complete.py. services/bank_entry_service.py:417-431 `_pairs_by_txn` calls `detect_pairs` for the WHOLE client, and it is invoked once per redraft chunk (bank_entry_service.py:372, inside `redraft`, which processes REDRAFT_CHUNK = 100 lines). domain/banking/transfers.py:158-171 then builds every out\u00d7in candidate pairing, which is quadratic in the scanned set.",
   "ca_experience": "Two effects. (1) On a client with more than about a year of statement lines, an inter-account transfer in an older month is never proposed \u2014 the CA codes both halves separately and the P&L is overstated by the transfer, which is precisely what the feature exists to prevent. (2) Proposing for a 3,000-line back-log import runs the 1,000-row fetch and the quadratic pair scan thirty times, adding thirty Mumbai round trips and a lot of CPU to an operation the CA is watching a progress bar for.",
   "market_standard": "Zoho Books and QuickBooks both detect transfers across the whole account history, and neither recomputes the candidate set per page of the review queue.",
   "confidence": "high",
   "suggested_fix": "Compute the pair index ONCE per redraft run and pass it into each chunk (it is already a parameterised argument \u2014 `detect_pairs(..., txns=...)`). Replace the flat 1,000-row cap with a date window anchored on the chunk's own lines (\u00b1 DEFAULT_WINDOW_DAYS), which is both complete and far smaller.",
   "effort": "hours",
   "verification": {
    "status": "confirmed",
    "by": "agent",
    "corrected_severity": "medium",
    "what_is_actually_true": "Both halves hold. detect_pairs reads the newest 1,000 rows for the whole client with no date window (bank_transfer_service.py:54-58), and because _eligible filters AFTER that cap (transfers.py:113-129) the effective horizon is shorter than 1,000 lines \u2014 a client with two accounts at ~300 lines a month crosses it in under two months, not 'about a year'. And _pairs_by_txn (bank_entry_service.py:417-424) re-runs the fetch and the quadratic out x in scan (transfers.py:158-171) once per 100-line redraft chunk, even though detect_pairs already accepts a pre-fetched txns list.",
    "probe": "grep -n 'transfer' apps/api/tests/test_bank_reads_are_complete.py returned nothing, confirming bank_transfer_service is the one bank read that test does not cover.",
    "lens_notes": {
     "literal": "Confirmed, and slightly understated. services/bank_transfer_service.py:29 SCAN_LIMIT = 1000 and :54-58 order by transaction_date desc and .limit(SCAN_LIMIT) \u2014 a flat cap, not core.db_paging.fetch_all. The cap is applied in SQL BEFORE eligibility, and domain/banking/transfers.py:113-129 (_eligible) then discards posted, ignored, already-paired and document-matched lines, so on a worked client most of the 1,000-row budget is spent on lines that cannot pair at all. services/bank_entry_service.py:417-431 _pairs_by_txn calls detect_pairs for the whole client and is invoked at :372 inside redraft, which is capped at REDRAFT_CHUNK = 100 (:48) / MAX_CHUNK = 200 (:50,:360) and driven chunk-by-chunk from the browser (apps/web/components/banking/EntriesTab.tsx:134,295). domain/banking/transfers.py:158-171 builds every out x in candidate pairing before any greedy choice \u2014 quadratic in the eligible set.",
     "guards": "None. tests/test_bank_reads_are_complete.py exercises bank_register_service, bank_matching_service and bank_reconciliation_service (lines 334, 356, 366, 387, 400) and never touches bank_transfer_service, so the finding is right that this is the uncovered read. detect_pairs already takes an optional txns= argument (bank_transfer_service.py:48-50), so the per-chunk recomputation is avoidable today; nothing calls it that way. There is no date-window narrowing on the fetch.",
     "reach": "Live on the Entries tab: every redraft chunk pays for a full 1,000-row fetch plus the quadratic scan, and the transfer draft is one of the four sources E.choose ranks (bank_entry_service.py:400-415)."
    }
   },
   "rescore_2026_09_08": {
    "state": "still_open",
    "what_is_true_now": "Unchanged. The newest-1,000-row cap, the missing date window, the quadratic out x in pairing and the per-chunk re-run are all present, and it is still the one bank read the completeness test does not cover.",
    "remaining": null,
    "probe": "services/bank_transfer_service.py:29 still `SCAN_LIMIT = 1000` and :58 still `.limit(SCAN_LIMIT).execute()` with no date window and no fetch_all. services/bank_entry_service.py:417-431 _pairs_by_txn still calls `bank_transfer_service.detect_pairs(db, firm_id, client_id)` for the whole client, and :372 still calls it inside redraft, which runs REDRAFT_CHUNK = 100 lines at a time (:48). `grep -n transfer apps/api/tests/test_bank_reads_are_complete.py` -> no output.",
    "against_commit": "46bd46c"
   }
  },
  {
   "id": "BANK-16",
   "kind": "data_integrity",
   "severity": "medium",
   "title": "The nightly trusted-rule sweep will auto-post a line the CA coded themselves, and credit it to the person who trusted the rule",
   "evidence": "services/bank_entry_service.py:635-651 `pass_ready(only_trusted=True)` selects rows on `entry_state = 'ready'` AND `draft_rule_id IN (trusted rules)` \u2014 it does not exclude a row that is ready because a HUMAN coded it. For such a row `pass_entry` (:497) skips `_apply_draft` (correctly, `E.coded_by_a_human` is true) and posts the CA's own coding, but with `actor = rule.get(\"trusted_by\")` (:648) and then stamps `posted_by_rule_id` and an audit row with `source: bank_trusted_rule` (:536-551). Nothing clears `draft_rule_id` when a CA sets an account: banking_service.set_account (banking_service.py:307-333) writes account_id/category only, and migration 322's trigger clears `draft_error` but not the draft. jobs/bank_trusted_rules_job.py:75-83 runs this unattended every day.",
   "ca_experience": "The CA opens a bank-charge line that a trusted rule proposed, decides it is actually a Loan Processing Fee, picks that ledger, and moves on without pressing Pass. Overnight the sweep posts it \u2014 a journal the CA never authorised, with `created_by` set to the Manager who trusted an unrelated rule and an audit trail saying the rule did it. The amount is right; the authorisation and the attribution are not.",
   "market_standard": "Zoho Books auto-categorisation rules apply only to uncategorised feed transactions and never act on a transaction a user has touched. QuickBooks rules likewise stop at the first manual edit.",
   "confidence": "high",
   "suggested_fix": "In `pass_ready`, when `only_trusted` is set, skip any row where `E.coded_by_a_human(row)` is true \u2014 the rule's authority covers the rule's proposal, not a human's different answer. Cheap and exactly matches the design's own reasoning in docs/architecture/09-bank-entries.md.",
   "effort": "hours",
   "verification": {
    "status": "confirmed",
    "by": "agent",
    "corrected_severity": "medium",
    "what_is_actually_true": "Confirmed exactly as written. A line a trusted rule drafted, then re-coded by the CA to a different ledger and left unpassed, is picked up by the nightly sweep and posted with created_by/posted_by = the rule's trusted_by and an audit trail saying the rule did it (services/bank_entry_service.py:635-651, :497, :536-551). The ledger posted is the CA's own choice, so the amount and account are right; the authorisation and the attribution are not.",
    "probe": "Wrote a script against tests/test_bank_entry_service.py's TriggeredDB fake: seed a trusted rule (account acc-charges), redraft one 'NEFT CHARGES' line, then banking_service.set_account(..., RENT, derive_category=True) as the CA, then pass_ready(only_trusted=True). Printed: 'after CA codes it: account_id=acc-rent draft_rule_id=trusted state=ready coded_by_a_human=True' then 'sweep: {passed:1,...}' / 'post calls: [{txn_id: chg, actor_id: mgr-1, ...}]' / 'row after: account_id=acc-rent posted_by=mgr-1 posted_by_rule_id=trusted'.",
    "lens_notes": {
     "literal": "Exact. services/bank_entry_service.py:635-641 builds the sweep query as .eq('entry_state', E.READY).is_('draft_error','null').in_('draft_rule_id', trusted) \u2014 nothing excludes a row that is READY because a human coded it. domain/banking/entry.py:141-149 makes account_id alone enough for READY, and E.coded_by_a_human (entry.py:152-162) is true for it. pass_entry (bank_entry_service.py:497) then SKIPS _apply_draft and posts the CA's own coding, but with actor = rule['trusted_by'] (:648) and stamps posted_by_rule_id plus an audit row metadata={'source':'bank_trusted_rule'} (:536-551).",
     "guards": "None found. banking_service.set_account (services/banking_service.py:307-334) writes account_id/match_status/category only \u2014 draft_rule_id survives. Migration 322's trigger (migrations/322_...sql, the entry_state function) clears draft_error on a human answer but never the draft columns. redraft only touches rows with drafted_at IS NULL or older than stale_before (bank_entry_service.py:317-333), so a row already drafted keeps its draft_rule_id, and even a re-draft would recompute the same rule hit. Nothing in tests/test_bank_entry_service.py covers a human-coded row reaching the trusted sweep.",
     "reach": "Live. jobs/bank_trusted_rules_job.py:75-83 calls pass_ready(only_trusted=True) in a loop; jobs/scheduler.py:345-359 runs it as job #12 of the daily sweep, gated only on ENABLE_SCHEDULER. Trusted rules are a shipped feature (migration 322 \u00a72, Manager+ promotion)."
    }
   },
   "rescore_2026_09_08": {
    "state": "still_open",
    "what_is_true_now": "Unchanged. The nightly sweep still posts a line the CA re-coded by hand, credits it to the person who trusted the rule, and stamps posted_by_rule_id and a 'bank_trusted_rule' audit row. The ledger and the amount are right; the authorisation and the attribution are not.",
    "remaining": null,
    "probe": "Re-ran the verifier's scenario at HEAD against tests/test_bank_entry_service.TriggeredDB: seed a trusted rule (account Bank Charges, trusted_by mgr-1), redraft one 'NEFT CHARGES' line, then banking_service.set_account(..., RENT, derive_category=True) as the CA, then pass_ready(only_trusted=True, actor_id='exec-1'). Printed: 'after CA codes it: account_id=acc-rent draft_rule_id=trusted state=ready coded_by_a_human=True' / \"sweep: {'passed': 1, 'failed': 0, 'skipped': 0, 'remaining': 0}\" / \"post calls: [{'txn_id': 'chg', 'actor_id': 'mgr-1', 'by_rule': None}]\" / 'row after: account_id=acc-rent posted_by=mgr-1 posted_by_rule_id=trusted'. services/bank_entry_service.py:635-651 still has no E.coded_by_a_human filter in pass_ready's query.",
    "against_commit": "46bd46c"
   }
  },
  {
   "id": "BANK-17",
   "kind": "data_integrity",
   "severity": "medium",
   "title": "Undo reverses the journal of a line that sits inside a completed, certified reconciliation, with no check and no warning",
   "evidence": "services/bank_posting_service.py:690-820 `undo` checks match_status, the journal's source_type and the period lock \u2014 I grepped services/bank_posting_service.py, services/bank_matching_service.py and services/bank_entry_service.py for `reconciliation_id` and there are ZERO hits. So a line with `reconciliation_id` pointing at a session whose status is 'completed' is reversed like any other. The certified report itself is safe (bank_reconciliation_service.py:407-413 freezes a snapshot at completion), and the divergence surfaces later as `opening_balance_check` (domain/banking/reconciliation.py:78-111) the next time someone opens a session on that account \u2014 a warning, deliberately not a block.",
   "ca_experience": "A CA undoes a March line in June. March's BRS PDF still says it tied out; the books no longer agree with it. The register shows the line as 'R' (reconciled) with no journal \u2014 domain/banking/register.py:126-144 derives the flag from `reconciliation_id` alone. Nobody is told at the moment of the undo; the only signal is a mismatch figure on the NEXT reconciliation, which reads like a different problem.",
   "market_standard": "QuickBooks warns explicitly (\"this transaction is reconciled \u2014 changing it will affect your reconciliation\") and marks the period. Tally refuses to alter the bank date of a reconciled voucher without re-opening the reconciliation. Zoho Books requires you to un-reconcile first.",
   "confidence": "high",
   "suggested_fix": "In `undo` (and in `unmatch`), read the line's `reconciliation_id` and its session status. If the session is completed, either refuse with \"this line is part of a completed reconciliation for <period> \u2014 reopen it first\" (the reopen path with its mandatory reason already exists), or require an explicit acknowledgement and clear `reconciliation_id` so the register stops claiming 'R'.",
   "effort": "hours",
   "verification": {
    "status": "confirmed",
    "by": "agent",
    "corrected_severity": "low",
    "what_is_actually_true": "undo (services/bank_posting_service.py:690-820) genuinely never reads reconciliation_id: it does not refuse, does not warn at the moment, and does not clear the flag, so domain/banking/register.py:126-144 keeps reporting the line as 'R'. But the consequence is detected \u2014 services/reconciliation_service.py:427-437 raises a critical 'journal_not_posted' finding for exactly this, nightly and on demand, and it reaches the CA on the Verify Books tab. What is missing is only the inline warning at the click, not the detection, so this is polish rather than a market-standard gap.",
    "probe": null,
    "lens_notes": {
     "literal": "The mechanical claim holds. `grep -rn reconciliation_id apps/api/services/bank_posting_service.py apps/api/services/bank_matching_service.py apps/api/services/bank_entry_service.py` returns zero hits, so undo (bank_posting_service.py:690-820) checks match_status, the journal's source_type and the period lock and nothing else. Its final update (:793-796) writes match_status/posted_at/posted_by/posted_journal_id only, leaving reconciliation_id set \u2014 and domain/banking/register.py:126-144 derive_cleared returns 'R' from reconciliation_id + a completed session, so the register does go on showing 'R' with no journal behind it.",
     "guards": "The finding's severity rests on 'nobody is told', and that is FALSE. services/reconciliation_service.py:350-448 check_bank_reconciliation_discrepancies compares every completed session's frozen snapshot against live data and raises a CRITICAL finding with reason='journal_not_posted' when 'the journal behind a transaction reconciled in the completed reconciliation ... is no longer posted \u2014 it was reversed or deleted after the period was certified'. Its own docstring names it: 'This is the check QuickBooks calls the Reconciliation Discrepancy Report.' It is in _CHECKS (reconciliation_service.py:518-527), runs nightly as scheduler job #9 (jobs/scheduler.py:315-330) and on demand via POST /api/reconciliation/verify (routers/reconciliation.py:37-60). So the market-standard detection the finding says is absent is implemented.",
     "reach": "routers/reconciliation.py is mounted (main.py:68, :284). The findings surface on a real screen: apps/web/app/clients/[id]/accounting/page.tsx:39,51,301 has a 'Verify Books' tab, linked from app/clients/[id]/reports/page.tsx:88. The undo itself is reachable from the posted-row Undo button."
    }
   },
   "rescore_2026_09_08": {
    "state": "still_open",
    "what_is_true_now": "Unchanged. Undo still silently reverses a line inside a completed, certified reconciliation and the register keeps reporting it as 'R'; the Verify Books sweep still catches the consequence later, which is why this is polish rather than a market gap.",
    "remaining": null,
    "probe": "`grep -n reconciliation_id apps/api/services/bank_posting_service.py apps/api/services/bank_matching_service.py apps/api/services/bank_entry_service.py` -> exit 1, zero hits. So undo still never reads the line's reconciliation, never refuses, never warns and never clears the flag. The after-the-fact detection is still there: services/reconciliation_service.py:442-445 still raises the 'journal_not_posted' finding for a certified period.",
    "against_commit": "46bd46c"
   }
  },
  {
   "id": "BANK-18",
   "kind": "ux",
   "severity": "medium",
   "title": "The import module's most important refusals reach the CA as raw JSON: \"API error 422: {\\\"detail\\\":\\\"...\\\"}\"",
   "evidence": "apps/web/lib/api/index.ts:917-928 `uploadStatement` throws `new Error(\\`API error ${res.status}: ${await res.text()}\\`)`. The very next function, `postStatementForm` (:930-944), does the right thing with `errorMessage(res)` (:450-476), which unwraps `detail` \u2014 and the comment there even says \"A 422 here is a real answer \u2026 and its message is written for the CA\". AccountsPanel.tsx:704-707 renders whatever the error's `message` is. The sentences being mangled are the carefully-written ones in domain/banking/tie_out.py:110-115 and :264-268 (\"The statement does not add up. Opening \u20b91,30,000.00 plus credits \u2026\") and the scan refusals in routers/banking.py:706-741.",
   "ca_experience": "The one moment the module most needs to be understood \u2014 an import refused because the arithmetic does not land \u2014 the CA is shown a developer string wrapped around the explanation. The Indian-grouped rupee formatting in tie_out._r, written specifically so the figures can be compared against the paper statement, is buried inside escaped JSON.",
   "market_standard": "Every competitor shows a plain-language import error. This is not a market gap, it is a one-line regression against the codebase's own standard two functions below.",
   "confidence": "high",
   "suggested_fix": "Use `errorMessage(res)` in `uploadStatement`, exactly as `postStatementForm` does. One line. Keep `looksLikeAFormatProblem` working by matching on the unwrapped sentence.",
   "effort": "hours",
   "verification": {
    "status": "confirmed",
    "by": "agent",
    "corrected_severity": "medium",
    "what_is_actually_true": "Confirmed, one line. uploadStatement (apps/web/lib/api/index.ts:927) wraps the raw response body instead of calling errorMessage, so the import module's most important refusals \u2014 the tie-out arithmetic at domain/banking/tie_out.py:110-115 and the scan refusals at routers/banking.py:706-741 \u2014 reach the CA as `API error 422: {\"detail\":\"The statement does not add up. \u2026\"}`. The sibling function two lines below (:930-944) already does it correctly.",
    "probe": null,
    "lens_notes": {
     "literal": "Exact. apps/web/lib/api/index.ts:917-928 uploadStatement ends `if (!res.ok) throw new Error(`API error ${res.status}: ${await res.text()}`)`. postStatementForm immediately below (:930-944) does `throw new Error(await errorMessage(res))`, and errorMessage (:450-476) unwraps `detail`. apps/web/components/banking/AccountsPanel.tsx:702-707 renders `err.message` verbatim into setError.",
     "guards": "None. The refusals really are HTTPException with a `detail` string, so the browser sees {\"detail\":\"...\"}: routers/banking.py:854 raises 422 with check['refusal'] (the tie_out sentences at domain/banking/tie_out.py:110-115 and :264-268, Indian-grouped via _r at :119-126), :843 with StatementParseError text, and :856-866 with the scan refusal. looksLikeAFormatProblem (AccountsPanel.tsx:552-558) still matches because it does .includes() on the whole raw body, so the auto-open-the-mapper behaviour survives the bug \u2014 which is why it went unnoticed.",
     "reach": "Live and central. AccountsPanel.tsx:697 is the only caller of uploadStatement and is the product's only statement-import path (grep for statements/upload in apps/web finds nothing else)."
    }
   },
   "rescore_2026_09_08": {
    "state": "still_open",
    "what_is_true_now": "Unchanged. The tie-out and scan refusals still reach the CA as `API error 422: {\"detail\":\"...\"}` while the function immediately below unwraps them correctly. Still a one-line fix.",
    "remaining": null,
    "probe": "apps/web/lib/api/index.ts:956 (moved from :927 by 46bd46c's edits to this file, still the same line of code): `if (!res.ok) throw new Error(\\`API error ${res.status}: ${await res.text()}\\`);` inside uploadStatement. Its sibling postStatementForm at :972 still does `throw new Error(await errorMessage(res));`.",
    "against_commit": "46bd46c"
   }
  },
  {
   "id": "BANK-19",
   "kind": "glitch",
   "severity": "medium",
   "title": "Two banking screens parse typed rupees outside lib/money/rupeeInput, against the \"one parser, and only one\" rule",
   "evidence": "CLAUDE.md, \"Money in the browser \u2014 one parser, and only one\": all 61 call sites converted, with three named exceptions, all in gstLine. Two banking call sites are not among them. (1) apps/web/lib/banking/splitLegs.ts:36-42 `rupeesToPaise` does `Number(cleaned)` then `Math.round(n * 100)`, and apps/web/components/banking/SplitAcrossLedgersModal.tsx:140 sends its output straight to the server as `amount_paise`. It strips commas, so the 1,25,000 disaster does not apply, but it accepts \"1e3\" as \u20b91,000 and silently rounds a three-decimal entry rather than refusing. (2) apps/web/components/banking/RulesTab.tsx:104 `boundToPaise = rsToP(parseFloat(v) || 0)`, where shared.ts:29 `rsToP` is `Math.round(rs * 100)` \u2014 literally the form CLAUDE.md says was eradicated. Its inputs are `type=\"number\"` (RulesTab.tsx:235,240), which blunts it: a browser reports value=\"\" for \"1,25,000\", so the bound silently vanishes rather than becoming \u20b91. Every other banking screen uses the right parser (ReconcileTab.tsx:11,100; AccountsPanel.tsx:17,353,597-598).",
   "ca_experience": "A CA typing an amount band of 1,25,000 into a rule sees the digits in the box, presses Save, and the rule is stored with no upper bound at all \u2014 it then fires on lines it should not, and if the rule is trusted it posts them. In the split modal, a leg typed as 12.345 becomes \u20b912.35 with no warning.",
   "market_standard": "Not a market issue \u2014 an internal consistency issue with a documented failure history in this repo.",
   "confidence": "high",
   "suggested_fix": "Replace both with `paiseFromRupeeInput` and handle its null as a refusal, the way ReconcileTab already does with its NOT_AN_AMOUNT message. Delete `rsToP` from components/banking/shared.ts so it cannot come back.",
   "effort": "hours",
   "verification": {
    "status": "confirmed",
    "by": "agent",
    "corrected_severity": "medium",
    "what_is_actually_true": "Confirmed, and understated. Four banking files parse typed rupees outside lib/money/rupeeInput \u2014 lib/banking/splitLegs.ts:36-42, components/banking/RulesTab.tsx:104, FindMatchModal.tsx:86-87 and SettleDocumentsModal.tsx:149-193 \u2014 all through components/banking/shared.ts:29 rsToP, the exact Math.round(rs*100) form CLAUDE.md says was eradicated. So CLAUDE.md's 'All 61 call sites across 28 files are converted' with 'three deliberate exceptions, all in the same place (gstLine)' is not true of the banking subsystem; the code does not match what CLAUDE.md claims. Practical damage is bounded by the type=\"number\" inputs and by the split's exact-sum block, so the live failure is a rule bound that silently becomes null and '1e3' being accepted as \u20b91,000.",
    "probe": "grep -rn 'rsToP' apps/web --include=*.ts --include=*.tsx: 4 banking call files plus shared.ts's definition; apps/web/scripts/payroll-money-is-exact.test.ts:62 asserts `/function rsToP/` is absent only from the payroll files it lists.",
    "lens_notes": {
     "literal": "Both cited sites are real, and the finding UNDERCOUNTS. apps/web/lib/banking/splitLegs.ts:36-42 rupeesToPaise does Number(cleaned) then Math.round(n*100), fed straight to the server at components/banking/SplitAcrossLedgersModal.tsx:140. components/banking/shared.ts:29 rsToP is literally Math.round(rs*100), and it is called with parseFloat in FOUR banking places, not one: RulesTab.tsx:104, FindMatchModal.tsx:86-87, and SettleDocumentsModal.tsx:149, :154, :170, :193 \u2014 the last of which builds the allocated_paise and TDS figures actually posted by a settlement.",
     "guards": "Partial, and they are what keeps this out of 'high'. Every one of these inputs is type=\"number\" (RulesTab.tsx:235,240; SettleDocumentsModal.tsx:248,267; FindMatchModal.tsx:180,186; SplitAcrossLedgersModal.tsx:218), so a comma'd Indian amount yields value=\"\" rather than parseFloat's 1 \u2014 the rule bound silently vanishes instead of becoming \u20b91. The split total is also pinned: splitBlock (lib/banking/splitLegs.ts:75-90) and its server twin refuse anything that does not sum exactly to the line's paise, so a rounded leg shows as short/over rather than saving quietly. The enforcement test that exists (apps/web/scripts/payroll-money-is-exact.test.ts:60-63) only scans PAYROLL files, so nothing stops rsToP in components/banking/.",
     "reach": "All four components are live banking screens. Also worth reporting under the same pattern but outside this batch: apps/web/app/income-tax/deductions/page.tsx:108 rsToPaise is the full forbidden Math.round(parseFloat(x)*100) form. By contrast app/accounting/suppliers/page.tsx:66 and app/income-tax/capital-gains/page.tsx:73 define a local rsToP that DELEGATES to paiseFromRupeeInput, so those are fine."
    }
   },
   "rescore_2026_09_08": {
    "state": "partially_fixed",
    "what_is_true_now": "Three of the four banking call sites (RulesTab, FindMatchModal, SettleDocumentsModal, all via components/banking/shared.ts::rsToP) were fixed by 46bd46c and now go through paiseFromRupeeInput and refuse rather than coerce. The split editor was not.",
    "remaining": "apps/web/lib/banking/splitLegs.ts::rupeesToPaise still does Math.round(Number(cleaned) * 100) and still coerces an unreadable amount to 0 instead of refusing; it feeds amount_paise on the split-replace RPC, which posts to the GL. The comma-stripping means the 1,25,000 case is safe, but '1e3' is accepted as Rs 1,000, '0x10' as Rs 16, a three-decimal entry is silently rounded, and nonsense becomes a zero leg. The new guard test does NOT cover it: its three forbidden regexes are all parseFloat-shaped (/rsToP\\s*\\(\\s*parseFloat/, /Math\\.round\\s*\\(\\s*parseFloat/, /parseFloat\\s*\\([^)]*\\)\\s*\\*\\s*100\\b/) and this file uses Number().",
    "probe": "`grep -rn rsToP apps/web --include=*.ts --include=*.tsx`: components/banking/shared.ts:44 is now `export function rsToP(rs: string): number | null { return paiseFromRupeeInput(rs || \"0\"); }` \u2014 the Math.round(rs*100) form is gone, and RulesTab.tsx:107, FindMatchModal.tsx:90-91 and SettleDocumentsModal.tsx:159,167 all handle the null (RulesTab refuses the save with \"The minimum amount isn't a number\"; SettleDocumentsModal blocks on badAllocIds). Closed by 46bd46c. BUT apps/web/lib/banking/splitLegs.ts:38-44 is untouched and still reads `const n = Number(cleaned); if (!Number.isFinite(n)) return 0; return Math.round(n * 100);`, and SplitAcrossLedgersModal.tsx:140 still sends `amount_paise: rupeesToPaise(l.amount)` to the split-replace RPC. Ran its exact code: '\"1e3\" -> 100000 paise = 1000 rupees', '\"0x10\" -> 1600', '\"1.005\" -> 100 (silently rounded, not refused)', '\"abc\" -> 0 (coerced to 0)'. And `node --test scripts/every-amount-field-uses-the-one-parser.test.ts` PASSES 4/4 with that file in the tree.",
    "against_commit": "46bd46c"
   }
  },
  {
   "id": "BANK-20",
   "kind": "missing_feature",
   "severity": "high",
   "title": "There is no cash book \u2014 no cash vouchers, no cash register, no petty-cash screen",
   "evidence": "A repo-wide case-insensitive grep for 'cash book', 'cashbook', 'petty cash' and 'petty_cash' across apps/api and apps/web returns four hits and none of them is a feature: services/coa_seed_service.py:52 seeds a \"1002 Petty Cash\" ledger, tests/test_bank_account_first_coding.py uses it as a fixture, domain/dpdp/retention.py:111 mentions the statutory cash book in prose, and apps/web/components/banking/EntryDetailModal.tsx:514 uses the phrase in help text. apps/web/app/clients/[id]/reports/ contains ageing, bank-book, ratios and trend \u2014 no cash book. The only route into cash is `account_category` deriving Transfer when a CA picks a Cash-subtype ledger on a bank line (domain/banking/account_category.py).",
   "ca_experience": "Every SME client of an Indian CA runs cash. Wages, conveyance, tea, freight, small purchases \u2014 hundreds of vouchers a month that never touch a bank statement. There is no way to enter them as cash vouchers, no running cash balance, no negative-cash check, and no Cash Book report to print. The CA has to keep that half of the client's books in Tally, which defeats the whole premise of the platform.",
   "market_standard": "The Cash Book is one of Tally's headline reports and cash Receipt/Payment/Contra vouchers are its most-used entry screens; Busy and Marg are the same. Zoho Books has Cash accounts with the same register as bank accounts. Every one of them has a negative-cash warning.",
   "confidence": "high",
   "suggested_fix": "The kernel already supports it \u2014 Receipt/Payment/Contra, the posting map, the register and the running-balance code are all account-agnostic. Add a Cash account type alongside bank_accounts (or reuse it with a Cash subtype), a voucher entry screen, and reuse domain/banking/register.py for the Cash Book report. Add a negative-balance warning, which is the one cash-specific rule.",
   "effort": "weeks",
   "verification": {
    "status": "confirmed",
    "by": "agent",
    "corrected_severity": "high",
    "what_is_actually_true": "The gap is real: no cash voucher entry, no Cash Book report, no negative-cash check (apps/web/app/clients/[id]/reports/ has none; no cash-voucher route in apps/api/routers/). Two corrections: cash entries CAN be recorded as manual journals and a running cash balance IS available through the per-account ledger (apps/api/routers/accounting.py:857), so 'no running cash balance' is overstated; and the sharper defect is that payment_mode='cash' is accepted on receipts and purchase payments while the posting always resolves the Bank account (services/receipt_service.py:178, services/purchase_payment_service.py:443), which puts cash movements in the bank ledger.",
    "probe": "grep -rni 'cash book|cashbook|petty cash' over apps/api and apps/web returned 6 files, all incidental; grep -n '_find_account.*%Bank%' apps/api/services/receipt_service.py apps/api/services/purchase_payment_service.py returned lines 178 and 443 with no cash alternative.",
    "lens_notes": {
     "literal": "The grep result holds. Repo-wide, 'cash book'/'cashbook'/'petty cash' hits only services/coa_seed_service.py:52 (the seeded '1002 Petty Cash' ledger), migrations 011 and 288, tests/test_bank_account_first_coding.py, domain/dpdp/retention.py:111 and apps/web/components/banking/EntryDetailModal.tsx:514 \u2014 no feature. apps/web/app/clients/[id]/reports/ contains only ageing, bank-book, ratios and trend. There is no cash voucher screen, no cash register, no negative-cash check anywhere.",
     "guards": "Two partial workarounds exist and the finding overstates their absence. (a) Manual journal entries are a live screen (apps/web/app/clients/[id]/accounting/page.tsx:854 'New Journal Entry'), so cash CAN reach the GL. (b) A running cash balance IS obtainable: GET /accounting/ledger (apps/api/routers/accounting.py:857-891) returns opening, per-line running and closing balances server-side for any account, and the frontend drill-down renders it (accounting/page.tsx:924-1064) \u2014 pointing it at '1002 Petty Cash' is a cash book in all but name and column headings. Migration 288 also reads cash accounts out of the GL for \u00a740A(3), so cash in the ledger is an expected state. What genuinely does not exist: cash Receipt/Payment/Contra vouchers, a labelled Cash Book report, and any negative-cash warning. Worse, and not mentioned by the finding: receipts and purchase payments ACCEPT payment_mode='cash' (migration 050:101-102, widened by 161; models/invoices.py:399) but the journal always debits/credits the BANK account \u2014 services/receipt_service.py:178 and services/purchase_payment_service.py:443 both call _find_account(..., '%Bank%', system_key='bank') with no cash branch (phase2_journal_service.py:213-220, :1404-1445). A receipt recorded as cash therefore lands in the bank ledger and overstates the bank balance.",
     "reach": "Confirms the gap rather than refuting it \u2014 there is no backend cash-voucher capability hidden behind a missing screen either. The mispost above is reachable from the live receipts and purchase-payments screens."
    }
   },
   "rescore_2026_09_08": {
    "state": "still_open",
    "what_is_true_now": "Unchanged, including the sharper defect the verifier added: payment_mode='cash' is still accepted on receipts and purchase payments while the posting still resolves the Bank account, so cash movements still land in the bank ledger.",
    "remaining": null,
    "probe": "`grep -rni 'cash book|cashbook|petty cash' apps/api apps/web` -> 5 incidental hits (coa_seed_service.py:52 seeds the ledger, two test fixtures, dpdp/retention.py:111 prose, EntryDetailModal.tsx:514 help text). `ls apps/web/app/clients/[id]/reports/` -> ageing, bank-book, page.tsx, ratios, trend \u2014 no cash book. `grep -n '%Bank%' apps/api/services/receipt_service.py apps/api/services/purchase_payment_service.py` -> :178 and :443, both still `K._find_account(db, firm_id, client_id, \"%Bank%\", system_key=\"bank\")` with no cash alternative.",
    "against_commit": "46bd46c"
   }
  },
  {
   "id": "BANK-21",
   "kind": "missing_feature",
   "severity": "medium",
   "title": "Credit-card accounts are not supported at all",
   "evidence": "migrations/054_v13_payroll_assets_banking.sql:100-101 \u2014 `account_type CHECK (account_type IN ('Current','Savings','Cash Credit','Overdraft'))`. There is no 'Credit Card'. apps/web/components/banking/AccountsPanel.tsx:416 offers exactly those four. The ledger creation path (routers/banking.py:250) would in any case give a card an Asset ledger, and the posting map treats every statement line as money in or out of a bank asset (domain/banking/posting_map.py:43-78).",
   "ca_experience": "A company credit card is normal for any SME with travel or online spend. The CA cannot set the card up as an account, so the card statement cannot be imported, its expenses cannot be coded through the bank workflow, and the monthly card payment from the bank posts to whatever ledger someone picks \u2014 with the underlying expenses never recorded from the card statement at all.",
   "market_standard": "QuickBooks and Zoho Books both have first-class credit-card accounts (a liability, with the payment recorded as a transfer from bank to card). Tally handles it as a liability ledger with a card statement imported like any other.",
   "confidence": "high",
   "suggested_fix": "Add 'Credit Card' to the account_type CHECK and the picker, give it a Liability ledger (same change as BANK-02), and make the card-payment line a Contra between the bank and the card ledger \u2014 the transfer machinery already handles that shape.",
   "effort": "days",
   "verification": {
    "status": "confirmed",
    "by": "agent",
    "corrected_severity": "medium",
    "what_is_actually_true": "Confirmed. A credit-card account cannot be created at all: the live CHECK constraint (migrations/093_payroll_assets_banking_modernized.sql:131-132) admits only Current/Savings/Cash Credit/Overdraft, the picker offers the same four (AccountsPanel.tsx:416), and the auto-created ledger is always an Asset (routers/banking.py:249). So a company card statement cannot be imported and its expenses cannot be coded through the bank workflow.",
    "probe": null,
    "lens_notes": {
     "literal": "Confirmed, and the constraint is live in the CURRENT migration, not just 054. migrations/093_payroll_assets_banking_modernized.sql:131-132 recreates bank_accounts with account_type TEXT NOT NULL DEFAULT 'Current' CHECK (account_type IN ('Current','Savings','Cash Credit','Overdraft')); no later migration alters it (only 054, 093 and 135 mention account_type near bank_accounts, and 135 is chart_of_accounts). apps/web/components/banking/AccountsPanel.tsx:416 hardcodes the same four in the picker. routers/banking.py:249 creates the linked ledger as account_type 'Asset' / subtype 'Bank' unconditionally.",
     "guards": "None \u2014 the DB CHECK is the enforcement, and models/banking.py:19 does not even validate, so an out-of-list value is a 500-shaped DB error rather than a refusal. grep -rni 'credit card|credit_card' across apps/api finds only domain/banking/narration.py:142 and tests/test_bank_narration.py:155-158, which deliberately treat 'CREDIT CARD' as a product name in a narration, not an account type.",
     "reach": "Confirms the gap rather than refuting it: there is no backend capability hidden behind a missing screen. Nothing anywhere in apps/api or apps/web models a card account, a card liability ledger, or a card-payment contra."
    }
   },
   "rescore_2026_09_08": {
    "state": "still_open",
    "what_is_true_now": "Unchanged. A credit-card account still cannot be created at all, so a company card statement cannot be imported through the bank workflow.",
    "remaining": null,
    "probe": "`grep -rn 'Cash Credit' apps/api/migrations/*.sql` -> 054:101 and 093:132, both `CHECK (account_type IN ('Current','Savings','Cash Credit','Overdraft'))`; no migration since adds 'Credit Card'. AccountsPanel.tsx:416 still renders `[\"Current\",\"Savings\",\"Cash Credit\",\"Overdraft\"]`. routers/banking.py:250 still hardcodes an Asset ledger.",
    "against_commit": "46bd46c"
   }
  },
  {
   "id": "BANK-22",
   "kind": "gap",
   "severity": "medium",
   "title": "A statement imported without a bank account posts every line to a generic \"Bank\" ledger and is invisible to the register, the reconciliation and the account filter",
   "evidence": "bank_transactions has no bank_account_id column (migrations/006_transactions_bank_compliance.sql:90-109 and nothing since adds one); every account-scoped read resolves it through the statement (services/bank_entry_service.py:92-99 `_statement_ids`, services/bank_transfer_service.py:41-46 `_account_by_statement`, services/bank_register_service.py:76-84, services/bank_reconciliation_service.py:55-58). `bank_account_id` is Optional on both import entry points: models/banking.py:83 (`StatementImportIn`) and routers/banking.py:768. When it is absent, services/bank_posting_service.py:68-105 `_resolve_bank` falls through to `phase2_journal_service._find_account(db, firm_id, client_id, \"%Bank%\", system_key=\"bank\")` \u2014 the firm's generic master Bank account.",
   "ca_experience": "Lines imported this way appear in the Entries queue (client-scoped) and can be passed, but every journal debits or credits a generic Bank ledger rather than the client's actual bank account. They never appear in any Bank Book, never appear in any reconciliation, and the account filter cannot find them. The web UI does always send an account (AccountsPanel.tsx:684 refuses without one), so this is reachable through the API and through the JSON /statements/import endpoint rather than through the normal screen.",
   "market_standard": "Zoho Books and QuickBooks make the account mandatory on a statement import \u2014 there is no unassigned state.",
   "confidence": "high",
   "suggested_fix": "Make `bank_account_id` required on both import endpoints (it is already validated for ownership at banking_service.py:164-169 when supplied), and make `_resolve_bank`'s generic fallback raise rather than post to a master Bank account \u2014 an unattributable bank line should refuse, not guess. Longer term, denormalise bank_account_id onto bank_transactions so every scoped read is one filter instead of a statement-id round trip.",
   "effort": "hours",
   "verification": {
    "status": "confirmed",
    "by": "agent",
    "corrected_severity": "low",
    "what_is_actually_true": "The mechanism is real and the fallback is genuinely unattributable \u2014 with no bank_account_id, _resolve_bank (bank_posting_service.py:105) posts to the firm-wide system_account_key='bank' master ledger, and the line is then invisible to the register, the reconciliation and the account filter, all of which resolve the account through the statement. But no screen can produce it: the sole UI path refuses without an account, and the one helper that omits it (apps/web/lib/data/bankStatements.ts:150-170) is dead code with no callers. Low, with the caveat that wiring that helper up would reproduce it immediately.",
    "probe": "python -c on tests/fixtures/production_schema_2026-09-03.json printed bank_transactions' 50 column names \u2014 statement_id present, bank_account_id absent. grep -rn 'importStatement' apps/web returns only its definition (lib/api/index.ts:915) and lib/data/bankStatements.ts:167; grep for importBankStatement finds no consumer.",
    "lens_notes": {
     "literal": "Every mechanical claim checks out. bank_transactions really has no bank_account_id \u2014 confirmed against the production snapshot, not just the migrations: tests/fixtures/production_schema_2026-09-03.json lists 50 columns for bank_transactions and bank_account_id is not among them. bank_account_id is Optional on both entry points (models/banking.py:86 StatementImportIn, routers/banking.py:767 Form(None)), and services/bank_posting_service.py:68-105 _resolve_bank falls through to phase2_journal_service._find_account(..., '%Bank%', system_key='bank') \u2014 which is looked up FIRM-WIDE, not client-scoped (phase2_journal_service._find_account, the system_key branch filters on firm_id only). Every account-scoped read does go through the statement (bank_entry_service.py:92-99, bank_transfer_service.py:41-46, bank_register_service.py:76-84, bank_reconciliation_service.py:55-58).",
     "guards": "banking_service.py:164-169 validates ownership when bank_account_id IS supplied but never requires it. No CHECK, no Pydantic validator, no router guard.",
     "reach": "This is what pulls the severity down. The web UI has exactly one caller of the upload endpoint and it always sends an account (AccountsPanel.tsx:668-684 refuses with 'Select a bank account.' before building the form). The JSON /statements/import endpoint has one frontend wrapper, importBankStatement at apps/web/lib/data/bankStatements.ts:150-170, which omits bank_account_id entirely \u2014 but it is DEAD: the only import of that module (AccountsPanel.tsx:22) pulls getBankStatements/getBankTransactions and not importBankStatement, and grep finds no other caller. So the only live route to the bug is a hand-crafted API call."
    }
   },
   "rescore_2026_09_08": {
    "state": "still_open",
    "what_is_true_now": "Unchanged, including the caveat that keeps it low: the mechanism is real and the generic fallback still posts to the firm-wide master Bank ledger, but the only helper that omits bank_account_id is still dead code with no callers.",
    "remaining": null,
    "probe": "models/banking.py:83 still `bank_account_id: Optional[str] = None` on StatementImportIn, and routers/banking.py:767 still `bank_account_id: Optional[str] = Form(None)` on the upload route. services/bank_posting_service.py:104 still ends _resolve_bank with `return phase2_journal_service._find_account(db, firm_id, txn[\"client_id\"], \"%Bank%\", system_key=\"bank\")`. `grep -rn importBankStatement apps/web` -> only its own definition at lib/data/bankStatements.ts:150; still zero callers.",
    "against_commit": "46bd46c"
   }
  },
  {
   "id": "BANK-23",
   "kind": "ux",
   "severity": "medium",
   "title": "The reconciliation shows only POSTED statement lines, so an unposted line in the period produces a difference with nothing on screen naming it",
   "evidence": "services/bank_reconciliation_service.py:60-72 `_posted_account_txns` ends with `return [t for t in rows if t.get(\"posted_journal_id\")]`. `_classify` (:84-100) therefore buckets only posted lines into reconciled / unreconciled / exceptions, and `_compute_report` (:602-628) renders only those three lists. `complete()` (:389-395) refuses on unreconciled POSTED lines with a count \u2014 it says nothing about lines that were never posted. The screen (apps/web/components/banking/ReconcileTab.tsx:565) offers exactly those three views.",
   "ca_experience": "A CA opens a reconciliation for April, ticks everything the screen offers, and the tie-out still shows a \u20b91,84,300 difference. Nothing on the Reconcile tab lists the forty April lines that are still sitting unpassed in Entries, and the completion refusal does not mention them either. The CA's next move is either to hunt through another tab or to type the difference into Adjustments (see BANK-05).",
   "market_standard": "Zoho Books shows uncategorised statement lines inside the reconciliation with a \"categorise\" action. QuickBooks blocks reconciliation and names the count of unreviewed transactions in the period.",
   "confidence": "high",
   "suggested_fix": "Add a fourth bucket \u2014 in-period statement lines with no journal \u2014 computed from the same fetch (drop the `posted_journal_id` filter and split), show it as a \"Not yet passed (N)\" view with a link into Entries filtered to that account and period, and name it in the completion refusal.",
   "effort": "hours",
   "verification": {
    "status": "confirmed",
    "by": "agent",
    "corrected_severity": "medium",
    "what_is_actually_true": "Confirmed. The reconciliation is built entirely from lines with a posted_journal_id (services/bank_reconciliation_service.py:71), so in-period statement lines still sitting unpassed in Entries contribute to the tie-out difference while appearing in none of the three buckets, and complete()'s refusal (:391-395) counts only posted-but-unreconciled lines. Nothing on the Reconcile tab or in the refusal names them.",
    "probe": null,
    "lens_notes": {
     "literal": "Exact. services/bank_reconciliation_service.py:60-71 _posted_account_txns ends `return [t for t in rows if t.get('posted_journal_id')]`; _classify (:84-100) buckets only those into reconciled / unreconciled / exceptions; _compute_report (:601-628) emits exactly those three lists and a counts block of the same three. complete() (:389-395) refuses with '{n} transaction(s) in this period are still unreconciled', counted from buckets['unreconciled'] \u2014 which by construction can only contain posted lines.",
     "guards": "None that names unposted lines. The opening-balance warning (domain/banking/reconciliation.py:78-111, rendered at ReconcileTab.tsx:373-390) is about a PREVIOUS completed session's closing balance, not about lines in this period with no journal. grepping ReconcileTab.tsx for pass/post/queue/unposted returns only an interface field and an unrelated sentence. The Bank Register does have a status='unposted' filter (bank_register_service.py:106-109) but that is a different tab and is not linked from the reconciliation.",
     "reach": "Live. apps/web/components/banking/ReconcileTab.tsx:565 hardcodes the three buttons ['unreconciled','reconciled','exceptions'] from report.counts, so the screen offers exactly what the service computes."
    }
   },
   "rescore_2026_09_08": {
    "state": "still_open",
    "what_is_true_now": "Unchanged. An in-period statement line still sitting unpassed in Entries still contributes to the tie-out difference while appearing in none of the three buckets and in none of the refusal text.",
    "remaining": null,
    "probe": "services/bank_reconciliation_service.py:71 still `return [t for t in rows if t.get(\"posted_journal_id\")]`; _classify (:84-100) still buckets only those into reconciled/unreconciled/exceptions; complete() at :392-395 still refuses with a count of unreconciled POSTED lines only and says nothing about lines never passed.",
    "against_commit": "46bd46c"
   }
  },
  {
   "id": "BANK-24",
   "kind": "gap",
   "severity": "medium",
   "title": "Bank-charge input tax credit is posted with no supplier GSTIN or invoice reference, so it can never be reconciled to GSTR-2B",
   "evidence": "domain/banking/charge_gst.py:74-110 splits the inclusive charge and services/bank_posting_service.py:233-274 `_charge_lines` books the tax to the GST Input Credit account (CGST Act s.16, cited at charge_gst.py:19-21). The journal it produces carries only `account_id`, `debit_paise`, `credit_paise` (domain/banking/charge_gst.build_inclusive_lines) \u2014 no counterparty GSTIN, no supplier invoice number, no place-of-supply record beyond the boolean. The rule that supplies the rate (`bank_matching_rules`, migration 254) carries `suggested_gst_rate_bps` and `suggested_is_interstate` and nothing identifying the bank as a supplier.",
   "ca_experience": "The ITC on bank charges lands in the GST Input ledger and reduces the net payable, but when the CA runs the 2A/2B reconciliation it appears as credit claimed with no counterparty invoice to match \u2014 an unexplained difference every month. Under s.16(2)(aa) credit is only available if the supplier's invoice appears in GSTR-2B, so this is credit the CA has to defend without the evidence the product could have captured.",
   "market_standard": "Zoho Books requires a vendor on a GST-bearing expense so it flows into the purchase register and the 2B match. Tally requires a party ledger with a GSTIN on any voucher claiming input credit \u2014 you cannot claim ITC on a bank charge without one.",
   "confidence": "medium",
   "suggested_fix": "Let a bank account (or the rule) carry the bank's GSTIN and state, stamp it onto the charge journal and onto a purchase-register row, so bank-charge ITC joins the same 2A/2B reconciliation as every other input credit. At minimum, tag these journals so the GST reconciliation can report them as a named category rather than an unexplained difference.",
   "effort": "days",
   "verification": {
    "status": "confirmed",
    "by": "agent",
    "corrected_severity": "medium",
    "what_is_actually_true": "The premise is confirmed \u2014 a bank-charge journal carries no supplier GSTIN and no invoice reference (domain/banking/charge_gst.py:111-160), and nothing in bank_matching_rules could supply one. The consequence stated is not: the return is document-driven (services/gst_return_service.py:678-691), so the credit is NOT claimed in Table 4(A) and does not appear in the 2A/2B report (routers/gst_workspace.py:844-868) either. The real effect is the mirror image \u2014 the ledger recognises input credit the return never claims, so the client under-claims it, and gst_return_service.py:833's itc_matched goes false every such month, showing up as an unexplained ITC books-vs-ledger difference on apps/web/app/clients/[id]/compliance/gst/page.tsx:934.",
    "probe": null,
    "lens_notes": {
     "literal": "The underlying fact is right, the consequence chain is wrong. domain/banking/charge_gst.py:74-110 splits the inclusive charge and build_inclusive_lines (:111-160) emits lines of account_id/debit_paise/credit_paise only \u2014 no counterparty, no invoice number, no GSTIN; services/bank_posting_service.py:233-274 books the tax to the GST Input account resolved by _gst_input_account. bank_matching_rules carries suggested_gst_rate_bps/suggested_is_interstate and nothing identifying a supplier (migration 254).",
     "guards": "The finding's stated harm does not occur. GSTR-3B Table 4(A) ITC is built from POSTED PURCHASE BILLS, not from the ledger \u2014 services/gst_return_service.py:678-691 loops _posted_bills into PurchaseTransaction \u2014 so bank-charge ITC never enters the return and cannot 'reduce the net payable'. The 2A/2B reconciliation (routers/gst_workspace.py:844-868) matches caller-supplied book INVOICES against 2B docDetails by (invoice_no, GSTIN), so a bank charge is not in that report at all. What DOES fire is a different and sharper check: _gl_gst_movements (gst_return_service.py, the gst_input net-debit read) sums the same GST Input account the charge debited, and gst_return_service.py:833 sets itc_matched = books_itc == gl['itc_paise'] \u2014 so the books-vs-ledger ITC comparator reports a difference every month there is a GST-bearing bank charge.",
     "reach": "The bank-charge GST split is live (a rule can carry the rate; bank_posting_service._charge_lines is on the posting path). The mismatch it produces is surfaced: apps/web/app/clients/[id]/compliance/gst/page.tsx:931-934 renders 'ITC \u2014 Books: \u2026 vs Ledger: \u2026' from the reconciliation block. The GSTR-3B screen itself (app/gst/gstr3b/page.tsx) does not render it."
    }
   },
   "rescore_2026_09_08": {
    "state": "still_open",
    "what_is_true_now": "Unchanged. The bank-charge journal still carries no supplier GSTIN and no invoice reference, so \u2014 per the verifier's correction \u2014 the ledger recognises input credit the document-driven return never claims, and the mismatch surfaces as an unexplained ITC books-vs-ledger difference.",
    "remaining": null,
    "probe": "`grep -n 'gstin|GSTIN|invoice' apps/api/domain/banking/charge_gst.py` -> no match. build_inclusive_lines (charge_gst.py:114-165) still emits legs of only {account_id, debit_paise, credit_paise, narration}; bank_matching_rules still carries only suggested_gst_rate_bps and suggested_is_interstate.",
    "against_commit": "46bd46c"
   }
  },
  {
   "id": "BANK-25",
   "kind": "glitch",
   "severity": "medium",
   "title": "Every imported statement shows an amber \"pending\" chip forever \u2014 import_status is written once and never advanced",
   "evidence": "services/banking_service.py:204 writes `\"import_status\": \"pending\"` on the bank_statements row. A repo-wide grep for `import_status` across apps/api returns exactly two hits: that line, and the CHECK constraint in migrations/006_transactions_bank_compliance.sql:81. Nothing ever updates it. apps/web/components/banking/AccountsPanel.tsx:132-136 maps pending\u2192amber, reviewed\u2192blue, posted\u2192green, and :243 renders the chip in the Status column of the statements table.",
   "ca_experience": "The Accounts panel is the screen a CA opens to check what has been brought in. Every statement they have ever imported and fully passed still shows an amber \"pending\" badge. The one status column on that table conveys nothing, and looks like a backlog.",
   "market_standard": "Zoho Books shows a per-statement progress (\"312 of 312 categorised\"). QuickBooks shows the count still in For Review per import.",
   "confidence": "high",
   "suggested_fix": "Either drive it from the lines (pending \u2192 reviewed once every line has left needs_you, posted once every line is passed or set aside \u2014 a cheap SQL count on the existing entry_state index), or drop the column from the table and show \"N of M passed\" instead, which is what the CA actually wants to know.",
   "effort": "hours",
   "verification": {
    "status": "confirmed",
    "by": "agent",
    "corrected_severity": "low",
    "what_is_actually_true": "The claim is literally right: bank_statements.import_status is written 'pending' at services/banking_service.py:204 and never advanced by any code path or trigger, so the chip at apps/web/components/banking/AccountsPanel.tsx:243 is permanently amber. Severity corrected to low: it is a display-only label in a setup modal, no figure is wrong and no task is blocked \u2014 the state a CA works from is the per-line entry_state on Entries, which is maintained by migration 322's trigger.",
    "probe": "grep -rn \"import_status\" apps/api apps/web \u2192 5 hits: banking_service.py:204 (write), migrations/006:81 (CHECK), AccountsPanel.tsx:243 (render), lib/data/bankStatements.ts:33,187 (type + mock). grep for from(\"bank_statements\") in apps/web \u2192 no output (no direct PostgREST writer either).",
    "lens_notes": {
     "literal": "Confirmed. services/banking_service.py:204 writes \"import_status\": \"pending\" in the bank_statements insert payload. A repo-wide grep over apps/ for import_status returns exactly five hits and only two on the write side: that line and the CHECK constraint at migrations/006_transactions_bank_compliance.sql:81 (DEFAULT 'pending' CHECK IN ('pending','reviewed','posted')). The other three are display-side: apps/web/lib/data/bankStatements.ts:33 (type) and :187 (mock row, also \"pending\"), and apps/web/components/banking/AccountsPanel.tsx:243 (the chip). Nothing in apps/api or apps/web ever UPDATEs it.",
     "guards": "None. No trigger or function in apps/api/migrations touches import_status \u2014 the only other bank_statements references in migrations are the GRANT statements (010:30, 039:22, 041:21). The frontend has no PostgREST second path here either: grep for from(\"bank_statements\") in apps/web returns nothing, so the column is written once by the API and never again. STATUS_COLORS at AccountsPanel.tsx:131-135 maps pending\u2192amber / reviewed\u2192blue / posted\u2192green; only the amber branch is ever reachable.",
     "reach": "Reachable but shallow. The chip is inside BankAccounts (AccountsPanel.tsx:25-307), which is rendered only inside the \"Bank accounts and statements\" modal opened from the Entries screen (EntriesTab.tsx:791, guarded by showAccounts) \u2014 a setup dialog, not the working queue. The screen a CA actually works is Entries, whose per-line entry_state (needs_you/proposed/ready/passed/set_aside) is trigger-maintained and correct."
    }
   },
   "rescore_2026_09_08": {
    "state": "still_open",
    "what_is_true_now": "Unchanged. The chip is still permanently amber. Still display-only in a setup modal, which is why the verifier corrected it to low.",
    "remaining": null,
    "probe": "`grep -rn import_status apps/api apps/web` -> 5 hits: services/banking_service.py:204 writes `\"import_status\": \"pending\"`, migrations/006:81 is the CHECK, AccountsPanel.tsx:243 renders the chip, lib/data/bankStatements.ts:33,187 are a type and a mock. Nothing updates it, and there is still no PostgREST writer.",
    "against_commit": "46bd46c"
   }
  },
  {
   "id": "BANK-26",
   "kind": "fine_tune",
   "severity": "medium",
   "title": "Six statement layouts are auto-detected; every other bank needs a manual mapping, and the mapping cannot be shared between accounts or clients",
   "evidence": "domain/banking/normalizer.py:97-125 `_ADAPTERS` holds exactly six: hdfc, generic_cheque, sbi, icici, axis, generic, generic_amount_drcr (six adapter keys plus the amount/drcr variant). `detect_format` (:128-174) picks between them on header words; anything else raises the \"Unsupported bank statement format\" message at :216-243. The escape hatch is a per-account column mapping keyed on a header fingerprint: services/bank_column_mapping_service.py, saved against `(firm_id, bank_account_id, header_fingerprint)` and looked up the same way (routers/banking.py:840-844, 892-903). There is no firm-level or bank-name-level reuse.",
   "ca_experience": "The design decision not to guess eight more adapters is right and CLAUDE.md-consistent. But a firm with thirty clients banking at Kotak maps the Kotak layout thirty times \u2014 once per bank account \u2014 even though the header fingerprint is identical. And there is no library screen showing which layouts the firm has taught; only a per-account list (GET /statements/column-mappings).",
   "market_standard": "Perfios, Setu and the PDF-to-Tally converters ship hundreds of bank templates. Zoho Books remembers a CSV mapping per organisation and offers it for any account. Tally has no mapping at all \u2014 you must reshape the file.",
   "confidence": "high",
   "suggested_fix": "Fall back from the account-scoped mapping to a firm-scoped one on the same header fingerprint before asking again \u2014 the fingerprint already makes reuse safe, which is the whole argument in normalizer.py:591-607. Add a Settings screen listing the firm's learned layouts with the bank name, so a CA can see and correct them. Adding real adapters, if ever, should come from real files the firm has already mapped.",
   "effort": "days",
   "verification": {
    "status": "confirmed",
    "by": "agent",
    "corrected_severity": "medium",
    "what_is_actually_true": "Confirmed at medium. Seven layouts are auto-detected (normalizer.py:97-125) and everything else needs a mapping that is looked up strictly per bank account (bank_column_mapping_service.py:41-49), so thirty clients at the same bank teach the identical header fingerprint thirty times. Correction to the finding's last sentence: the list endpoint is not merely per-account \u2014 it accepts a firm-wide query (routers/banking.py:1056-1068) \u2014 but no UI calls it at all, so there is no mapping list on any screen, per-account or otherwise (apps/web/lib/api/index.ts:951-954 are dead client wrappers).",
    "probe": "grep -rn \"listColumnMappings|deleteColumnMapping\" apps/web \u2192 only lib/api/index.ts:951 and :953 (definitions, zero call sites). grep -n on bank_column_mapping_service.py confirms find_mapping's three .eq filters at lines 43-45.",
    "lens_notes": {
     "literal": "Confirmed. domain/banking/normalizer.py:97-125 holds exactly seven adapter keys (hdfc, generic_cheque, sbi, icici, axis, generic, generic_amount_drcr \u2014 six banks/layouts plus the amount+Dr/Cr variant, as the finding says). detect_format (:128-174) picks on header words/positions and falls through to \"generic\"; _validate_adapter (:199-243) raises the \"Unsupported bank statement format\" StatementParseError when the debit/credit columns are not where the adapter expects. The escape hatch is exactly as described: services/bank_column_mapping_service.py:31-52 find_mapping filters .eq(firm_id).eq(bank_account_id).eq(header_fingerprint), and routers/banking.py:829-836 calls it with the uploading account's bank_account_id; save_mapping (:70-121) writes the same triple. No firm-level or bank-name-level fallback exists anywhere.",
     "guards": "The account scoping is deliberate only in ONE direction, and it is a different direction. find_mapping's docstring and tests/test_bank_statement_column_mapping.py:232-248 pin 'never reuse a mapping for a DIFFERENT layout on the same account' \u2014 nothing pins 'never reuse the SAME layout across accounts', which is what the finding asks for. header_fingerprint (normalizer.py:591-607) is a sha256 of the case/whitespace-normalised header row, so cross-account reuse on an identical fingerprint would carry the same safety argument the module already makes for itself.",
     "reach": "Real, and the second half of the finding is worse than stated. GET /api/banking/statements/column-mappings (routers/banking.py:1055-1068) actually DOES support a firm-wide listing \u2014 client_id and bank_account_id are both optional Query params, and list_mappings (bank_column_mapping_service.py:56-68) applies them only when present. But apps/web/lib/api/index.ts:951-954 defines listColumnMappings and deleteColumnMapping and NO component calls either (grep across apps/web returns only those two definition lines). So the library capability exists in the backend and is reachable from no screen \u2014 which confirms the gap rather than refuting it."
    }
   },
   "rescore_2026_09_08": {
    "state": "still_open",
    "what_is_true_now": "Unchanged. Every other bank still needs a manual mapping, the mapping is still keyed per bank account so thirty clients at one bank teach the same fingerprint thirty times, and no screen lists learned layouts.",
    "remaining": null,
    "probe": "domain/banking/normalizer.py:97-125 still holds exactly seven adapter keys (hdfc, generic_cheque, sbi, icici, axis, generic, generic_amount_drcr). services/bank_column_mapping_service.py:42-46 still looks a saved mapping up with three .eq filters \u2014 firm_id, bank_account_id, header_fingerprint \u2014 so there is still no firm-level or bank-name-level reuse. `grep -rn 'listColumnMappings|deleteColumnMapping' apps/web` -> still only the two definitions in lib/api/index.ts with zero call sites.",
    "against_commit": "46bd46c"
   }
  },
  {
   "id": "BANK-27",
   "kind": "bug",
   "severity": "medium",
   "title": "An opening balance can be saved with no opening-balance date, and the Bank Book then folds pre-opening lines into the running balance",
   "evidence": "models/banking.py:20-21 \u2014 `opening_balance_paise: int = 0` and `opening_balance_date: Optional[str] = None`, no cross-validation. apps/web/components/banking/AccountsPanel.tsx:419-427 renders both fields with neither required and no check that a non-zero balance has a date. domain/banking/register.py:176-179 then computes `precedes = bool(opening_date and d and d < opening_date)` \u2014 with a null date NOTHING precedes, so every imported line is added on top of the opening figure, and `delta = stated_int - running` (:184) compares the result against the bank's own balance column.",
   "ca_experience": "A CA enters \u20b94,50,000 as the opening balance, leaves the date blank, then imports the full year including months before that figure. The Bank Book's running balance is overstated by every pre-opening movement, and the self-check reports a divergence on the very first line \u2014 which reads as a parsing or import fault rather than a setup mistake. The register's careful note-2 protection is silently disabled.",
   "market_standard": "Tally requires an opening balance to be entered against the books-beginning date; there is no undated opening balance. Zoho Books requires an \"as of\" date on a bank opening balance.",
   "confidence": "high",
   "suggested_fix": "Require `opening_balance_date` whenever `opening_balance_paise` is non-zero, in `BankAccountIn`/`BankAccountUpdateIn` and in the form. Default it to the client's books-beginning date. Where existing rows have a balance and no date, surface it as a named setup gap on the account row rather than silently mis-computing.",
   "effort": "hours",
   "verification": {
    "status": "confirmed",
    "by": "agent",
    "corrected_severity": "medium",
    "what_is_actually_true": "Confirmed at medium. Nothing requires an opening-balance date when a non-zero opening balance is saved (models/banking.py:20-21, routers/banking.py:426-473, AccountsPanel.tsx:419-427), and domain/banking/register.py:179 treats a null date as 'nothing precedes', so pre-opening lines are re-added to the running balance and the self-check reports a false divergence on the first line carrying a stated balance. The GL half of the same setup does not have this hole \u2014 opening_balance_service.py:114-152 falls back to the client's FY start \u2014 so the register and the GL opening entry can disagree about the same account.",
    "probe": "cd apps/api && venv/bin/python -c \"from domain.banking.register import build_register; \u2026\" with opening_balance_paise=450000, one \u20b91,000 credit dated 2026-01-10 and one \u20b9500 credit dated 2026-04-05 whose stated balance is \u20b95,000. opening_date=None \u2192 balances 550000, 600000 and balance_delta_paise \u2212100000 on the second line (a fabricated divergence). opening_date='2026-04-01' \u2192 balances 450000, 500000 and delta 0.",
    "lens_notes": {
     "literal": "Confirmed, and reproduced. models/banking.py:20-21 (BankAccountIn) and :57-58 (BankAccountUpdateIn) declare opening_balance_paise with a non-negative validator and opening_balance_date: Optional[str] = None with no cross-validation \u2014 the only model_validator/field_validators present are not_empty, upper_currency and non_negative. domain/banking/register.py:179 computes precedes = bool(opening_date and d and d < opening_date), so a null opening_balance_date makes precedes False for every row; :180-181 then adds every pre-opening movement onto the running balance and :187 computes delta = stated_int - running against the bank's own balance column.",
     "guards": "Partial and in the wrong place. routers/banking.py:426-473 (create) and :477-509 (update) validate the ledger link and the currency but never the date. The frontend form (AccountsPanel.tsx:419-427) renders 'Opening Balance (\u20b9)' and 'Opening Balance Date' side by side, neither marked required, and submits opening_balance_date: openingDate || null (:364, :370). The GL side DOES have a fallback \u2014 services/opening_balance_service.py:114-152 _default_opening_date dates the opening journal from the posted family, else the client's financial_year_start, else the earliest master date \u2014 so the GL opening entry gets a sensible date while the Bank Book's register gets None. That makes the two disagree rather than making either safe. BankBook.tsx:288-299 even renders the 'N transactions are dated before this account's opening balance' note with the date omitted when it is null \u2014 a note that can never fire, because with a null date precedes_opening_count is always 0.",
     "reach": "Live. bank_register_service.py:196 passes account.get(\"opening_balance_date\") straight into build_register, and BankBook.tsx (the Bank \u203a Book tab) renders both the running balance column and the divergence banner (BankBook.tsx:270-287) built from it."
    }
   },
   "rescore_2026_09_08": {
    "state": "still_open",
    "what_is_true_now": "Unchanged and reproduced. Nothing requires an opening-balance date when a non-zero opening balance is saved, and a null date still means 'nothing precedes', so pre-opening lines are folded into the running balance and the self-check reports a false divergence.",
    "remaining": null,
    "probe": "Ran build_register at HEAD with opening_balance_paise=450000, a Rs 1,000 credit dated 2026-01-10 and a Rs 500 credit dated 2026-04-05 whose stated balance is Rs 5,000. opening_date=None -> [('a',550000,None,False), ('b',600000,-100000,False)] \u2014 the pre-opening line is re-added and the second line reports a fabricated Rs 1,000 divergence. opening_date='2026-04-01' -> [('a',450000,None,True), ('b',500000,0,False)]. models/banking.py:20-21 still has `opening_balance_paise: int = 0` and `opening_balance_date: Optional[str] = None` with no cross-validation.",
    "against_commit": "46bd46c"
   }
  },
  {
   "id": "BANK-28",
   "kind": "gap",
   "severity": "low",
   "title": "The narration parser extracts channel, UTR, VPA, IFSC and counterparty but never a cheque number",
   "evidence": "domain/banking/narration.py:104-119 `ParsedNarration` has raw, channel, utr, vpa, counterparty, ifsc, direction, tokens \u2014 no cheque number field. `_CHANNELS` (:51) recognises CHQ/CHEQUE/CLG/MICR as a channel, but `_is_noise_word` (:155-167) discards any pure-digit token, so the number itself is thrown away. The number IS captured when the export has its own cheque column (the `ref` index in the hdfc/generic_cheque/sbi/axis adapters, normalizer.py:99-113 \u2192 `reference_no`), and it is shown by the register (domain/banking/register.py:91) and the reconciliation report (bank_reconciliation_service.py:609).",
   "ca_experience": "For a PDF or scanned statement where the cheque number appears inside the narration rather than in its own column, the CA loses it. That matters most for exactly the report that needs it \u2014 the BRS, where an unpresented cheque is identified by its instrument number.",
   "market_standard": "Tally's BRS lists the instrument number against every reconciling item, and its bank-statement import maps a cheque-number column explicitly.",
   "confidence": "high",
   "suggested_fix": "Add a `cheque_no` field to ParsedNarration, populated when the channel is CHEQUE/CLG and a 6-digit token is present, and fall back to it when `reference_no` is empty. Small, and it becomes load-bearing the moment BANK-04 is built.",
   "effort": "hours",
   "verification": {
    "status": "confirmed",
    "by": "agent",
    "corrected_severity": "low",
    "what_is_actually_true": "Confirmed at low. ParsedNarration has no cheque-number field (domain/banking/narration.py:104-118) and _is_noise_word discards pure-digit tokens (:158-159), so an instrument number written inside a narration is never extracted into reference_no. It is a structured-extraction gap rather than data loss: the raw narration is stored and displayed on both the register (register.py:192) and the BRS lines (bank_reconciliation_service.py:608), and the vision path already asks for a separate reference column (vision.py:89, :202-206).",
    "probe": null,
    "lens_notes": {
     "literal": "Confirmed. domain/banking/narration.py:104-118 ParsedNarration carries raw, channel, utr, vpa, counterparty, ifsc, direction, tokens \u2014 there is no cheque-number field. _CHANNELS at :50 recognises CHQ|CHEQUE|CLG|MICR as a channel, and _is_noise_word (:155-167) returns True for any token with no letter in it ('pure digits'), so a bare 6-digit instrument number is dropped from the counterparty candidates. The UTR regexes (:60-62) match 12 digits or an alphanumeric NEFT/RTGS reference, neither of which a 6-digit cheque number satisfies. The number IS carried when the export has its own column: the ref index in the hdfc/generic_cheque/sbi/axis adapters (normalizer.py:99-113) becomes reference_no at :455, shown by register.py:193 and by the BRS line builder at services/bank_reconciliation_service.py:606-613.",
     "guards": "Two mitigations the finding does not credit. (1) The vision/scanned path is not blind to it: domain/banking/vision.py:89 asks the model for a 'reference' field per row and :202-206 puts it on reference_no, so a photographed statement WITH a cheque column still yields the number \u2014 the gap is confined to statements where the number appears only inside the narration text. (2) Nothing is lost from the CA's view: the full narration is stored as description and is emitted on every BRS line (bank_reconciliation_service.py:608) and every register line (register.py:192), so the instrument number is on screen, just not in a structured field.",
     "reach": "parse_narration is consumed by bank_matching_service.py:648, bank_entry_service.py:215, bank_payee_service.py:131, matcher.py:164 and history.py:100 \u2014 all live \u2014 but none of them matches on an instrument number today, so adding the field changes nothing until something reads it."
    }
   },
   "rescore_2026_09_08": {
    "state": "still_open",
    "what_is_true_now": "Unchanged. A cheque number written inside a narration is still discarded as noise; it survives only where the export has its own cheque column feeding reference_no.",
    "remaining": null,
    "probe": "domain/banking/narration.py:104-115 ParsedNarration still has raw, channel, utr, vpa, counterparty, ifsc, direction, tokens and no cheque field (`grep -n cheque domain/banking/narration.py` -> no match). _is_noise_word at :155-159 still returns True for any token with no letter in it, i.e. pure digits.",
    "against_commit": "46bd46c"
   }
  },
  {
   "id": "BANK-29",
   "kind": "glitch",
   "severity": "low",
   "title": "A statement's opening/closing-balance row becomes a zero-amount bank line that clogs the queue and can never be passed",
   "evidence": "domain/banking/normalizer.py:400-459 `_rows_to_txns` skips a row only when it has fewer than three non-empty cells, or no parseable date, or no description. A row like `01/04/2026 | OPENING BALANCE | | | | 1000.00` has a date, a description and a balance, so it is emitted with debit_paise = 0 and credit_paise = 0. I verified this: the three-transaction CSV I parsed in this session included the \"OPENING BAL\" row as a transaction. Note the amount/drcr branch DOES guard this (`if amount == 0 \u2026 continue`, :444) \u2014 the two-column branch does not. Downstream, `_plan` refuses it with \"Transaction has zero amount\" (services/bank_posting_service.py:290-291), so it can never be passed.",
   "ca_experience": "Every imported statement leaves one or two permanent \"needs you\" lines \u2014 Opening Balance, Closing Balance, B/F \u2014 that the CA cannot pass and must Set aside one by one. On a firm importing 50 statements a month that is 50-100 pointless clicks, and the \"33 need you\" counter on Entries is never truthful.",
   "market_standard": "Zoho Books and QuickBooks both drop zero-amount rows on import. Tally's Excel import ignores rows with no debit and no credit.",
   "confidence": "high",
   "suggested_fix": "Skip a row with no debit and no credit in the two-column branch of `_rows_to_txns`, matching what the amount/drcr branch already does. Zero-amount rows carry no money and no information the balance column does not already give.",
   "effort": "hours",
   "verification": {
    "status": "confirmed",
    "by": "agent",
    "corrected_severity": "low",
    "what_is_actually_true": "Confirmed at low. A statement's OPENING/CLOSING BALANCE row survives every filter in the two-column branch of _rows_to_txns and is stored as a zero-amount transaction that lands in needs_you and can never be passed (bank_posting_service.py:296-298). Two corrections: the queue offers a bulk 'Set aside' (EntriesTab.tsx:559-562 \u2192 batchExclude), so the clean-up is one selection per statement; and the proposed fix would regress the statement header's opening balance (banking_service.py:52-82 reads it off that very row) and drop the first pair from balance_agreement's tie-out (normalizer.py:638-660), so a label-aware skip that preserves the balance anchor is the shape it needs, not a bare zero-amount continue.",
    "probe": "cd apps/api && venv/bin/python -c \"from domain.banking.normalizer import parse_csv; \u2026\" on an HDFC-header CSV with OPENING BALANCE / one UPI debit / CLOSING BALANCE \u2192 three NormalizedTxn rows, the first and third with debit_paise=0, credit_paise=0 and balance_paise=1000000/950000. Then entry_state({'description':'OPENING BALANCE','debit_paise':0,'credit_paise':0,'match_status':'unmatched'}) \u2192 'needs_you'.",
    "lens_notes": {
     "literal": "Confirmed and reproduced. domain/banking/normalizer.py:400-460 _rows_to_txns skips a row only for <3 non-empty cells (:401-402), an unparseable date, or a missing description (:410-429). The amount/drcr branch guards zero at :444 (if amount == 0 or \u2026: continue); the two-column branch at :448-450 does not, and appends unconditionally at :452-459. Downstream refusal is real: services/bank_posting_service.py:296-298 raises 422 'Transaction has zero amount.' (and again at :957), so such a line can never be passed. domain/banking/entry.py:126-147 puts it in needs_you (verified by probe).",
     "guards": "Bulk Set aside exists and the finding misses it: apps/web/components/banking/EntriesTab.tsx:559-562 offers a multi-select 'Set aside' action calling api.banking.batchExclude, so it is one selection per statement, not 50-100 individual clicks. More importantly the suggested fix has a real cost the finding does not weigh: the OPENING BALANCE row is the anchor for two other things \u2014 services/banking_service.py:52-82 _opening_closing_balance derives the statement header's opening_balance_paise from the earliest-dated row's balance column (drop the row and the header's 'opening balance' becomes the balance AFTER the first transaction), and normalizer.py:638-660 balance_agreement checks consecutive deltas pairwise, so removing the first row removes the pair that validates the first real transaction.",
     "reach": "Live on the ordinary CSV/XLSX import path (parse_csv \u2192 import_normalized), which is the only way bank data enters the product. The vision path already skips these rows \u2014 vision.py:198-201 returns None for a row with a date and a narration but no money."
    }
   },
   "rescore_2026_09_08": {
    "state": "still_open",
    "what_is_true_now": "Unchanged and reproduced. An opening/closing-balance row is still stored as a zero-amount transaction that sits in needs_you and can never be passed (bank_posting_service refuses it as 'Transaction has zero amount'); bulk 'Set aside' is still the only clean-up.",
    "remaining": null,
    "probe": "Ran parse_csv at HEAD on an HDFC-header CSV of OPENING BALANCE / one UPI debit / CLOSING BALANCE. Printed three transactions: '2026-04-01 OPENING BALANCE 0 0 1000000', '2026-04-02 UPI/DR/RAMESH 50000 0 950000', '2026-04-30 CLOSING BALANCE 0 0 950000'. Then `entry_state({'description':'OPENING BALANCE','debit_paise':0,'credit_paise':0,'match_status':'unmatched'})` -> 'needs_you'. The amount/drcr branch still has its `if amount == 0 ... continue` guard; the two-column branch still does not.",
    "against_commit": "46bd46c"
   }
  }
 ],
 "maturity": {
  "grade": "B",
  "rationale": "The engine is genuinely strong and in places ahead of anything on the Indian market: three independent import verifications that refuse before writing, a PDF text parser with geometric column recovery (TallyPrime cannot import a PDF at all), a vision path whose totals are read by a deliberately independent second call, a single posting path that refuses to debit and credit the same ledger, an exact tax-inclusive GST split, a transfer double-count guard, an append-only undo that revokes party credit and re-uses an existing reversal, and an entry_state column maintained by a trigger with a Python twin pinned by a real parity test. The screens are honest \u2014 a verified import and an unverified one cannot look the same. What holds it back from an A is that the pillar a CA judges the module by is the wrong shape: the document titled \"Bank Reconciliation Statement\" is a statement-line tick-off with no unpresented cheques and no deposits in transit, and it carries an unexplained Adjustments plug that can force it to tie out and be certified. Alongside that sit two ledger-classification faults that put wrong figures on the balance sheet (OD/CC accounts booked as assets, foreign-currency accounts posted at 1:1), no cash book at all, no credit cards, no way to undo a mis-imported statement, an import that can be falsely refused by a page subtotal, and a rule engine markedly thinner than Zoho's or QuickBooks'. None of those is deep \u2014 the kernel underneath is right and most are days of work, not months.",
  "would_a_ca_buy_this_module_alone": "Not yet \u2014 the import and coding loop is better than anything in the Indian market, but a CA will open Reconcile first, find it is not the BRS they have prepared every month of their career, and no cash book anywhere, and conclude they still need Tally beside it."
 },
 "open_questions_for_owner": [
  "The BRS is the module's shop window and it is currently the wrong document. Do you want the classic two-sided Indian BRS \u2014 Balance as per Cash Book, add cheques issued not presented, less cheques deposited not credited, Balance as per Pass Book \u2014 built on the book side (weeks), or is the statement tick-off deliberate and the PDF simply mistitled?",
  "Is the cash book in scope? Every SME client runs cash, the kernel already supports Receipt/Payment/Contra on any ledger, and its absence is the clearest single reason a CA keeps Tally open beside this. Or is cash deliberately out of scope for now?",
  "Overdraft and Cash Credit accounts are offered in the account-type dropdown but always given an Asset ledger, and the ledger picker will not show a Liability. Fixing it changes existing clients' balance sheets \u2014 do you want a migration that re-classifies existing OD/CC ledgers and reports what moved, or a forward-only fix with the old ones flagged?",
  "Multi-currency bank accounts are permitted today and post at 1:1. Do you want them refused until an FX path exists (one line, honest, today) or the FX path built (days)? Is any live client actually using a non-INR bank account?",
  "The reconciliation's Adjustments field is a free plug with no reason and no journal. Should a non-zero adjustment (a) require a substantive reason like the reopen path already does, (b) require Manager approval, or (c) become an actual posted journal so the books and the certificate agree?",
  "domain/banking/exceptions.py is a complete, 25-test, partner-review engine \u2014 materiality, duplicates, cash withdrawals, new payees \u2014 wired to nothing, and its service file was never written. It is the closest thing in this module to the analytics you want CAs addicted to. Was it deliberately parked, and do you want it finished?",
  "How far do you want the rule engine pushed against Zoho and QuickBooks? A priority column alone would fix the \"first rule wins forever\" problem in hours; multi-condition rules and split actions are weeks. Which do your first CAs actually ask for?",
  "Trusted rules currently post unattended overnight, including over lines the CA has hand-coded differently. Should a line a human has touched be permanently exempt from the sweep, or is posting the human's own coding acceptable?",
  "Should a mis-imported statement be deletable? It requires hard-deleting bank_transactions, which sits close to the immutability rules \u2014 is \"unposted and unreconciled lines only, with the whole statement written to audit_log\" the boundary you want?",
  "The Bank Book fetches the whole account and computes the running balance in Python, which the reporting rule forbids. Is a SQL function plus a parity test (the cash_flow_report pattern) the right investment now, or is it acceptable until a client's line count actually hurts?"
 ],
 "strengths": [
  "The import verification is three independent checks that refuse BEFORE anything is written, and it is better than anything on the Indian market. `normalizer.balance_agreement` (normalizer.py:609-678) tests consecutive deltas against the bank's own running balance and detects a newest-first statement rather than calling it an error; `tie_out.totals_agreement` (tie_out.py:224-269) sums against the bank's own printed \"Grand Total\"; `tie_out.tie_out` (:62-116) checks opening + credits \u2212 debits = closing. `statement_check` (:272-333) combines them so that either failing refuses the import (routers/banking.py:849-853), and a `verified` flag means an unchecked import and a checked one cannot look the same on screen (AccountsPanel.tsx:747-760).",
  "PDF import with a geometric fallback. `_pdf_rows` (normalizer.py:791-874) tries ruled-table extraction first, then assigns each word to a column by its x-midpoint against the header's own coordinates (`_rows_by_position`, :908-951, with `_merge_label_words` splitting at one em, :883-905). The docstring records exactly why a whitespace split is worse than useless. TallyPrime's own documentation tells users to convert a PDF to CSV first; this reads it.",
  "The vision path's independence argument is the right one and is actually implemented. `read_printed_totals` (vision.py:275-343) is a SECOND model call given the last page alone and shown no transactions, so the totals cannot be a sum of the reading being checked, and it refuses unless the label matches the same `_TOTAL_LABEL` regex the deterministic parser uses and both figures look like money. The gate is applied one page-sized call in, before twenty pages are rasterised (routers/banking.py:721-733).",
  "entry_state is a trigger-maintained column with a Python twin and a real parity test \u2014 not a claimed one. Migration 322's `bank_transaction_entry_state()` and `domain/banking/entry.py::entry_state` (:126-147) have the same branches in the same order, and tests/test_bank_entry_state_parity_pg.py parametrises BOTH over the same shared `STATE_TABLE` imported from tests/test_bank_entry.py:42-69, asserting the trigger and the twin agree on every case plus UPDATE-time behaviour Postgres alone can prove.",
  "One posting path, and it refuses the failure mode that balances. `posting_map.build_lines` (:50-67) raises when the bank and counter accounts are the same \u2014 an entry that satisfies the double-entry assertion, nets to zero, posts, becomes immutable, and classifies nothing. The comment explaining why nothing else catches it is exactly the standard of reasoning this codebase should be judged by.",
  "The tax-inclusive bank-charge split is exact by construction and direction-aware. `split_inclusive_charge` (charge_gst.py:75-110) backs the taxable value out with integer arithmetic and takes the tax as the REMAINDER so the journal balances with no plug, and `_charge_lines` (bank_posting_service.py:233-274) picks GST Input for money out (s.16) and GST Output for money in (s.9) rather than using one set for both. Place of supply under IGST s.12(12) is stated by the CA on the rule, never inferred from an IFSC.",
  "Undo is a genuine append-only reversal with the hard cases handled. `bank_posting_service.undo` (:690-820) reverses on the original date while the period is open and today's when locked, RE-USES an existing reversal so a retried undo cannot swing the ledger twice, revokes the party credit granted from an overpayment before unsettling the document that produced it, refuses a multi-invoice settlement that belongs to a receipt, and returns the line to `matched` rather than `unmatched` so the CA does not have to re-find the invoice.",
  "Transfer double-counting is prevented structurally, not by convention. `domain/banking/transfers.detect` (:150-224) counts every possible pairing BEFORE the greedy choice so ambiguity can be reported rather than resolved, pairing is written through an RPC (migration 258) so a half-paired state cannot exist, and `_plan` (bank_posting_service.py:346-352) refuses to post the receiving side of an already-posted pair.",
  "The narration parser classifies by SHAPE rather than enumerating bank layouts (narration.py:24-31), and `party_matches` (:265-301) normalises entity suffixes so \"Acme Pvt Ltd\" matches \"ACME PRIVATE LIMITED\" \u2014 with the single-shared-word coincidence explicitly excluded. Tested by 35 cases in tests/test_bank_narration.py.",
  "Row-addressed authorisation is done properly. `_assert_row_scope` (routers/banking.py:105-133) resolves the row inside the firm and then applies `assert_client_access`, returning 404 for both \"no such row\" and \"not your client\"; `_assert_txn_batch_scope` (:143-166) checks every distinct client in a batch BEFORE any row is touched, with the reasoning about ids leaking written down. Every row-addressed banking endpoint uses one of them.",
  "The register self-checks against the bank's own balance column and reports only the FIRST divergence (register.first_divergence, :210-231), because every line below inherits the same offset \u2014 and it excludes rows dated before the account's opening-balance date from the arithmetic rather than double-counting them (:176-185). That check is structurally impossible in QuickBooks, which has no bank-stated balance per line.",
  "Reconciliation completion freezes a snapshot (bank_reconciliation_service.py:407-413) so a certified period cannot silently change, reopening requires a substantive reason enforced by both a Pydantic validator and a DB CHECK (models/banking.py:312-329, migration 253), and `opening_suggestion` (:140-215) derives the next period's opening from the previous frozen snapshot and warns when the books have moved under it.",
  "A refused bank-account deletion names the statute, whose duty it is and the date it lapses. services/bank_erasure.py:60-98 splits the retention reason (Companies Act s. 128(5), dated from the newest statement's own period through domain/dpdp/retention.decision_for_record_date) from the referential reason, and refuses to invoke a statute it cannot date.",
  "The frontend genuinely holds no business logic in the core loop: entry_state, the kind, the draft and the register balance are all server-computed and the components say so (BankBook.tsx:24-26, EntriesTab.tsx:74-75). Money is parsed by lib/money/rupeeInput in ReconcileTab and AccountsPanel, and the long jobs are chunked, resumable loops with progress rather than a job table."
 ],
 "subsystem": "Banking \u2014 statement import (CSV/XLSX/PDF/vision), normalisation, bank entries and drafts, rules and trusted rules, matching, transfers, splits, charges with GST, reconciliation and BRS, bank register/bank book, bank erasure",
 "test_coverage_notes": "Roughly 700 mock-mode tests across ~45 files under apps/api/tests (test_bank_*, test_statement_*, test_reconciliation_*, test_e2e_banking*). I ran tests/test_bank_feed_import.py, test_statement_printed_totals.py, test_bank_entry.py and test_bank_transfers.py together: 120 passed in 5.8s. Depth is real, not decorative \u2014 53 cases on candidate search, 49 on charge-GST posting, 35 on narration parsing, 30 on the vision path with an injected fake model so the real prompt and real refusals are exercised with no key and no traffic.\\n\\nWhat is genuinely well pinned. tests/test_bank_entry_state_parity_pg.py is a REAL parity proof, not a claimed one: it imports STATE_TABLE from tests/test_bank_entry.py:42-69 and parametrises both the Postgres trigger and the Python twin over the same 18 cases, plus UPDATE-time draft_error clearing that only a database can show, and tests/test_bank_entry.py:77 asserts every state the trigger can produce appears in the table. tests/test_bank_reads_are_complete.py builds a stub that truncates exactly like PostgREST and proves the register, the categorisation queue and the reconciliation each read past 1,000 rows.\\n\\nGaps I can name from the code I read. (1) No test covers a statement that prints a page subtotal labelled \\\"Total\\\" before its Grand Total \u2014 I grepped test_statement_printed_totals.py for a subtotal or per-page case and found none, and the behaviour I reproduced is a false refusal of a correct file (BANK-01). (2) No parity test between apps/web/lib/banking/splitLegs.ts and apps/api/domain/banking/splits.py; the repo has only shared/gst-parity-vectors.json, so the browser's split arithmetic and the server's are two implementations with nothing holding them together \u2014 exactly the situation CLAUDE.md's cash-flow rule exists to prevent. (3) tests/test_bank_exception_rules.py has 25 passing tests over domain/banking/exceptions.py, which nothing in the product imports \u2014 25 tests of confidence in a feature no CA can reach. (4) domain/banking/transfers is tested only with small in-memory sets, so bank_transfer_service's hard SCAN_LIMIT = 1000 and its per-chunk re-run are not covered by the reads-are-complete suite. (5) No test asserts that a non-INR bank account's statement is handled at all, which is why BANK-03 passes CI. (6) The real-Postgres tests (test_bank_entry_state_parity_pg.py, test_bank_undo_status_vocabulary_pg.py, test_statement_opening_parity_pg.py, test_r253_reconciliation_reopen_pg.py) self-skip without HARNESS_PG, so the parity guarantees hold only in the migration-apply CI job, not in the default run.\\n\\nI found no test asserting a WRONG statutory behaviour. The GST-on-charges tests (test_bank_charge_gst.py, test_bank_gst_both_directions.py) assert the inclusive split and the input/output direction correctly against s.16 and s.9; the matcher's TDS-shape tests label a shortfall without computing a liability, which is the right line.",
 "what_exists": "Statement import. `POST /api/banking/statements/upload` (routers/banking.py:761) takes a CSV, XLSX, PDF or image. `domain/banking/normalizer.py` detects six layouts from the header row \u2014 hdfc, sbi, icici, axis, a six-column cheque layout (generic_cheque, built against a real 33-page Cosmos Co-op statement), a generic Date/Desc/Debit/Credit/Balance, and a generic single-Amount + Dr/Cr variant \u2014 validates that the chosen adapter actually fits, and otherwise raises \\\"Unsupported bank statement format\\\". The way past that is an explicit per-account column mapping keyed on a SHA of the header row (`validate_mapping`, `header_fingerprint`, services/bank_column_mapping_service.py), reachable through /statements/inspect and /statements/preview. PDFs are parsed from a text layer, first by ruled-table extraction and then by a geometric fallback that assigns words to columns by x-midpoint. A scanned PDF or phone photo goes to Gemini (`domain/banking/vision.py`, services/statement_vision.py) only when the CA opts in.\\n\\nVerification. Three checks, in `domain/banking/tie_out.py` and `normalizer.balance_agreement`: the rows against the bank's running-balance column, the rows against the \\\"Grand Total\\\" the bank printed on the file, and opening + credits \u2212 debits = closing from two figures the CA types. `statement_check` runs whichever are available; either failing refuses the import before anything is written, and a scan is never imported unverified.\\n\\nStorage and dedup. `services/banking_service._import_core` fingerprints every row (client, bank account, date, amounts, balance, description, reference), drops within-file and already-stored duplicates, and chunk-inserts into `bank_transactions` under a `(client_id, import_hash)` unique index, with a `bank_statements` header row carrying the period, totals and file metadata.\\n\\nEntries. Migration 322 put the machine's proposal ON the row (`draft_*` columns) and the state in a trigger-maintained `entry_state` (needs_you / proposed / ready / covered / passed / set_aside) with a Python twin in `domain/banking/entry.py`. `bank_entry_service.redraft` writes each open line's best proposal from four sources \u2014 a rule the CA wrote, an open document the matcher ranked, a transfer counterpart, the payee's coding history \u2014 graded ready or proposed with a reason sentence. `pass_ready` passes every ready line in chunks of fifty through `bank_posting_service.post`, the one posting path. Endpoints: /entries, /entries/counts, /entries/{id}, /entries/redraft, /entries/pass-ready, /transactions/{id}/pass, plus the older per-line verbs (categorize, match, unmatch, undo, match-multi, ignore, unignore, splits, payee, transfer-pair, attachments, posting-preview, post).\\n\\nRules. `bank_matching_rules` with one narration substring, an amount range and a direction, proposing a category, one ledger, a narration and a GST rate/interstate flag. A Manager+ can mark a rule TRUSTED, after which its ready drafts post with no click \u2014 on import from the screen, and nightly from jobs/bank_trusted_rules_job.py, with `journal_entries.created_by = trusted_by`.\\n\\nPosting. `bank_posting_service` resolves the bank ledger from the statement's account, the counter from the category or the CA's pick, builds two-leg, n-leg (splits), contra (transfers) or GST-inclusive charge lines, posts through `phase2_journal_service._create_journal`, then settles the matched invoice or bill against true outstanding with the excess granted as party credit. `undo` reverses append-only.\\n\\nReconciliation. Sessions per bank account per period (`bank_reconciliations`), manual tick-off of POSTED statement lines, an arithmetic tie-out with a free Adjustments figure, completion frozen into a snapshot, reopen with a mandatory reason, and a report as JSON, CSV and a \\\"Bank Reconciliation Statement\\\" PDF.\\n\\nScreens. apps/web/app/clients/[id]/bank is a shell over three tabs \u2014 Entries, Reconcile (BRS), Rules \u2014 with an Accounts panel and import modal reached from the toolbar, and the Bank Book (register) under Reports."
}