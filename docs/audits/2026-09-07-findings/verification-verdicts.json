{
 "wf_4acb9ddf-0b0": [
  {
   "kind": "single",
   "finding_id": "GST-01",
   "lens": "LITERAL",
   "refuted": false,
   "verdict_reason": "CONFIRMED in the code, with two corrections to the finding's framing.\n\nWhat I verified:\n- apps/api/domain/gst/gstr3b_computer.py:645-651 accumulates `rcm_igst/cgst/sgst` from reverse-charge purchases (Table 3.1(d)).\n- The Table 6 set-off is at lines 718-745 (the finding's cite of 712-731 is off \u2014 712-716 is the 4(D)(1) reclaims loop). Lines 729-745 compute `net_igst/net_cgst/net_sgst/net_cess` purely from `outward_taxable_*` minus `itc_net_*`. `rcm_*` never appears on the liability side; grep for `rcm` in that file returns only 149-151 (fields), 290-298 (Table 4(A)(3) ISRC credit), 423-425 (payload 3.1(d)) and 649-651 (accumulation).\n- lines 663-669: `book_igst = sum(p.igst_paise \u2026)` sums over ALL purchases including RCM ones, so the RCM tax is taken as credit and does reduce `net_*`.\n- I ran compute_gstr3b directly: one intra-state sale (CGST 90000p + SGST 90000p) plus one RCM inward supply (IGST 500p) \u2192 rcm_igst=500, itc_avail_igst=500, net total = 179500p; the same sale with no RCM purchase gives 180000p; an RCM purchase alone gives net total 0 with rcm_igst=500. So the RCM credit is spent against outward liability while its own liability is never raised.\n- services/gst_return_service.py:847 `\"net_tax_paise\": result.net_igst + result.net_cgst + result.net_sgst` \u2014 and note line 805 sets `tax_liability_paise = books_output` which DOES add `books_rcm`, so the same response reports a liability including RCM and a net tax that excludes it.\n- routers/gst_workspace.py:583 and 1509-1511 persist that value to `gstr3b_returns.net_tax_paise`; line 752 passes `rec[\"net_tax_paise\"]` into `record_filing(... tax_payable_paise=...)`, so the understated figure also lands in the permanent filing record.\n- apps/web/app/gst/gstr3b/page.tsx:417-432 renders `w.net_payable.*` as \"Table 6 \u2014 Net Tax Payable\" incl. \"Total\"; grep for `rcm` in that page and in apps/web/app/clients/[id]/compliance/gst/page.tsx returns nothing, while apps/web/lib/data/gst.ts:96-100 types `rcm_inward` \u2014 confirmed.\n- services/filing_demo/gstr3b.py:292-297 builds `head_liability` from `taxable_{head}_paise` only, and its note at 330-332 says the 3.1(d) tax \"is paid in cash separately \u2014 it is not part of this set-off\", so the demo's Table 6.1 cash total is likewise not the full cash due.\n- tests/test_gst_rcm_gstr3b.py:40-45 (`test_c4_c5_rcm_net_liability_offsets_itc`, \"net effect zero\") pins the current premise, so no existing test contradicts the behaviour.\n\nLaw: \u00a749(4) read with \u00a72(82) (\"output tax\" excludes tax payable on reverse charge basis) means the credit ledger cannot discharge 3.1(d) \u2014 it is cash-only. The finding states this correctly.",
   "corrected_severity": "high",
   "what_is_actually_true": "The core defect is real: `compute_gstr3b` takes the reverse-charge credit but never raises the matching reverse-charge liability, so every derived \"net tax payable\" figure is understated for any client with GTA/legal/director/rent RCM.\n\nTwo corrections to the finding.\n\n1. The magnitude is ONE times the RCM tax, not twice. Correct treatment for the worked example: liability 180000 + 500 (3.1(d)) = 180500, less 500 ITC = 180000 total payable (of which 500 must be cash). The code shows 179500. The shortfall is 500p, exactly the RCM tax \u2014 the finding's own numbers show this; the \"twice\" in `ca_experience` is wrong arithmetic (it would only hold if the same-period RCM credit were itself inadmissible, which it is not \u2014 the portal auto-populates 4(A)(3) from 3.1(d)).\n\n2. The FILED RETURN is not wrong, so \"the return short-pays\" is overstated. `as_gstn_payload` emits 3.1(d) correctly (lines 421-427, `isup_rev`) and 4(A)(3) ISRC correctly (lines 290-298), and it contains no Table 6.1/payment section at all \u2014 the portal computes the payment table itself from 3.1(d) and will not accept a filing that leaves the RCM cash unpaid. The damage is therefore: (a) the in-app \"Net Tax Payable\" and its Total on apps/web/app/gst/gstr3b/page.tsx, (b) the stored `gstr3b_returns.net_tax_paise`, (c) `gst_filings.tax_payable_paise` written at routers/gst_workspace.py:752, and (d) the filing-demo Table 6.1 cash total \u2014 all understated by the RCM amount, so the CA generates a PMT-06 challan for the wrong figure and discovers it at the portal. That is a wrong number in front of a CA and in a permanent record, but not a mis-filed return, which is why I put it at high rather than critical.\n\nOne sub-claim in the evidence does not stand on its own: `itc_consumed_paise` (lines 343-349) omitting `rcm_*` from `owed` is currently CONSISTENT, not a second bug \u2014 since `net_*` is defined over outward only, `owed - still_payable` correctly equals credit spent. It only becomes wrong once the liability side is fixed, at which point `owed` must gain the RCM heads too (and `itc_carried_forward_paise`, which is derived from it, must be re-checked).\n\nAlso note the internal inconsistency that makes the omission easy to see without any test: services/gst_return_service.py:805-847 returns `tax_liability_paise` WITH the RCM heads and `net_tax_paise` WITHOUT them, in the same response.\n\nThe suggested fix's shape is right (cash-only RCM head, separate `cash_only_rcm_paise` in `working.net_payable`, render 3.1(d) on both screens); the note in filing_demo/gstr3b.py:330-332 shows the correct statutory reading was already written down but not implemented in the numbers."
  },
  {
   "kind": "single",
   "finding_id": "GST-01",
   "lens": "GUARDS",
   "corrected_severity": "high",
   "refuted": false,
   "verdict_reason": "Core mechanic confirmed by reading and running the code. domain/gst/gstr3b_computer.py:717-745 computes net_igst/cgst/sgst from result.outward_taxable_* only; rcm_igst/cgst/sgst (accumulated at :645-651) never enter the liability side, while book ITC at :663-666 sums EVERY purchase including the reverse-charge ones, so the RCM credit is spent and the matching liability is never raised. itc_consumed_paise (:346-351) has the same outward-only base. services/gst_return_service.py:847 exports net_tax_paise from those three, routers/gst_workspace.py:583 stores it on gstr3b_returns.net_tax_paise and :752 copies it into gst_filings.tax_payable_paise on Mark-as-Filed, and apps/web/app/gst/gstr3b/page.tsx:417-432 renders it as Table 6 \"Net Tax Payable / Total\". I reproduced the author's example (one intra-state sale CGST 90000p + SGST 90000p, one RCM inward IGST 500p): rcm_igst=500, net total=179500p; and an RCM-only period gives net total 0 with itc_carried_forward_paise=500 \u2014 i.e. nil payable shown where Rs 5 is owed in cash. GUARDS searched and none bounds it: no test asserts net_* carries RCM (tests/test_gst_rcm_gstr3b.py:40-46 asserts only rcm_igst - itc_igst == 0, deliberately outside the Table 6 set-off); domain/gst/validator.py:204-232 validate_gstr3b has no RCM check at all; tests/test_gstr3b_screen_contract.py is one-directional (screen reads must exist in the response), so the never-rendered working.rcm_inward is invisible to it; migrations/036_gst_engine.sql:83-85 only defines rcm_*_paise columns, no CHECK or trigger; no feature flag or sweep touches it. Three parts of the finding are wrong or overstated and I correct them below.",
   "what_is_actually_true": "TRUE: Table 6 / net_tax_paise omits the 3.1(d) reverse-charge liability while spending its credit, no screen renders working.rcm_inward (grep across apps/web returns only the type at lib/data/gst.ts:96), and itc_carried_forward is correspondingly overstated. Nothing else guards it.\n\nCORRECTION 1 \u2014 magnitude. \"Understated by twice the reverse-charge tax\" is wrong; it is understated by exactly 1x the RCM tax. The RCM credit is genuinely available in the same return (s.16 with Rule 36(1)(b)); only the discharge of the 3.1(d) liability is barred from the credit ledger by s.49(4). Correct cash = rcm + max(0, outward - ITC); the code gives max(0, outward - ITC). The author's own repro proves it: correct total cash 180000p, code shows 179500p \u2014 a 500p gap, one times the RCM tax, not 1000p.\n\nCORRECTION 2 \u2014 \"the return short-pays\" overstates the blast radius. The product is prepare-only. The GSTN JSON it emits IS correct: gstr3b_computer.py:421-427 writes sup_details.isup_rev from rcm_*, and the ISRC row at :290-299 declares the matching credit \u2014 both pinned by tests/test_gstr3b_payload_matches_gstn_utility.py:168-171. The portal computes its own Table 6.1 from that and will not let a return file with an undischarged cash liability. So the filed return is not short; what is wrong is the figure the CA reads on screen to raise the PMT-06 challan, and the tax_payable_paise recorded on the gst_filings row.\n\nCORRECTION 3 \u2014 the filing demo does NOT silently repeat the omission. services/filing_demo/gstr3b.py:214-218 renders Table 3.1(d) with its IGST/CGST/SGST figures, :198-201 says 3.1(d) is self-assessed and paid in cash because s.49(4) does not let the credit ledger discharge it, the Table 6.1 liability column is labelled \"Liability (3.1(a))\" (:335) and :329-332 states the 3.1(d) tax \"is paid in cash separately \u2014 it is not part of this set-off.\" Its 6.1 cash total is still incomplete versus the real portal screen, but that is a disclosed scope limit, not the same defect.\n\nAlso note a real inconsistency the finding missed, which strengthens it: gst_return_service.py:804-806 DOES include RCM in the exported/stored tax_liability_paise (books_output = outward + books_rcm), so the stored header liability, the GL comparator and the journal (phase2_journal_service.py:700-721 credits the GST output accounts for RCM) all carry the liability correctly. Only the Table 6 net figure drops it, so liability - itc_claimed and net_tax_paise disagree by exactly the RCM tax on every stored return that has one.\n\nSeverity high, not critical: no filing is made from the wrong figure, the portal-bound JSON and the GL are both right, and the error is 1x the RCM tax \u2014 but the headline \"Net Tax Payable\" a CA pays from is wrong, is persisted to the filing record, and an RCM-only period shows nil payable."
  },
  {
   "kind": "single",
   "finding_id": "GST-01",
   "lens": "REACH",
   "findings": "[]",
   "refuted": false,
   "corrected_severity": "high",
   "verdict_reason": "Core defect confirmed by reading and by execution, but two of the claim's assertions are wrong and one is overstated, so severity drops from critical to high.\n\nCONFIRMED. apps/api/domain/gst/gstr3b_computer.py:645-651 accumulates rcm_igst/cgst/sgst from reverse-charge purchases; the Table 6 set-off at :725-745 reads only outward_taxable_* and never adds rcm_*. Book ITC at :662-665 sums ALL purchases, so the RCM credit IS in 4(C) (payload \"ISRC\" row, :290-292). GSTR3BResult.itc_consumed_paise (:344-350) has the same omission. services/gst_return_service.py:847 exports net_tax_paise = net_igst+net_cgst+net_sgst, stored on gstr3b_returns.net_tax_paise (routers/gst_workspace.py:583) and rendered as the Table 6 \"Total\" (apps/web/app/gst/gstr3b/page.tsx:417-432).\n\nREACH is real: gst_workspace and filing_demo routers are mounted (apps/api/main.py:321,325), apps/web/lib/data/gst.ts:399 POSTs /api/gst/gstr3b/from-books and the page renders working.net_payable. grep confirms no \"rcm_inward\" / \"3.1(d)\" anywhere under apps/web/app \u2014 the screen's \"Table 3.1\" block (page.tsx:300-338) has rows (a)/(b)/(c) only. The understated figure also propagates to the statutory filing record: routers/gst_workspace.py:752 writes tax_payable_paise=rec[\"net_tax_paise\"].\n\nREPRODUCED (venv, compute_gstr3b): sale CGST 90000p + SGST 90000p with one RCM inward IGST 500p -> rcm_igst=500, itc_igst=500, net total 179500; same sale with no RCM -> 180000. RCM purchase alone -> net total 0, carried_forward 500.\n\nCORRECTIONS to the claim:\n1. Magnitude is 1x, not \"twice\". Correct cash for that case is 500 (RCM, cash-only) + 179500 (outward after credit) = 180000; the app shows 179500. The understatement equals rcm_total exactly, because the RCM credit is genuinely available in 4(C). Carried-forward is not affected (the RCM-only case correctly reports 500 carried forward).\n2. \"The return short-pays\" is wrong. as_gstn_payload emits sup_details.isup_rev with the RCM heads (gstr3b_computer.py:420-426; verified in the run above: isup_rev iamt=5, itc ISRC iamt=5). GSTN computes Table 6.1 from 3.1(d) itself, so the JSON the CA uploads declares the liability correctly. The damage is the app's on-screen \"Net Tax Payable\"/challan figure and the stored gst_filings tax_payable_paise, not the declared return.\n3. tax_liability_paise is NOT affected: gst_return_service.py:804-806 deliberately adds books_rcm to books_output, with a comment saying why. So the screen's liability header and the net-payable total are computed on different bases and cannot be reconciled by the reader.\n4. The filing-demo repetition is real but partially deliberate: services/filing_demo/gstr3b.py:213-219 renders Table 3.1(d) from working.rcm_inward, and :330-332 states in words that 3.1(d) \"is paid in cash separately - it is not part of this set-off\". Its Table 6.1 liability column (:292) is correctly labelled \"Liability (3.1(a))\". The gap there is only that no total cash-including-RCM figure is offered, and stage 1 labels net_tax \"Net tax payable in cash (Table 6)\", which is the same understated number.\n5. tests/test_gst_rcm_gstr3b.py:40-45 pins the mistaken premise (\"Classic RCM: liability and ITC -> net effect zero\"), which is true of the economic net over time but not of the cash payable in the period under \u00a749(4). Any fix has to revisit that test.",
   "what_is_actually_true": "The Table 6 set-off in domain/gst/gstr3b_computer.py omits the 3.1(d) reverse-charge liability while including its credit in 4(C), so the app's \"Net Tax Payable\" (and the itc_consumed/\"set off against tax\" figure, and the tax_payable_paise recorded on the filing) is understated by exactly the total reverse-charge tax for the period \u2014 not twice it. The GSTN payload itself is correct (isup_rev and the ISRC ITC row are both populated), so the filed return is not short-declared; the portal would compute the right cash demand. The harm is that a CA reading the GSTR-3B screen \u2014 which never renders 3.1(d) at all, and shows a liability header that does include RCM \u2014 plans and pays a challan short by the RCM amount, and the recorded filing carries the wrong tax paid. Reach is confirmed: router mounted in main.py, screen calls /api/gst/gstr3b/from-books, no flag involved."
  },
  {
   "kind": "single",
   "finding_id": "GST-02",
   "lens": "LITERAL",
   "refuted": false,
   "verdict_reason": "The cited lines exist and do exactly what the finding says. domain/gst/gstr3b_computer.py:619-620 \u2014 `elif s.supply_type == \"zero_rated\": result.outward_zero_rated += sign * s.taxable_amount_paise` and nothing else; igst/cgst/sgst/cess are never read on that branch (the tax heads are accumulated only in the `taxable` branch, 621-627). domain/gst/gstr3b_computer.py:414-417 hardcodes `\"osup_zero\": {\"txval\": r(self.outward_zero_rated), \"iamt\": 0, \"camt\": 0, \"samt\": 0, \"csamt\": 0}`. Reproduced by executing the module (PYTHONPATH=apps/api): one zero_rated sale of 10_000_00p with igst_paise=1_800_00 gives outward_zero_rated=1000000, outward_taxable_igst=0, net_igst=0, and payload osup_zero={'txval': 10000, 'iamt': 0, 'camt': 0, 'samt': 0, 'csamt': 0}. The input path is real, not hypothetical: services/gst_return_service.py:616-640 (`gstr3b_from_books`) builds SalesTransaction with `supply_type=inv.get(\"supply_type\") or \"taxable\"` and `igst_paise` straight off the invoice row, and routers/sales_invoices.py:723-747, 844-851 compute IGST purely from the line rate and is_interstate \u2014 supply_type never forces tax to zero \u2014 while apps/web/lib/invoices/classification.ts:51,58 offers exactly the pair \"Zero-rated\" + \"SEZ with payment (IGST) \u2014 CGST \u00a716(3)\". The codebase elsewhere assumes zero_rated CAN carry IGST: gstr1_builder.py:497-500 infers EXPWP from `inv.igst_paise` being non-zero, and _build_exports (535-553) emits WPAY items with the IGST \u2014 so GSTR-1 declares the export IGST that GSTR-3B drops, which is the GSTR-1-vs-3B mismatch the finding describes. Two things to correct/add. (1) `camt`/`samt` hardcoded to 0 is CORRECT, not part of the bug \u2014 Table 3.1(b) has only Total taxable value, Integrated tax and Cess columns; only `iamt` and `csamt` are wrongly nailed to 0. (2) The impact is larger than stated: because Table 6 sets off against `outward_taxable_igst` only (gstr3b_computer.py:731-743), the export IGST never enters net_igst either, so net tax payable is understated by the same amount, not merely a display/payload field; `itc_consumed_paise` (346-350) likewise. The reconciliation claim also holds \u2014 services/gst_return_service.py:804-806 builds `books_output` from outward_taxable_* + RCM only, so the GL's output credit for the export IGST shows as a permanent unexplained difference. No test pins the current behaviour as deliberate: tests/test_gst_rcm_gstr3b.py:73 sets outward_zero_rated directly, and every zero_rated fixture elsewhere is SEZ_without_payment/LUT (zero IGST by construction). On the law I am confident the 3.1(b) row carries an integrated-tax column (export with payment of IGST under \u00a716(3)(b) is one of the two statutory routes); I have no live portal access to diff the current form layout.",
   "corrected_severity": "critical",
   "what_is_actually_true": "A zero-rated supply carrying IGST (export or SEZ supply WITH payment of tax, \u00a716(3)(b)) has its IGST and cess silently discarded by compute_gstr3b: only the taxable value reaches `outward_zero_rated`, and the payload's `osup_zero.iamt`/`csamt` are hardcoded 0. Beyond the payload, the same omission flows into Table 6 \u2014 net_igst is computed from `outward_taxable_igst` alone, so the return understates tax payable by the whole export IGST \u2014 and into `gstr3b_from_books`'s books-vs-GL comparator, which builds `books_output` without it and therefore reports a permanent mismatch against the GL output credit. GSTR-1 does report the same invoice's IGST (EXP WPAY items), so 3B and 1 disagree by the export IGST. The `camt`/`samt` zeros are correct and should stay: 3.1(b) has no CGST/SGST column. A related, separate gap (not this finding): an invoice marked invoice_type=\"SEZ_with_payment\" but left at supply_type=\"taxable\" lands in 3.1(a) rather than 3.1(b), since compute_gstr3b never looks at invoice_type."
  },
  {
   "kind": "single",
   "finding_id": "GST-02",
   "lens": "GUARDS",
   "refuted": false,
   "corrected_severity": "high",
   "verdict_reason": "CONFIRMED at the cited lines, and reproduced. apps/api/domain/gst/gstr3b_computer.py:619-620 accumulates only `result.outward_zero_rated += sign * s.taxable_amount_paise` for `supply_type == \"zero_rated\"` \u2014 no igst/cgst/sgst/cess \u2014 and :414-417 emits `\"osup_zero\": {\"txval\": r(self.outward_zero_rated), \"iamt\": 0, \"camt\": 0, \"samt\": 0, \"csamt\": 0}` with the tax heads as literal zeros. Running compute_gstr3b on one zero-rated sale of 10_000_00p carrying 1_800_00p IGST gives outward_zero_rated=1000000, outward_taxable_igst=0, osup_zero={'txval': 10000, 'iamt': 0, ...} and net_igst=0, exactly as claimed. The case is reachable: supply_type is a free field on the invoice (routers/sales_invoices.py:844/884 \u2014 `data.get(\"supply_type\") or \"taxable\"`), tax is computed per line from the rate by `_compute_line_gst` (routers/sales_invoices.py:723) with no coupling to supply_type, so nothing forces IGST to zero on a zero-rated invoice; services/gst_return_service.py:626-641 passes that supply_type and igst_paise straight into SalesTransaction. GSTR-1 handles the same invoice correctly \u2014 domain/gst/classifier.py:110-112 maps SEZ_with_payment to EXP_WP and gstr1_builder.py:535 emits exp_typ \"WPAY\" with the IGST in its items \u2014 so the two returns disagree by exactly the export IGST. GUARDS searched and found: (1) services/gst_return_service.py:805-833 computes `books_output` from outward_taxable_* + RCM only, while `_gl_gst_movements` (:77-98) reads the real credit on gst_cgst/sgst/igst, so `reconciliation.output_gst.matched` (:947) comes back False by precisely the dropped amount and is surfaced on apps/web/app/clients/[id]/compliance/gst/page.tsx:556 \u2014 detection, not prevention, and it names no cause. (2) `ca_review_required: True` (:958) and nothing auto-files; the portal's own 3.1(b) is auto-populated from GSTR-1 Table 6A, which is right. (3) NO test pins the current zeros as intended \u2014 tests/test_gst_rcm_gstr3b.py:86 and tests/test_gstr3b_payload_matches_gstn_utility.py:204 assert only txval and the key set, and every zero-rated fixture (tests/e2e_gst_verification.py:115, test_gst_rcm_gstr3b.py:70-75) carries igst=0, so the gap is untested rather than sanctioned. No CHECK constraint, trigger, migration, Pydantic validator or frontend guard touches it. It is a known, logged gap, not a design decision: docs/audits/2026-07-02-executive-product-audit.md:608 records \"`osup_zero` hardcodes zero IGST (exports-with-payment unmodelled)\" as out of scope for that milestone.",
   "what_is_actually_true": "The defect is real and unguarded at the point of computation: a zero-rated supply's IGST/cess never reaches GSTR-3B Table 3.1(b), so output tax, `tax_liability_paise` and `net_igst` are all understated for an exporter or SEZ supplier who supplies WITH payment of IGST under \u00a716(3)(b). Two corrections to the finding. (a) Severity is high rather than critical, because a second layer bounds it: the same `gstr3b_from_books` response carries a books-vs-GL reconciliation that fails by exactly the dropped IGST and is rendered on the client GST compliance screen, the return is prepare-only with `ca_review_required`, and the blast radius is limited to the with-payment route \u2014 the classifier deliberately defaults a bare `zero_rated` supply to EXP_WOP (LUT), which is the common case and genuinely carries no IGST. The number filed is still wrong if the CA transcribes it, so this is mitigation, not mootness. (b) The suggested fix is sound and does not need an invoice_type: a without-payment export carries no IGST by construction (gstr1_builder.py:497-501 relies on the same fact to pick EXPWP vs EXPWOP), so accumulating `sign * s.igst_paise` and `sign * s.cess_paise` into new `outward_zero_rated_igst`/`_cess` fields and emitting them in `osup_zero` is correct for both routes. Whoever fixes it must also add those fields to `books_output` in services/gst_return_service.py:805-808, or the fix will convert a real GL mismatch into a silent one; and cess has no cell in `osup_zero`'s csamt today either, which the same change should cover."
  },
  {
   "kind": "single",
   "finding_id": "GST-03",
   "lens": "LITERAL",
   "corrected_severity": "high",
   "refuted": false,
   "verdict_reason": "Every cited line exists and says exactly what the finding claims. apps/web/app/gst/reconciliation/page.tsx:628 renders, unconditionally in the page's main JSX (a blue `bg-blue-50` info banner right under the header at :610), the text \"**CGST Rule 36(4):** ITC is restricted to 105% of eligible credit appearing in GSTR-2A/2B. Reconcile every period before filing GSTR-3B to avoid ITC reversal notices.\" The same claim is repeated in the results-table footer at :894 (\"CGST Act Section 16 \u00b7 Rule 36(4) \u2014 ITC subject to 105% cap of GSTR-2A eligible credit\") and in the two file docstrings at :7 and :217, plus apps/web/app/gst/gstr3b/page.tsx:7. That is five occurrences, exactly as stated; a grep of the whole web tree finds \"105%\"/\"36(4)\" in only those two files. The backend does contradict it at the cited lines: apps/api/domain/gst/gstr3b_computer.py:5-7 (\"Notification 40/2021-Central Tax (w.e.f. 1 Jan 2022) removed the prior 105% provisional buffer; ITC is now capped strictly at 100% (Section 16(2)(aa))\") and :538-542 with `_RULE_36_4_NUMERATOR = 100` / `_RULE_36_4_DENOMINATOR = 100`, used by `_apply_rule_36_4_cap` at :546-558; apps/api/tests/test_gst_engine.py:373-405 pins the 100% behaviour. The law is as the finding states: Rule 36(4)'s 5% provisional buffer ran only to 31-12-2021 and was substituted by Notification 40/2021-CT w.e.f. 01-01-2022 to match \u00a716(2)(aa) \u2014 credit only on invoices communicated in GSTR-2B, no percentage buffer. The one correction is to severity, not to the facts: this is display text only. Nothing in apps/web/app/gst/reconciliation/page.tsx applies a 1.05 multiplier \u2014 grep finds \"105\" nowhere but those four prose lines, `reconcile()` (:225+) is a pure invoice-key match producing matched/mismatch/missing_in_2a/extra_in_2a rows, and the filed return's cap is computed server-side at 100%. So no number the product computes, displays or exports is wrong; what is wrong is a statutory statement made to the CA on the screen where they decide ITC.",
   "what_is_actually_true": "The reconciliation and GSTR-3B screens do state superseded law in five places (reconciliation/page.tsx:7, :217, :628, :894 and gstr3b/page.tsx:7), telling the CA ITC is restricted to 105% of GSTR-2A/2B credit \u2014 a rule dead since 01-01-2022 (Notification 40/2021-CT substituting Rule 36(4), aligned to \u00a716(2)(aa)). The backend is correct and explicitly says so (gstr3b_computer.py:5-7, 538-542, `_RULE_36_4_NUMERATOR = 100`), so the two halves of the product do state different law about the same rule. But the defect is confined to prose: the recon page performs no cap arithmetic (no 1.05 anywhere \u2014 `reconcile()` is pure invoice-level matching), the 105% figure never appears as a number in any total, and the return actually computed and exported for filing is capped at 100%. The harm is therefore misdirection of a CA reading the screen, not a wrong computed or filed figure \u2014 hence high rather than critical. The suggested fix stands: replace all five strings with \u00a716(2)(aa) + Rule 36(4) as substituted, and add a test grepping the web tree for \"105%\" near \"36(4)\"."
  },
  {
   "kind": "single",
   "finding_id": "GST-02",
   "lens": "REACH",
   "refuted": false,
   "verdict_reason": "Confirmed line by line, and reachable. apps/api/domain/gst/gstr3b_computer.py:617-621 \u2014 the `zero_rated` branch accumulates only `outward_zero_rated += sign * s.taxable_amount_paise`; igst/cgst/sgst/cess are never read (the dataclass has no zero-rated tax field at all \u2014 only `outward_zero_rated: int` at :137). apps/api/domain/gst/gstr3b_computer.py:414-417 hardcodes `\"osup_zero\": {\"txval\": r(self.outward_zero_rated), \"iamt\": 0, \"camt\": 0, \"samt\": 0, \"csamt\": 0}`. Reproduced with the repo's own venv: a zero_rated sale of 10_000_00p carrying 1_800_00p IGST gives `outward_zero_rated=1000000, outward_taxable_igst=0`, payload `osup_zero={'txval': 10000, 'iamt': 0, ...}`, `net_igst=0`. Nothing in the file documents this as a deliberate refusal, and `invoice_type` (which carries `SEZ_with_payment`) is not even a field on `SalesTransaction` (:20-38), so an explicitly with-payment supply loses its IGST too. REACH: (a) the data really can carry it \u2014 `_compute_line_gst` (routers/sales_invoices.py:168-183) computes IGST from the line rate with no reference to supply_type, and routers/sales_invoices.py:844 stores supply_type independently, so an export invoice with 18% inter-state lines is `supply_type='zero_rated'` + `igst_paise>0`; the frontend offers both controls (apps/web/lib/invoices/classification.ts:51 \"Zero-rated\", :59 \"SEZ with payment (IGST)\"). (b) the GSTR-1 side already handles this route (domain/gst/gstr1_builder.py:497-501, :532-536 EXP WPAY), so 3B contradicts 1 for the same invoice. (c) mounted and consumed: `app.include_router(gst.router)` main.py:265 and `gst_workspace` :321; services/gst_return_service.py:795-806 builds `books_output` from `outward_taxable_*` only, so the export IGST that IS on the GL creates a permanent, unexplained books-vs-ledger mismatch exactly as the finding says; apps/web/app/gst/gstr3b/page.tsx:170 downloads `result.payload` as the GSTN JSON the CA uploads. No validator catches it (domain/gst/validator.py:204-232 only warns when ITC > 3x output). No test pins the with-payment case \u2014 tests/test_gst_rcm_gstr3b.py:73-86 and e2e_gst_verification.py:456 only assert the taxable value.",
   "corrected_severity": "high",
   "what_is_actually_true": "The bug is exactly as described: IGST (and cess) on a `zero_rated` supply is dropped from Table 3.1(b), from the outward-tax totals and from the net-payable computation, and the downloaded GSTN JSON emits `osup_zero.iamt = 0`. It is not dead code \u2014 a real invoice screen can produce the input and a real screen downloads the wrong payload. Two things justify high rather than critical: (1) the dominant export route in practice, LUT/bond without payment, has no IGST and is computed correctly, so only export/SEZ-with-payment clients are affected; (2) nothing auto-files \u2014 the CA uploads manually, and `gstr3b_from_books` already returns `output_matched=False` with a `difference_paise` for exactly this case, so a signal exists even though it is unexplained. One correction to the suggested fix: GSTR-3B Table 3.1(b) statutorily has only integrated-tax and cess columns (a zero-rated supply is always inter-state under IGST \u00a77(5)), so the fix should populate `iamt`/`csamt` and leave `camt`/`samt` at 0; and it should also consider `invoice_type == \"SEZ_with_payment\"`, which `SalesTransaction` does not currently carry."
  },
  {
   "kind": "single",
   "finding_id": "GST-03",
   "lens": "GUARDS",
   "refuted": false,
   "verdict_reason": "Confirmed at the text level, but the evidence and severity both need correcting. I read the cited lines myself. apps/web/app/gst/reconciliation/page.tsx:628 does render a live blue Info banner reading \"CGST Rule 36(4): ITC is restricted to 105% of eligible credit appearing in GSTR-2A/2B. Reconcile every period before filing GSTR-3B...\", and :894 renders a table footer \"CGST Act Section 16 \u00b7 Rule 36(4) \u2014 ITC subject to 105% cap of GSTR-2A eligible credit\". The backend does contradict it: apps/api/domain/gst/gstr3b_computer.py:538-541 sets `_RULE_36_4_NUMERATOR = 100` with the comment \"Notification 40/2021-Central Tax (w.e.f. 1 Jan 2022) withdrew the prior 105% provisional buffer\", and _apply_rule_36_4_cap (:545-559) caps at 100%. HOWEVER: (1) three of the five cited occurrences are NOT user-visible \u2014 reconciliation/page.tsx:7 and :217 and gstr3b/page.tsx:7 are JSDoc source comments, so exactly TWO strings reach a CA's screen, both on the reconciliation page; the GSTR-3B screen shows no 105% text at all (grep for cap/36_4/rule_36 in apps/web/app/gst/gstr3b/page.tsx returns nothing). (2) No computation anywhere uses 105%: grep for \"1.05|105\" in reconciliation/page.tsx returns only those four comment/copy lines, and the page's reconcile() (:225+) is a pure browser-side supplier_gstin+invoice_number matcher over uploaded XLSX. The page's only network call is getClients() at :527 \u2014 it POSTs nothing, writes no table, and does not feed the return. GUARDS: mixed. The engine is pinned \u2014 tests/test_gst_engine.py:373-405 asserts the 100% cap; I ran it from apps/api (`pytest tests/test_gst_engine.py -k \"36_4 or rule_36\"`): 3 passed. So the GSTR-3B a CA actually files through this product is capped at 100%, not 105%, whatever the recon banner says. But there is NO guard on the copy itself: none of the ~32 tests in apps/web/scripts greps for \"36(4)\" or \"105\", there is no migration/constraint/flag in play (this is static JSX), and the page is reachable \u2014 apps/web/app/gst/page.tsx:809 links to /gst/reconciliation from the GST hub. So the wrong statement is live and unguarded, but it is advisory prose beside a correct engine, not a wrong figure in a return.",
   "corrected_severity": "medium",
   "what_is_actually_true": "Two live on-screen strings on the GSTR-2A reconciliation page (apps/web/app/gst/reconciliation/page.tsx:628 banner and :894 table footer) state superseded law \u2014 the 5% provisional buffer under the pre-2022 Rule 36(4), which Notification 40/2021-Central Tax substituted w.e.f. 01-01-2022 (credit only on invoices communicated in GSTR-2B, no percentage buffer, with \u00a716(2)(aa)). The other three cited occurrences (reconciliation/page.tsx:7, :217, gstr3b/page.tsx:7) are source comments no CA ever sees, so the fix is two rendered strings plus three comments, not \"five occurrences\" of equal weight. Nothing computes 105%: the recon page applies no cap at all (client-side XLSX invoice matcher, no API write), and the backend caps at exactly 100% (gstr3b_computer.py:541 `_RULE_36_4_NUMERATOR = 100`), pinned by tests/test_gst_engine.py:373-405 which passes. So the return the product produces is correct and the defect cannot turn into a wrong filed figure through this software \u2014 the harm requires a CA to act on the banner manually on gst.gov.in against their own product's computed return. That bounds it to a wrong-statutory-guidance/copy defect rather than a computation bug: real, unguarded (no web test greps the copy; the page is linked from apps/web/app/gst/page.tsx:809), worth fixing in an hour, but not critical. The suggested fix and effort are otherwise right, and a copy-lint test would be the first guard of its kind for this string."
  },
  {
   "kind": "single",
   "finding_id": "GST-03",
   "lens": "REACH",
   "refuted": false,
   "verdict_reason": "Every cited line exists verbatim. apps/web/app/gst/reconciliation/page.tsx:628 renders a blue Info banner \"CGST Rule 36(4): ITC is restricted to 105% of eligible credit appearing in GSTR-2A/2B.\"; line 894 renders a table footnote \"CGST Act Section 16 \u00b7 Rule 36(4) \u2014 ITC subject to 105% cap of GSTR-2A eligible credit\"; lines 7 and 217 of the same file and line 7 of apps/web/app/gst/gstr3b/page.tsx carry the same wording. The backend contradicts it explicitly: apps/api/domain/gst/gstr3b_computer.py:538-541 (\"Notification 40/2021-Central Tax (w.e.f. 1 Jan 2022) withdrew the prior 105% provisional buffer\") with `_RULE_36_4_NUMERATOR = 100`, and the docstring at 545-551 repeats it; tests/test_gst_engine.py:376-377 pins the 100% behaviour. REACH is real: /gst is in the nav (apps/web/lib/workspace/workspaceConfig.ts:165, components/panels/DeadlinesPanel.tsx:31), /gst/reconciliation is linked from apps/web/app/gst/page.tsx:809 and present in lib/workspace/knownRoutes.generated.ts:83; no feature flag gates it. Two corrections to the finding's framing, both severity-relevant. (1) Only 2 of the 5 occurrences are user-visible \u2014 lines 7 and 217 of the recon page and line 7 of the gstr3b page are file-header/function comments that never render; the gstr3b page renders no 105 at all (grep \"105\" on that file returns only line 7). (2) Nothing in the frontend COMPUTES at 105%: grep for `105`/`1.05` in reconciliation/page.tsx hits only those four prose strings, the reconcile() engine at line ~225 does per-invoice matching (matched / mismatch / missing_in_2a / extra_in_2a), and the ITC figure a CA actually files comes from the backend, which caps at 100% (services/gst_return_service.py:781-790 feeds real 2A rows into `_apply_rule_36_4_cap` and returns a `rule_36_4` block at line 910). So no wrong rupee figure ever reaches a return; the defect is a superseded statement of law displayed on screen, contradicted by the very screen it sits on. That makes it a genuine, reachable correctness-of-content bug but not \"critical\" \u2014 I downgrade to medium.",
   "corrected_severity": "medium",
   "what_is_actually_true": "The 105% language is genuinely present and genuinely superseded (Rule 36(4) as substituted by Notification 40/2021-CT, w.e.f. 01-01-2022, with \u00a716(2)(aa): credit only on invoices communicated in GSTR-2B, no percentage buffer), and it is displayed to a CA on a nav-reachable, unflagged screen at apps/web/app/gst/reconciliation/page.tsx:628 and :894. But it is prose only \u2014 three of the five cited occurrences are non-rendering code comments (recon page 7 and 217, gstr3b page 7), and no frontend or backend code multiplies by 1.05. The computation is correct throughout: `_RULE_36_4_NUMERATOR = 100` in apps/api/domain/gst/gstr3b_computer.py:541, wired into the filed return by services/gst_return_service.py:781-790 and pinned by tests/test_gst_engine.py:376-377. So the product states one law and applies another; the risk is a CA misled into manually overriding a correctly computed figure, not a wrong number produced by the software. Fix is the text edit the finding proposes (2 rendered strings, 3 comments) plus a guard test; effort \"hours\" is right."
  },
  {
   "kind": "single",
   "finding_id": "GST-05",
   "lens": "LITERAL",
   "refuted": false,
   "verdict_reason": "Confirmed by reading and executing the code. The set-off block is at apps/api/domain/gst/gstr3b_computer.py:729-743 (the finding cites 714-728 \u2014 a ~15-line offset; the substance is exactly as described). Line 731 computes `igst_after_itc = outward_taxable_igst - avail_igst`; when it is positive (IGST liability remains) lines 741-743 write `net_igst = igst_after_itc`, `net_cgst = max(0, outward_cgst - avail_cgst)`, `net_sgst = max(0, outward_sgst - avail_sgst)`. There is no branch anywhere that applies leftover CGST or SGST credit to an IGST liability; the only cross-utilisation is the IGST-excess split at 735-739. I ran the exact scenario (interstate sale of 10,00,000p @18% -> outward IGST 180000p; intra-state purchase generating CGST 99,999p + SGST 99,999p): output was `net igst 180000 net cgst 0 net sgst 0` with `itc cgst 99999 sgst 99999` untouched \u2014 cash demanded where nil is payable. Nothing downstream corrects it: services/gst_return_service.py:923-930 emits `result.net_*` verbatim as `net_payable`, routers/gst.py:324-327 the same, and services/report_transactions_service.py:248-254 re-reads that dict. The second half of the finding also holds: `itc_consumed_paise` (gstr3b_computer.py:344-349) is a residual of `owed - still_payable`, so in the repro consumed=0 and `itc_carried_forward_paise` (358-362) = 199,998 \u2014 the frontend tile at apps/web/app/gst/gstr3b/page.tsx:439-451 then shows the stranded credit as \"Carried forward\" beside a non-zero cash demand, both wrong together. On the law: \u00a749(5)(b) lets CGST credit remaining after CGST be used towards IGST, \u00a749(5)(c) the same for SGST (subject to its proviso that CGST credit be exhausted first), \u00a749(5)(e)/(f) bar CGST<->SGST, and \u00a749A/Rule 88A only require IGST credit to be used first \u2014 which is vacuous here since IGST credit is nil. So nil payable is the correct answer and the code's 180000 is wrong. Test coverage: tests/test_gst_engine.py:319-331 covers only the IGST-excess direction; no test in tests/ exercises CGST/SGST credit against an IGST liability. Note the latent contradiction in tests/test_gstr3b_itc_carried_forward.py:115-124, whose docstring asserts the very invariant this bug breaks (\"Credit left over AND tax to pay cannot both be true... If this ever fails, the set-off stopped short\") but whose fixture is IGST-only, so it passes.",
   "corrected_severity": "critical",
   "what_is_actually_true": "The finding is accurate. `compute_gstr3b` implements only one direction of \u00a749(5) cross-utilisation: IGST credit -> IGST, then excess split 50/50 to CGST and SGST (gstr3b_computer.py:733-739). CGST credit is only ever netted against CGST and SGST credit only against SGST (741-743), so \u00a749(5)(b) and (c) \u2014 remaining CGST/SGST credit applied to IGST \u2014 are not implemented at all. Reproduced live: outward IGST 180000p with CGST 99999p + SGST 99999p credit yields net_igst=180000 (correct: nil), and the derived `itc_carried_forward_paise` simultaneously reports 199998p carried forward, so the Table 6 figure and the utilisation tile are wrong together. The bug is on the live from-books path (routers/gst.py:288 and :371, routers/gst_workspace.py:1419/1471, services/gst_return_service.py:795) and affects any taxpayer buying intra-state and selling inter-state. Only correction to the finding: the cited line range 714-728 is stale \u2014 the code is at 729-743, and the frontend tile is at page.tsx:439-451. Severity stays critical: it overstates cash payable on a return a CA files from."
  },
  {
   "kind": "single",
   "finding_id": "GST-05",
   "lens": "GUARDS",
   "refuted": false,
   "verdict_reason": "CONFIRMED at the cited lines, and reproduced. apps/api/domain/gst/gstr3b_computer.py:729-743 is the whole of the set-off: `avail_igst/cgst/sgst = itc_net_*`, `igst_after_itc = outward_taxable_igst - avail_igst`, and the only cross-head move is the `igst_after_itc <= 0` branch spilling surplus IGST credit 50/50 onto CGST and SGST (738-739). The else branch (741-743) is a pure per-head subtraction \u2014 CGST credit touches only CGST, SGST only SGST \u2014 so a positive IGST residual is never reduced by CGST/SGST credit. Ran the auditor's case: sale 10,00,000p taxable / IGST 1,80,000p, purchase with CGST 1,00,000p + SGST 1,00,000p (2A matched) -> net_igst=180000, net_cgst=0, net_sgst=0, itc_carried_forward_paise=200000. Under a cross-utilised set-off it is nil payable with 20,000p carried forward, so the \"Carried forward\" tile (apps/web/app/gst/gstr3b/page.tsx:438-451, fed from working.itc_utilisation, gstr3b_computer.py:358-362) is wrong in the same breath \u2014 cf is derived from net_* by design (gstr3b_computer.py:345-352), so the two cannot disagree, they are wrong together.\n\nGUARDS lens \u2014 nothing catches it. (a) Tests: the only cross-utilisation tests are the IGST-excess direction \u2014 tests/test_gst_engine.py:318-331 and tests/test_gstr3b_table4_reversals.py:306-317; the auditor's \"no test covers this direction\" is accurate. tests/test_report_transactions_service.py:335-346 asserts only that net_* is the return's own figure and >= 0, which the buggy value satisfies. (b) There is exactly ONE set-off implementation: services/report_transactions_service.py:170-260 explicitly delegates to gst_return_service, and services/filing_demo/gstr3b.py:284-292 says \"nothing is re-computed here, the saved net_payable IS that set-off's answer\" \u2014 so the demo Table 6.1 inherits the same wrong cash column. (c) No DB guard: migrations/036_gst_engine.sql:100-103 just stores net_*_paise as BIGINT DEFAULT 0, no CHECK. (d) No exception report: services/gst_exception_service.py only has gstr1_exceptions. (e) Frontend derives nothing (page.tsx:421 note says only \"IGST ITC cross-utilised against CGST/SGST if excess\" \u2014 it documents the one-directional behaviour rather than compensating for it). (f) No feature flag or scheduler bounds it.\n\nWHAT BOUNDS IT (why I cut severity one notch, not why it is refuted): the wrong number never reaches GSTN. as_gstn_payload (gstr3b_computer.py:398-540) emits sup_details and itc_elg (itc_net_* at 500-505) and NO Table 6 / tx_pmt payment block at all, so the prepared JSON and any real filing are unaffected \u2014 the portal computes its own 6.1 set-off from the taxpayer's preference. And \u00a749(5)(b)/(c) word the onward leg as \"may be utilised towards payment of integrated tax\", so what the engine produces is a legally PERMISSIBLE set-off, just one no taxpayer would choose and not what the portal suggests; the unused credit is not destroyed (it carries forward) and cash overpaid sits in the electronic cash ledger. So the harm is a materially overstated cash-payable shown to the CA on the GSTR-3B screen, the reports GST summary and the filing-demo payment stage \u2014 advice and working capital, not a wrong filed return or lost credit.",
   "corrected_severity": "high",
   "what_is_actually_true": "The mechanism, the arithmetic and the \"no test covers it\" claim are all correct: gstr3b_computer.py implements only \u00a749(5)(a)/Rule 88A (IGST credit spilling to CGST/SGST) and never the \u00a749(5)(b)/(c) onward leg (CGST balance -> IGST, SGST balance -> IGST). Any client who buys intra-state and sells inter-state is shown IGST cash payable while CGST/SGST credit sits idle, and the carried-forward tile repeats the error because it is derived from the same net_* figures. There is exactly one set-off implementation and no test, constraint, migration, exception report or frontend check bounds it.\n\nTwo corrections to the finding's framing. First, \"requires\" overstates the statute: \u00a749(5)(b) and (c) say the remaining balance \"may\" be utilised towards integrated tax, so the engine's output is a permissible \u2014 if commercially absurd, and never the portal's suggested \u2014 set-off, not an illegal one. Second, the ca_experience should not imply a wrong filing: as_gstn_payload emits no Table 6/payment block (gstr3b_computer.py:398-540), so nothing wrong is transmitted and the portal recomputes 6.1 itself; the damage is the headline cash figure the product tells the CA to pay by challan, the carried-forward balance, the reports summary and the filing-demo payment stage. Credit is not lost and overpaid cash is recoverable from the electronic cash ledger, which is why this is high rather than critical. The suggested fix and effort stand."
  },
  {
   "kind": "single",
   "finding_id": "GST-05",
   "lens": "REACH",
   "refuted": false,
   "verdict_reason": "Confirmed by reading and executing the code. apps/api/domain/gst/gstr3b_computer.py:729-743 is the whole Table 6 set-off: `igst_after_itc = outward_taxable_igst - avail_igst`; when it is positive (IGST liability exceeds IGST credit) the else-branch at 741-743 sets `net_igst = igst_after_itc` and computes CGST/SGST purely per-head (`max(0, outward_taxable_cgst - avail_cgst)`). There is no path in which `avail_cgst`/`avail_sgst` ever touch `net_igst`. Only the IGST-surplus direction is implemented (733-739, 50/50 spill). Reproduced the auditor's numbers exactly: SalesTransaction(igst=180000, interstate) + PurchaseTransaction(cgst=100000, sgst=100000) gives net_igst=180000, net_cgst=0, net_sgst=0 with itc_net_cgst/sgst = 100000 each unused, and itc_carried_forward_paise (gstr3b_computer.py:357-362) = 200000, so the \"Carried forward\" tile at apps/web/app/gst/gstr3b/page.tsx:439-451 reports the stranded credit at the same time the payable tile asks for cash \u2014 both halves wrong together, as claimed. Test coverage claim also holds: tests/test_gst_engine.py:319-330 and tests/test_gstr3b_table4_reversals.py:306-318 both test only the IGST-surplus spill; grep for is_interstate=True in the 3B test files shows no CGST/SGST-credit-against-IGST-liability case anywhere. REACH is real, not dead code: routers/gst.py (main.py:265) and routers/gst_workspace.py (main.py:321) are mounted; gst_return_service.py:927-930 saves these figures onto the gstr3b_returns row; services/report_transactions_service.py:248-254 deliberately delegates to this single computation and its comment claims it is \"the \u00a749(5) set-off ... cross-utilised\"; services/filing_demo/gstr3b.py:290-321 renders the cash column from the same saved net_payable while its note recites \u00a749(5)(a)/Rule 88A; apps/web/app/gst/gstr3b/page.tsx is linked from apps/web/app/gst/page.tsx and apps/web/app/clients/[id]/compliance/gst/page.tsx. Two corrections to the finding's framing: (1) \u00a749(5)(b)/(c) say the remaining CGST/SGST credit \"may be utilised\" towards IGST \u2014 permissive, not mandatory \u2014 so this is a failure to perform the utilisation every taxpayer and the GSTN 6.1 screen performs, not a breach of a \"shall\"; (2) severity is high rather than critical because the GST net_payable is never posted to the GL (the net_payable_paise hits elsewhere in services/ are vendor-bill fields, unrelated), the product is prepare-only so the portal recomputes 6.1 at actual filing, and the credit is not lost \u2014 it shows as carried forward and survives in the credit ledger. The harm is an overstated cash/challan figure on the workspace, reports and demo screens, i.e. real money paid early plus a working figure that will not tie to the portal.",
   "corrected_severity": "high",
   "what_is_actually_true": "The set-off in domain/gst/gstr3b_computer.py:729-743 implements only one of the four \u00a749(5) directions: IGST credit against IGST, with the surplus split 50/50 over CGST and SGST. CGST credit is set off only against CGST and SGST credit only against SGST; neither is ever applied to a remaining IGST liability. So a client who buys intra-state and sells inter-state is shown IGST cash payable while CGST and SGST credit sits idle \u2014 verified live: IGST liability 1,80,000p with 1,00,000p CGST and 1,00,000p SGST credit returns net_igst=180000 and carried_forward=200000, where nil is payable. No test exercises that direction. The path reaches users (routers gst and gst_workspace mounted in main.py; figures saved to gstr3b_returns and rendered on /gst/gstr3b, the client compliance GST page, /reports and the filing-demo 6.1 screen). Downgraded from critical to high: nothing is auto-filed, the GST net_payable never posts to the ledger, the portal performs its own 6.1 set-off at filing, and the unused credit is carried forward rather than lost \u2014 the defect overstates cash to be paid by challan and makes the app's working disagree with the portal. The suggested fix (full ladder: IGST\u2192IGST then CGST/SGST per Rule 88A; CGST\u2192CGST then IGST; SGST\u2192SGST then IGST; never CGST\u2194SGST under \u00a749(5)(e)/(f)) is correct, noting \u00a749(5)(b)/(c) are permissive (\"may be utilised\"), which is why the portal offers a set-off preference rather than forcing one."
  },
  {
   "kind": "single",
   "finding_id": "GST-06",
   "lens": "LITERAL",
   "refuted": false,
   "corrected_severity": "medium",
   "verdict_reason": "Code confirms the claim, with only line-number drift. apps/api/domain/gst/gstr3b_computer.py:617-621 is an if/elif chain over supply_type covering nil_rated|exempt, zero_rated and taxable with no else \u2014 `non_gst` falls through to nothing, and GSTR3BResult has no outward_non_gst attribute at all. apps/api/domain/gst/gstr3b_computer.py:428 hardcodes \"osup_nongst\": {\"txval\": 0} inside sup_details. Ran the computer directly: a 50_000_00p non_gst sale yields outward_nil_exempt=0, outward_zero_rated=0, outward_taxable_value=0 and as_gstn_payload emits osup_nongst {'txval': 0}. The value is reachable, not theoretical: migrations 036/268/270 all permit 'non_gst' in the supply_type CHECK, apps/web/lib/invoices/classification.ts:54 offers it in the UI as \"Non-GST \u2026 petrol, alcohol\", and services/gst_return_service.py:633 passes the column straight through. The GSTR-1 side does declare it \u2014 domain/gst/gstr1_builder.py:386-393 (_NIL_AMOUNT_FIELD maps non_gst\u2192ngsup_amt at :392), fed into Table 8 at :435 \u2014 so the two returns disagree on the same invoice, as claimed. No test pins the zero (tests/test_gstr3b_payload_matches_gstn_utility.py:205 asserts only the key set), and services/filing_demo/gstr3b.py:190 independently records \"(e) non-GST outward supplies is not tracked upstream\", i.e. the gap is already known in the demo layer. The finding's cited lines are wrong (617-621 not 663-671; 428 not 424; gstr1_builder 386-393 not 379-383) and the working tree is clean, so that is auditor imprecision, not a different code version. Law: GSTR-3B Table 3.1(e) is \"Non-GST outward supplies\" and petrol/diesel/alcohol/electricity turnover belongs there \u2014 the finding states the form correctly.",
   "what_is_actually_true": "The defect is real and reachable. A sales invoice classified `non_gst` is silently dropped by compute_gstr3b: it is added to no Table 3.1 accumulator, and the GSTN payload always emits osup_nongst.txval = 0 \u2014 an affirmative nil rather than a missing figure, which is a false declaration when the books hold non-GST turnover. The same invoice IS declared in GSTR-1 Table 8 (ngsup_amt), so 1-vs-3B turnover will not tie and GSTR-9 will not reconcile. Two corrections to the finding's framing: (1) the cited line numbers are off \u2014 real locations are gstr3b_computer.py:617-621 and :428, gstr1_builder.py:386-393; (2) impact is disclosure-only: non-GST supplies bear no tax, so no output liability, no ITC and no cash figure is misstated, and the GL-vs-return reconciliation in gst_return_service compares tax heads, so it cannot catch this either. Severity lowered from high to medium on that basis \u2014 a wrong statutory disclosure line that will draw a portal turnover-comparison mismatch for petrol-pump/liquor/ancillary-non-GST clients, not a misstatement of tax payable. The suggested fix (an outward_non_gst accumulator emitted in osup_nongst.txval, plus a GSTR-1 Table 8 \u2194 3B 3.1(e) parity test) is the right shape."
  },
  {
   "kind": "single",
   "finding_id": "GST-06",
   "lens": "GUARDS",
   "findings": "[]",
   "refuted": false,
   "verdict_reason": "Confirmed by reading the code and running it. apps/api/domain/gst/gstr3b_computer.py:617-621 is an if/elif chain over supply_type covering only (\"nil_rated\",\"exempt\") \u2192 outward_nil_exempt, \"zero_rated\" \u2192 outward_zero_rated, \"taxable\" \u2192 the 3.1(a) heads; `non_gst` matches nothing and there is no `else`, so the value is silently discarded. There is no non-GST accumulator on GSTR3BResult at all (only outward_nil_exempt at :138), and the payload hardcodes `\"osup_nongst\": {\"txval\": 0}` at :428 \u2014 an affirmative \"nil\" for Table 3.1(e), not an omission. Reproduced with the repo venv: one 50_000_00p `non_gst` sale gives outward_nil_exempt=0, outward_taxable_value=0, outward_zero_rated=0 and sup_details.osup_nongst {'txval': 0}. `non_gst` genuinely reaches the computer: services/gst_return_service.py:633 passes `inv.get(\"supply_type\") or \"taxable\"` straight through, the column CHECKs allow it (migrations/036_gst_engine.sql:11, 268_sales_invoice_gst_classification.sql:38, 270), and apps/web/lib/invoices/classification.ts:54 offers \"Non-GST\" as a pickable option. The GSTR-1 side does handle it (domain/gst/gstr1_builder.py:392 `\"non_gst\": \"ngsup_amt\"`), so the two returns do disagree on the same invoice. GUARDS lens found nothing: `grep -rn osup_nongst` over apps/api and apps/web returns exactly two hits \u2014 the hardcoded 0 and tests/test_gstr3b_payload_matches_gstn_utility.py:205, which only asserts the KEY exists in sup_details, never its value; no test in tests/ exercises supply_type=\"non_gst\" through compute_gstr3b (the only non_gst tests are GSTR-1 ones, tests/test_gstr1_classification_from_invoice.py:107,401); the GL reconciliation in gstr3b_from_books compares tax movements, and a non-GST supply carries no tax, so it cannot flag the gap; the working block and the screen (services/gst_return_service.py:862, apps/web/app/gst/gstr3b/page.tsx:327) expose only nil_exempt_paise, so a CA sees no non-GST row to notice missing; domain/gst/exception_report.py has no non_gst logic; the detail drill-down covers only (\"3.1a\",\"4A\",\"4B1\",\"4B2\") (gst_return_service.py:443).",
   "corrected_severity": "high",
   "what_is_actually_true": "The finding is accurate as written, including the cited lines and the reproduction. `non_gst` outward supplies fall out of the GSTR-3B if/elif chain with no accumulator, and Table 3.1(e) is emitted as a hardcoded zero, while the same invoice is declared in GSTR-1 Table 8 `ngsup_amt`. No test, constraint, trigger, reconciliation, exception report, flag or frontend check catches it anywhere \u2014 the only thing near it (test_gstr3b_payload_matches_gstn_utility.py:205) asserts the key's presence, not its value, so it would pass unchanged with the bug fixed or unfixed. Two things bound the blast radius without excusing it: (1) no tax is understated \u2014 a non-GST supply carries no GST, so 3.1(a)/(c) tax heads, the ITC tables and net payable are unaffected; the defect is a turnover DISCLOSURE that reads as an affirmative nil, and the GSTR-1-vs-3B turnover comparison on the portal and GSTR-9 tie-up are where it surfaces; and (2) it only bites clients who actually classify a line as `non_gst` (petrol/diesel/alcohol/electricity), and the platform is prepare-only \u2014 the CA transcribes onto the portal \u2014 though the screen shows no non-GST row at all, so there is nothing prompting them to correct it. Severity high stands: an unguarded wrong figure in a statutory payload, with the sibling return disagreeing on the same invoice."
  },
  {
   "kind": "single",
   "finding_id": "GST-06",
   "lens": "REACH",
   "findings": "[]",
   "refuted": false,
   "corrected_severity": "medium",
   "verdict_reason": "Confirmed, though the cited line numbers are off by a few. apps/api/domain/gst/gstr3b_computer.py:617-621 is the supply-type branch: `nil_rated`/`exempt` \u2192 outward_nil_exempt, `zero_rated` \u2192 outward_zero_rated, `taxable` \u2192 outward_taxable_*. `non_gst` matches none and falls out of the loop with no accumulator \u2014 GSTR3BResult has no `outward_non_gst` field at all (dir() of the result object confirms it). Line 428 hardcodes `\"osup_nongst\": {\"txval\": 0}` in as_gstn_payload. Reproduced: a single 50_000_00p `non_gst` sale gives outward_nil_exempt=0, outward_taxable_value=0, outward_zero_rated=0 \u2014 the turnover vanishes. GSTR-1 does handle it: domain/gst/gstr1_builder.py:389-393 maps `\"non_gst\": \"ngsup_amt\"` into Table 8, and domain/gst/classifier.py:91 routes it to NIL_EXEMPT. So the same invoice is declared in GSTR-1 Table 8 and omitted from GSTR-3B. REACH is real and not dead code: `non_gst` is a CHECK-constrained column value (migration 268:38, 036:11), a selectable option in the invoice UI (apps/web/lib/invoices/classification.ts:54 \"Non-GST \u2014 Outside GST altogether (e.g. petrol, alcohol)\", rendered by components/invoices/InvoiceEditor.tsx:746-752), carried into the 3B computation verbatim by services/gst_return_service.py:632 (`supply_type=inv.get(\"supply_type\") or \"taxable\"`), and routers gst / gst_workspace are both mounted (main.py:265, 321) with the working rendered at apps/web/app/gst/gstr3b/page.tsx:321-327 \u2014 which shows rows (a)-(d) and no (e). services/filing_demo/gstr3b.py:190 states \"(e) non-GST outward supplies is not tracked upstream\", which is itself stale: it IS tracked upstream, in the same column GSTR-1 reads.",
   "what_is_actually_true": "A sale classified `non_gst` in the invoice editor is accumulated nowhere in compute_gstr3b and `osup_nongst.txval` is a hardcoded 0, so GSTR-3B Table 3.1(e) is always nil while GSTR-1 Table 8 `ngsup_amt` declares the same turnover \u2014 the two returns disagree on the same invoice and the portal's GSTR-1-vs-3B turnover comparison will flag it. Severity corrected high \u2192 medium, not high: 3.1(e) is a disclosure line only. Non-GST supplies bear no output tax and no ITC, so no tax, liability, set-off or ITC figure is wrong \u2014 nothing is under-paid; nil-rated and exempt supplies are still correctly reported in 3.1(c). The defect needs a client with actual non-GST turnover (petrol/diesel/alcohol/electricity) and is a wrong disclosure plus a reconciliation mismatch rather than a wrong tax computation. The suggested fix is right in shape: add an `outward_non_gst` accumulator, emit it in `osup_nongst.txval`, surface it in the `working.outward` dict (services/gst_return_service.py:862 area) and the 3.1(e) row in both apps/web/app/gst/gstr3b/page.tsx and services/filing_demo/gstr3b.py (whose \"not tracked upstream\" comment must be corrected at the same time), with a GSTR-1 Table 8 \u2194 3B 3.1(e) parity test."
  },
  {
   "kind": "single",
   "finding_id": "GST-07",
   "lens": "LITERAL",
   "refuted": false,
   "corrected_severity": "high",
   "verdict_reason": "Confirmed by reading the code and by executing the real builder. (1) apps/api/domain/gst/classifier.py:100-115 \u2014 is_export is true when invoice_type in (\"SEZ_with_payment\",\"SEZ_without_payment\",\"Deemed_export\"), returning EXP_WP (:113) / EXP_WOP (:111,:115); nothing routes SEZ or deemed exports to B2B. (2) TransactionForClassification (classifier.py:60-79) carries no igst/tax field, so the comment at :114 \"assume no IGST payment (LUT)\" is the whole story \u2014 a paid-IGST export cannot be detected. (3) apps/api/domain/gst/gstr1_builder.py:122-123 merges EXP_WP+EXP_WOP into one exp section; :531-556 _build_exp sets exp_typ from the category only (:535) and hard-codes \"sbpcode\":\"\", \"sbnum\":\"\", \"sbdt\":\"\" (:546-548), with no ctin. (4) _build_b2b (gstr1_builder.py:213-236) emits \"inv_typ\": inv.invoice_type if != \"Regular\" else \"R\" (:232) \u2014 the raw literal, not SEWP/SEWOP/DE (unreachable today for SEZ, exactly as the finding words it). (5) Executed classify_transaction -> build_gstr1: a Deemed_export invoice to 27AAAAA0000A1Z5 with 1_800_00p IGST produced exp:[{exp_typ:\"WOPAY\", inv:[{inum:\"INV1\", val:11800.0, sbpcode:\"\", sbnum:\"\", sbdt:\"\"}]}] with no ctin and no b2b key; SEZ_with_payment produced WPAY in exp, also no ctin; a zero_rated pos=96 invoice carrying 1_800_00p IGST produced WOPAY. (6) Live, not theoretical: services/gst_return_service.py:1019-1070 reads r.get(\"invoice_type\") off client_sales_invoices (migration 268 adds exactly those four values) and apps/web/lib/invoices/classification.ts:59-61 offers SEZ/Deemed export in the UI. (7) No shipping-bill data exists anywhere: grep for shipping_bill/sbnum across apps hits only the two hard-coded empty-string sites (gstr1_builder.py:546-548, amendments.py:141) and tests that merely assert the keys are present. On the law/schema: GSTR-1 Table 6A is exports, 6B SEZ supplies, 6C deemed exports, and in the GSTN JSON 6B/6C ride in the b2b section under inv_typ SEWP/SEWOP/DE with the recipient's ctin, while exp_typ WPAY/WOPAY turns on whether IGST was charged \u2014 as the finding states. I am not certain sbnum/sbdt are schema-mandatory on every 6A row (services exports are commonly filed blank), so that sub-claim should read \"needed for the ICEGATE/GSTN refund match\" rather than \"schema-rejected\".",
   "what_is_actually_true": "The finding is accurate as written. SEZ_with_payment, SEZ_without_payment and Deemed_export all classify as EXP_WP/EXP_WOP (classifier.py:103,110-115) and are emitted in the exp section by _build_exp (gstr1_builder.py:531-556), which carries no ctin and always-empty sbpcode/sbnum/sbdt \u2014 so the recipient GSTIN is lost and the supply never reaches the recipient's GSTR-2B or a \u00a754 deemed-export refund match. Because TransactionForClassification holds no tax amounts, a genuine export with payment of IGST is unconditionally declared WOPAY (classifier.py:114-115); reproduced by running the builder. The b2b inv_typ literal (gstr1_builder.py:232) is a latent second defect, correctly flagged as conditional. Only corrections: cosmetic line drift (_build_exp is 531-556, not 527-553; the inv_typ line is 232, not 219), and one softening \u2014 sbnum/sbdt are required for the refund cross-check rather than provably rejected by the GSTN schema. Severity high stands: the product only prepares the JSON, but a CA uploading it files a wrong return and loses the refund/ITC match."
  },
  {
   "kind": "single",
   "finding_id": "GST-07",
   "lens": "GUARDS",
   "findings": "[]",
   "refuted": false,
   "corrected_severity": "high",
   "verdict_reason": "Every element of the claim reproduces. (1) apps/api/domain/gst/classifier.py:100-115 \u2014 `is_export` includes `invoice_type in (\"SEZ_with_payment\",\"SEZ_without_payment\",\"Deemed_export\")`, and the branch returns only EXP_WP/EXP_WOP; the final `return GSTInvoiceCategory.EXP_WOP` carries the comment \"assume no IGST payment (LUT)\". (2) `TransactionForClassification` (classifier.py:60-79) has no igst field, so payment of IGST genuinely cannot be seen \u2014 even though the caller has it: services/gst_return_service.py:1026-1043 builds the dataclass and never passes `igst_paise`, while passing it into `InvoiceForGSTR1` two lines later (gst_return_service.py:1056). (3) apps/api/domain/gst/gstr1_builder.py:527-554 `_build_exp` emits `{exp_typ, inv:[{inum, idt, val, sbpcode:\"\", sbnum:\"\", sbdt:\"\", itms}]}` \u2014 no `ctin` field exists in that shape at all. I ran the builder directly: a `Deemed_export` invoice to 27AAAAA0000A1Z5 with 1,800.00 IGST produced `exp: [{\"exp_typ\":\"WOPAY\", ...}]`, `b2b: null`, recipient GSTIN absent; a `zero_rated` invoice carrying IGST also produced `exp_typ: \"WOPAY\"`. GUARDS lens found nothing that catches it, and two layers that cement it: tests/test_gstr1_classification_from_invoice.py:92-104 asserts SEZ_without_payment\u2192EXP_WOP, SEZ_with_payment\u2192EXP_WP, Deemed_export\u2192EXP_WOP as intended behaviour, and tests/e2e_gst_verification.py:584 accepts the raw literals `\"SEZ_with_payment\"/\"SEZ_without_payment\"/\"Deemed_export\"` as valid `inv_typ` values (GSTN's enum is R/SEWP/SEWOP/DE). apps/api/domain/gst/validator.py (232 lines) contains no `exp`, `exp_typ`, `sbnum`, `ctin` or `inv_typ` check; domain/gst/exception_report.py builds its books-side index from the same builder, so a filed-vs-books comparison cannot see the error. No migration anywhere adds shipping-bill columns (grep for sbnum/sbdt/sbpcode/shipping_bill hits only the two builders and two tests that merely assert the keys exist). The invoice_type CHECK constraints (migrations 036:12-13, 268:41-42, 270:38-39) permit the four values but say nothing about which GSTR-1 table they land in. This is not simulation-only: apps/web/app/gst/gstr1/page.tsx:11,286 downloads this exact payload for manual upload to gst.gov.in.",
   "what_is_actually_true": "The finding is correct as written, with one precision. Live defects: SEZ (with and without payment) and Deemed_export supplies are classified EXP_WP/EXP_WOP and emitted in the `exp` section, losing the recipient GSTIN entirely \u2014 GSTR-1 tables 6B and 6C belong in the `b2b` section with `inv_typ` SEWP/SEWOP/DE and a `ctin`; and a genuine export that charged IGST is filed WOPAY, because the classifier is never given the IGST amount even though its only caller has it. `sbpcode`/`sbnum`/`sbdt` are hardcoded empty strings in both gstr1_builder.py:546-548 and amendments.py:141, and no column for shipping-bill data exists in any migration, so 6A rows can never carry the refund-match key. The one sub-claim that is latent rather than live: `_build_b2b` (gstr1_builder.py:219) would emit the raw literal `\"SEZ_with_payment\"` as `inv_typ`, but no SEZ invoice can currently reach `_build_b2b` because the classifier diverts them all to exports \u2014 so it is a second bug waiting on the first being fixed, and e2e_gst_verification.py:584 would let the wrong literals through. Severity high is right, not lower: nothing downstream bounds it, the CA downloads this JSON and uploads it to the live portal, and the two existing tests would have to be rewritten as part of any fix rather than failing to warn."
  },
  {
   "kind": "single",
   "finding_id": "GST-07",
   "lens": "REACH",
   "refuted": false,
   "verdict_reason": "Confirmed by reading and executing the code. (1) apps/api/domain/gst/classifier.py:100-115 \u2014 `is_export` is true for `invoice_type in (\"SEZ_with_payment\",\"SEZ_without_payment\",\"Deemed_export\")`, returning EXP_WP/EXP_WOP; `Deemed_export` falls through to the final `return GSTInvoiceCategory.EXP_WOP` (\"assume no IGST payment (LUT)\"). (2) TransactionForClassification (classifier.py:63-81) has no igst field at all, so WPAY/WOPAY cannot be decided from tax charged; a zero_rated/POS-96 invoice carrying IGST returns EXP_WOP. (3) apps/api/domain/gst/gstr1_builder.py:529-555 `_build_exp` groups only on the category, and every row hardcodes `\"sbpcode\": \"\"`, `\"sbnum\": \"\"`, `\"sbdt\": \"\"` and carries no `ctin`. I reproduced it: a Deemed_export invoice to 27AAAAA0000A1Z5 with 1,80,000p IGST produced `exp:[{exp_typ:\"WOPAY\", inv:[{inum:\"INV1\", val:11800.0, sbnum:\"\", ...}]}]` with no recipient GSTIN and no b2b section, and a zero_rated POS-96 invoice with IGST also classified EXP_WOP. (4) `_build_b2b` (gstr1_builder.py:233) emits `inv_typ = inv.invoice_type` raw, i.e. the literal \"SEZ_with_payment\", not GSTN's SEWP/SEWOP/DE \u2014 currently unreachable because such invoices never reach b2b, but it is the shape the fix must change, and tests/e2e_gst_verification.py:584 pins the wrong enum. (5) No shipping-bill data exists anywhere: the only occurrences outside tests are the hardcoded blanks in gstr1_builder.py:547 and domain/gst/amendments.py:141 (expa nodes), and no migration adds such a column. REACH is real, not dead code: migration 268 added supply_type/invoice_type to client_sales_invoices, apps/web/lib/invoices/classification.ts offers \"SEZ with payment (IGST)\", \"SEZ under LUT/Bond\" and \"Deemed export\" in the invoice editor (components/invoices/InvoiceEditor.tsx, app/clients/[id]/sales/page.tsx), services/gst_return_service.py:1055-1082 feeds those values straight into classify_transaction/build_gstr1, and apps/web/lib/data/gst.ts:500 posts /api/gst/gstr1/from-books and stores the payload the CA uploads (routers/gst.py:489-522; gst_workspace router mounted at main.py:321). The only correction to the finding is its own hedge: the b2b inv_typ literal is not currently emitted, and the codebase does know how to decide payment from tax elsewhere (`_cdnur_type`, gstr1_builder.py:487-505 uses `inv.igst_paise`), which makes the export path's inability to do so an inconsistency rather than a missing capability. Note also that the current behaviour is deliberate and test-pinned (tests/test_gstr1_classification_from_invoice.py:94-105, tests/test_gst_engine.py:196-201), so fixing it means rewriting those assertions.",
   "corrected_severity": "high",
   "what_is_actually_true": "SEZ supplies (with and without payment) and deemed exports are all routed to GSTR-1 Table 6A's `exp` section, which has no `ctin`, so the recipient's GSTIN is dropped \u2014 GSTN puts 6B (SEZ) and 6C (deemed export) inside the `b2b` section with `inv_typ` SEWP/SEWOP/DE and the recipient GSTIN, and reserves 6A for physical exports. WPAY vs WOPAY is decided purely from `invoice_type == \"SEZ_with_payment\"`; the classifier's input dataclass carries no IGST amount, so a genuine export (or a deemed export) on which IGST was paid is declared WOPAY. Shipping bill code/number/date are hardcoded empty strings in both `_build_exp` and the `expa` amendment node, and no column anywhere stores them. All of it is reachable: a CA picks the invoice type in the invoice editor, and the same value drives the JSON produced by /api/gst/gstr1/from-books that they upload to the portal. The one part of the finding that is only prospective is the `_build_b2b` `inv_typ` literal \u2014 with today's classifier no SEZ/deemed-export invoice ever reaches b2b, so that raw \"SEZ_with_payment\" string is dead until the routing is fixed (and tests/e2e_gst_verification.py:584 currently asserts the wrong enum). The current routing is deliberate and pinned by tests/test_gstr1_classification_from_invoice.py:94-105 and tests/test_gst_engine.py:196-201, so the fix is a behaviour change those tests must be rewritten for."
  },
  {
   "kind": "single",
   "finding_id": "GST-08",
   "lens": "LITERAL",
   "refuted": false,
   "verdict_reason": "Confirmed by reading and running the code. `_build_b2cs` at apps/api/domain/gst/gstr1_builder.py:295-343 loops over invoices and computes `rate = _infer_rate(inv)` (line 307), keying the grouping on `(rate, place_of_supply, is_interstate)` and accumulating invoice-header totals (`inv.taxable_amount_paise`, `inv.cgst_paise`, ...) \u2014 it never touches `inv.lines`. `_infer_rate` (lines 284-290, not 273-279 as the finding cites \u2014 the only inaccuracy, an off-by-eleven line number) is `total_tax * 10000 // taxable_amount_paise / 100`, i.e. a blended effective rate. By contrast `_build_invoice_items` (lines 240-280), used by B2B/B2CL/CDNR/CDNUR/EXP, groups `inv.lines` by `line.gst_rate` and only falls back to `_infer_rate` when `inv.lines` is empty (line 241). Lines ARE populated for B2CS invoices on both real construction paths \u2014 services/gst_return_service.py:1045 (built from `lines_by_doc` off `client_sales_invoice_lines`) and routers/gst.py:202 (`lines=[_parse_invoice_line(l) for l in raw_lines]`) \u2014 so the fallback is not what is happening; the data is there and is ignored. Reproduced: one intra-state B2CS invoice with a 5% line (1,000_00p) and an 18% line (1,000_00p) yields exactly one row `{'sply_ty':'INTRA','rt':11.5,'typ':'OE','pos':'27','txval':2000.0,'camt':115.0,'samt':115.0}` \u2014 11.5% is not a notified GST rate. No test locks this in as deliberate: the b2cs tests (tests/test_gst_engine.py:426, 650-663; tests/test_gstr1_payload_matches_gstn_utility.py:82-113) all use single-rate invoices and assert `sply_ty`/`typ`/aggregation, never a multi-rate invoice. On the law I did not need to resolve anything contested: GSTR-1 Table 7 (B2C Others; GSTN JSON key `b2cs`) is consolidated rate-wise per place of supply, and the file's own docstring at line 8 and the `rt` field concede that.",
   "corrected_severity": "high",
   "what_is_actually_true": "The finding is accurate as stated, with one line-number slip (`_infer_rate` is at 284-290, not 273-279) and one scope narrowing worth recording. The blending only occurs WITHIN a single invoice that carries more than one GST rate. Separate single-rate B2CS invoices still group correctly, because `_infer_rate` on a uniform invoice returns the true rate \u2014 I verified a 5% invoice and an 18% invoice to the same POS produce two correct rows (5.0 and 18.0). So the blast radius is \"any B2C invoice with mixed-rate lines\", not \"all B2CS\". That is still the ordinary case for the businesses named (pharmacy, restaurant, kirana, hardware), where one counter bill routinely spans 5%/12%/18%, so `high` stands. Two secondary consequences the finding does not spell out: the invented `rt` also lands in the Table 12 tie-up only indirectly (HSN summary reads lines separately and is unaffected), and floor division means the blended figure is truncated, so the row's own tax will not reconcile to `rt \u00d7 txval` either \u2014 GSTN's arithmetic check on the b2cs row fails independently of the rate-list check. The suggested fix is the right shape: iterate `inv.lines`, key on `(line.gst_rate, place_of_supply, is_interstate)`, and keep `_infer_rate` only for the header-only case, mirroring `_build_invoice_items`."
  },
  {
   "kind": "single",
   "finding_id": "GST-08",
   "lens": "REACH",
   "refuted": false,
   "verdict_reason": "Confirmed, and it reaches real users. `_build_b2cs` (apps/api/domain/gst/gstr1_builder.py:295-341) keys its grouping on `rate = _infer_rate(inv)` at line 307 \u2014 and `_infer_rate` (lines 284-290) is `total_tax * 10000 // taxable_amount_paise / 100`, a header-level blend. It never touches `inv.lines`, unlike `_build_invoice_items` (lines 256-281), which correctly builds one `itm_det` per line rate and is what B2B (line 234), B2CL (line 364), CDNR (line 464), CDNUR (line 517) and EXP (line 549) all use. B2CS is the only outward table on the blended path. Reproduced exactly: a B2CS invoice with a \u20b91,000 line at 5% and a \u20b91,000 line at 18% emits `[{\"sply_ty\":\"INTRA\",\"rt\":11.5,\"typ\":\"OE\",\"pos\":\"27\",\"txval\":2000.0,\"camt\":115.0,\"samt\":115.0}]` \u2014 11.5% is not a notified GST rate. Lines are genuinely populated on this path, so the bug is live rather than latent: `services/gst_return_service.py:1005-1015` fetches `client_sales_invoice_lines` via `_document_lines` and line 1078 passes them into `InvoiceForGSTR1.lines`, with per-line `gst_rate_bps` on the line table (migrations/050_sales_purchase_cycle.sql:68), so a multi-rate invoice is representable and normal. `routers/gst.py:_parse_invoices_for_gstr1` (line 222) does the same for the `/gstr1/build` endpoint. REACH: `app.include_router(gst.router, ...)` at apps/api/main.py:265; `POST /api/gst/gstr1/from-books` (routers/gst.py:411) is called from apps/web/app/clients/[id]/compliance/gst/page.tsx:473 and apps/web/lib/data/gst.ts:500. No feature flag gates it. Nothing downstream repairs `rt` \u2014 `domain/gst/amendments.py:103,188` and `domain/gst/exception_report.py:183` pass it through, and `domain/gst/validator.py` only checks a header rate against tax totals (line 132), which a blended rate satisfies by construction. No test pins the blended behaviour, so it is not a deliberate choice. One cosmetic nit in the finding: the module header at line 8 labels B2CS \"4B\" (the form actually numbers B2CS as Table 7 and B2CL as Table 5), so the file's own table mapping is mislabelled \u2014 immaterial to the defect.",
   "corrected_severity": "high",
   "what_is_actually_true": "`_build_b2cs` groups Table 7 rows on a rate inferred from invoice-level totals (`_infer_rate`, gstr1_builder.py:284-290) instead of iterating `inv.lines`, so any B2C invoice carrying more than one GST rate is filed as a single row at a blended rate that does not exist in the notified rate list (verified: 5% + 18% in equal amounts yields `rt: 11.5`). Taxable value and tax totals stay correct in aggregate \u2014 only the rate attribution is invented \u2014 but GSTN validates `rt` against the notified list, so the upload is rejected or, if accepted, the rate-wise B2C turnover is misdeclared. Single-rate B2C invoices are unaffected: `_infer_rate` returns the true rate there, which is why this has survived. Every other outward table already uses the correct line-level grouping via `_build_invoice_items`, so the fix is to give B2CS the same treatment, keeping `_infer_rate` only as the header-only fallback for invoices with no stored lines."
  },
  {
   "kind": "single",
   "finding_id": "GST-08",
   "lens": "GUARDS",
   "refuted": false,
   "corrected_severity": "high",
   "verdict_reason": "Reproduced exactly as claimed, and no guard layer prevents, catches or bounds it.\n\nCODE: apps/api/domain/gst/gstr1_builder.py:295-341 `_build_b2cs` loops over invoices and does `rate = _infer_rate(inv)` (line 307), keying on `(rate, place_of_supply, is_interstate)`. `inv.lines` is never touched. `_infer_rate` (lines 284-290) is `(cgst+sgst+igst) * 10000 // taxable_paise / 100` \u2014 a blended invoice-level rate. Contrast `_build_invoice_items` (lines 240-280), which groups `inv.lines` by `line.gst_rate` and is used by B2B, B2CL (line 361) and CDNUR (line 517); B2CS is the only table that bypasses it.\n\nREPRODUCED (PYTHONPATH=apps/api, scratchpad venv): one intra-state B2CS invoice with a 5% line (\u20b91,000) and an 18% line (\u20b91,000) gives `b2cs = [{\"sply_ty\":\"INTRA\",\"rt\":11.5,\"typ\":\"OE\",\"pos\":\"27\",\"txval\":2000.0,\"camt\":115.0,\"samt\":115.0}]`. The identical invoice routed to B2B yields two correct itm_dets at rt 5.0 and 18.0. Tax totals are preserved; only the rate attribution is invented.\n\nTHE LINES ARE REALLY THERE IN PRODUCTION. Both live constructors populate them: services/gst_return_service.py:256-303 `_document_lines` reads `client_sales_invoice_lines` and maps `gst_rate_bps/100 \u2192 gst_rate` per line (wired in at :1044-1073 `lines=lines_by_doc...`), and routers/gst.py:150-163 `_parse_invoice_line` / :222 does the same. migrations/050_sales_purchase_cycle.sql:58-68 gives each line its own `gst_rate_bps`, so multi-rate invoices are a first-class shape, not an edge case. So the header-only fallback excuse does not apply.\n\nGUARDS \u2014 searched and none exist:\n- domain/gst/validator.py (232 lines) has no notified-rate whitelist; it validates GSTIN, period, duplicates, intra/inter tax-head placement and a header-level tax-arithmetic tolerance (:131-142) using `inv.gst_rate` \u2014 it never inspects the built payload's `rt`, and a blended-rate invoice passes its arithmetic check by construction, since the blend is derived from the very totals it compares.\n- No migration, CHECK constraint or trigger touches GSTR-1 rates; `gst_rate_bps INTEGER NOT NULL DEFAULT 1800` has no domain restriction.\n- domain/gst/exception_report.py:162-193 `index_b2cs` keys on `\"{sply_ty}|{rt}|{pos}\"` and indexes BOTH the books-derived payload and the filed payload through the same builder, so both sides carry the identical blended rate and the report shows no difference. It is not a detection layer here.\n- Tests do not cover it: tests/test_gst_engine.py:426-442 (`test_b2cs_aggregated_by_rate_and_pos`) uses two single-rate invoices, and the Bug#1 regression helper `make_b2cs_invoice` (tests/test_gst_engine.py:605-639) hardcodes `lines=[]`, so every existing B2CS assertion runs through the fallback path and cannot see this. tests/e2e_gst_verification.py:596-614 only asserts `rt` is present, never its value.\n- No feature flag, RLS policy or frontend check is involved; apps/web/app/gst/gstr1/page.tsx renders the payload the backend returns.",
   "what_is_actually_true": "The finding is accurate as written. `_build_b2cs` (apps/api/domain/gst/gstr1_builder.py:295-341) is the only GSTR-1 table builder that ignores `inv.lines` and keys its grouping on `_infer_rate(inv)` (:284-290), a blended invoice-level rate. A two-rate B2C counter sale (5% + 18%, \u20b91,000 each) files as one row at `rt: 11.5` \u2014 a rate that does not exist \u2014 while the same invoice through B2B/B2CL/CDNUR splits correctly via `_build_invoice_items`.\n\nPrecisions worth carrying into the write-up, none of which reduce severity:\n\n1. Scope is B2CS only. B2B (:230), B2CL (:361), CDNR and CDNUR (:517) all call `_build_invoice_items` and are correct. So the defect is a single function, and the suggested fix (iterate `inv.lines`, key on (line rate, POS, is_interstate), keep `_infer_rate` only for the header-only fallback) is the right shape and genuinely hours of work.\n\n2. Tax and taxable totals are preserved \u2014 camt+samt equals the true tax. So GSTR-3B tie-up and the tax actually paid are unaffected; what is wrong is the rate-wise attribution GSTN reads, plus the upload validity of `rt`.\n\n3. Single-rate B2C invoices are unaffected (the blend equals the line rate), and a header-only invoice with no lines legitimately falls back. So the blast radius is exactly multi-rate B2C invoices \u2014 which is the entire retail/restaurant/pharmacy segment the finding names.\n\n4. Two smaller wrinkles in `_infer_rate` that ride along on the same fix: it floor-divides (`//`), so a blend truncates rather than rounds, and it excludes cess from the numerator (correct for `rt`, but it means a cess-bearing B2CS invoice gets its cess pooled under the blended rate row too).\n\n5. The builder's module docstring maps B2CS to \"4B\" while the finding calls it Table 7. Table 7 is the current GSTR-1 form layout and the finding's label is the right one; the docstring at gstr1_builder.py:9 is stale prose, not a second bug \u2014 worth a one-line correction alongside the fix."
  },
  {
   "kind": "single",
   "finding_id": "GST-09",
   "lens": "LITERAL",
   "refuted": false,
   "verdict_reason": "Traced the cited lines and the whole call graph. domain/gst/correction_window.py:76-113 `window_for` takes `annual_return_filed_on` and forwards it to `correction_window_closes(end, annual_return_filed_on)` at line 89; services/compliance_engine.py:196-211 implements the earlier-of rule correctly. But `window_for` has exactly one production caller: services/gst_amendment_service.py:101-102 inside `outstanding_amendments`, whose own `annual_return_filed_on: Optional[date] = None` (line 72) is supplied by nobody \u2014 routers/gst_workspace.py:1123-1125 calls `outstanding_amendments(get_supabase(), current_user[\"firm_id\"], client_id, period)`, routers/gst.py:471 the same, and the `GET /gstr1/amendments` signature (routers/gst_workspace.py:1086-1090) accepts only client_id and period, so no caller could pass one. A repo-wide grep for `annual_return_filed_on` (apps/web included) hits only compliance_engine, correction_window, gst_amendment_service and two test files (tests/test_correction_window.py:78,90; tests/test_gstr1_amendment_service.py:292), which pass it straight to the functions. So in production `closes` always equals `november_30_cutoff(end)` (correction_window.py:88-89), `shortened_by_annual_return` is always False (line 109), and the actionable/expired bucketing at gst_amendment_service.py:110-117 is always measured against 30 November. The law is as the finding states: CGST \u00a737(3)/\u00a739(9)/\u00a716(4) as amended by the Finance Act 2022 close on 30 November following the FY end or the furnishing of the annual return, whichever is earlier \u2014 matching CLAUDE.md and the module docstring (correction_window.py:11-25). tests/test_correction_window.py passes (25 tests), so the unit logic is right and the defect is purely wiring. The data for the fix exists: migrations/053_hardening.sql:305-310 and 234_gst_tds_schema_drift_fixes.sql:27-36 give gstr1_returns `return_type='gstr9'`, and migrations/036_gst_engine.sql:51-57 give it `status` and `submitted_at`.",
   "corrected_severity": "high",
   "what_is_actually_true": "Confirmed as written, with one addition and one refinement. Addition \u2014 there is a FIFTH affected site, and it is the one that claims immunity: services/filing_demo/gstr3b.py:88-104 `_correction_window` carries the docstring \"Never november_30_cutoff: that is only the statutory outer limit, and on its own it tells a CA a correction is available when the client's early GSTR-9 has already shut the window\", then calls `correction_window_closes(fy_end)` with no second argument at line 103, which returns exactly november_30_cutoff. So the GSTR-3B filing demo shows the outer limit while asserting it does not. (services/filing_demo/gstr9.py:95 deliberately uses november_30_cutoff and labels it as the outer limit in its warning text, so that one is honest.) Refinement \u2014 the harm is not confined to a displayed date: `is_actionable(window)` (correction_window.py:116-118) drives the split at gst_amendment_service.py:110-117, so a period whose window an early GSTR-9 already shut lands in `entries`/`closing_soon` instead of `expired`, and its amendment entries are then folded into the built return payload by `apply_amendments` at routers/gst.py:473. Severity stays high: a silently wrong statutory date plus a wrong actionable/expired classification reaching the payload, in exactly the failure mode CLAUDE.md names as the reason `correction_window_closes()` exists."
  },
  {
   "kind": "single",
   "finding_id": "GST-09",
   "lens": "GUARDS",
   "findings": "[]",
   "refuted": false,
   "corrected_severity": "medium",
   "verdict_reason": "Confirmed on the code, with one bounding fact the finding missed that lowers severity from high to medium.\n\nCONFIRMED \u2014 the wiring is genuinely absent:\n- services/gst_amendment_service.py:69-73 declares `annual_return_filed_on: Optional[date] = None` and forwards it at :101-102 to `window_for`.\n- The only two production callers omit it: routers/gst_workspace.py:1123-1125 (`outstanding_amendments(get_supabase(), current_user[\"firm_id\"], client_id, period)`) and routers/gst.py:465-471 (same, in POST /gst/gstr1/with-amendments).\n- A repo-wide grep for `annual_return_filed_on` outside tests returns only the four definition/pass-through sites (compliance_engine.py:196-210, correction_window.py:77-89, gst_amendment_service.py:72/102). So `correction_window_closes(end, None)` at domain/gst/correction_window.py:89 always returns `november_30_cutoff(end)` and `shortened_by_annual_return` (:109) is always False.\n- The data needed for the fix does exist: GSTR-9 rows live in `gstr1_returns` with `return_type='gstr9'` (migrations/053_hardening.sql:304-310, writer at routers/gst_workspace.py:934-975) and the table carries `status IN (...,'submitted')`, `submitted_at`, `arn` (migrations/036_gst_engine.sql:42-61).\n- The effect is functional, not just a label: `is_actionable(window)` at gst_amendment_service.py:110-118 gates whether entries are merged into the generated payload, and routers/gst.py:445-448 asserts in its own docstring that \"nothing here can declare an amendment that the law no longer allows\" \u2014 an assertion that only holds if the earlier-of limb is wired.\n\nGUARDS \u2014 searched apps/api/tests, apps/api/migrations, apps/web:\n- No DB trigger, CHECK, RLS policy, Pydantic validator or feature flag touches this.\n- Tests do NOT guard it, they conceal it: tests/test_correction_window.py:71-83 and tests/test_gstr1_amendment_service.py:285-297 both pass `annual_return_filed_on` explicitly, proving the domain function works while nothing asserts a caller supplies it. tests/test_gstr1_amendment_service.py:409-420 exercises the router and asserts only counts.\n- The one real bound: NO frontend caller reaches either endpoint. `grep -rn \"with-amendments|gstr1/amendments\" apps/web` returns nothing; the GST screens use `/api/gst/gstr1/from-books` (apps/web/lib/data/gst.ts:500, apps/web/app/clients/[id]/compliance/gst/page.tsx:473), which does not call `outstanding_amendments`. And apps/web consumes none of `closes_on` / `statutory_cutoff` / `shortened_by_annual_return`. Also routers/gst_workspace.py:1115-1122 short-circuits to empty in mock mode. So the ca_experience as written (\"PracticeSync tells the CA the window is open until 30 November and lists the amendment as actionable\") is not reachable from any screen today \u2014 it is reachable only by an API consumer.\n\nSECOND SITE the finding missed: services/filing_demo/gstr3b.py:88-104 `_correction_window` calls `correction_window_closes(fy_end)` with no annual-return argument, under a docstring that says \"Never november_30_cutoff: that is only the statutory outer limit\" \u2014 which is exactly what the one-argument call returns. That surface IS rendered (FilingDemoWizard), but it is mitigated: the demo warning text states the earlier-of rule in prose (gstr3b.py:162 with the warning stage; the GSTR-9 demo does the same at services/filing_demo/gstr9.py:165-179), and tests/test_filing_demo_gstr3b.py:376-388 pins that wording.",
   "what_is_actually_true": "The \"or the annual return, whichever is earlier\" limb is genuinely never supplied by any production caller, so `window_for` always reports the 30 November outer limit and `shortened_by_annual_return` is always False \u2014 and because `is_actionable` gates the merge, a period closed by an early GSTR-9 can still have amendments folded into a generated GSTR-1 payload the portal would refuse. The parameter, the domain logic and the tests for it all exist; only the resolution of the GSTR-9 filing date (available as `gstr1_returns` where `return_type='gstr9'` and `status='submitted'`, column `submitted_at`) is missing at the two call sites, plus a third one-argument call in services/filing_demo/gstr3b.py:88-104. Severity is medium rather than high because no frontend screen consumes the affected endpoints \u2014 `/gst/gstr1/with-amendments` and `/gst-workspace/gstr1/amendments` have zero callers in apps/web, and the rendered filing demos state the earlier-of rule in prose beside the date. The suggested fix and effort estimate are right."
  },
  {
   "kind": "single",
   "finding_id": "GST-12",
   "lens": "LITERAL",
   "refuted": false,
   "corrected_severity": "high",
   "verdict_reason": "Traced literally and it holds. apps/web/app/gst/page.tsx:65-72 `buildMonthOptions()` pushes `{ value: label, label }` where label = `${MONTH_NAMES[d.getMonth()]} ${d.getFullYear()}` \u2014 the option VALUE is \"May 2026\", not \"2026-05\". That is the only source of the `period` state: the modal initialises it at page.tsx:160 from `MONTH_OPTIONS[...].value`, and the only writer is the Period select at page.tsx:275-284 (`onChange={(e) => setPeriod(e.target.value)}` over `<option value={o.value}>`). Nothing else assigns `period`. In `handleSave`, page.tsx:187-189 is exactly as cited: `const [pYear, pMonth] = period.split(\"-\").map(Number); const periodStart = `${pYear}-${String(pMonth).padStart(2,\"0\")}-01`; const periodEnd = new Date(pYear, pMonth, 0).toISOString().slice(0,10);`. Ran it in node with period=\"May 2026\": pYear=NaN, pMonth=undefined, periodStart=\"NaN-undefined-01\", and `new Date(NaN, undefined, 0).toISOString()` throws RangeError: Invalid time value. The throw is inside the try opened at page.tsx:181 and is caught at page.tsx:222, which sets `err` and renders it in the modal's red error strip. The modal is genuinely reachable: `showAddModal` is set by two \"Add Filing\" buttons (page.tsx:816, 927) and `AddFilingModal` renders at page.tsx:960-967 whenever at least one client exists. No test covers it \u2014 nothing under apps/web/scripts/*.test.ts references app/gst/page or AddFilingModal. `getDueDate` (page.tsx:78-101) parses the same string correctly with `split(\" \")`, which is what makes line 187 an isolated slip. No statutory question is involved in the defect.",
   "what_is_actually_true": "The finding is accurate as written. The Period select on /gst emits \"MMM YYYY\" (e.g. \"May 2026\") and handleSave parses it with split(\"-\"), so `new Date(NaN, undefined, 0).toISOString()` at apps/web/app/gst/page.tsx:189 throws RangeError \"Invalid time value\", caught at page.tsx:222 and displayed as the modal's error text. Every save from the \"Add GST Filing\" modal fails, for every return type and every period option. Two refinements, neither weakening it: (1) the throw happens BEFORE the `sb.from(\"compliance_calendar\").insert(...)` at page.tsx:190-201, so nothing is written and there is no corrupt \"NaN-undefined-01\" row \u2014 the failure is clean and total; (2) `getDueDate` on the same page parses the identical string correctly via split(\" \"), so the auto-filled due date is right and the only broken consumers are period_start/period_end. Severity high stands: a primary create path that cannot succeed, with no test coverage. The suggested fix (machine value YYYY-MM separate from the display label, read by both getDueDate and handleSave, plus a save test) is the right shape."
  },
  {
   "kind": "single",
   "finding_id": "GST-09",
   "lens": "REACH",
   "refuted": false,
   "corrected_severity": "medium",
   "verdict_reason": "Mechanics confirmed, reach not. services/gst_amendment_service.py:69-73 takes annual_return_filed_on and forwards it at :101-102 to domain/gst/correction_window.py:76-115 (finding's cite of :240-261 is wrong \u2014 the file is shorter), where closes=correction_window_closes(end, annual_return_filed_on) and shortened_by_annual_return=closes<statutory. Neither production caller supplies it: routers/gst_workspace.py:1123-1124 (finding cites 1105-1106) and routers/gst.py:465-471; a repo-wide grep for annual_return_filed_on outside tests returns only those pass-throughs plus the definition at services/compliance_engine.py:196-210. So on both paths closes==november_30_cutoff and shortened_by_annual_return is always False. BUT the REACH lens downgrades it: both consumers (GET /api/gst-workspace/gstr1/amendments, POST /api/gst/gstr1/with-amendments) are mounted (main.py:265, :321) and rbac-guarded yet have no caller anywhere \u2014 apps/web calls only /api/gst-workspace/gstr1, /gstr1/{id}/status (app/clients/[id]/compliance/gst/page.tsx:439,457,486) and /api/gst/gstr1/from-books (lib/data/gst.ts:500), and no .ts/.tsx file references \"amendment\" as an API path or renders closes_on / shortened_by_annual_return. The only CA-facing text about the window, app/clients/[id]/compliance/gst/page.tsx:1435-1436, states the earlier-of rule correctly in prose, as does the filing demo's freeze text (services/filing_demo/gstr3b.py:349-353) even though _correction_window at :98-103 also omits the date. Tests at tests/test_gstr1_amendment_service.py:285-307 prove the parameter works when passed, which is why the wiring looks complete.",
   "what_is_actually_true": "The \"or the annual return, whichever is earlier\" limb is implemented and unit-tested but never supplied by any production caller, so every window returned by outstanding_amendments reports the 30 November outer limit and shortened_by_annual_return is permanently False. That is a real gap and the suggested fix (resolve the GSTR-9 filing date from gstr1_returns where return_type='gstr9' \u2014 routers/gst_workspace.py:933-1000 shows those rows exist \u2014 and pass it in) is right. What is not true is the CA-facing narrative: no screen in apps/web calls either endpoint or displays this window, so no CA is currently shown \"open until 30 November\" for an amendment. It is a latent defect on an API-only surface, plus the same omission in the filing demo's _correction_window (services/filing_demo/gstr3b.py:98-103), which is mitigated because the demo spells the earlier-of rule out in words. Severity medium, not high: correct to fix, but it does not reach a user through the deployed product today."
  },
  {
   "kind": "single",
   "finding_id": "GST-12",
   "lens": "GUARDS",
   "findings": "null",
   "refuted": false,
   "verdict_reason": "Confirmed by reading the file and executing the exact expression. /home/user/caflow-ai/apps/web/app/gst/page.tsx:65-76 `buildMonthOptions()` pushes `{ value: label, label }` where `label = \\\"${MONTH_NAMES[d.getMonth()]} ${d.getFullYear()}\\\"` \u2014 so every option's VALUE is \\\"May 2026\\\", never \\\"2026-05\\\". The select at :275-283 sets `period` from `o.value`, and the initial state at :161 takes `MONTH_OPTIONS[last].value`, so `period` is always \\\"MMM YYYY\\\". `getDueDate` at :82 and :87 confirms that intended format by splitting on a SPACE. But `handleSave` at :187-189 splits on a HYPHEN: `const [pYear, pMonth] = period.split(\\\"-\\\").map(Number)`. Executed in node with period=\\\"May 2026\\\": pYear=NaN, pMonth=undefined, periodStart=\\\"NaN-undefined-01\\\", and `new Date(NaN, undefined, 0).toISOString()` throws `RangeError: Invalid time value`. The throw is at :189, BEFORE the `.from(\\\"compliance_calendar\\\").insert(...)` at :190, so nothing is written and the catch at :223-225 renders the raw message \\\"Invalid time value\\\" in the modal's error bar (:240-242). GUARDS lens \u2014 nothing catches it: (a) no test touches this screen \u2014 `apps/web/scripts/*.test.ts` has no test for app/gst/page.tsx or AddFilingModal, and `grep -rn 'period.split(' app lib components` returns only these three sites, so the bug is unique and untested; (b) it is a direct PostgREST insert, so no Pydantic validator or `rbac()` runs; (c) DB constraints on `compliance_calendar` are irrelevant because the exception precedes the network call; (d) it is type-valid TypeScript, so `tsc --noEmit`/`pnpm lint` pass; (e) the backend column tests (`apps/api/tests/test_frontend_columns_exist_pg.py`, `_frontend_select_parser.py`) parse select lists for column existence, not runtime values. The only mitigation is that GST obligation rows are generated server-side (`apps/api/services/compliance_obligation_service.py:104-122` emits GSTR1/GSTR3B specs; `apps/api/routers/compliance.py:46 seed_compliance_calendar`), so the tracker is not necessarily empty \u2014 which the finding already states. It is also the only insert path into `compliance_calendar` in the frontend for GST types (the only other insert is `app/income-tax/page.tsx:512`), so there is no in-app workaround.",
   "corrected_severity": "high",
   "what_is_actually_true": "The finding is accurate as written. Every click of \"Save Filing\" in the Add GST Filing modal on /gst throws `RangeError: Invalid time value` at page.tsx:189 and surfaces it as the modal's error text; no row is ever inserted (the throw precedes the insert), so there is no data corruption \u2014 only a 100%-broken create path. The root cause is that `buildMonthOptions` (:71) uses the display label as the option value (\"May 2026\"), `getDueDate` correctly parses it with `split(\" \")` (:82, :87), and `handleSave` alone parses it with `split(\"-\")` (:187). No test, migration, constraint, RLS policy, validator or feature flag bounds it. Severity stays high: it is a primary, deterministic, no-workaround failure of a create action on a top-level module screen \u2014 though it is a loud failure rather than a silently-wrong statutory number, and GST obligation rows are still generated server-side by services/compliance_obligation_service.py so the tracker itself remains populated."
  },
  {
   "kind": "single",
   "finding_id": "GST-12",
   "lens": "REACH",
   "refuted": false,
   "verdict_reason": "Confirmed as written. apps/web/app/gst/page.tsx:63-73 `buildMonthOptions()` pushes `{ value: label, label }` where label is `${MONTH_NAMES[d.getMonth()]} ${d.getFullYear()}` \u2014 i.e. the option VALUE is \"May 2026\", not \"2026-05\" (the GSTFiling interface at line 44 even documents `period: string; // \"MMM YYYY\"`). The `<select>` at page.tsx:279-283 sets `period` straight from `o.value`, and the initial state at line 160 is `MONTH_OPTIONS[...].value`, so `period` is always \"MMM YYYY\". `getDueDate` (page.tsx:78-101) correctly parses it with `split(\" \")`, but `handleSave` (page.tsx:186-188) does `const [pYear, pMonth] = period.split(\"-\").map(Number)` \u2014 for \"May 2026\" that yields pYear=NaN, pMonth=undefined; `periodStart` becomes \"NaN-undefined-01\" and `new Date(NaN, undefined, 0).toISOString()` throws RangeError \"Invalid time value\". Reproduced in node: pYear NaN, pMonth undefined, periodStart \"NaN-undefined-01\", THREW RangeError Invalid time value. The throw happens BEFORE the PostgREST insert (page.tsx:189-201), so nothing is written; the catch at page.tsx:216 renders the raw message \"Invalid time value\" in the modal's red error strip (page.tsx:240-242). REACH: the page is live and navigable \u2014 components/panels/DeadlinesPanel.tsx:31 lists `{ href: \"/gst\", label: \"GST\" }` in FILING_WORKSPACE_ITEMS, lib/workspace/workspaceConfig.ts:165 maps /gst to the deadlines workspace, lib/auth/permissions.ts:57 grants it, and lib/workspace/knownRoutes.generated.ts:80 has [\"gst\"]. The modal is opened by two live controls: the header \"Add Filing\" button (page.tsx:815-818) and the empty-state action (page.tsx:926-929), rendered at page.tsx:960. No feature flag gates it; it is a direct PostgREST write, not a backend route. It is also the only UI insert into compliance_calendar for GST \u2014 `grep` finds just one other insert, app/income-tax/page.tsx:512. No test covers the save path.",
   "corrected_severity": "high",
   "what_is_actually_true": "Every \"Save Filing\" click in the Add GST Filing modal on /gst fails with \"Invalid time value\" before any database write, because the period select's value is the display label (\"May 2026\") while handleSave parses it as \"YYYY-MM\" via split(\"-\"). Two same-file functions disagree on the format: getDueDate uses split(\" \") and works; handleSave uses split(\"-\") and does not. The row is never inserted (the RangeError precedes the insert), so nothing is half-written \u2014 the CA simply cannot add a GST filing row manually from the tracker; rows can only come from elsewhere. The page is mounted, linked from the Deadlines panel's filing-workspace nav, permitted in lib/auth/permissions.ts, and behind no feature flag, so this reaches a real user. A second latent defect sits in the same two lines: even if the Date call did not throw, period_start would be written as the garbage string \"NaN-undefined-01\"."
  },
  {
   "kind": "single",
   "finding_id": "GST-14",
   "lens": "LITERAL",
   "refuted": false,
   "verdict_reason": "Traced every cited path and all of it holds literally. apps/web/app/gst/page.tsx:643-670 (`handleMarkFiled`) does `getSupabaseClient().from(\"compliance_calendar\").update({filing_status:\"filed\", filed_date, arn_number}).eq(\"id\", id)` \u2014 no fetch/apiClient anywhere in the file (the only network client imported is `@/lib/supabase/client`, line 24; the sole other API-ish call is `getClients()`), so no FastAPI route and therefore no `rbac()` dependency runs. The bulk path at :375-393 (`BatchMarkFiledModal.handleSubmit`) does the identical PostgREST update per row. The page's loader (:577-596) also reads its state from `compliance_calendar` (`filing_status`, `filed_date`), so the tracker is a self-contained second store. `arn_number` really is a column on that table (migrations/006_transactions_bank_compliance.sql:128), so the write succeeds rather than erroring \u2014 it lands in a table nothing else reads for filing state. The genuine path is routers/gst_workspace.py:523-541 (GSTR-1) and 728-746 (GSTR-3B), which call `record_filing(...)` into `filings` only when `body.status == \"submitted\" and not _USE_MOCK`. `grep` for writers of `filings` shows exactly one GST writer: services/gst_filing_record_service.py:139-149, reached only from those two router lines. The lock reads only `filings`: migrations/266_editable_journals_until_locked_or_filed.sql:290-330 (`SELECT filing_type, filed_date ... FROM public.filings WHERE client_id = p_client ...`), and 267 moved the body to `period_lock_reason` with the same `FROM public.filings` (267:64-77) \u2014 neither ever consults `compliance_calendar`. No trigger on `compliance_calendar` writes `filings` (only `trg_filings_updated_at`, 001:200). services/gst_filing_record_service.py:12-21 states in prose that an empty `filings` table makes that half of the lock unreachable. /gst is a live, linked route (components/panels/DeadlinesPanel.tsx:31) and its modal says \"This records already-filed returns\" (page.tsx:464) with no caveat that it neither locks the period nor updates the workspace return. Two nuances, neither refuting: (a) `compliance_calendar` writes are not wholly unguarded \u2014 migration 261 gives it a role-aware RLS write policy (Executive+, 261:55; tracked by tests/test_direct_write_tables_are_role_guarded.py:43) \u2014 the finding's precise claim, that `rbac()` never runs, is still true; (b) a `compliance_calendar` row is a deadline row, not a return row, which is why the two stores never met, but the tracker collects an ARN and a filed date and shows \"Filed\", so the CA-facing contradiction described is real. Law not in issue here; no statutory claim to check.",
   "corrected_severity": "high",
   "what_is_actually_true": "Confirmed as written. The /gst tracker marks GST returns filed by writing `filing_status`/`filed_date`/`arn_number` directly to `compliance_calendar` over PostgREST (page.tsx:643-670 single, :375-393 bulk), and reads its own state back from the same table (:577-596). It never calls the API, so `rbac()` does not run (RLS's role-aware policy from migration 261 is the only check) and `record_filing` \u2014 the sole writer of `filings`, reached only from routers/gst_workspace.py:526 and :747 \u2014 is never invoked. `journal_period_lock_reason` (266:290-330, re-homed in 267:64-77) queries `public.filings` alone, so a return marked filed from the tracker produces no period lock and journals inside it stay editable, while the client GST workspace, which reads `gstr1_returns`/`gstr3b_returns.status`, still shows the return as draft. One small correction to the finding's framing: the write is not entirely unauthorised \u2014 `compliance_calendar` carries an Executive+ role-aware write policy \u2014 and a `compliance_calendar` row is structurally a deadline row rather than the return itself, which is the root of the divergence rather than an excuse for it."
  },
  {
   "kind": "single",
   "finding_id": "GST-14",
   "lens": "GUARDS",
   "refuted": false,
   "verdict_reason": "The mechanism is exactly as claimed, but the evidence overstates one half and misses one direction, so severity drops to medium.\n\nCONFIRMED. apps/web/app/gst/page.tsx:641-661 (`handleMarkFiled`) and :375-392 (BatchMarkFiledModal) do `getSupabaseClient().from(\"compliance_calendar\").update({filing_status:\"filed\", filed_date, arn_number}).eq(\"id\", id)` \u2014 no `apiFetch`, so no FastAPI route and no `record_filing`. The same page loads its rows from `compliance_calendar` (:577-584) and inserts new ones there (:188-201), so the tracker is a self-contained second store of \"filed\". The only writer of `filings` for GST is `record_filing` at routers/gst_workspace.py:526 (GSTR-1) and :747 (GSTR-3B), reached only from `PATCH /api/gst-workspace/{gstr1,gstr3b}/{id}/status`, whose sole frontend caller is the client workspace (apps/web/app/clients/[id]/compliance/gst/page.tsx:457, :748). `journal_period_lock_reason` (migrations/266_...:314-320, generalised as `period_lock_reason` in 267) selects only `FROM public.filings`; nothing reads `compliance_calendar` there. I grepped every migration touching `compliance_calendar` (006, 007, 008, 010, 012, 039, 041, 087, 111, 168, 170, 260, 261, 321, 336) \u2014 no trigger, no view, nothing that derives a `filings` row from it. So a tracker-only \"Filed\" produces no period lock: correct.\n\nGUARDS THAT DO EXIST (which the evidence's \"rbac() never runs\" line understates): migration 261 gives `compliance_calendar` a RESTRICTIVE role-aware write policy at `['compliance_calendar','Executive','Partner']` (261:55), with its header (261:38-40) noting the tier deliberately matches `gst:compute` (Executive+). tests/test_direct_write_tables_are_role_guarded.py:43 and tests/test_role_write_policies_pg.py:307 list it as GUARDED, and tests/test_sql_role_tiers_match_python.py:52 pins the tier. So the authorization half is covered by RLS at the same tier the API would have enforced; only the *effect* half (no `filings` row) is unguarded. No test, trigger or sweep covers that half.\n\nWHAT THE FINDING MISSES: the divergence is bidirectional. The workspace status PATCH writes `gstr1_returns/gstr3b_returns` + `filings` and never touches `compliance_calendar`, so a return genuinely filed through the API still shows \"Pending\"/\"Overdue\" on the /gst tracker \u2014 the more likely everyday inconsistency, since the tracker rows are hand-added (AddFilingModal) or seeded, not derived from the returns.\n\nKNOWN, NOT NEW: docs/audits/2026-07-02-executive-product-audit.md:3640-3648 records `app/gst/page.tsx` as one of four pages still writing `compliance_calendar` directly, deliberately left unmigrated (\"Not done... tracked explicitly as follow-up work\").",
   "corrected_severity": "medium",
   "what_is_actually_true": "Two independent \"filed\" stores exist and neither syncs. The /gst firm-wide tracker writes `compliance_calendar.filing_status/filed_date/arn_number` straight from the browser (app/gst/page.tsx:641-661 single, :375-392 bulk) and no `filings` row is ever created, so `journal_period_lock_reason`/`period_lock_reason` (migrations 266:314-320, 267) cannot fire for a filing recorded that way \u2014 manual journals inside that period stay editable and deletable. The genuine path (routers/gst_workspace.py:526/:747 \u2192 services/gst_filing_record_service.record_filing) is only reachable from the client GST workspace screen. The inconsistency runs both ways: workspace filings never update `compliance_calendar`, so the tracker shows a genuinely filed return as still pending. Two things bound the severity: (a) `compliance_calendar` direct writes are role-guarded by RLS at Executive+/Partner (migration 261:55, pinned by three tests), so this is not an RBAC bypass, only a missing side effect; (b) the period lock is not globally dead \u2014 it fires for returns filed through the workspace, which is the path CLAUDE.md documents as canonical. It is also a knowingly deferred migration item, not a regression (docs/audits/2026-07-02-executive-product-audit.md:3640-3648). The suggested fix direction is right; the cleanest is to have the tracker read filing state from the returns/`filings` tables rather than maintain a second one."
  },
  {
   "kind": "single",
   "finding_id": "GST-14",
   "lens": "REACH",
   "refuted": false,
   "corrected_severity": "high",
   "verdict_reason": "Confirmed by reading the code; if anything the finding is understated. (1) Tracker path is exactly as cited: apps/web/app/gst/page.tsx:580-583 loads GSTR1/GSTR3B/GSTR9 rows from `compliance_calendar` over PostgREST, :191 inserts them, and handleMarkFiled at :644-655 plus the bulk modal at :379-388 do `sb.from(\"compliance_calendar\").update({filing_status:\"filed\", filed_date, arn_number}).eq(\"id\",...)`. No apiFetch, so rbac() never runs (only RLS plus the Executive+ role policy in migrations/261_role_aware_write_policies_part2.sql:55) and record_filing is never called. The columns exist (migrations/006_transactions_bank_compliance.sql:113-135), so the write succeeds silently. (2) The lock reads only `filings`: migrations/266_editable_journals_until_locked_or_filed.sql:316-327 selects FROM public.filings WHERE client_id=p_client AND deleted_at IS NULL AND filed_date IS NOT NULL AND p_date BETWEEN period_start AND period_end \u2014 nothing reads compliance_calendar. The sole writer of `filings` is services/gst_filing_record_service.py:139-149 (record_filing), invoked only from routers/gst_workspace.py:526 and :747 under `body.status == \"submitted\" and not _USE_MOCK`; that router is mounted (apps/api/main.py:321). (3) Reach is real: /gst is in FILING_WORKSPACE_ITEMS (apps/web/components/panels/DeadlinesPanel.tsx:31) and allowed in apps/web/lib/auth/permissions.ts:57. The one correction: the finding's \"genuine path\" is not exercised by the UI either. No frontend caller sends status=\"submitted\" to /api/gst-workspace/{gstr1,gstr3b}/{id}/status \u2014 apps/web/app/clients/[id]/compliance/gst/page.tsx:457 and :748 only ever pass \"validated\"/\"ca_approved\" (buttons at :627/:631, :1102/:1108), and the comment at :1124-1127 says a mark-filed control is deliberately absent there. The other reachable mark-filed-with-ARN controls, /gst/gstr1 and /gst/gstr3b (linked from apps/web/app/gst/page.tsx:795,802), call markGSTR1Filed/markGSTR3BFiled in apps/web/lib/data/gst.ts:657-696, which update gstr1_returns/gstr3b_returns directly over PostgREST. So record_filing is unreachable from the product UI and the filed-return branch of the lock never fires \u2014 precisely the failure gst_filing_record_service.py:12-21 documents.",
   "what_is_actually_true": "There are three UI paths that mark a GST return filed and none writes the `filings` row the period lock reads: /gst writes filing_status/filed_date/arn_number to compliance_calendar (apps/web/app/gst/page.tsx:644-655 and :379-388); /gst/gstr1 and /gst/gstr3b write status='submitted' + arn + submitted_at directly to gstr1_returns/gstr3b_returns (apps/web/lib/data/gst.ts:657-696); the client workspace at apps/web/app/clients/[id]/compliance/gst/page.tsx offers no mark-filed control at all and stops at ca_approved. All three go over PostgREST, so rbac() runs on none of them. record_filing (apps/api/services/gst_filing_record_service.py:139-149) is reached only by PATCH /api/gst-workspace/{gstr1,gstr3b}/{id}/status with status='submitted' (apps/api/routers/gst_workspace.py:526, :747), and no frontend code sends that status. Consequence: the filed-return branch of journal_period_lock_reason (migrations/266_...:316-327) can never fire in the deployed product \u2014 journals inside a filed period stay editable \u2014 and up to three screens can disagree about whether the same return is filed. The finding's high severity and its suggested fix both stand, but the fix must also cover /gst/gstr1 and /gst/gstr3b, not just the compliance_calendar tracker."
  },
  {
   "kind": "single",
   "finding_id": "GST-15",
   "lens": "LITERAL",
   "refuted": false,
   "verdict_reason": "All three prongs verified in the code as written. (1) COMPUTES: services/gst_advance_service.py:126-182 `table_11_sections` returns `{\"at\": ..., \"txpd\": ...}` built by `_table_11_rows` (lines 91-123), which calls `split_inclusive_charge` and emits `rt`, `ad_amt`, and `iamt` or `camt`+`samt` per row \u2014 a real tax split \u2014 gated only on `clients.gst_advance_tax_applicable` (line 138) and on the receipt carrying `gst_rate_bps` + `place_of_supply` (lines 157-158; columns added by migrations/286_advance_receipt_gst.sql:39-64). (2) MERGED INTO THE FILED PAYLOAD: services/gst_return_service.py:1096-1099 \u2014 `table_11 = gst_advance_service.table_11_sections(...)` then `payload.payload[key] = table_11[key]` for `at`/`txpd`; tests/test_gstr1_advances_are_surfaced.py:288-303 asserts exactly that (`payload[\"at\"][0][\"itms\"][0][\"ad_amt\"] == 1000.0`). (3) THE CONTRADICTION IS REAL AND IN THE SAME MODULE: `advances_report` returns `\"table_11_computed\": False` (line 235) with \"PracticeSync does not compute GSTR-1 Table 11\" (lines 236-244); the module docstring heading is \"WHY THIS REPORTS AND DOES NOT COMPUTE\" (line 3) and lines 27-32 say \"Nothing has ever been declared in 11A, so there is nothing to adjust\" while `txpd` (11B) is computed 20 lines below; routers/gst_workspace.py:1280 repeats \"This reports; it computes no tax\" and its mock branch (line 1287) hardcodes `table_11_computed: False`. tests/test_gstr1_advances_are_surfaced.py holds both halves of the contradiction \u2014 test_no_tax_figure_is_produced_anywhere (line 107) and test_it_states_that_table_11_is_not_computed (line 119) alongside test_table_11_reaches_the_gstr1_payload (line 288). (4) 3B GAP: `grep -n advance domain/gst/gstr3b_computer.py` returns nothing; `compute_gstr3b` (line 593) takes only sales, purchases, gstr2a_records, reversals, reclaims, and 3.1(a) is accumulated purely from `sales` (lines 612-627). Both call sites \u2014 services/gst_return_service.py:795 and routers/gst.py:288 \u2014 pass invoice-derived sales only, so no advance liability enters 3.1(a) by any path. Only citation drift: the router text is at line 1280, not 1245-1249.",
   "corrected_severity": "high",
   "what_is_actually_true": "The finding is accurate. For a client with `gst_advance_tax_applicable = true` whose receipts carry a rate and place of supply, GSTR-1 Tables 11A/11B are computed with a full CGST/SGST/IGST split and merged into the GSTN payload the CA uploads, while the advances screen and API for the same client still answer `table_11_computed: false` with prose saying the product computes no Table 11 \u2014 and the module docstring still claims nothing has ever been declared in 11A even though 11B (`txpd`) is computed on that premise. GSTR-3B carries no advance liability at all: `compute_gstr3b` has no advance input and 3.1(a) is summed from invoices only, so a liability declared in GSTR-1 is never paid in 3B and will surface as a GSTR-1 vs 3B mismatch on the portal. Two mitigations that do not remove the defect but bound it: the behaviour is opt-in per client (migration 286 defaults the switch to false) and requires a CA to have entered a rate and place of supply on the receipt, so it cannot fire on a default-configured goods client; and receipts lacking a rate or POS are skipped rather than guessed. The stale text and the missing 3B leg are the live defects; \"high\" is the right severity because the wrong half is the one that leaves tax declared and unremitted. On the law I am confident there is no separate advances row in GSTR-3B (tax on advances for services is carried in 3.1(a) and adjusted when the invoice is raised), but that is settled practice/portal layout rather than a rule I can cite by number, so treat the statutory framing as strong-but-not-quoted."
  },
  {
   "kind": "single",
   "finding_id": "GST-15",
   "lens": "REACH",
   "refuted": false,
   "verdict_reason": "The three code facts are real, but the harm is unreachable in the deployed product, so the severity is wrong.\n\nCONFIRMED: `services/gst_advance_service.py:126-182` really does compute `at`/`txpd` with a CGST/SGST/IGST split via `split_inclusive_charge`, and `services/gst_return_service.py:1093-1099` merges them into `payload.payload` \u2014 the from-books GSTR-1 that `apps/web/app/clients/[id]/compliance/gst/page.tsx:473` (`POST /api/gst/gstr1/from-books`) and `routers/gst.py:411-427` serve. `advances_report` (same file, 235-247) still returns `\"table_11_computed\": False` with \"PracticeSync does not compute GSTR-1 Table 11\", the module docstring (27-32) still says \"Nothing has ever been declared in 11A\", and `routers/gst_workspace.py:1279` repeats \"This reports; it computes no tax\". `domain/gst/gstr3b_computer.py` has no advance concept at all, and `gst_return_service.gstr3b_from_books:616-680` builds `SalesTransaction`s only from posted sales invoices, credit notes and sales debit notes \u2014 receipts are never read.\n\nREFUTED \u2014 the two gates the computation needs are written by NOTHING in the product:\n1. `clients.gst_advance_tax_applicable` (migration 286:40) is `NOT NULL DEFAULT false` and appears in exactly three places repo-wide: the service, the migration and one test. It is absent from `models/client.py` `ClientCreate`/`ClientUpdate` (99-112), from every router, and from every `apps/web` form or `.from(\"clients\").update(...)`. There is no way to switch it on short of hand-editing Postgres.\n2. Even switched on, line 157 skips any receipt where `gst_rate_bps` is NULL or `place_of_supply` is empty. `models/invoices.py:387-433` `ReceiptIn` has neither field, and neither of the two receipt inserts (`services/receipt_service.py:212-223` and `:640-680`) writes them; a repo-wide grep for `gst_rate_bps` across `apps/api/routers`, `apps/api/services` and `apps/web` shows writers only for invoice/note lines, bank charges and bank rules \u2014 never for `receipts`.\n\nSo `at`/`txpd` are always `[]`, nothing is merged, and no advance tax has ever entered a filed GSTR-1. The \"screen that exists to review advances\" also does not exist: grep of `apps/web` for `gstr1/advances`, `advances_report` and `table_11` returns nothing \u2014 `GET /api/gst-workspace/gstr1/advances` (`routers/gst_workspace.py:1262`) is mounted but has no caller. `pytest tests/test_gstr1_advances_are_surfaced.py -q` \u2192 24 passed, and its Table 11 tests only pass because they seed `gst_advance_tax_applicable` and the receipt rate columns directly into the FakeDB (lines 152-166), which is precisely the state no product path can produce.",
   "corrected_severity": "low",
   "what_is_actually_true": "The code contradiction is real but inert. `table_11_sections` computes advance tax and `gstr1_from_books` merges it into the filed payload, while the same module's report, its docstring and the router docstring all still say Table 11 is not computed \u2014 and GSTR-3B has no advance input, so if the merge ever fired, GSTR-1 would declare a liability 3B never pays. However both inputs the computation depends on are unwritable through the product: `clients.gst_advance_tax_applicable` is not in `ClientCreate`/`ClientUpdate` or any web form, and `receipts.gst_rate_bps` / `place_of_supply` are not in `ReceiptIn` or either receipt insert. Turning the path on takes two manual SQL edits. What actually reaches a CA today is uniform: no advance tax anywhere \u2014 not in GSTR-1, not in GSTR-3B \u2014 plus an unwired `/gstr1/advances` endpoint no screen calls. So this is stale prose over dormant code and a real feature gap (advances never reach 3B 3.1(a)), not a live wrong-liability-on-a-filed-return bug. The fix is still the one suggested \u2014 make the two halves agree \u2014 but it is a low-severity tidy plus a feature decision, not days of urgent work."
  },
  {
   "kind": "single",
   "finding_id": "GST-15",
   "lens": "GUARDS",
   "refuted": false,
   "verdict_reason": "Every cited fact checks out, and I reproduced the contradiction in one request. services/gst_advance_service.py:126-182 `table_11_sections` really does compute `at`/`txpd` with a CGST/SGST/IGST split via `split_inclusive_charge` (line 107) whenever `clients.gst_advance_tax_applicable` is true and the receipt carries `gst_rate_bps` + `place_of_supply`; services/gst_return_service.py:1096-1099 merges those sections into `payload.payload`. The same module's `advances_report` (lines 227-247) returns `\"table_11_computed\": False` and \"PracticeSync does not compute GSTR-1 Table 11\" UNCONDITIONALLY \u2014 it never reads `gst_advance_tax_applicable` \u2014 and the module docstring lines 27-32 (\"Nothing has ever been declared in 11A\") is stale, since `txpd_buckets` (line 176) now computes 11B. routers/gst_workspace.py:1274-1284 repeats \"computes no tax\". domain/gst/gstr3b_computer.py:593-643 takes only sales/purchases/2A/reversals/reclaims \u2014 there is no advance input anywhere, so 3.1(a) (`outward_taxable_value`, line 623) cannot include it. Reproduced against the repo's own FakeDB harness with the flag on: report says `table_11_computed: False`, GSTR-1 payload carries `at` = [{pos 27, rt 18.0, ad_amt 1000.0, camt 90.0, samt 90.0}], GSTR-3B `osup_det` = all zeros \u2014 same client, same period. GUARDS lens found none that catch it; tests/test_gstr1_advances_are_surfaced.py PINS BOTH SIDES of the contradiction (test_table_11_reaches_the_gstr1_payload:283-300 asserts the merge; test_it_states_that_table_11_is_not_computed:119-126 asserts the \"not computed\" text), so the suite entrenches it rather than flagging it. Migration 286 has only CHECK constraints on the columns and its own header comment (\"services/gst_advance_service says so in its own response\") is now stale too. No RLS, trigger, Pydantic validator, feature flag or frontend check bounds it. What DOES bound it \u2014 and is why I lower the severity \u2014 is reachability: nothing in the product can turn it on. `gst_advance_tax_applicable` appears nowhere outside gst_advance_service.py, migration 286 and that one test (no API route, no apps/web reference at all), and routers/receipts.py + services/receipt_service.py never read or write `gst_rate_bps` / `place_of_supply` / `is_interstate` on a receipt. There is also no advances SCREEN: nothing in apps/web calls /api/gst-workspace/gstr1/advances or mentions `table_11_computed`.",
   "corrected_severity": "medium",
   "what_is_actually_true": "The mechanism is exactly as claimed and I reproduced it: with `clients.gst_advance_tax_applicable` true and a receipt carrying a rate and place of supply, GSTR-1's filed payload gains a Table 11A `at` section with real tax, the advances endpoint in the same codebase still answers `table_11_computed: False` with \"PracticeSync does not compute GSTR-1 Table 11\", and GSTR-3B 3.1(a) stays zero \u2014 so GSTR-1 would declare a liability 3B never pays, and the portal's GSTR-1-vs-3B comparison would flag it. Two things the finding gets wrong, both about reach rather than mechanism. (1) There is no advances screen \u2014 no frontend file references the endpoint, `table_11_computed`, or `unadjusted_advances` \u2014 so the \"screen tells the CA it is not computed\" framing is really an API/docstring contradiction, not something a CA reads today. (2) The enabling data is unreachable from the product: no endpoint, service or web code writes `clients.gst_advance_tax_applicable` or the three receipt columns migration 286 added, so today the `at`/`txpd` merge can only fire if someone sets those columns straight in Postgres. It is a live latent contradiction (the code, the response text, the docstring and migration 286's header all disagree, and the tests pin both halves), and it becomes exactly the high-severity filing defect described the moment anyone wires up the switch \u2014 but nothing files a wrong return through the product as it stands. One extra wrinkle in the same direction, not in the finding: even when the merge fires, `gstr1_from_books`' own `tax_total_paise` and `total_cgst_paise` stayed 0 in my run, so the saved GSTR-1 record's totals and its GL reconciliation also exclude the advance tax that the uploaded JSON declares. Correct fix is still the finding's: make 3B carry it and fix the text, or stop merging."
  },
  {
   "kind": "single",
   "finding_id": "GST-17",
   "lens": "LITERAL",
   "refuted": false,
   "verdict_reason": "Every mechanical claim checks out against the code, with only a line-number drift. apps/api/domain/gst/gstr1_builder.py:560-570 defines `_HSN_DIGITS_5CR_PAISE = 5_00_00_000_00`, `_HSN_DIGITS_1_5CR_PAISE = 1_50_00_000_00` and `_required_hsn_digits()` returning 6 / 4 / 0 (I ran it: 10cr\u21926, 2cr\u21924, 1cr\u21920). The slice the finding quotes is at line 594, not 587, but is textually identical: `code = (line.hsn_sac_code or \"\")[:max(required_digits, len(line.hsn_sac_code or \"\"))]` \u2014 the slice bound is never below the code's own length, so it truncates nothing. Ran `_build_hsn_summary` directly with lines carrying \"99\" and \"72142090\": at \u20b910 crore turnover the emitted `hsn_sc` values are `['99', '72142090']`, unchanged; identical at \u20b92 crore. `required_digits` has exactly one use (line 594) and is read nowhere else \u2014 grep across apps/api finds no HSN digit-length check in domain/gst/validator.py, domain/gst/exception_report.py, services/filing_demo/gstr1.py, or anywhere in apps/web (the only HSN shape check is `^\\d{2,8}$` in components/lookups/FirmHsnLibraryQuickAddModal.tsx:63, which is a master-data format check, not an AATO check). So nothing validates and nothing warns. Table 12 is emitted as one flat list \u2014 gstr1_builder.py:163-165 sets `payload[\"hsn\"] = {\"data\": hsn}` from a single `by_hsn` dict, with no B2B/B2C split. The comments at services/gst_return_service.py:266-273 and tests/test_gstr1_classification_from_invoice.py:464-468 both restate the \u20b91.5 crore tier as current and both say the digit count \"had nothing to truncate\", so the dead computation is documented rather than hidden. Statutory side: Notification 78/2020-Central Tax (15-10-2020, w.e.f. 01-04-2021) is as the finding states \u2014 AATO above \u20b95 crore, 6 digits on all invoices; AATO up to \u20b95 crore, 4 digits on B2B (B2C optional) \u2014 so the code's \"optional below \u20b91.5 crore\" is wrong for B2B supplies of a small taxpayer. I am reasonably but not fully certain of the GSTN Table 12 Phase-3 B2B/B2C tab split from the Jan-2025 period and do not rest the verdict on it. Two corrections to the finding's wording below.",
   "corrected_severity": "medium",
   "what_is_actually_true": "Confirmed, with two corrections. (1) The slice is at gstr1_builder.py:594, not 587 (the constants/function are 560-570, not 558-568) \u2014 same code, drifted numbers. (2) \"The thresholds are the pre-2021 ones\" is imprecise. The pre-2021 table (Notification 12/2017) was nil below \u20b91.5 crore, 2 digits \u20b91.5\u20135 crore, 4 digits above \u20b95 crore. The code's \u20b95 crore \u2192 6 digits is the CURRENT rule under Notification 78/2020 and is right; what is stale is only the bottom of the ladder \u2014 under 78/2020 there is no \u20b91.5 crore tier and a taxpayer below it still needs 4 digits on B2B, where the code returns 0 (\"optional\"). So the code is a hybrid, not the old table. What is squarely true and is the live defect: `_required_hsn_digits` feeds nothing but a `max(required, len(code))` slice that can never shorten a string, so the number is computed and discarded \u2014 at \u20b910 crore turnover a 2-digit \"99\" is emitted verbatim as `hsn_sc: \"99\"`, which I reproduced. There is no HSN digit validation, warning or exception-report row anywhere in the backend or frontend, and no check that a code is a real HSN at all. The wrong-threshold half therefore has zero live effect today (the value is never applied), which is what keeps this medium rather than high; it also means fixing the thresholds alone would change no output. Worth adding: apps/web/lib/data/gst.ts:503 sends `aggregate_turnover_paise: 0` hardcoded (\"CA can override on the client GST screen\"), and routers/gst.py:341 defaults it to 0, so on the normal web path `_required_hsn_digits` returns 0 regardless of the client's actual turnover \u2014 the input to the dead computation is itself usually unset. Table 12 is a single flat list with no B2B/B2C split (gstr1_builder.py:163-165)."
  }
 ],
 "wf_6cec6423-101": [
  {
   "kind": "single",
   "finding_id": "ACC-01",
   "lens": "LITERAL",
   "refuted": false,
   "corrected_severity": "critical",
   "verdict_reason": "Confirmed line by line. apps/api/routers/year_end_adjustments.py:504-514 builds journal_entry with keys id, firm_id, entry_date, reference_no, narration, \"source\": \"YEAR_END_ADJUSTMENT\", \"source_ref_id\": adjustment_id, is_posted, created_at \u2014 exactly as cited \u2014 and :531-532 does two separate db.table(\"journal_entries\").insert(journal_entry).execute() / db.table(\"journal_lines\").insert(journal_lines).execute() calls, no _create_journal, no transaction. The columns do not exist: migrations/104_journal_draft_workflow.sql:10-11 adds source_type text and source_id uuid (NOT source/source_ref_id), and no other migration adds either name (grep over migrations/*.sql). tests/fixtures/production_schema_2026-09-03.json's journal_entries key set matches the finding and holds source_id/source_type only. NOT NULL: migrations/003_phase3a_foundation.sql:122 \"client_id UUID NOT NULL REFERENCES clients(id)\" and :126 \"entry_type TEXT NOT NULL CHECK (...)\"; the production fixture confirms both nullable=\"NO\" with no default \u2014 neither is supplied. The path is the live production path: _USE_MOCK = not os.environ.get(\"SUPABASE_URL\") (line 25), so with Supabase configured control reaches line 531 with a real supabase-py/PostgREST client (core/supabase_client.py), which rejects unknown columns (PGRST204); the exception propagates as a 500 and the status update at :535+ never runs, leaving the adjustment 'approved'. Router mounted at apps/api/main.py:346. The UI is genuinely reachable \u2014 I tested the underscore-file counter-hypothesis and it fails: the year-end _page.tsx files are stage components imported by app/clients/[id]/year-end/[engagementId]/_workspace.tsx:48 (AdjustmentsStage) and rendered by the real page.tsx; adjustments/_page.tsx:109 calls yearEndApi.adjustments.post \u2192 apps/web/lib/api/yearEnd.ts:308-311. Tests are green for the reason claimed: tests/test_r240_year_end_critical_gaps.py:338-341 asserts len(db.rows(\"journal_entries\")) == 1 against FakeDB (26 passed when I ran the file), and tests/_backend_query_parser.py:226-240 only walks inline ast.Dict payloads, so a variable argument is invisible to the column check. Docs claim verified at docs/architecture/01-accounting-engine.md:32 (\"There are no alternative posting paths\").",
   "what_is_actually_true": "The finding is accurate as written. The only nit is mechanism, not substance: PostgREST rejects the unknown source/source_ref_id keys with a schema-cache error (PGRST204) rather than at SQL parse time, and the client_id/entry_type not-null violations would only surface once those keys were removed \u2014 either way the insert fails and the request 500s. Worth adding to the fix: production journal_lines also carries multi-currency NOT NULL columns (base_currency, exchange_rate, rate_type, txn_currency \u2014 all defaulted, so they survive, but _create_journal is what actually reasons about them). Severity stays critical: posting a year-end adjustment cannot succeed in production, the adjustment is left permanently 'approved' with nothing in the GL, and the UI shows only a generic \"Action failed\" toast (adjustments/_page.tsx:113-115) rather than saying the ledger was untouched."
  },
  {
   "kind": "single",
   "finding_id": "ACC-01",
   "lens": "GUARDS",
   "findings": "[]",
   "refuted": false,
   "corrected_severity": "high",
   "verdict_reason": "Confirmed by reading the code, the schema and the guards; only the \"silent\" framing and \"critical\" grading are overstated.\n\nCONFIRMED \u2014 the payload. apps/api/routers/year_end_adjustments.py:504-514 builds the entry with \"source\" and \"source_ref_id\", and apps/api/routers/year_end_adjustments.py:531-532 does two independent `db.table(...).insert(...).execute()` calls. journal_entries has neither column: apps/api/migrations/003_phase3a_foundation.sql:118-135 creates it, and the only later ALTERs are 007 (firm_id), 012 (status), 055:60 (reversal_of), 104:9-11 (posted_by, source_type, source_id \u2014 note source_TYPE/source_ID, not source/source_ref_id), 138 (attachments), 147 (rate_*), 213 (is_reversed). The production fixture agrees: apps/api/tests/fixtures/production_schema_2026-09-03.json's journal_entries key set has source_type/source_id and no source/source_ref_id. PostgREST rejects the whole statement at parse time (PGRST204/42703) \u2014 the failure mode tests/test_backend_columns_exist_pg.py's own docstring describes.\n\nCONFIRMED \u2014 the NOT NULLs. 003_phase3a_foundation.sql:122 `client_id UUID NOT NULL` and :126 `entry_type TEXT NOT NULL CHECK (...)`, both with no default, both confirmed nullable:\"NO\", default:\"\" in the production fixture. Neither is supplied. (`status` is also left at its 'draft' default while is_posted=true \u2014 a second inconsistency behind the first failure.)\n\nCONFIRMED \u2014 kernel bypass. `grep insert` over routers/services/domain finds exactly two direct journal_entries inserts: this one and services/phase2_journal_service.py:1738 (the kernel). So this is the only alternative posting path, and it is also non-atomic (entry and lines are separate PostgREST calls, no transaction).\n\nCONFIRMED \u2014 reachable. main.py:346 mounts the router. The `_page.tsx` name is not dead code: apps/web/app/clients/[id]/year-end/[engagementId]/_workspace.tsx:47 imports `AdjustmentsStage from \"./adjustments/_page\"` (eight stages collapsed onto one route to stay under Cloudflare's redirect budget), and handleAction(\"post\") in adjustments/_page.tsx calls yearEndApi.adjustments.post \u2192 POST /api/year-end/{eng}/adjustments/{id}/post (apps/web/lib/api/yearEnd.ts:310).\n\nCONFIRMED \u2014 no guard catches it. tests/_backend_query_parser.py:225-241 only reads inline ast.Dict payloads and counts a variable payload as \"unreadable\", so test_backend_columns_exist_pg.py is blind here (its own docstring already admits it cannot see a missing NOT NULL either). No trigger or function on year_end_adjustments exists in any migration (067/154/155/293/294/319 are table/column/RLS only). core/supabase_client.py is a thin factory with no payload sanitising. tests/test_r240_year_end_critical_gaps.py:324-341 passes against FakeDB \u2014 I ran it: 26 passed in 0.60s.\n\nWHY NOT CRITICAL. Three bounds the finding does not credit. (1) It fails LOUDLY, not silently: the entry insert is the first write, so nothing at all reaches the GL \u2014 no orphan header, no half-balanced entry, no corruption \u2014 and the year_end_adjustments row is only updated at :535, after the inserts, so the row stays \"approved\" and the UI's catch block (adjustments/_page.tsx handleAction) shows a red \"Action failed\" toast. The CA sees the action fail and the status not advance. (2) There is a working in-product route to the same result: an ordinary manual journal entry through phase2_journal_service posts fine, which is exactly what the \"market standard\" paragraph says Tally/Zoho do. (3) Nothing else double-counts around it \u2014 no other router or service reads year_end_adjustments for statements (only year_end.py and year_end_exports.py reference the table), so the statements are simply unadjusted rather than inconsistent.",
   "what_is_actually_true": "The bug is real and the feature has never worked against a real database. POST /api/year-end/{engagement}/adjustments/{id}/post (routers/year_end_adjustments.py:504-532) inserts into journal_entries with two columns that do not exist (`source`, `source_ref_id` \u2014 the real ones are `source_type`/`source_id`, added by migration 104) and omits two NOT NULL columns with no default (`client_id`, `entry_type` \u2014 migration 003:122,126, confirmed in tests/fixtures/production_schema_2026-09-03.json). PostgREST rejects the statement at parse time, so in production the call 500s, no journal row is written, the adjustment stays \"approved\" and the ledger is untouched. It is also the only direct journal_entries write outside services/phase2_journal_service.py:1738, and the entry/lines pair is written non-atomically \u2014 so even after fixing the columns it would still contradict the one-posting-kernel rule and could strand an unbalanced entry. The endpoint is genuinely reachable (main.py:346; apps/web _workspace.tsx imports adjustments/_page and lib/api/yearEnd.ts:310 posts to it). No guard exists: the FakeDB test passes, and tests/_backend_query_parser.py:225-241 cannot see a variable insert payload so the pg column test skips it. What the finding overstates: the failure is loud, not silent (error toast, status stays \"approved\"), nothing is corrupted or half-written, and the CA has a working alternative \u2014 the same adjustment entered as a manual journal posts correctly through the kernel. Hence high, not critical. The suggested fix (route through _create_journal with client_id, entry_type='Journal', source_type/source_id, created_by) plus a real-Postgres test is the right shape."
  },
  {
   "kind": "single",
   "finding_id": "ACC-01",
   "lens": "REACH",
   "refuted": false,
   "verdict_reason": "Confirmed on every load-bearing point, with one correction to the user-facing narrative.\n\n1. The insert payload is exactly as claimed. apps/api/routers/year_end_adjustments.py:501-532 builds `journal_entry = {id, firm_id, entry_date, reference_no, narration, \"source\": \"YEAR_END_ADJUSTMENT\", \"source_ref_id\": adjustment_id, is_posted, created_at}` and executes `db.table(\"journal_entries\").insert(journal_entry).execute()` then a separate `db.table(\"journal_lines\").insert(journal_lines).execute()`.\n\n2. The columns genuinely do not exist. `grep -rn source_ref_id migrations/` returns NOTHING anywhere in the repo, and the only source columns ever added to journal_entries are `source_type`/`source_id` (migrations/104_journal_draft_workflow.sql:10-11) \u2014 not `source`/`source_ref_id`. tests/fixtures/production_schema_2026-09-03.json's journal_entries key set confirms it: source_id and source_type are present, `source` and `source_ref_id` are absent. PostgREST rejects an insert naming an unknown column (PGRST204), so the statement fails before any row is written.\n\n3. The two NOT NULL omissions are real. migrations/003_phase3a_foundation.sql:122 `client_id UUID NOT NULL REFERENCES clients(id)` and :126-128 `entry_type TEXT NOT NULL CHECK (...)`; the production fixture agrees (client_id nullable \"NO\", entry_type nullable \"NO\"). Neither is in the payload, and neither has a default. So even after deleting the two phantom keys the insert still fails.\n\n4. Kernel bypass confirmed. services/phase2_journal_service.py:1506-1531 `_create_journal` takes exactly the `source_type`/`source_id`/`client_id`/`entry_type` arguments this endpoint should have used, and docs/architecture/01-accounting-engine.md:32 states \"There are no alternative posting paths\"; docs/architecture/02-posting-kernel.md:3 repeats it. (The finding's doc path was right; my first grep missed only because of cwd.) The two inserts are also non-atomic \u2014 a journal_entries row without its lines is possible in principle.\n\n5. REACH is full, not dead code. Router mounted: apps/api/main.py:92 imports year_end_adjustments and :346 `app.include_router(year_end_adjustments.router, prefix=\"/api\")`. `_USE_MOCK = not os.environ.get(\"SUPABASE_URL\")` (year_end_adjustments.py:25), so production takes the real branch, and core/supabase_client.py:125-138 `get_supabase()` returns a genuine Supabase/PostgREST client with no column filtering. Frontend: `_page.tsx` is NOT dead \u2014 it is a documented convention (apps/web/scripts/private-components.test.ts:5-19) where page.tsx renders the underscore client component; app/clients/[id]/year-end/[engagementId]/_workspace.tsx:48 imports `AdjustmentsStage from \"./adjustments/_page\"` and :62 registers it as the \"Adjustments\" stage, and lib/workspace/ClientNavContext.tsx:74 links `/clients/{id}/year-end/`. The Post button at adjustments/_page.tsx:109 calls `yearEndApi.adjustments.post`, wired to POST /api/year-end/{eng}/adjustments/{id}/post at lib/api/yearEnd.ts:308-312.\n\n6. The test blind spot is real. tests/test_r240_year_end_critical_gaps.py:337-340 asserts `len(db.rows(\"journal_entries\")) == 1` against FakeDB, and tests/_backend_query_parser.py:230-236 only walks inline `ast.Dict` write payloads \u2014 a variable payload increments `unreadable` and is skipped, so test_backend_columns_exist_pg.py never sees these keys.\n\nONE CORRECTION to the ca_experience: the failure is loud, not silent. lib/api/yearEnd.ts goes through the shared `request()` helper and adjustments/_page.tsx:104-117 does `if (!res.success) throw` \u2192 catch \u2192 `showToast(err.message, false)`. The CA sees a red \"Action failed\" toast and the row stays \"approved\" \u2014 so there is no silent ledger corruption or a falsely-posted status. What is true is that the adjustment can NEVER post in production, and nothing tells the CA the failure is permanent.",
   "corrected_severity": "high",
   "what_is_actually_true": "Year-end adjustment posting is completely non-functional against the real database. `POST /api/year-end/{engagement_id}/adjustments/{adjustment_id}/post` (apps/api/routers/year_end_adjustments.py:501-532) inserts two columns that exist nowhere in the schema (`source`, `source_ref_id` \u2014 journal_entries only ever gained `source_type`/`source_id` in migration 104) and omits two NOT NULL columns with no default (`client_id`, `entry_type` \u2014 migration 003:122,126). PostgREST rejects the statement; the adjustment is stuck at \"approved\" and nothing reaches the GL. The endpoint also bypasses `phase2_journal_service._create_journal`, violating the one-posting-kernel rule the architecture docs state explicitly, and its two inserts are not atomic.\n\nThe reach is genuine end to end: router mounted in main.py:346, production takes the non-mock branch (`_USE_MOCK` keys off SUPABASE_URL), the Adjustments stage is a live tab in the nav-linked year-end workspace, and its Post button calls this exact route. The whole class is invisible to CI because the tests run against an in-memory FakeDB that accepts any column, and the column-existence linter's AST parser skips write payloads passed as a variable rather than an inline dict.\n\nSeverity corrected from critical to high on one point only: the failure is visible, not silent. The frontend surfaces a \"Action failed\" toast and the adjustment's status is not advanced, so no wrong data is written and no adjustment is falsely marked posted \u2014 the financial statements are simply the unadjusted trial balance, and the CA is left retrying a button that can never succeed. That is a broken core feature rather than silent statutory corruption, which is what separates it from critical. Everything else in the finding, including the suggested fix (route through `_create_journal` with entry_type \"Journal\", the adjustment's client_id, source_type/source_id, created_by) and the need for a real-Postgres regression test, stands."
  },
  {
   "kind": "single",
   "finding_id": "ACC-02",
   "lens": "LITERAL",
   "refuted": false,
   "corrected_severity": "high",
   "verdict_reason": "Traced every cited path; it checks out literally. apps/api/services/phase2_journal_service.py:210 is exactly `bank_id = self._find_account(db, firm_id, client_id, \"%Bank%\", system_key=\"bank\")` inside receipt_journal_lines, and payment_mode is never inspected there (lines 206-239). journal_for_purchase_payment (phase2_journal_service.py:749-795) resolves payables_id (764) and bank_id (767, same `%Bank%`/system_key=\"bank\") and unconditionally emits `{\"account_id\": bank_id, \"credit_paise\": payment[\"amount_paise\"], \"narration\": \"Bank payment to vendor\"}`; the payment dict carries payment_mode and it is read nowhere in the function. services/receipt_service.py:178 and services/purchase_payment_service.py:443 do the same `%Bank%` lookup on the foreign-currency paths; the plain-INR production path is _settle_receipt_via_atomic_rpc, which at receipt_service.py:397 calls the same receipt_journal_lines \u2014 so mode 'cash' still debits Bank. `_find_account` (phase2_journal_service.py:1404-1466) matches only on system_account_key then account_name ILIKE; no mode or account is passed by any caller. payment_mode is only persisted (receipt_service.py:566/634, purchase_payment_service.py:180/246/478, routers/purchase_payments.py:372/390/475) and the DB CHECK allows 'cash' (migrations/050_sales_purchase_cycle.sql:101-102, 323-324). The UI does offer it: apps/web/app/clients/[id]/purchases/page.tsx:2219 and sales/page.tsx:165 (`PAYMENT_MODES`, sent at :3476). Cash in Hand (1001) / Petty Cash (1002) are seeded at services/coa_seed_service.py:51-52 (finding says 56-57 \u2014 five off, same content; also migrations/011_seed_chart_of_accounts.sql:12-13), and no automatic path posts to them: grep finds no 'cash' system key anywhere, only manual journals and the bank-entry Contra where a human names the other ledger (services/bank_posting_service.py:361-384). Two immaterial imprecisions: receipt_service.py:178 sits in create_foreign_receipt rather than the atomic settlement path (that is line 397, same helper), and the coa_seed line numbers are off by five. Severity stays high, not critical \u2014 double entry balances and Cash+Bank in total is right, so nothing is lost or unbalanced; composition is wrong and the bank ledger cannot reconcile.",
   "what_is_actually_true": "Confirmed as written. Every automatic receipt and vendor-payment journal hits the single account resolved by system_account_key 'bank' (fallback account_name ILIKE '%Bank%') regardless of the payment_mode the CA chose; payment_mode is stored and never consulted when choosing a GL account, and no automatic flow ever touches Cash in Hand (1001) or Petty Cash (1002). One consequence the finding misses, and it widens the blast radius beyond the balance sheet: migrations/288_s40a3_detects_payments_not_receipts.sql:76-97 detects \u00a740A(3) disallowable cash payments by scanning CREDITS to Asset accounts whose name or subtype matches '%cash%'. Because cash vendor payments are credited to Bank, that detector can never fire from the payment flow \u2014 a statutory disallowance check that silently reports nothing rather than erroring. Two related instances not named in the finding: journal_for_fixed_asset (phase2_journal_service.py:1191) hardcodes the same '%Bank%' credit for an asset purchase, and _find_account's system_key lookup is firm-wide with .limit(1), so a client with several bank accounts collapses them all onto one ledger too. The suggested fix should therefore be an explicit payment_account_id picker over all Bank and Cash ledgers, not merely a cash/bank switch derived from payment_mode."
  },
  {
   "kind": "single",
   "finding_id": "ACC-02",
   "lens": "GUARDS",
   "refuted": false,
   "verdict_reason": "Confirmed against the code; no guard layer exists. (1) Every automatic settlement leg resolves the SAME generic ledger: phase2_journal_service.py:210 `bank_id = self._find_account(..., \"%Bank%\", system_key=\"bank\")` in receipt_journal_lines, :766-769 in journal_for_purchase_payment, receipt_service.py:178 (foreign receipt), purchase_payment_service.py:442-443 (foreign payment), routers/purchase_payments.py:643 (re-allocation). The plain-INR receipt path is receipt_service.py:397, which calls that same receipt_journal_lines \u2014 so the evidence's cited line 178 is actually the foreign path, but the substance holds on every path. (2) `payment_mode` is stored (purchase_payment_service.py:180/246/478, receipt_service.py:217/566/634) and grep across apps/api shows it is never read to choose an account \u2014 phase2_journal_service.py and domain/ contain no reference to it at all. (3) The UI offers cash: apps/web/app/clients/[id]/purchases/page.tsx:2219 and sales/page.tsx:165 `PAYMENT_MODES = [\"bank\",\"cash\",...]`. (4) coa_seed_service.py:51-52 seeds 1001 Cash in Hand / 1002 Petty Cash and no automatic flow posts to them; seeding sets no system_account_key, so `_find_account` (phase2_journal_service.py:1404-1450) falls through to the `%Bank%` name match. GUARDS searched and none found: no CHECK/trigger/migration ties payment_mode to an account (only migrations 050/161 widen the mode's CHECK list); no Pydantic validator beyond a value list (models/invoices.py:395-450); no test asserts a cash-mode document posts to a cash ledger (grep of apps/api/tests for \"Cash in Hand\" hits only test_bank_posting, test_s40a3_detection_pg and the trial-balance importer); no frontend check on mode === \"cash\". Two corrections to the finding's blast radius, one shrinking it and one widening it. Shrinking: the Balance Sheet and cash-flow TOTALS are genuinely unaffected, not merely \"right in total\" by luck \u2014 domain/reporting/schedule_iii.py:75-76 buckets both `cash` and `bank` subtypes into \"Cash & Cash Equivalents\", and migration 277_cash_flow_report_in_sql.sql:41-43 defines cash as key='bank' or name/subtype matching 'bank'/'cash'. So the cash-flow claim in ca_experience is overstated: composition is wrong, the operating-cash figure is not. Widening: migration 288_s40a3_detects_payments_not_receipts.sql:97 detects \u00a740A(3) disallowable cash payments by scanning `coa.account_name ILIKE '%cash%' OR account_subtype ILIKE '%cash%'` on the credit side \u2014 so a \u20b940,000 cash vendor payment recorded through this flow is invisible to the tool built to find exactly that exposure, and a clean run reads as a clean bill of health (the same failure mode that file's own docstring warns about). Also, `bank_accounts.coa_account_id` already exists and is used per-account by bank_posting_service.py:87-91, so the mapping the suggested fix wants is half-built; the document path ignores it, which means a firm with two bank accounts also collapses both into the one generic %Bank% ledger, not just cash.",
   "corrected_severity": "high",
   "what_is_actually_true": "payment_mode is metadata only: every receipt and vendor-payment journal \u2014 INR, foreign, and re-allocation \u2014 debits/credits the single generic ledger resolved by system_key='bank' / name '%Bank%', regardless of the mode the CA picked, and Cash in Hand (1001) / Petty Cash (1002) are seeded but never touched by any automatic flow. Nothing in the migrations, triggers, models, tests or frontend prevents, catches or bounds it. The consequences are narrower than claimed in one respect and wider in another: Balance Sheet \"Cash & Cash Equivalents\" and the cash-flow statement are correct in amount because both bucket cash and bank together (schedule_iii.py:75-76; migration 277:41-43), so this is a composition and sub-ledger error, not a totals error; but bank reconciliation against the passbook is permanently off by every cash movement, and \u00a740A(3) auto-detection (migration 288:97, which scans cash-named ledgers) returns nothing for cash payments recorded this way \u2014 silence that reads as compliance. The same defect also collapses multiple bank accounts onto one ledger on the document path, even though bank_accounts.coa_account_id already gives per-account resolution on the statement path (bank_posting_service.py:87-91)."
  },
  {
   "kind": "single",
   "finding_id": "ACC-02",
   "lens": "REACH",
   "refuted": false,
   "verdict_reason": "Every cited line checks out and the path is live, so the claim survives.\n\nGL account selection is unconditional. `services/phase2_journal_service.py:210` (`receipt_journal_lines`) and `:768` (`journal_for_purchase_payment`) both resolve `_find_account(..., \"%Bank%\", system_key=\"bank\")`; `services/receipt_service.py:178` (foreign receipt) and `services/purchase_payment_service.py:443` (foreign payment) do the same. `_find_account` (phase2_journal_service.py:1404-1464) resolves `system_account_key='bank'` FIRM-WIDE, then falls back to an `account_name ILIKE '%Bank%'` \u2014 there is no per-transaction input at all.\n\n`payment_mode` is captured and stored and then dropped. `routers/purchase_payments.py:372` reads it, `:390`/`:475` persist it, but `:455-463` builds `payment_dict = {id, payment_no, payment_date, amount_paise}` and passes only that to `journal_for_purchase_payment` \u2014 the mode never reaches the journal. Same for receipts (`receipt_service.py:566`, `:634` store it; `_settle_receipt_via_atomic_rpc` at `:397` just calls `receipt_journal_lines`). Grep across `services/`, `routers/`, `domain/` finds no `system_key=\"cash\"` and no cash-account resolution on any automated flow. \"Cash in Hand\" (1001) and \"Petty Cash\" (1002) exist only as seeds (`services/coa_seed_service.py:51-52`).\n\nThe auditor's one soft spot \u2014 `receipt_journal_lines`' own docstring says it is \"never [reached] in production\" \u2014 is not a refutation: that disclaimer is about `journal_for_receipt`, and the production atomic path `_settle_receipt_via_atomic_rpc` (`services/receipt_service.py:397`) calls `receipt_journal_lines` directly to build the very same `%Bank%` line.\n\nREACH is real, not dead code. `main.py:309` mounts `receipts.router` and `:318` mounts `purchase_payments.router`, both under `_CLIENT_GUARD`. The frontend posts to the mounted endpoint (`apps/web/app/clients/[id]/sales/page.tsx:3220`, `:3469` \u2192 `/api/receipts/`) and offers cash explicitly at `sales/page.tsx:165` and `purchases/page.tsx:2219` (`[\"bank\",\"cash\",\"cheque\",\"upi\",\"neft\",\"rtgs\"]`), plus `components/invoices/InvoiceViewDrawer.tsx:26` and `components/purchases/PurchaseBillViewDrawer.tsx:28`. No feature flag gates it.\n\nTwo things I found that the auditor did not, both aggravating: (1) `models/invoices.py:399-400` and `:450-451` already accept `bank_account_id: Optional[str]` on both `ReceiptIn` and `PurchasePaymentIn`, and grep shows it is referenced NOWHERE in `routers/receipts.py`, `routers/purchase_payments.py`, `services/receipt_service.py` or `services/purchase_payment_service.py` \u2014 the API accepts an account selection and silently discards it, so even choosing between two real bank ledgers does not work. (2) There is a statutory consequence beyond presentation: `migrations/288_s40a3_detects_payments_not_receipts.sql:86-97` detects \u00a740A(3) disallowable cash expenditure by scanning `credit_paise` on Asset accounts matching `%cash%`. Because no automated vendor payment ever credits a cash account, a \u20b940,000 cash payment is invisible to that detector \u2014 and migration 288's own header says a clean run \"read as a clean bill of health on the real exposure\". So the same bug that misplaces the ledger also blinds the disallowance scan.",
   "corrected_severity": "high",
   "what_is_actually_true": "Confirmed as written, with the fix scope slightly larger than claimed.\n\nEvery automated receipt and vendor-payment journal \u2014 INR and foreign, mock and atomic-RPC production paths \u2014 debits/credits the single firm-wide account resolved by `system_account_key='bank'` (or `account_name ILIKE '%Bank%'`). `payment_mode` is validated, stored and CHECK-constrained (migration 050, widened by 161) but never consulted when choosing the GL account, and `routers/purchase_payments.py:455-463` demonstrably strips it out of the dict handed to the journal builder. So a receipt or payment marked \"cash\" moves the Bank ledger and leaves Cash in Hand at nil.\n\nThree consequences, in increasing order of how hard they are to notice:\n- the bank ledger drifts from the passbook by every cash transaction and bank reconciliation cannot close;\n- the Balance Sheet's Cash & Cash Equivalents total is right but its composition is wrong, and the cash-flow statement inherits the same ledger;\n- \u00a740A(3) auto-detection (`public.get_cash_payments_above_threshold`, migration 288) reads credits on cash-named Asset accounts, so cash payments recorded through this flow are never surfaced as disallowable expenditure \u2014 the CA sees a clean run that means nothing.\n\nCorrection to the suggested fix: the schema and the request models are further along than the finding assumes. `bank_accounts.coa_account_id` already exists (migration 093:135), and both `ReceiptIn` and `PurchasePaymentIn` already carry an unused `bank_account_id` field. The work is therefore mostly to make the journal builders take an explicit settlement-account argument (defaulted from `payment_mode` for 'cash'/'petty cash', and from `bank_account_id \u2192 coa_account_id` where supplied) rather than to add new columns from scratch \u2014 and, per CLAUDE.md, the posting still goes through `_create_journal` only. Existing rows should be listed for the CA, not silently re-pointed: a posted entry's correction is an append-only reversal.\n\nNot refuted, and not downgraded \u2014 severity stays high."
  },
  {
   "kind": "single",
   "finding_id": "ACC-03",
   "lens": "LITERAL",
   "refuted": false,
   "verdict_reason": "Every cited line exists and does what the finding says. (1) apps/api/services/bank_posting_service.py:68-105 `_resolve_bank` reads `bank_statements.bank_account_id` \u2192 `bank_accounts.coa_account_id` (lines 87-91: `db.table(\"bank_accounts\").select(\"coa_account_id\")\u2026 return ba[\"coa_account_id\"]`) and only falls back to `phase2_journal_service._find_account(db, firm_id, client_id, \"%Bank%\", system_key=\"bank\")` at line 104 \u2014 with an in-code comment that the fallback \"posts and balances fine but potentially to the WRONG bank sub-ledger, corrupting that specific account's balance/reconciliation\". (2) apps/api/services/phase2_journal_service.py:210 (`receipt_journal_lines`) and :765-767 (`journal_for_purchase_payment`) call `_find_account(..., \"%Bank%\", system_key=\"bank\")` unconditionally \u2014 no bank-account parameter exists on either. (3) That receipt helper IS the production path: services/receipt_service.py:397 (`_settle_receipt_via_atomic_rpc`, the migration-160 atomic INR path) builds its lines from `receipt_journal_lines`; the multi-currency path does the same generic lookup itself at receipt_service.py:178. Vendor payments reach `journal_for_purchase_payment` from routers/purchase_payments.py:462. (4) The schema confirms there is nowhere to record the bank: tests/fixtures/production_schema_2026-09-03.json lists `receipts` and `purchase_payments` with `payment_mode` but no bank/coa reference, while `bank_accounts` carries `coa_account_id`; `grep` over apps/api/migrations shows no later migration adding one, and no apps/web receipt/payment form references `bank_account_id` (only banking/payroll components do). (5) apps/api/services/opening_balance_service.py:243-256 does post per-bank (`bank_by_account[coa] += amt`), using the generic `%Bank%` only for accounts with a NULL `coa_account_id`. So the asymmetry the finding describes \u2014 opening balances and statement lines per-bank, receipts/vendor payments generic \u2014 is literal.",
   "corrected_severity": "high",
   "what_is_actually_true": "Confirmed as written, with two clarifications that make it slightly worse rather than better. First, `_find_account` (phase2_journal_service.py:1404-1467) resolves `system_key=\"bank\"` with `.eq(\"firm_id\", \u2026).eq(\"system_account_key\",\"bank\").limit(1)` \u2014 firm-wide, NOT client-scoped and with no ORDER BY. So the \"generic Bank Account\" is not merely one per client: it is one arbitrary row per FIRM, shared across every client, and migration 092_coa_system_account_key.sql:53-57 stamped `system_account_key='bank'` on EVERY active Asset account whose name or subtype matches '%bank%' or '%cash%'. A firm that already has \"HDFC Bank\" and \"ICICI Bank\" ledgers therefore has several rows carrying the key, and `limit(1)` picks a non-deterministic one \u2014 receipts can land in a real but wrong bank ledger, which is harder to spot than a third generic line. Only the name-pattern fallback (line 1449) is client-or-NULL scoped. Second, the same generic resolution is used for cash receipts (`receipts.payment_mode` can be cash) \u2014 nothing routes a cash receipt to a Cash ledger either. What the finding does NOT claim and is also true: the GL still balances and the firm's total bank asset is right; the damage is per-account (no bank ledger ties to its passbook, per-account BRS cannot be tied to the GL, Balance Sheet shows a phantom bank line). The suggested fix is sound \u2014 a `payment_account_id` on `receipts`/`purchase_payments` populated from the chosen `bank_accounts.coa_account_id`, with bank_posting_service and the receipt/payment paths sharing one resolver."
  },
  {
   "kind": "single",
   "finding_id": "ACC-03",
   "lens": "GUARDS",
   "refuted": false,
   "verdict_reason": "Confirmed by reading the code, and no guard exists. services/phase2_journal_service.py:210 (receipt_journal_lines) and :768 (journal_for_purchase_payment) call _find_account(..., \"%Bank%\", system_key=\"bank\"); _find_account at phase2_journal_service.py:1404-1456 selects chart_of_accounts by firm_id + system_account_key='bank' with limit(1) and takes no bank-account argument at all. services/coa_seed_service.py:53 seeds that key's row as (\"1101\",\"Bank Account\",\"Asset\",\"Bank\") with client_id NULL (line 136) \u2014 one generic firm-wide ledger. Meanwhile routers/banking.py:446-460 (_ensure_bank_ledger, plus _bank_ledger_conflict at :182-196) creates and enforces a DEDICATED coa row per bank account, with the comment \"One ledger per bank ... which is how two banks ended up sharing 1101 and showing as one line\" \u2014 and those rows are inserted with no system_account_key (banking.py:246-251), so the receipt/payment path can never reach them. services/bank_posting_service.py:85-91 resolves bank_accounts.coa_account_id and only falls back to the generic at :104; services/opening_balance_service.py:240-256 posts each linked bank into its own coa_account_id and aggregates only UNLINKED banks into the generic. tests/fixtures/production_schema_2026-09-03.json confirms receipts and purchase_payments have no bank reference column while bank_accounts has coa_account_id. GUARDS lens found nothing: no CHECK/trigger/migration on this, no test asserting the receipt/payment bank leg (searched apps/api/tests for the bank system key \u2014 hits are cash-flow, ledger and parity tests only), no frontend bank picker on the receipt/payment forms (bank_account_id in apps/web appears only under components/banking and payroll's DisburseModal.tsx:48, which correctly filters to accounts with coa_account_id), and no architecture doc recording the generic posting as deliberate. Only bound found: the aggregate is not lost \u2014 entries still balance and the sum of all bank lines is right \u2014 so this is misattribution across sub-ledgers, correctable by reclassification journals, not a wrong total.",
   "corrected_severity": "high",
   "what_is_actually_true": "The finding is correct and is in fact broader than stated. Every non-statement cash leg resolves the single firm-wide generic \"Bank Account\" (COA 1101, client_id NULL): receipts (phase2_journal_service.py:210 via the PRODUCTION atomic path receipt_service.py:397 -> _settle_receipt_via_atomic_rpc, so the \"never in production\" caveat in receipt_journal_lines' docstring does not exempt it), vendor payments (phase2_journal_service.py:768), the foreign-currency receipt path (receipt_service.py:178), purchase_payment_service.py:443, routers/purchase_payments.py:639, and phase2_journal_service.py:1191 and :1296. Bank-statement posting (bank_posting_service.py:85-91) and opening balances (opening_balance_service.py:246-253) resolve the real per-bank coa_account_id, and routers/banking.py auto-creates and uniqueness-guards one ledger per bank account, so the per-bank ledgers definitely exist and are populated by two paths and bypassed by all the others. Consequences as described: no individual bank ledger ties to its passbook, the Balance Sheet carries an extra generic Bank line beside the per-bank ones, and per-account BRS cannot be tied to the GL. Two corrections/additions to the finding: (1) the misposting is a sub-ledger attribution error, not a balancing or total error \u2014 the sum across the bank lines is still right and is correctable by reclassification journals, which is the only thing bounding severity; (2) the same code path also debits the Bank ledger for receipts and payments recorded with payment_mode='cash' (the CHECK on receipts.payment_mode in migration 050 allows 'cash'), so cash transactions never reach Cash in Hand either \u2014 the fix should resolve the leg from the chosen bank/cash account, not just from a bank account id."
  },
  {
   "kind": "single",
   "finding_id": "ACC-03",
   "lens": "REACH",
   "refuted": false,
   "verdict_reason": "Confirmed by reading the code. (1) Divergence is real: apps/api/services/bank_posting_service.py:82-91 reads bank_statements.bank_account_id \u2192 bank_accounts.coa_account_id and returns that per-bank ledger, falling back to the generic resolver only at :104. apps/api/services/opening_balance_service.py:243-256 does the same \u2014 it builds bank_by_account keyed on each bank's coa_account_id and only routes an *unlinked* bank's balance to _find_account(\"%Bank%\", system_key=\"bank\"). (2) The receipt and vendor-payment paths never look at bank_accounts at all: phase2_journal_service.py:210 (receipt_journal_lines) and :766-769 (journal_for_purchase_payment) call _find_account(..., \"%Bank%\", system_key=\"bank\") unconditionally, as do services/receipt_service.py:178, services/purchase_payment_service.py:443 and routers/purchase_payments.py:639. (3) It is the production path, not the dead mock path: receipt_journal_lines' own docstring says journal_for_receipt is mock-only, but services/receipt_service.py:397 calls receipt_journal_lines directly to build the lines it hands to the settle_receipt_atomic RPC at :428 \u2014 that is the live route. (4) No column exists to carry the choice: tests/fixtures/production_schema_2026-09-03.json shows receipts and purchase_payments with payment_mode but no bank/payment account column, and no migration adds one (grep over migrations/*.sql for bank_account_id on those tables returns nothing), while bank_accounts does carry coa_account_id. (5) REACH: routers/receipts.py and routers/purchase_payments.py are both mounted in main.py:309/318 under _CLIENT_GUARD, and the frontend posts to them with no bank field \u2014 apps/web/components/invoices/InvoiceViewDrawer.tsx:469-478 and apps/web/app/clients/[id]/sales/page.tsx:3220/3469 send client_id, customer_id, amount_paise, payment_mode, allocations only. (6) The resolver is worse than \"one generic account\", not better: _find_account (phase2_journal_service.py:1404-1467) does a firm-wide, non-client-scoped system_account_key lookup with .limit(1) and no ordering, then an ILIKE '%Bank%' fallback with .limit(1). Migration 092_coa_system_account_key.sql:54-57 stamps key 'bank' on every Asset row matching bank OR cash (so \"Cash in Hand\" is a candidate), and it is a one-time backfill \u2014 services/coa_seed_service.py sets no system_account_key at all, so firms onboarded after 092 have none and fall to the name pattern, which matches \"Bank Account\" (1101), the per-bank ledgers created by routers/banking.py:246-251 (\"HDFC Bank \u2014 7890\"), and \"Bank Charges\" (Expense 5005, coa_seed_service.py:100) alike, unordered. Nothing refutes the finding; the only correction is that the destination is arbitrary rather than reliably one generic account.",
   "corrected_severity": "high",
   "what_is_actually_true": "Receipts and vendor payments never resolve the bank the money actually moved through \u2014 receipts/purchase_payments have no bank_account_id column and the UI sends none \u2014 so every one of them posts its cash leg to whatever single row _find_account(\"%Bank%\", system_key=\"bank\") happens to return, while bank-statement postings (bank_posting_service.py:87-91) and bank opening balances (opening_balance_service.py:243-252) post to the correct per-bank coa_account_id. Two clarifications sharpen rather than soften the finding: (a) the destination is not a stable \"generic Bank Account\" \u2014 the system_key lookup is firm-wide, unordered, .limit(1), migration 092 stamped key 'bank' onto Cash accounts too, and coa_seed_service sets no keys at all, so newly seeded firms fall through to an unordered ILIKE '%Bank%' that can match a real per-bank ledger or even the \"Bank Charges\" expense account; (b) the same generic resolution is used by fixed-asset acquisition and disposal (phase2_journal_service.py:1191, :1296), so the mismatch is broader than receipts and vendor payments. Net effect is as described: no per-bank GL ledger ties to its passbook, the Balance Sheet shows a bank line the client does not have, and per-account BRS cannot be tied to the GL."
  },
  {
   "kind": "single",
   "finding_id": "ACC-04",
   "lens": "LITERAL",
   "refuted": false,
   "verdict_reason": "Every citation checks out literally. (1) apps/api/routers/accounting.py:452-490 \u2014 `@router.patch(\"/journal/{entry_id}\")` (router prefix `/api/accounting`, line 46; mounted in main.py:243) does only `_prod_db()`, `_assert_journal_scope_db` (accounting.py:666-686, which selects nothing but `client_id` and checks firm + client assignment), then calls `manual_journal_service.update`. No source_type or entry_type filter anywhere on the path. (2) apps/api/services/manual_journal_service.py:185-297 \u2014 `update()` reads the entry via `get()` (lines 148-165, which SELECTs `source_type` only to echo it back), refuses on `is_reversed` (211-214), on `lock_reason` (215-216), validates \u22652 lines and Dr=Cr (218-231), re-checks the moved date (234-240), then calls `db.rpc(\"edit_posted_journal\", \u2026)` (249-264). `source_type` is never compared to anything; the only occurrences in that file are MANUAL_SOURCE on the CREATE path (115, 129) and the select list (150). (3) The SQL: `CREATE OR REPLACE FUNCTION public.edit_posted_journal` exists only in apps/api/migrations/266_editable_journals_until_locked_or_filed.sql (grep across migrations/ shows 267/275/276 mention the name only in comments; none redefines it). Its body (266: ~line 330-441) locks the row on `id/firm_id/client_id/deleted_at`, raises on `is_reversed`, calls `journal_period_lock_reason` for the old and new date, checks count\u22652 and `v_debit <> v_credit`, then sets `app.journal_edit`, UPDATEs the header, DELETEs and re-INSERTs the lines, rebuilds the passbook. No reference to `source_type` in the whole file (grep returns nothing). (4) The DELETE path does guard it: migrations/275_\u2026sql:82 `IF COALESCE(v_entry.source_type, '') <> 'manual' THEN` with the \"\u2026or the document is left pointing at nothing\" message, mirrored at 276:224, and routers/accounting.py:538 selects `source_type` for that path. (5) Auto-posted entries really do carry a non-manual source_type \u2014 services/phase2_journal_service.py:269 `source_type=\"receipt\"`, :796 `\"purchase_payment\"`, bank_posting_service.py:561 `\"bank_transaction\"`, receipt_service.py:206, purchase_payment_service.py:467. (6) Tests: the pg test seeds `source_type` as 'manual' at test_r266_journal_edit_until_locked_pg.py:107/226/261 and the mock test at test_journal_edit_until_locked.py:130; no test anywhere exercises a non-manual entry through the edit path. (7) I confirmed it dynamically with a scratch probe reusing the repo's own _DB stub (no repo files modified): an entry with `source_type=\"sales_invoice\"` passed straight through `svc.update` and produced RPCs `['journal_period_lock_reason', 'edit_posted_journal', 'journal_period_lock_reason']` \u2014 i.e. the auto-posted entry was rewritten, not refused.",
   "corrected_severity": "high",
   "what_is_actually_true": "The defect is real and the asymmetry is exactly as described: DELETE refuses a non-manual journal in SQL with a written-for-the-CA sentence, EDIT refuses nothing of the kind \u2014 not in the router, not in manual_journal_service.update, not in edit_posted_journal. A posted sales-invoice/purchase-bill/payroll/depreciation journal can have its accounts, amounts, narration, reference and date rewritten while the source document is untouched, and the passbook is rebuilt so the reports agree with the *new* GL and silently disagree with the sub-ledger and the GSTR-1 source rows.\n\nTwo corrections to the finding's framing, neither of which rescues the code:\n1. The actor cannot be an Executive. `rbac(\"accounting\", \"write\")` is `_AT_LEAST_MANAGER` (core/permissions.py:166-169; Executive gets `read` only), so this is Manager or Partner, not delivery staff.\n2. It is not one click away in the UI. The journal list filters to `e.source_type === \"manual\"` (apps/web/app/clients/[id]/accounting/page.tsx:648-651, with a long comment saying auto-posted entries are deliberately unlistable), and `journalEditorHref` is the only link to the editor (page.tsx:606-607, 851, 867). Reaching an auto-posted entry means a hand-typed URL or a direct PATCH \u2014 but the editor page itself (app/clients/[id]/accounting/journal/[entryId]/edit/_page.tsx) loads whatever id the path names via `api.accounting.getJournalEntry` and has no source_type check, so a typed URL renders a fully working editor for it.\n\nMitigations that bound the damage but do not close the hole: the period lock and is_reversed still apply, Dr=Cr is still enforced, and the header and every line change is written to audit_log by trg_audit_capture / trg_audit_capture_line (migration 266 \u00a71). So the edit is recorded \u2014 it is just permitted, and nothing reconciles the GL against the document afterwards. The suggested fix (same `source_type = 'manual'` gate in edit_posted_journal, mirrored in the service for the message) is the right shape."
  },
  {
   "kind": "single",
   "finding_id": "ACC-04",
   "lens": "GUARDS",
   "corrected_severity": "medium",
   "refuted": false,
   "verdict_reason": "The mechanism is confirmed at every layer I could read. `PATCH /api/accounting/journal/{entry_id}` (apps/api/routers/accounting.py:452-490) guards only firm/client scope via `_assert_journal_scope_db` (routers/accounting.py:666-686 \u2014 selects `client_id` only, no `source_type`) and then calls `manual_journal_service.update` for any entry. That method (services/manual_journal_service.py:184-297) checks `is_reversed`, `lock_reason`, line count, Dr=Cr and the moved-into-period lock \u2014 never `source_type`. The single definition of the SQL function is migrations/266_editable_journals_until_locked_or_filed.sql:344-440 (grep confirms no other `CREATE OR REPLACE FUNCTION public.edit_posted_journal` in any migration): it locks the row `FOR UPDATE`, rejects `is_reversed`, checks both dates through `journal_period_lock_reason`, requires >=2 lines and Dr=Cr, then rewrites the lines \u2014 no `source_type` test. The DELETE path does have exactly that test (275_discard_manual_journal_until_locked.sql:82, mirrored at 276:224), and the repo's own docs state the intended rule as \"a **manual** entry may be edited or soft-deleted while its period is open\" (docs/architecture/01-accounting-engine.md:50, and CLAUDE.md's Money-and-the-GL section) \u2014 so the edit path under-enforces its own documented rule. I proved it executably without touching the repo: a scratchpad test reusing the harness in tests/test_journal_edit_until_locked.py seeded a posted entry with `source_type=None, entry_type=\"Sales\", reference_no=\"INV-0007\"` and with `source_type=\"receipt\"`; `svc.get` reported `editable: True, lock_reason: None` and `svc.update` reached `edit_posted_journal` in both cases (2 passed). GUARDS found, which bound the blast radius but do not prevent it: (1) RBAC \u2014 `accounting:write` is `_AT_LEAST_MANAGER` (core/permissions.py:166-169), so the finding's \"an executive\" narrative is wrong; an Executive gets 403. (2) No UI route reaches it \u2014 the journal list filters `e.source_type === \"manual\"` and is the only screen calling `journalEditorHref` (apps/web/app/clients/[id]/accounting/page.tsx:606-607, 867, and the \"THIS SCREEN SHOWS MANUAL ENTRIES ONLY\" comment at ~618); the ledger table has no onRowClick to the editor (page.tsx:1068-1071). Reaching it needs a hand-built URL with the entry UUID or a direct API call. (3) The period gate already blocks the worst case: `journal_period_lock_reason` (266:290-330, extended by 267) refuses once the FY is locked or any `filings` row covering the date has `filed_date` \u2014 so a GSTR-1 already filed cannot be desynced this way. (4) The edit is not silent: 266's `trg_audit_capture_line` plus `log_event` in the router record header and line before/after. (5) Detective control: `check_ar_subledger_vs_gl` / `check_ap_subledger_vs_gl` (services/reconciliation_service.py:292-349, in `_CHECKS` at 518-527) assert the Trade Receivables/Payables GL balance equals the open-document sub-ledger, run nightly per firm by jobs/scheduler.py:315-336 and persisted as findings \u2014 so the specific \"AR sub-ledger no longer ties to the control account\" harm is caught within a day. Not covered by any check: swapping the revenue account or amount within an unfiled period without touching the control account (trial-balance check still passes because edit_posted_journal enforces Dr=Cr), and GST-account edits surface only later as a books-vs-ledger difference during GSTR-3B prep. Also: most auto-posted journals (sales invoice at phase2_journal_service.py:160-167, purchase bill 728-737, payroll, depreciation, credit/debit notes) pass NO source_type at all, i.e. NULL \u2014 the suggested `source_type = 'manual'` guard must be written as 275's `COALESCE(source_type,'') <> 'manual'` to catch them.",
   "what_is_actually_true": "Confirmed: nothing on the edit path \u2014 router, service, or `edit_posted_journal` \u2014 looks at `source_type`, so a posted auto-generated journal (invoice, bill, payroll, depreciation, receipt, bank) can be rewritten through `PATCH /api/accounting/journal/{id}` whenever its period is still open, decoupling the GL from its source document. This contradicts the delete path's explicit guard (migration 275:82) and the repo's own documented rule. Two corrections to the finding: (a) the actor cannot be an Executive \u2014 `accounting:write` is Manager+, and there is no UI route to an auto-posted entry's editor (the journal list filters to `source_type === 'manual'`), so it takes a hand-built URL or a direct API call by a Manager/Partner; (b) it is not unrecorded or undetected \u2014 266's audit triggers plus `log_event` capture the change, a filed return or locked FY already blocks the edit outright, and the nightly reconciliation sweep's `check_ar_subledger_vs_gl` / `check_ap_subledger_vs_gl` detect control-account divergence within a day. What remains genuinely uncaught is an edit that changes the revenue/expense or GST account inside an unfiled period, which stays balanced and so passes `check_trial_balance`. Severity medium, not high. The suggested fix is right but must use `COALESCE(source_type,'') <> 'manual'`, since invoice/bill/payroll journals carry NULL rather than a named source."
  },
  {
   "kind": "single",
   "finding_id": "ACC-04",
   "lens": "REACH",
   "corrected_severity": "medium",
   "refuted": false,
   "verdict_reason": "The mechanism is exactly as claimed and I could not refute any link in the chain. (1) apps/api/routers/accounting.py:452-490 \u2014 `PATCH /api/accounting/journal/{entry_id}` guards only firm/client scope via `_assert_journal_scope_db` (routers/accounting.py:666-686, which selects only `client_id`) and rbac(\"accounting\",\"write\"); it then calls manual_journal_service.update for any entry id. (2) apps/api/services/manual_journal_service.py:184-297 \u2014 `update` checks `is_reversed`, `lock_reason`, line count, Dr=Cr and the moved-into-period lock; `source_type` is SELECTed in `get` (line 150) and never tested anywhere in `update`. (3) migrations/266_editable_journals_until_locked_or_filed.sql:344-442 \u2014 `edit_posted_journal` checks id/firm/client/deleted_at (363-370), is_reversed (371-373), both dates against journal_period_lock_reason (380-385), >=2 lines and Dr=Cr (393-401), then rewrites the lines. `grep -n source_type migrations/266_...sql` returns nothing, and the function is never CREATE OR REPLACEd again (only 266 defines it; 267/275/276 merely reference it in comments). (4) The asymmetry is real: migrations/275:82 and migrations/276:224 both do `IF COALESCE(v_target.source_type,'') <> 'manual' THEN` with the \"an auto-posted journal is corrected by correcting its document\u2026\" message on the DELETE path. (5) No test covers the auto-posted case \u2014 tests/test_r266_journal_edit_until_locked_pg.py:105,224,258 and tests/test_journal_edit_until_locked.py:130 all seed `source_type='manual'`. (6) Reach: `accounting.router` is mounted in main.py:243; the frontend calls this exact path (apps/web/lib/api/index.ts:743) from apps/web/app/clients/[id]/accounting/journal/[entryId]/edit/_page.tsx, which loads whatever entryId is in the URL and reads no `source_type` (the only source_type reference on the web side is the type declaration at lib/api/index.ts:117). The `editable` flag returned by `get` is `lock_reason is None and not is_reversed` \u2014 it does not consider source_type either, so the editor renders a fully functional form for an auto-posted entry.",
   "what_is_actually_true": "The gap is genuine: nothing on the edit path \u2014 router, service, or `edit_posted_journal` \u2014 restricts the correction to `source_type = 'manual'`, while `discard_posted_journal` (275/276) explicitly does. An authenticated staff member with accounting:write and access to the client can rewrite the lines, amounts, accounts, date and narration of the GL entry behind a sales invoice, purchase bill, receipt, payment, bank posting, depreciation run, opening balance or trial-balance import, provided the period is open and the entry is not reversed. Two corrections to the finding, both of which lower the practical severity rather than the technical one. First, reach is API-shaped, not click-shaped: the journal list deliberately filters to `e.source_type === \"manual\"` (apps/web/app/clients/[id]/accounting/page.tsx:648-651) and `journalEditorHref` (line 606) is only ever called from that filtered table (line 867) or with \"new\" (851); the Recent Entries block on the overview tab does load unfiltered rows (page.tsx:383-388) but renders them as non-clickable <tr>s (489-511). So the \"opens it by URL\" scenario needs a UUID obtained out-of-band (PostgREST from the browser, devtools, a script) or a direct API call \u2014 it is not a mis-click an executive stumbles into, which is why I put this at medium rather than high. Second, a detail of the evidence: the sales-invoice journal is created with source_type NULL, not a tag \u2014 services/phase2_journal_service.py:159-169 passes no `source_type` to `_create_journal` (receipts, purchase payments, bank postings, opening balances and TB imports do, at 269, 796, 561, 360, 232). That does not weaken the fix, since migration 275's `COALESCE(source_type,'') <> 'manual'` already treats NULL as non-manual, but it does mean an untagged auto entry is invisible to any guard written as `source_type <> 'manual'` without the COALESCE. Mitigations that keep this below \"silent corruption\": the edit is fully audit-logged (audit_capture_journal_line trigger, 266:78-127, plus log_event at accounting.py:487), it is atomic, it re-asserts Dr=Cr, and it rebuilds the passbook with apb_assert_no_drift (266:430-431). What it does not do is keep the GL tied to client_sales_invoices \u2014 the source document is left saying something different, with nothing recording why. The suggested fix (mirror 275's guard into edit_posted_journal and into manual_journal_service.update so the CA gets the sentence rather than a SQLSTATE) is the right shape."
  },
  {
   "kind": "single",
   "finding_id": "ACC-08",
   "lens": "LITERAL",
   "refuted": false,
   "verdict_reason": "Mechanism confirmed line by line. domain/reporting/service.py:218-233 \u2014 `trial_balance(self, firm_id, client_id, as_of_date, basis)` has no start parameter; the legacy path calls `self.source.snapshot(firm_id, client_id, None, as_of)` (:223) and the fast path `self._passbook_accrual_lines(firm_id, client_id, None, as_of)` (:230). `_in_range` (domain/reporting/sources.py:36-41) treats `start=None` as no lower bound, and domain/reporting/balance_cache.py:131-132 states outright that \"start=None means 'from the beginning' (cumulative, for a Balance Sheet / Trial Balance as-of-date)\". builders.trial_balance (domain/reporting/builders.py:103-151) sums every projected line per account and nets it \u2014 no account-type branch, so income/expense rows are cumulative exactly like assets. routers/accounting.py:894-911 exposes only `as_of_date`, `client_id`, `basis`. The screen: apps/web/app/clients/[id]/accounting/page.tsx:1151-1157 builds `period` from the picker, :1158 `const asOf = period.end;`, :1176 calls `trialBalance({ basis, as_of_date: asOf, client_id })` \u2014 start discarded. The FY Excel export does the same: page.tsx:3176-3180 destructures `{ start, end } = fyDateRange(financialYear)` and then passes only `as_of_date: end` for the sheet named \"Trial Balance\", while the P&L branch at :3194 passes both start and end. No closing entries exist: builders.py:506-517 synthesises \"Retained Earnings / Net Profit\" from `income_net - expense_net` over all balances, and services/year_end_financial_service.py:216-232 says explicitly that nothing marks a closing entry and it derives rather than posts one. So for a client in year two the Sales row on the TB does carry both years. Two parts of the finding are wrong, though. (1) \"Every Schedule III and tax working built off that TB is wrong\" is refuted: the year-end statements do not read this endpoint at all \u2014 services/year_end_financial_service.py:455-470 splits the one fetch into `fy_window_lines` (>= fy_start, for P&L) and `cumulative_lines` (<= fy_end, for the balance sheet), with a comment naming the \"F10\" bug of pairing those wrongly; profit_loss/cash_flow in the same service take start_date. (2) The screen is not silent about being point-in-time: page.tsx:1141-1148 carries a comment that \"a trial balance is a point-in-time statement, so the picker's END date is the whole of it\", the picker's ariaLabel is \"As at\" (:1223), and the header renders \"Trial Balance \u2014 as at {asOf}\" (:1216). apps/web/scripts/report-period-pickers.test.ts:155-175 records TrialBalance as deliberately \"cleared\" on that basis.",
   "corrected_severity": "medium",
   "what_is_actually_true": "The Trial Balance really is inception-to-date and there is no closing-entry mechanism, so in year two a P&L row shows both years' turnover; the FY Excel export inherits it (only `end` is sent). It stays balanced, so nothing flags it. What the finding overstates is the blast radius and the intent: the product's own P&L, Balance Sheet, cash flow and Schedule III / year-end statements window P&L by FY themselves (year_end_financial_service.py:455-470) and are unaffected, so no statutory output the product generates is wrong. The screen and the API also present it honestly as an \"as at\" statement \u2014 the design decision is written down, not an oversight \u2014 so the defect is that a period trial balance (what Tally's F2 view, Winman and CompuTax expect as input, and what an auditor's working paper means) cannot be produced at all, rather than that a correct-looking report silently corrupts filings. Effort estimate (\"days\") and the suggested fix \u2014 add start_date, show opening + movement + closing for balance-sheet accounts and movement only for P&L \u2014 are sound; note the export path at page.tsx:3176 must be changed with it."
  },
  {
   "kind": "single",
   "finding_id": "ACC-08",
   "lens": "GUARDS",
   "refuted": false,
   "verdict_reason": "Mechanically confirmed, and I reproduced the exact scenario. domain/reporting/service.py:218-233 `trial_balance(firm_id, client_id, as_of_date, basis)` has no start parameter and passes `None` as the window start to both `self.source.snapshot(...)` and `_passbook_accrual_lines(...)`; routers/accounting.py:894-911 exposes only `as_of_date`; apps/web/app/clients/[id]/accounting/page.tsx:1154-1158 computes `period` from the picker then keeps only `const asOf = period.end`, and :1176 sends `as_of_date: asOf`. Probe (in-memory source, two entries \u2014 Sales 1.1cr dated 2025-06-01 and 80L dated 2026-06-01): `trial_balance(as_of=\"2027-03-31\")` returns Sales credit 1900000000 paise (\u20b91.9cr) while `profit_loss(\"2026-04-01\",\"2027-03-31\")` returns 800000000 (\u20b980L). The finding's illustrative numbers are literally what the code produces. GUARDS lens found NOTHING that prevents or catches it: no test asserts period scoping (tests/test_trial_balance_domain.py is the CSV *import* validator; tests/test_reporting_builders_ist_fallback.py only tests the as-of default; tests/test_reporting_reads_are_bounded.py:139 only checks row counts), no migration, trigger, CHECK, RLS policy or Pydantic validator touches report scoping, and services/reconciliation_service.py:114 `check_trial_balance` is an unrelated debits==credits invariant on posted entries. Two corrections to the finding, though. (1) The blast-radius sentence is wrong: nothing statutory is computed from this TB. service.py:339-350 `schedule_iii` calls `profit_loss(start, end)` and `balance_sheet(end)` directly \u2014 it never reads `trial_balance` \u2014 and `profit_loss` (service.py:303-320) IS period-scoped. Grep across services/ and routers/ finds no other consumer of `ReportingService.trial_balance`. So \"Every Schedule III and tax working built off that TB is wrong\" does not hold. (2) builders.py:506-517 is cited as if it were part of the defect; the inception-to-date `net_profit` posted as \"Retained Earnings / Net Profit\" is the CORRECT balance-sheet treatment when no closing entries exist (accumulated surplus = cumulative profit). It only establishes the premise that P&L accounts are never zeroed, which is true. Partly mitigating: the screen is labelled honestly \u2014 page.tsx:1215 renders \"Trial Balance \u2014 as at {asOf}\" and the PeriodPicker carries `ariaLabel=\"As at\"`, with a comment at :1141-1147 saying the end date is deliberately the whole of it. The genuinely misleading artefact is the export path, which has no such label: page.tsx:3179-3181 requests `as_of_date: end` (the FY end), :3244 writes `Trial-Balance-FY2026-27-Accrual.xlsx`, and :3259-3260 shares it to the CLIENT portal as \"Trial Balance (Accrual) \u2014 FY 2026-27\" with a `financial_year` column \u2014 an FY-titled workbook whose revenue rows are inception-to-date, sitting beside a P&L export for the same FY that is period-correct.",
   "corrected_severity": "medium",
   "what_is_actually_true": "The Trial Balance is inception-to-date and the FY picker only moves the as-at date \u2014 that part of the finding is exactly right and reproducible (Sales \u20b91.9cr on a TB as at 31-03-2027 where the year's sales were \u20b980L), and no test, constraint, trigger or validator anywhere bounds it. But the consequences are narrower than claimed. No downstream statutory output reads this report: Schedule III (service.py:339-350), P&L, Balance Sheet and Cash Flow each take their own start/end and are period-correct, so no filing or tax working is corrupted by it. The Balance Sheet's cumulative \"Retained Earnings / Net Profit\" (builders.py:506-517) is correct accounting, not a second bug. On screen the report is truthfully labelled \"as at {date}\", so a CA reading it is not told it is a year's figures. The real user-facing harm is confined to the export and client-portal share path (page.tsx:3244, 3259-3260), which titles an inception-to-date sheet \"Trial Balance (Accrual) \u2014 FY 2026-27\" and stores it with a financial_year, and to the general absence of the period-scoped TB (opening + movement + closing for balance-sheet accounts, period movement for P&L accounts) that Tally, Zoho, Winman and CompuTax all assume. The suggested fix is the right shape; the effort estimate holds."
  },
  {
   "kind": "single",
   "finding_id": "ACC-08",
   "lens": "REACH",
   "refuted": false,
   "verdict_reason": "Every cited line checks out and I could not refute the mechanism. domain/reporting/service.py:218-232 \u2014 `trial_balance(firm_id, client_id, as_of_date, basis)` has no start parameter, and BOTH paths pass `None` as the window start (`self.source.snapshot(firm_id, client_id, None, as_of)` at :223 and `self._passbook_accrual_lines(firm_id, client_id, None, as_of)` at :230). domain/reporting/builders.py:103-151 then sums every line into one net per account with no period notion, so nominal accounts carry inception-to-date turnover. routers/accounting.py:894-911 exposes only `as_of_date`/`client_id`/`basis`. The frontend confirms the discard: apps/web/app/clients/[id]/accounting/page.tsx:1153-1158 computes `period` from `splitPeriodColumns(...)` and keeps only `const asOf = period.end;`, then :1176 calls `trialBalance({ basis, as_of_date: asOf, client_id })`. And nothing closes the nominal accounts: builders.py:505-517 synthesises `net_profit = income_net - expense_net` over ALL balances as a \"Retained Earnings / Net Profit\" equity row, and services/year_end_financial_service.py:222-251 states expressly that no field marks a closing entry (`entry_type` is CHECK-constrained by migration 003 to Sales/Purchase/Payment/Receipt/Journal/Contra/Opening) and that hand-posted closes are only heuristically DETECTED and reported, never generated; :637-645 records that deriving rather than posting a close is deliberate. So in year two the TB's Sales row is genuinely cumulative. REACH is real: the router is mounted (main.py:59, :243), the page is in the client nav (apps/web/lib/workspace/ClientNavContext.tsx:63), no flag gates it. I also found an aggravating fact the auditor missed: page.tsx:3178-3191 builds the \"trial\" XLSX with `as_of_date: end` only, and :3243 names the file `Trial-Balance-FY{financialYear}-...xlsx` while :3258 shares it to the client portal labelled \"Trial Balance (Accrual) \u2014 FY 2026-27\" \u2014 the P&L branch of that same builder (:3244-3247) IS windowed `start_date..end_date`, so two files exported in one click disagree. What I DID refute is the blast radius: the statutory statements are not built off this TB. services/year_end_financial_service.py:556-566 aggregates P&L lines from `fy_debit_totals/fy_credit_totals` (windowed to the FY) and only the balance-sheet side cumulatively, and its `trial_balance_hash` (:158-161, :208) is struck over balance-sheet lines only; ReportingService.profit_loss (:303-311), schedule_iii (:339-350) and cash_flow_statement (:471-) all take start/end. So \"Every Schedule III and tax working built off that TB is wrong\" is false. Mitigating, but not exculpating: the screen labels itself \"Trial Balance \u2014 as at {asOf}\" (page.tsx:1215) with the picker's ariaLabel \"As at\" (:1223), and page.tsx:1142-1147 argues the as-at shape deliberately \u2014 so it is a design decision, not an oversight, and the exported/shared \"FY\"-labelled copy is where it stops being honest.",
   "corrected_severity": "medium",
   "what_is_actually_true": "The mechanism is exactly as claimed: `ReportingService.trial_balance` accepts only an as-of date, passes `None` as the window start on both the legacy and passbook paths, and no year-end closing journal is ever posted (the balance sheet synthesises retained earnings instead, by explicit design). So from year two onwards the Trial Balance's income and expense rows are inception-to-date, and picking an FY only moves the as-at date. It reaches real users \u2014 router mounted at main.py:243, tab linked from ClientNavContext.tsx:63, no feature flag \u2014 and it is worse in the export hub than the auditor noticed: `buildReportSheet(\"trial\")` (page.tsx:3178) is written to a file named `Trial-Balance-FY2026-27.xlsx` and shared to the client portal as \"Trial Balance (Accrual) \u2014 FY 2026-27\", while the P&L exported by the same function IS FY-windowed, so the two disagree by every prior year's revenue. What is overstated is the consequence. No statutory output is computed from this report: the year-end Schedule III path strikes P&L lines from FY-windowed totals and only the balance sheet cumulatively (year_end_financial_service.py:556-566), and profit_loss / schedule_iii / cash_flow_statement all take start and end dates. The damage is therefore a misleading management report and a misleadingly-labelled shared file that will not tie to the P&L \u2014 a real defect a CA would hit and could not explain, but not a wrong return, wrong ledger or wrong filing. Medium rather than high; the suggested fix (add `start_date`, show balance-sheet accounts as opening + movement + closing and P&L accounts as period movement) is the right shape, and the FY-labelled XLSX/portal export should be fixed in the same change."
  },
  {
   "kind": "single",
   "finding_id": "ACC-10",
   "lens": "LITERAL",
   "refuted": false,
   "corrected_severity": "high",
   "verdict_reason": "Every cited line checks out. apps/web/app/accounting/schedule-iii-mapping/page.tsx:46 selects chart_of_accounts.schedule_iii_mapping over PostgREST, :82 renders the \"N of M active accounts mapped\" count, :93 says \"Set schedule_iii_mapping during import (Import COA) to assign a mapping\", and the file contains no .update/.insert/.upsert at all \u2014 it is strictly read-only (155 lines, read end to end). It is reachable and linked from components/panels/AccountingPanel.tsx:36. `grep -rn schedule_iii_mapping --include=*.py` over the repo returns ZERO non-test hits: the only Python occurrences are apps/api/tests/test_coa_architecture.py (asserting on a hand-built dict, not a production path) and migrations/057_coa_architecture_correction.sql:8,15 which merely add the column and its index. The only writer anywhere is the frontend CSV import (apps/web/app/accounting/coa-import/page.tsx:171); the only other reader is the CSV export (coa-export/page.tsx:74). Meanwhile apps/api/domain/reporting/schedule_iii.py:22-80 bs_bucket does exactly `sub = (account_subtype or \"\").lower()` then substring-matches 'trade payable', 'creditor', 'payable', 'short term', 'term loan', 'intangible', 'inventor', 'receivable', 'cash', 'advance', falling through to `return \"Other Current Assets\"` / `\"Other Current Liabilities\"`; pl_bucket (:83+) does the same on 'material', 'salary', 'finance', 'depreciation'. apps/web/app/clients/[id]/accounting/page.tsx:2107-2109 calls the TS twin bsBucket(b.account_type, b.account_subtype) \u2014 also subtype, never the mapping column. The year-end path is indeed a third store: services/year_end_financial_service.py:392 and :502-505 load account_group_mappings (account_id \u2192 schedule_line), and routers/year_end_statements.py:365 queries the same table. The coa_seed_service.py:38-47 comment quoted in the evidence is verbatim, including \"there is no shared/enforced vocabulary between the two.\"",
   "what_is_actually_true": "Confirmed, with two corrections and one strengthening.\n\nCORRECTION 1 \u2014 the screen is not merely ineffective, it is READ-ONLY. There is no editing control anywhere on it, so the CA cannot \"spend an afternoon on the Schedule III Mapping screen\"; the only way to populate schedule_iii_mapping is a CSV column on Import COA (coa-import/page.tsx:171), written straight to PostgREST. The wasted effort is in preparing that import file. The dead-data claim is if anything stronger: a column a CA can fill and export, that no server code ever reads.\n\nCORRECTION 2 \u2014 \"a third, different mechanism\" is true as a TABLE, but the taxonomies are not independent at birth. routers/year_end_mappings.py:21 imports bs_bucket/pl_bucket and :422-439 auto-initialises account_group_mappings from them via _schedule_line_for_account(account_type, account_subtype) when a firm has none. So the year-end store is SEEDED from the same substring rules. But it is a snapshot, it is separately CA-editable (POST/PUT at :280 and :344-353), and nothing reconciles it back. The divergence is real and starts the moment a CA edits year-end mappings: the /year-end statements move, the live Balance Sheet (bs_bucket) does not, and chart_of_accounts.schedule_iii_mapping moves neither.\n\nSTRENGTHENING \u2014 the caption vocabularies do not even match. The screen's fixed list (page.tsx:20-23) offers \"Fixed Assets\", \"Long-term Borrowings\", \"Short-term Borrowings\", \"Goodwill & Intangibles\", \"Employee Benefits Expense\"; bs_bucket/pl_bucket emit \"Tangible Fixed Assets\", \"Intangible Fixed Assets\", \"Long Term Borrowings\", \"Short Term Borrowings\", \"Employee Benefit Expense\". Even if bs_bucket were wired to read the column, roughly half the values selectable on that screen would match no caption the statements produce.\n\nSeverity high stands. No rupee is misposted and the Balance Sheet still balances \u2014 this is presentation only \u2014 but a Schedule III caption is a statutory disclosure (Companies Act 2013 s.129 with Schedule III Div I), an account whose subtype misses the magic substring silently lands in \"Other Current Assets\" with no warning, and the screen's mapped/unmapped count actively asserts a stored decision is in force when nothing reads it."
  },
  {
   "kind": "single",
   "finding_id": "ACC-10",
   "lens": "REACH",
   "refuted": false,
   "corrected_severity": "medium",
   "verdict_reason": "Core claim verified, with two corrections. (1) apps/web/app/accounting/schedule-iii-mapping/page.tsx:46 selects `schedule_iii_mapping` from chart_of_accounts and :82 renders \"N of M active accounts mapped\"; :93 says \"Set schedule_iii_mapping during import (Import COA)\". A repo-wide grep for `schedule_iii_mapping` returns only three web files (this page, coa-import/page.tsx:171 which writes it, coa-export/page.tsx:74 which dumps it), migration 057_coa_architecture_correction.sql:8/:15 (column + index) and apps/api/tests/test_coa_architecture.py \u2014 NO production Python and no statement code reads it. (2) The live statements do bucket by substring: apps/api/domain/reporting/schedule_iii.py:22-80 `bs_bucket` does `sub = (account_subtype or \"\").lower()` and matches 'payable', 'inventor', 'term loan', 'receivable'; the Balance Sheet tab groups client-side by the mirror apps/web/lib/accounting/scheduleIiiCaptions.ts (used at apps/web/app/clients/[id]/accounting/page.tsx:2107-2109). (3) Year-end does read account_group_mappings (services/year_end_financial_service.py:502-512). REACH is real: the page is linked from apps/web/components/panels/AccountingPanel.tsx:36 and is in lib/workspace/knownRoutes.generated.ts:22. CORRECTIONS: (a) the screen is strictly READ-ONLY \u2014 grep for onClick/update(/upsert(/<select/<input in that file returns nothing \u2014 so \"the CA spends an afternoon on the screen\" cannot happen; the only way to populate the column is a CSV column in Import COA. (b) The \"third, different table\" overstates the divergence: account_group_mappings is auto-seeded from the SAME bs_bucket/pl_bucket (routers/year_end_mappings.py:21 imports them, :122 `_schedule_line_for_account` calls them, :80-114 `_CAPTION_TO_SCHEDULE_LINE` translates the caption, :425-439 auto-inserts on first read), so by default the year-end statements and the live Balance Sheet classify identically; drift needs an override via POST /api/year-end/mappings, and NOTHING in apps/web calls it (no \"mapping\" reference in apps/web/lib/api/yearEnd.ts, no /mappings caller anywhere in the web app).",
   "what_is_actually_true": "chart_of_accounts.schedule_iii_mapping is genuinely write-only dead data: migration 057 adds and indexes it, COA import writes it, COA export dumps it, and a nav-linked screen displays a \"N of M mapped\" count for it \u2014 but no backend or statement code anywhere reads it. Both statutory statement paths ignore it and classify by account_subtype substring instead (domain/reporting/schedule_iii.py bs_bucket/pl_bucket, mirrored in apps/web/lib/accounting/scheduleIiiCaptions.ts for the Balance Sheet tab); the year-end path reads account_group_mappings, which is itself auto-seeded from those same bucket functions (year_end_mappings.py:122, :439), so the two agree by default rather than being a genuinely independent third answer, and no frontend exposes the override that could make them disagree. The screen has no edit controls at all, so it misleads rather than wasting mapping work, and no rupee figure is wrong because of it. The real classification risk is the substring scan itself \u2014 a subtype missing the magic keyword falls silently into \"Other Current Assets\" \u2014 a pre-existing, separately documented fragility (coa_seed_service.py:38-47), not caused by this screen. Medium: a misleading, ineffective UI on a mounted, navigable route plus a schema dead end, not a live mis-statement."
  },
  {
   "kind": "single",
   "finding_id": "ACC-10",
   "lens": "GUARDS",
   "corrected_severity": "high",
   "refuted": false,
   "verdict_reason": "Confirmed by direct reading; the GUARDS lens found no layer that prevents, catches or bounds it \u2014 and one \"guard\" that is actually false assurance.\n\n1. The column is inert. `grep -rn schedule_iii_mapping` over the whole repo returns exactly: migration 057_coa_architecture_correction.sql:8 (bare `ADD COLUMN ... TEXT`, no CHECK, no enum) and :15 (partial index); three frontend files (coa-export/page.tsx:74, coa-import/page.tsx:171, schedule-iii-mapping/page.tsx:46); and tests/test_coa_architecture.py. **No Python module reads it.** No API route, no service, no domain module.\n\n2. The screen is not merely ineffective, it is read-only. schedule-iii-mapping/page.tsx contains no `<select>`, no `update`, no `insert`, no `upsert` \u2014 it only SELECTs (:44-49) and renders. Its counter at :82 (\"N of M active accounts mapped\") and its amber banner at :91-93 are computed from a column nothing consumes. It is linked from the live nav (components/panels/AccountingPanel.tsx:36), so it is reachable, not dead code.\n\n3. The live statements really do bucket on free-text substrings of `account_subtype`: domain/reporting/schedule_iii.py:26 `sub = (account_subtype or \"\").lower()` then `.py:31-80` / `:88-110` substring tests (\"payable\", \"inventor\", \"term loan\", \"receivable\", \"material\", \u2026). Its own in-code comments record two past live misclassifications from exactly this (Trade Payables showing \u20b90; every working-capital loan presented as non-current), and services/coa_seed_service.py:38-47 states plainly \"there is no shared/enforced vocabulary between the two.\"\n\n4. Vocabulary mismatch, so the column could not even be wired up 1:1 today: the screen offers \"Fixed Assets\", \"Goodwill & Intangibles\", \"Long-term Investments\", \"Short-term Borrowings\", \"Employee Benefits Expense\" (page.tsx:20-23) while bs_bucket/pl_bucket emit \"Tangible Fixed Assets\", \"Intangible Fixed Assets\", \"Long Term Investments\", \"Short Term Borrowings\", \"Employee Benefit Expense\". Two disjoint caption sets.\n\nGUARDS searched and found absent:\n- No CHECK constraint or lookup table on `chart_of_accounts.account_subtype` (migrations/003_phase3a_foundation.sql:110 is `account_subtype TEXT`) or on `schedule_iii_mapping` (057:8). Migration 197_coa_subtype_schedule_iii_fix.sql only back-fills the ~50 SEEDED firm-master rows so their subtypes contain the magic substrings \u2014 it bounds the DEFAULT chart only, and does nothing for a CA-imported COA, which is precisely the flow (coa-import/page.tsx:171) that populates the dead column.\n- tests/test_coa_architecture.py:239-256 looks like a guard and is not: `test_financial_statement_classification` builds its own local `by_mapping` dict inside the test body and never calls any production code. tests/test_schedule_iii_backend.py:11-42 pins bs_bucket's substring behaviour but never asserts it agrees with `schedule_iii_mapping`. No test anywhere would fail if the column were dropped.\n- No RLS/trigger/flag/scheduler touches it.\n\nCorrection to the finding (one detail overstated): `account_group_mappings` is not an independent third rule set at birth. routers/year_end_mappings.py:21 imports bs_bucket/pl_bucket, :116-126 `_schedule_line_for_account` calls them and maps the caption through `_CAPTION_TO_SCHEDULE_LINE`, and :424 uses that to auto-seed the table. So it is a third STORE derived from the same substring rules \u2014 which still diverges, because it is materialised once and then independently editable (POST/PUT at :220, :291) while the live Balance Sheet keeps recomputing from subtype; and unmapped accounts fall to `other_current_assets` (services/year_end_financial_service.py:525-529), which is a worse default than the live path. Also worth recording: auto-seeding fires ONLY inside `GET /year-end/mappings/defaults` (:365, :412-441), so a firm that never opens that screen generates year-end statements with an empty mapping table.\n\nThere is in fact a FOURTH implementation the finding missed: apps/web/lib/accounting/scheduleIiiCaptions.ts:18/47, used live for the client Balance Sheet at apps/web/app/clients/[id]/accounting/page.tsx:2107-2109 (the backend attaches `schedule_iii_caption` only on P&L lines, so the BS has no server caption to fall back to). Its captions differ again \u2014 \"Tangible Assets\", \"Non-Current Investments\", and a \"Tax Liabilities\" bucket the backend does not have at all (backend sends GST/TDS Payable to \"Other Current Liabilities\"). No parity test pins it to the Python; scheduleIiiCaptions.test.ts tests it against itself.",
   "what_is_actually_true": "`chart_of_accounts.schedule_iii_mapping` exists (migration 057), is writable only by the COA CSV import page, is displayed by the Schedule III Mapping screen and the COA export, and is read by no computation anywhere in apps/api. The screen is entirely read-only \u2014 it has no control to set a mapping at all \u2014 and its \"N of M mapped\" counter and unmapped-accounts banner report on a column that changes no number.\n\nStatutory presentation is instead decided by substring scans of the free-text `account_subtype`, in FOUR places: domain/reporting/schedule_iii.py (live BS/P&L, ratios, trend), apps/web/lib/accounting/scheduleIiiCaptions.ts (the client Balance Sheet page, live \u2014 the backend caption covers P&L only), routers/year_end_mappings.py (which reuses the Python buckets to auto-seed account_group_mappings), and that seeded `account_group_mappings` table itself, which the year-end statements read and which can be edited away from the live rules afterwards. Their caption vocabularies do not match each other, and none matches the caption list the mapping screen shows.\n\nNothing guards any of this: no CHECK constraint or vocabulary table on account_subtype or schedule_iii_mapping, no parity test between the Python and TypeScript bucket functions, and the one test that appears to cover the mapping (tests/test_coa_architecture.py:239-256) groups a dict it builds itself and never touches production code. The only real bound is migration 197, which normalises the seeded default chart's subtypes so a stock install buckets correctly \u2014 it does nothing for an imported chart of accounts, which is exactly the population the dead column was built for. The consequence is a wrong caption in a statutory Schedule III presentation, silently, with a green counter suggesting the CA has controlled it."
  },
  {
   "kind": "single",
   "finding_id": "ACC-15",
   "lens": "LITERAL",
   "refuted": false,
   "verdict_reason": "Traced literally and confirmed. (1) apps/api/services/journal_posting_service.py:31-33 `_SELECT` lists only `id, entry_date, reference_no, narration, entry_type, is_posted, status, source_type, source_id, created_by, created_at, posted_at, posted_by, journal_lines(...)` \u2014 no `deleted_at`, no `client_id`. Note list_journals at :46 explicitly prepends `\"client_id, \"` to the same constant, so the omission in post_draft is real, not an artefact of the constant. (2) post_draft's fetch at :84-85 is `.select(self._SELECT).eq(\"id\",...).eq(\"firm_id\",...).single()` \u2014 no `.is_(\"deleted_at\",\"null\")` filter either, so a soft-deleted row IS returned, and under PostgREST the response carries only the projected columns, so `je.get(\"deleted_at\")` at :89 is always None and the 409 guard never fires; `je.get(\"client_id\")` at :116 is always None, so every draft-approval timeline event is written with a null client. (3) The router does not compensate: apps/api/routers/accounting.py:704-717 `post_draft_journal` calls `_assert_draft_scope` (:689-700), whose own probe selects only `client_id` and has NO `.is_(\"deleted_at\", ...)` filter (contrast `_assert_journal_scope_db` at :682-684, which does have it) \u2014 so a discarded draft passes scope. (4) The discard path for a draft (accounting.py:575-599) only stamps `deleted_at`; is_posted stays false. (5) Nothing stops the write at the DB: `prevent_posted_journal_update` (migrations/266_editable_journals_until_locked_or_filed.sql) returns NEW immediately when `OLD.is_posted = FALSE`, so the :106-109 update to is_posted=true/status=posted succeeds on the deleted row. (6) The stranded-state consequence checks out: migrations/228_account_period_balances_triggers.sql:60 `AND NEW.deleted_at IS NULL` skips the balance update, and domain/reporting/sources.py:382 filters `.is_(\"deleted_at\",\"null\")`, so the entry is posted yet in no report and no balance. One correction to the suggested fix, not to the finding: the mock suite's fake Supabase (tests/test_journal_draft_workflow.py:36 `select()` records the column string but `_match`/`execute` return whole rows) ignores column projection, so a regression test written against that fake would pass even against the unfixed code \u2014 the test has to assert on the query's projection or run under HARNESS_PG.",
   "corrected_severity": "medium",
   "what_is_actually_true": "Exactly as claimed. `post_draft` neither filters out soft-deleted rows nor selects `deleted_at`, so its 409 \"has been deleted\" guard is dead code; a discarded draft can be re-approved (stale approval-queue tab, retried request, or any direct call), the DB trigger permits the draft\u2192posted flip because OLD.is_posted is false, and the result is is_posted=true with deleted_at set \u2014 invisible to account_period_balances (migration 228) and to every report (sources.py:382). Independently and on every single draft approval, `client_id` is also missing from the projection, so `timeline_service.log(None, ...)` writes the \"Journal Posted\" event with a null client and it never appears on the client's timeline. The one-line fix (add `deleted_at, client_id` to _SELECT) is correct; a `.is_(\"deleted_at\",\"null\")` filter in `_assert_draft_scope` would also close the resurrection half. Caveat on verification: the in-memory fake DB used by tests/test_journal_draft_workflow.py does not honour column projection, so the missing columns are invisible to the mock suite \u2014 which is why this survived."
  },
  {
   "kind": "single",
   "finding_id": "ACC-11",
   "lens": "LITERAL",
   "corrected_severity": "medium",
   "refuted": false,
   "verdict_reason": "Every cited line is exactly as claimed. models/accounting.py:19-25 \u2014 `AccountIn` = name, code, account_type, parent_id, description, is_active; no `account_subtype`. models/accounting.py:35-39 \u2014 `AccountUpdateIn` = name, code, description, is_active only. routers/accounting.py:119-127 \u2014 the insert payload is written inline with literal keys firm_id, client_id, account_code, account_name, account_type, parent_id, is_active; `account_subtype` is absent, and `chart_of_accounts.account_subtype` is a nullable TEXT with no default (migrations/003_phase3a_foundation.sql:110), so the row lands with NULL. routers/accounting.py:186-198 \u2014 the PATCH mapper builds `update` from only name/code/is_active and raises 422 \"Only name, code and is_active can be changed on an account.\" for anything else, so subtype/type/parent are unchangeable after creation. Presentation then falls through exactly as claimed: domain/reporting/schedule_iii.py:26 lowercases a NULL subtype to \"\", so no keyword branch matches and asset accounts hit :79 `return \"Other Current Assets\"`, liabilities :57 `return \"Other Current Liabilities\"`; build_schedule_iii (:151, :158) sums by that caption. Nothing rescues it upstream: bs_bucket/pl_bucket take only (type, subtype) and never consult parent_id, and sources.py:115/271 and service.py:682 pass the stored subtype straight through. The seed path (services/coa_seed_service.py:27-40, 138) and the CSV importer (apps/web/app/accounting/coa-import/page.tsx:167) DO set account_subtype \u2014 and coa_seed_service.py:38-47 documents this exact failure mode in prose \u2014 which is what makes the API endpoint the outlier. tests/test_accounts_endpoint_is_the_tenants_own.py:109-127 asserts the create payload's keys and never mentions a subtype, so nothing pins it.",
   "what_is_actually_true": "Confirmed, with two precision corrections and one severity-relevant mitigation.\n\n(1) \"Every account lands in a generic 'Other \u2026' caption\" is an overstatement. With subtype NULL, schedule_iii.py routes by type: Asset \u2192 \"Other Current Assets\" (:79), Liability \u2192 \"Other Current Liabilities\" (:57), Expense \u2192 \"Other Expenses\" (:110) \u2014 but Equity \u2192 \"Reserves & Surplus\" (:32) and Revenue \u2192 \"Revenue from Operations\" (:91), which are real captions, not catch-alls. So it is asset/liability/expense accounts that mis-present, which is still the substance of the finding. A related wrinkle the finding did not spot: `AccountType` (models/accounting.py:11-16) offers `INCOME = \"Income\"` while pl_bucket tests `typ == \"revenue\"` (:90), so an \"Income\"-typed account created here gets caption None and is dropped from the Schedule III P&L entirely rather than mis-bucketed.\n\n(2) The suggested_fix's \"(and parent_id)\" is wrong for creation \u2014 `parent_id` is already on AccountIn (:23) and is written by the router (:125). It is only the PATCH that cannot change it.\n\n(3) Mitigation on reach: no frontend screen calls this endpoint. `apps/web/lib/api/index.ts:728-729` defines createAccount/updateAccount wrappers, and grep across apps/web finds no caller for either; the only UI write path to chart_of_accounts is the CSV importer, which does set account_subtype (and skips existing codes, so it cannot correct one either). There is no single-account create/edit UI at all. So today the defect bites API integrations and any future screen built on this endpoint, not a live CA click-path \u2014 which keeps it at medium rather than high.\n\nUnrelated defect noticed in the same lines (not part of this finding): in mock mode (`_prod_db()` is None, routers/accounting.py:106-107) the router passes `data.model_dump()` \u2014 keys `name`/`code` \u2014 into `domain/accounting_service.create_account`, which reads `data[\"account_code\"]` and `data[\"account_name\"]` (accounting_service.py:199-200). That raises KeyError, which the `except ValidationError` does not catch, so POST /api/accounting/accounts appears to 500 in dev/demo mode; no test covers that branch."
  },
  {
   "kind": "single",
   "finding_id": "ACC-16",
   "lens": "LITERAL",
   "refuted": false,
   "verdict_reason": "Every cited fact is literally true. (1) apps/api/migrations/003_phase3a_foundation.sql:139-149 defines journal_lines with exactly id/journal_entry_id/account_id/debit_paise/credit_paise/narration/created_at \u2014 no ordering column \u2014 and `id UUID PRIMARY KEY DEFAULT uuid_generate_v4()` at :140 is a random v4, so id carries no sequence. (2) No later migration adds one: `grep -n 'ALTER TABLE .*journal_lines' migrations/*.sql` returns only RLS enables (005:26, 008:52) and the FX columns (147:20), and no migration mentions line_no/line_number/sequence/sort_order for journal_lines. tests/fixtures/production_schema_2026-09-03.json confirms the live column set is exactly the 16 the finding lists. (3) apps/api/services/manual_journal_service.py:148-153 selects `lines:journal_lines(id, account_id, debit_paise, credit_paise, narration)` with no .order(); that dict is returned straight to the CA and apps/web/components/journal/JournalEditor.tsx:114-115 renders `existing.lines` in the order received (toFormLines at :71-72 maps, does not sort). The other reader, apps/web/app/clients/[id]/accounting/page.tsx:668, embeds lines unordered too (it sorts entries, not lines, at :674-678). (4) apps/api/migrations/266_editable_journals_until_locked_or_filed.sql:414-423 does `DELETE FROM public.journal_lines WHERE journal_entry_id = p_entry_id;` then re-INSERTs from jsonb_array_elements; the draft path in manual_journal_service.py:285-291 does the same delete+insert. (5) The view public.journal_entry_lines (migrations/016_security_fixes.sql:19-36) adds no ORDER BY either, and created_at is `NOW()` (transaction timestamp), so all lines of one entry share it and it cannot even serve as a tiebreak. Two over-statements: the re-INSERT in 266 preserves the caller's array order (jsonb_array_elements is ordered), so an edit normally RESTORES typed order rather than reshuffling \u2014 reshuffle is possible (heap space reuse, plan change) but not what the code does by default; and PostgREST's embedded scan is stable between repeated reads of unchanged rows in practice, so \"a different order each time\" is not what the code produces, only what it fails to forbid.",
   "corrected_severity": "low",
   "what_is_actually_true": "journal_lines genuinely has no ordering column and no read path orders the lines, so a voucher's line order is whatever the query plan yields \u2014 unspecified by contract, not preserved as the order the CA typed. Impact is presentational only: no figure changes (totals and the double-entry assertion in phase2_journal_service._create_journal are order-independent, and the reporting sources at domain/reporting/sources.py:429-445 only sum). In practice Postgres returns lines in physical/insertion order, so a five-line salary accrual usually reads in the order it was entered and reads the same way twice; what is missing is any guarantee of that, and there is nothing that would keep the order stable across a rewrite or a plan change. So the defect is real but its lived symptom (\"interleaved, different every time\") is stronger than the code produces \u2014 hence low, not medium. The suggested fix is sound except for the backfill: created_at is NOW() and identical for all lines of one entry, so it cannot order a backfill; existing rows can only be given an arbitrary stable line_no (e.g. by ctid or id)."
  },
  {
   "kind": "single",
   "finding_id": "ACC-17",
   "lens": "LITERAL",
   "refuted": false,
   "verdict_reason": "Traced the whole path and the claim holds literally, with only line-number drift. The self-documenting comment the finding quotes is real, verbatim, at apps/api/routers/accounting.py:876-885 (finding cited 869-881). Each reporting endpoint does exactly `if client_id: assert_client_access(...)` and then passes `client_id` (possibly None) straight to `_reporting_service()`: /ledger 886-891, /trial-balance 894-910 (assert at ~907), /profit-loss 934-947, /balance-sheet 950-963, /schedule-iii 966-981, /cash-flow 1233-1252 (finding cited 1197). `rbac()` (core/permissions.py:444-468) checks only the role matrix \u2014 it never sees client_id \u2014 and \"accounting\".\"read\" is `_AT_LEAST_EXECUTIVE` = {PARTNER, MANAGER, EXECUTIVE} (core/permissions.py:46, 166-169). With `client_id=None` the scoping stops at firm_id: SupabaseLedgerSource guards every sub-query with `if client_id:` (domain/reporting/sources.py:247-248, 383-384, 504-505, 519-520, 553-554), so an omitted client_id returns every client in the firm; nothing in domain/reporting/service.py accepts or applies an allowed-client set. The contrast the finding draws is also real: /journals passes `allowed_client_ids=effective_client_ids(current_user)` (accounting.py:645-647) and services/journal_posting_service.py:55-56 filters on it; /ledger-span builds `scoped` from effective_client_ids (accounting.py:923-931). tests/test_accounting_client_scope.py:245-290 only pins the NAMED-client_id refusals, and tests/test_router_client_scope.py:504-508 states in terms that \"the client_id=None 'firm-wide consolidation' case is recorded, not fixed\".",
   "corrected_severity": "medium",
   "what_is_actually_true": "Confirmed as written, with three corrections. (1) Line numbers have drifted: the comment is at accounting.py:876-885 and /cash-flow is at :1233-1252, not :869-881/:1197. (2) The finding understates the surface by one endpoint \u2014 /statement-analysis (accounting.py:1255-1272) has the identical `if client_id: assert_client_access` shape and feeds the same reporting engine, so seven endpoints are affected, not six; the router's own test note (tests/test_router_client_scope.py:504) lists all seven. (3) It also understates who is affected: under the M3 decision `_FIRMWIDE_ROLES = {Role.PARTNER}` (core/authz.py:42), MANAGERs are assignment-scoped too, so a Manager omitting client_id likewise gets the whole practice's consolidated books, not just an Executive. Scope of the exposure is intra-firm only \u2014 firm_id is always applied, so there is no cross-tenant leak \u2014 and it requires a hand-made request since no frontend caller omits client_id, which keeps it at medium. The gap is deliberate and documented rather than accidental, but it is live behaviour, not a mischaracterisation."
  },
  {
   "kind": "single",
   "finding_id": "ACC-18",
   "lens": "LITERAL",
   "refuted": false,
   "verdict_reason": "Every cited line exists and does what the finding says. apps/web/app/accounting/lock-year/page.tsx:39-44 is literally `const FY_LIST = [{label:\"2025-26\",\u2026},{\"2024-25\"},{\"2023-24\"},{\"2022-23\"}]` \u2014 newest entry 2025-26. Line 249 `{FY_LIST.map((fy) => {` is the ONLY row source: inside it `const isLocked = lockedYears.includes(fy.label)` (line 250) merely decorates a hardcoded row, and `lockedYears` (set at :180/:202 from GET /api/accounting/year-lock) is never used to add rows. The only interactive control is `onClick={() => setPinDialog({ fy: fy.label, isLocked })}` (:266), so the FY sent to the API can only be one of those four strings. Backend has no such limit: apps/api/routers/accounting.py:425-428 `class YearLockIn: financial_year: FYLabel` accepts any valid FY label, :627-645 POST /year-lock gates on rbac(\"accounting\",\"approve\") only, and services/year_lock_service.py:41-95 does `current.append(financial_year)` into an arbitrary text[] with no allow-list. Today is 2026-09-07, so the current Indian FY is 2026-27 and it is absent. This page is also the sole frontend surface for the firm lock \u2014 grep for \"year-lock\"/\"locked_financial_years\" across apps/web returns only this file plus a comment block in apps/web/app/clients/[id]/accounting/page.tsx:3396-3413 explaining that the old browser-side control was deleted and \"the firm-wide lock is Accounting -> Lock Year\". The rot is not isolated: the client-level year-end screen the same comment points to, apps/web/app/clients/[id]/year-end/page.tsx:14, has the identical hardcoded `FY_OPTIONS = [\"2025-26\",\"2024-25\",\"2023-24\",\"2022-23\"]`. The suggested-fix ingredients exist as claimed: GET /api/accounting/ledger-span at apps/api/routers/accounting.py:914 (wired in apps/web/lib/api/index.ts:767) and getCurrentFinancialYear in apps/web/lib/workspace/ClientNavContext.",
   "corrected_severity": "medium",
   "what_is_actually_true": "Confirmed as written, with two clarifications to the narrative.\n\n(1) The core defect is exact: the Lock Financial Year screen renders four hardcoded rows topping out at 2025-26, so a Partner cannot lock (or even see) FY 2026-27, and the list silently loses relevance every 1 April. Practical impact today is somewhat softened \u2014 the year a CA would normally finalise and lock during 2026-27 is 2025-26, which IS in the list \u2014 so the blocked case right now is locking the in-progress current year. The real bite lands on 2027-04-01, when the just-closed FY 2026-27 becomes unlockable from the UI. That, plus the fact that the same hardcoded pattern is duplicated in the client year-end screen (clients/[id]/year-end/page.tsx:14), keeps this at medium rather than low.\n\n(2) The ca_experience's second sentence is imprecise about mechanism. A year \"locked by a client-level workflow\" is not stored in firms.locked_financial_years at all \u2014 services/year_end_workflow_service.py:36 records that it deliberately stopped calling set_lock, and client locks now live in client_year_locks (migration 289), managed from the client's Year End section. So this screen not unlocking them is by design, not a symptom of FY_LIST. What FY_LIST genuinely does hide is a FIRM lock on any year outside the four (e.g. one written directly through POST /api/accounting/year-lock, which accepts any FYLabel): it would be stored, would be enforced on posting, and would be invisible and un-unlockable on the only screen that manages firm locks."
  },
  {
   "kind": "single",
   "finding_id": "ACC-20",
   "lens": "LITERAL",
   "refuted": false,
   "corrected_severity": "low",
   "verdict_reason": "Every structural element of the claim is literally in the code. apps/api/routers/accounting.py:735-736 declares `@router.patch(\"/journal/{entry_id}/post\")` on `post_journal_entry(..., current_user = Depends(rbac(\"accounting\", \"approve\")))`; the body at :738-762 calls `_assert_journal_scope` (:720-732) then `accounting_service.post_journal_entry(entry_id)` (apps/api/domain/accounting_service.py:285-294, which mutates `JOURNAL_INDEX[entry_id][\"status\"]`, the module-level in-memory store built at :174 from `MOCK_JOURNAL_ENTRIES` :47; `MOCK_ACCOUNTS` is at :14, not :31 as cited \u2014 the only cite slip). The body contains no `_prod_db()`/SUPABASE_URL gate, unlike neighbouring routes (`post_draft_journal` :704-717, `create_journal_entry` :233-258), so it is reached regardless of SUPABASE_URL. It writes a real `log_event(..., \"approve\")` (:740-742) and a real `timeline_service.log_timeline_event` (:743-756). The router is mounted unconditionally at apps/api/main.py:243. The frontend comment is verbatim at apps/web/app/clients/[id]/accounting/journal/[entryId]/edit/_page.tsx:161-164, and the only reference to the route is the unused wrapper `postJournalEntry` at apps/web/lib/api/index.ts:732 \u2014 no component calls it, while the edit page calls `postDraftJournal` \u2192 POST /journals/{id}/post (:847).",
   "what_is_actually_true": "The route is real, mounted, Partner-permissioned and backed by the in-memory demo engine \u2014 the mechanics are correct. What is overstated is the harm, hence severity low: in a production deployment this endpoint cannot report a false success. `_assert_journal_scope` (routers/accounting.py:720-732) first calls `accounting_service.get_journal_entry`, which only knows `JOURNAL_INDEX` \u2014 the ~12 hardcoded `je-00N` demo entries \u2014 so any real journal id raises NotFoundError \u2192 404, with no audit or timeline row written. The demo ids that ARE in the index fail the second gate: with SUPABASE_URL set, `can_access_client` has `_USE_MOCK = False` (core/authz.py:34) and calls `_client_belongs_to_firm`, a real `clients` table lookup that cannot match the seeded `c-001`/`c-005` ids \u2014 404 again. And all but one seed is already `status: \"posted\"` (only domain/accounting_service.py:152 is a draft), so even a hypothetical hit mostly yields 422 \"Entry already posted\". The false-success path exists only in mock/dev mode, where there is no ledger and no real audit_log to lie to. So it is dead code that should be removed, not a live integrity risk to a CA's books; tests/test_accounting_client_scope.py:10,213,480 and tests/test_accounting_completion.py:13 exercise it in mock mode and would go with it. The suggested fix is right; the effort is smaller than the ca_experience implies."
  },
  {
   "kind": "single",
   "finding_id": "ACC-21",
   "lens": "LITERAL",
   "findings": "[]",
   "refuted": false,
   "corrected_severity": "medium",
   "verdict_reason": "Every cited line is exactly as described. (1) apps/api/migrations/271_post_journal_atomic_security_definer.sql:103-123 is the tenant guard verbatim \u2014 `IF (p_entry->>'firm_id')::uuid IS DISTINCT FROM v_my_firm` (:103), `IF NOT public.can_access_client(p_entry->>'client_id')` (:109), and the internal-client Partner rule (:115-122), all reading only p_entry. (2) The line insert at :166-185 is `SELECT v_id, (l->>'account_id')::uuid, ... FROM jsonb_array_elements(p_lines) AS l` with no lookup against chart_of_accounts and no reference to v_my_firm or client_id. (3) journal_lines' only account constraint is the global FK `account_id UUID NOT NULL REFERENCES chart_of_accounts(id)` (apps/api/migrations/003_phase3a_foundation.sql:142); chart_of_accounts carries firm_id NOT NULL and a nullable client_id (003:101-116), so a per-tenant check is possible and simply absent. (4) apps/api/services/manual_journal_service.py:98-103 builds kernel_lines as `{\"account_id\": l[\"account_id\"], ...}` straight from request input; models/accounting.py JournalLineIn declares `account_id: str` and its only validator (`exactly_one_side`) checks amounts; routers/accounting.py:232-256 calls assert_client_access(current_user, data.client_id) and nothing about accounts. phase2_journal_service._create_journal (~:1676-1700) copies account_id into line_payloads unexamined. (5) trg_archived_account_check / prevent_archived_account_journal_line (migrations/058_journal_immutability_and_validations.sql:42-63) selects only is_active and is indeed the sole account-level guard; 266 cites it at line 55. Two corrections to the evidence, neither of which changes the conclusion: migration 271 is NOT the live definition \u2014 migration 274_reversal_stamps_its_original_atomically.sql:58-186 re-creates post_journal_atomic and restates 271's guard verbatim while leaving the line insert (274:145-165) equally unvalidated, so the gap is current, not stale. And the same hole exists in the edit path the fix names: edit_posted_journal (migrations/266_editable_journals_until_locked_or_filed.sql:416-424) deletes and re-inserts lines from `(ln->>'account_id')::uuid` with no account check either. The stated consequence also checks out: domain/reporting/builders.py:19 `_acc` falls back to `Account(id=aid, code=\"\", name=aid, type=\"\")`, and apb_apply_posted_line (migrations/228_account_period_balances_triggers.sql:19-28) upserts a bucket keyed (e.firm_id, e.client_id, NEW.account_id) taking firm/client from the ENTRY and the account from the line, so a foreign-firm account_id lands in the caller's own passbook. No test asserts a cross-firm account is rejected (tests/test_atomic_journal_posting.py and tests/test_journal_posting_privileges_pg.py cover balance, SECURITY DEFINER and EXECUTE grants only).",
   "what_is_actually_true": "The claim holds literally, with the live definition being migration 274 rather than 271 (identical guard, identical omission), and with the same omission present in edit_posted_journal (266:416-424) \u2014 which the suggested fix already names. post_journal_atomic validates firm_id, client access and the internal-client Partner rule on the entry header only; account_id on each line is cast and inserted with no check that the account belongs to the entry's firm, or to its client, at any layer \u2014 not in the RPC, not in journal_lines' constraints (only a global FK to chart_of_accounts(id)), not in JournalLineIn, not in manual_journal_service.create, not in the router. Severity stays medium and not higher because there is no cross-tenant READ: the victim firm's ledger, trial balance and account_period_balances are all keyed by the ENTRY's firm/client, so the stray line never appears in the other firm's reports, and builders._acc shows the raw uuid rather than leaking the foreign account's name. The damage is confined to the caller's own books \u2014 an orphaned line, a mis-named ledger row, and an account_period_balances bucket pointing at a chart row the firm does not own. It requires a crafted API call (the UI's AccountLookup is firm-scoped) plus knowledge of another firm's account uuid, which makes the cross-firm case unlikely; the more reachable half of the same hole is the intra-firm one \u2014 nothing stops a line posting to another CLIENT's account within the caller's own firm, and that needs no malice at all."
  },
  {
   "kind": "single",
   "finding_id": "SALES-01",
   "lens": "LITERAL",
   "refuted": false,
   "verdict_reason": "Traced the exact path. services/invoice_pdf_service.py:355 `firm = _load_firm(firm_id)` and :356 `pdf = build_invoice_pdf(invoice_dict, firm, customer)` inside `get_sales_invoice_pdf`, which at :318-327 reads `client_sales_invoices` joined to `customers`. `_load_firm` (:297-303) selects from `firms`, and `firms` is documented as \"Core tenant entity\" \u2014 the CA practice (migrations/003_phase3a_foundation.sql:6-24), carrying gstin/pan/address via migrations/014_complete_missing_tables.sql:464-469. build_invoice_pdf (:140) renders that dict as the top-left supplier block at :155-160 \u2014 `firm_name = firm.get(\"name\") ... or \"Chartered Accountants\"`, `GSTIN: {firm['gstin']}`, `PAN: {firm['pan']}` \u2014 under \"TAX INVOICE\" / \"(Issued under Section 31, CGST Act 2017 read with Rule 46...)\" (:153-154), with the third argument rendered as \"Bill To:\" (:189). The supplier of a `client_sales_invoices` row is the CLIENT, not the firm: the row carries client_id + customer_id (migrations/050_sales_purchase_cycle.sql:12-13), customers hang off a client (migrations/049_customer_vendor_master.sql:6-7), and routers/sales_invoices.py:673 decides intra/inter-state from `client_rec.get(\"gstin\")` read from `clients`. Live consumers: routers/sales_invoices.py:1883 (download, any client, gated only by `_assert_invoice_scope`), :1965 (PDF emailed to the customer via Resend), routers/portal_data.py:54, services/collections_service.py:359. tests/test_invoice_pdf_line_items.py:191-197 stubs `_load_firm` to `{\"name\": \"Test & Co\", \"gstin\": \"27AAAAA9999A1Z5\"}` and asserts nothing about the supplier, so it pins the behaviour rather than catching it. services/statement_pdf_service.py:104-105 does the same `_load_firm` \u2192 `build_statement_pdf(statement, firm, customer)`, and the firm name is printed as the statement heading at :43-45. Nothing in the codebase overrides this: branding (routers/branding.py) is firm-scoped and never reaches build_invoice_pdf, and there is no second sales-invoice PDF builder.",
   "corrected_severity": "critical",
   "what_is_actually_true": "The finding is accurate as stated. Every sales-invoice PDF for a client's outward supply prints the CA practice (`public.firms` \u2014 name, address, GSTIN, PAN) as the supplier, with the client's customer under \"Bill To\", on a document headed \"TAX INVOICE (Issued under Section 31, CGST Act 2017 read with Rule 46)\". The client \u2014 the actual supplier, whose GSTIN the same codebase uses at routers/sales_invoices.py:673 to decide CGST/SGST vs IGST \u2014 appears nowhere on the PDF. The document is downloadable (routers/sales_invoices.py:1883), emailed to the customer (:1965) and reachable from collections (services/collections_service.py:359). Two refinements to the write-up, neither reducing severity: (1) the portal path routers/portal_data.py:54 is NOT an instance of the bug \u2014 services/portal_data_service.py:139-150 `invoice_in_scope` restricts it to the firm's own internal-client fee invoices, where the practice genuinely is the supplier (though the internal client record, not `firms`, would still be the correct source); (2) on the statement, the exact rendering line is services/statement_pdf_service.py:43-45 (`firm.get(\"name\")` as the heading), with :104-105 being the `_load_firm` + call site as cited. The suggested fix (load `public.clients` by `invoice.client_id`, firm-scoped, and pass that as the supplier; rename the parameter; keep `_load_firm` for the `fee_invoices` path at :282) is the right shape."
  },
  {
   "kind": "single",
   "finding_id": "SALES-01",
   "lens": "REACH",
   "findings": "[]",
   "refuted": false,
   "corrected_severity": "critical",
   "verdict_reason": "Confirmed at source and by execution. apps/api/services/invoice_pdf_service.py:355 `firm = _load_firm(firm_id)` and :356 `build_invoice_pdf(invoice_dict, firm, customer)`; `_load_firm` (:297-305) reads `public.firms`, which migrations/003_phase3a_foundation.sql:7 defines as the CA-practice tenant (\"Core tenant entity\") and migrations/014_complete_missing_tables.sql:464-465 plus 018_firms_pan_website.sql:4 give it `gstin` and `pan`. build_invoice_pdf (:148-160) renders that row as the supplier block under \"TAX INVOICE / (Issued under Section 31, CGST Act 2017 read with Rule 46...)\" and :~250 signs \"For {firm_name}\". The document being rendered is `client_sales_invoices` (migrations/050_sales_purchase_cycle.sql:9-16: firm_id + client_id + customer_id), whose supplier is the CLIENT \u2014 settled by routers/sales_invoices.py:673 `client_state_code = _get_state_code_from_gstin(client_rec.get(\"gstin\"))` driving the intra/inter-state decision, and by `customers` being the recipient. I reproduced it with a scratchpad pytest driving the real `get_sales_invoice_pdf` against the repo's own FakeDB: the PDF text is \"TAX INVOICE ... Sharma & Associates, Chartered Accountants / CA Office, Pune / GSTIN: 27AAAAA9999A1Z5 / PAN: AAAAA9999A ... Bill To: Acme Pvt Ltd ... For Sharma & Associates, Chartered Accountants\". The client never appears. tests/test_invoice_pdf_line_items.py:191-194 stubs `_load_firm` to `{\"name\": \"Test & Co\", \"gstin\": \"27AAAAA9999A1Z5\"}` and asserts only line items, so it pins the wrong supplier silently. REACH is real: routers/sales_invoices.py:1870 `@router.get(\"/{invoice_id}/pdf\")` is mounted at main.py:303 (`app.include_router(sales_invoices.router, ...)`), apps/web/components/invoices/InvoiceViewDrawer.tsx:44 and apps/web/lib/api/index.ts:1802 fetch `/api/sales-invoices/{id}/pdf`, and the Sales tab is in the client nav (apps/web/lib/workspace/ClientNavContext.tsx:64). The same bytes are emailed to the customer at routers/sales_invoices.py:1965. services/statement_pdf_service.py:104-105 has the same `_load_firm` shape.",
   "what_is_actually_true": "The core claim is correct and reproducible: every sales-invoice PDF for a client's outward supply is headed with the CA practice's name, address, GSTIN and PAN, and signed \"For <CA firm>\", while the client \u2014 the actual supplier \u2014 appears nowhere. Two details in the finding need correcting, neither of which reduces the severity of the main path:\n\n1. The portal path (routers/portal_data.py:54) is NOT defective. services/portal_data_service.py:139-150 `invoice_in_scope` gates it to `client_id = scope[\"internal_client_id\"]`, i.e. the firm-as-internal-client row (migrations/073_revenue_ops_foundation.sql:22-23, `clients.is_internal` / `firms.internal_client_id`). On that path the supplier genuinely IS the firm, which is exactly why the reuse looked safe. The defect reaches users through the CA-facing download (routers/sales_invoices.py:1870), the send/resend email (:1965) and services/collections_service.py:359 (payment-reminder attachments) \u2014 all of which serve arbitrary real clients.\n\n2. The customer-statement defect (services/statement_pdf_service.py:104-105) is materially milder than the invoice one, not \"identical\". build_statement_pdf (:43-45) prints only `firm.get(\"name\")` as a header \u2014 no GSTIN, no PAN, no supplier block \u2014 and the document ends with \"This is a statement of account, not a tax invoice.\" (:96). It is misleading branding on a client's customer-facing document, not a Rule 46 failure. Treat it as a separate low/medium item.\n\nAlso worth noting for the fix: `_compute_tax_splits` (:118-126) has a second, latent instance of the same confusion \u2014 its GSTIN-geography fallback compares `firm.get(\"gstin\")` against the recipient's, which for a client sale is the wrong pair. It is currently dead on this path only because client_sales_invoices always supplies stored cgst/sgst/igst heads. The suggested fix (load `public.clients` by `invoice.client_id`, firm-scoped, and pass that as supplier; rename the parameter; make the test fail on the old behaviour) is right, and the effort is closer to hours than days \u2014 the change is one loader plus the test."
  },
  {
   "kind": "single",
   "finding_id": "SALES-01",
   "lens": "GUARDS",
   "refuted": false,
   "verdict_reason": "CONFIRMED in substance, with two evidence corrections and no guard anywhere. (1) services/invoice_pdf_service.py:355 `firm = _load_firm(firm_id)` and :356 `build_invoice_pdf(invoice_dict, firm, customer)`; `_load_firm` (:299-307) selects from `firms`, the CA-practice tenant row (migrations/003_phase3a_foundation.sql:5-7 \"Core tenant entity\"). build_invoice_pdf renders that dict as the supplier block at :157-163 (`firm.get(\"name\")`, `GSTIN: {firm['gstin']}`, `PAN: {firm['pan']}`) under \"TAX INVOICE ... Section 31, CGST Act 2017 read with Rule 46\" (:148-150), and again at :257 `For {firm_name}` above \"Authorised Signatory\". I rendered it with a scratch script (not a repo file): passing `{\"name\":\"Test & Co\",\"gstin\":\"27AAAAA9999A1Z5\",\"pan\":\"AAAAA9999A\"}` produces exactly \"Test & Co / GSTIN: 27AAAAA9999A1Z5 / PAN: AAAAA9999A\" above \"Bill To: Acme Pvt Ltd\". (2) The client, not the firm, is the supplier of a `client_sales_invoice`: migrations/050_sales_purchase_cycle.sql:9-13 (firm_id / client_id / customer_id) and routers/sales_invoices.py:673-687, where `client_state_code = _get_state_code_from_gstin(client_rec.get(\"gstin\"))` decides `is_interstate` against the customer's place of supply. (3) Reach is real: routers/sales_invoices.py:1883 (download), :1965 (PDF emailed to the customer via Resend), routers/portal_data.py:54 (client portal), services/collections_service.py:359 (payment-reminder attachment). GUARDS: none. No test asserts the supplier identity \u2014 tests/test_invoice_pdf_line_items.py:176-200 only asserts line descriptions, HSN and filename; its `_load_firm` stub (:191-194) does not assert the firm appears, so it does not \"pin the wrong behaviour\", it merely fails to catch it. No CHECK constraint, trigger, RLS policy or migration touches PDF content (RLS/rbac gate access only). The one thing that bounds it is `_USE_MOCK` (routers/sales_invoices.py:1879, portal_data.py:50) raising 501 \u2014 irrelevant on a real deployment. EVIDENCE CORRECTION that lowers severity: nothing in the app ever writes `firms.gstin`. apps/web/app/settings/page.tsx:355 saves the firm GSTIN into `firms.gst_number` (the column created in migrations/003:17), while `firms.gstin` was added separately by migrations/014_complete_missing_tables.sql:464 and is nullable with no writer (grep for firm gstin writes finds only readers: routers/practice.py:66, routers/onboarding.py:216). Same for `firms.address` (:469) vs the settings page's `address_line1`. So in practice the block prints the CA firm's NAME and the CA firm's PAN with NO supplier GSTIN and no address. Also narrower than claimed: services/statement_pdf_service.py:41-44 puts only `firm.get(\"name\")` in the statement letterhead \u2014 no firm GSTIN or PAN \u2014 and the document ends \"This is a statement of account, not a tax invoice\" (:96), so that half is a branding defect, not a Rule 46 one.",
   "corrected_severity": "high",
   "what_is_actually_true": "`get_sales_invoice_pdf` really does render the CA practice (`public.firms`) as the supplier on the CLIENT's outward sale, and that PDF is downloaded, emailed to the client's customer, attached to payment reminders and served on the portal. The supplier identity is wrong and it is signed \"For <CA firm>\". Two details differ from the claim. First, the CA firm's GSTIN does not in fact print: `firms.gstin` is a column no code path populates (the settings page writes `firms.gst_number`), so the real output is the CA firm's name plus the CA firm's PAN with the GSTIN line omitted entirely. That is still a Rule 46(a)/(b) failure \u2014 wrong supplier name, no supplier GSTIN \u2014 but the specific harm described (the customer claiming ITC against the practice's GSTIN and failing GSTR-2B) cannot occur as the code stands, because no GSTIN is emitted. Second, the statement PDF prints only the firm's NAME and carries an explicit \"not a tax invoice\" line, so it is a branding defect rather than a statutory one. Nothing downstream is corrupted: the GL and GSTR-1 are computed from the books against the client's own GSTIN (routers/sales_invoices.py:673-687); only the printed document is wrong. No test, constraint, trigger, policy or frontend check catches any of this, so the fix is exactly as suggested \u2014 load `public.clients` by `invoice.client_id`, firm-scoped, and pass that as the supplier \u2014 with two additions the auditor missed: `clients` has no `address` column (address_line1/2/city/state, per migrations/001), so the supplier address needs composing, and the latent `firms.gstin` vs `firms.gst_number` split should be resolved at the same time since routers/practice.py:66 and routers/onboarding.py:216 read the dead column too."
  },
  {
   "kind": "single",
   "finding_id": "SALES-02",
   "lens": "LITERAL",
   "refuted": false,
   "verdict_reason": "Could not refute; confirmed by reading and by execution. apps/api/services/invoice_pdf_service.py:30 is literally `GST_RATE_PCT = 18`, and lines 223-226 emit `f\"CGST @ {GST_RATE_PCT // 2}%\"`, `f\"SGST @ {GST_RATE_PCT // 2}%\"`, `f\"IGST @ {GST_RATE_PCT}%\"` \u2014 a grep shows those four lines are the only uses of the constant in the whole API, so nothing else can override it. The amounts come from stored heads via _compute_tax_splits (lines 115-120), so label and amount have independent sources. The line table header at line 204 is exactly [\"#\", \"Description\", \"SAC\", \"Taxable Value (\u20b9)\"] and the row builder at 206-212 reads only description/hsn_sac/taxable_amount_paise. The discarded data exists and is already loaded: migration 050_sales_purchase_cycle.sql:58-74 defines quantity, unit, rate_paise, gst_rate_bps and per-line cgst/sgst/igst on client_sales_invoice_lines, and get_sales_invoice_pdf does select(\"*\") at line 335. Decisive test: I rendered a 5% invoice (taxable 100000.00, cgst 2500.00, sgst 2500.00) through build_invoice_pdf and pdfplumber extracted \"CGST @ 9% 2,500.00 / SGST @ 9% 2,500.00\" \u2014 \"9%\" present, \"2.5%\" absent, qty/unit absent. The frontend contrast is also accurate: apps/web/components/invoices/InvoiceEditor.tsx:606-612 guards the \"@ x%\" label on `uniformRate != null`. Existing test tests/test_invoice_pdf_line_items.py:88-89 asserts \"CGST @ 9%\" but its fixture is an 18% invoice, so it pins the constant instead of testing it. The mislabelled PDF is also emailed to the customer via routers/sales_invoices.py:1965.",
   "corrected_severity": "high",
   "what_is_actually_true": "The finding is literally correct in every cited detail; only the severity is overstated. The invoice PDF prints a rate label derived from a hardcoded `GST_RATE_PCT = 18` constant, never from the line data, even though every line row carries `gst_rate_bps` (plus quantity, unit, rate_paise and per-line tax) and those columns are already fetched by `select(\"*\")`. A verified 5% invoice renders \"CGST @ 9%\" beside a 2,500.00 amount. The line table also omits quantity, unit, per-line rate and per-line tax entirely, which is a Rule 46(g)/(h) defect on any goods invoice. The document is both downloadable and emailed to the customer as an attachment. Severity is high rather than critical because the impact is confined to the printed label on an outbound document: the stored tax heads, the general ledger and the GSTR-1 computation are all unaffected and correct, and the product's dominant case (18% professional services) prints correctly by coincidence \u2014 which is exactly why the defect survived and why an existing regression test asserting \"CGST @ 9%\" passes. The suggested fix (group lines by gst_rate_bps, emit one band per rate, add Qty/Unit/Rate/tax columns, add a 5% regression test) is the right shape."
  },
  {
   "kind": "single",
   "finding_id": "SALES-02",
   "lens": "GUARDS",
   "refuted": false,
   "verdict_reason": "Confirmed by reading and by execution. apps/api/services/invoice_pdf_service.py:30 `GST_RATE_PCT = 18`; :223-226 emit the labels `CGST @ {GST_RATE_PCT // 2}%`, `SGST @ \u2026`, `IGST @ {GST_RATE_PCT}%` as literal strings, while the amounts come from `_compute_tax_splits` (:95-137) which reads the stored heads \u2014 so amount and printed rate are independent. The line table built at :204-219 has exactly four columns (`#`, `Description`, `SAC`, `Taxable Value`), even though `client_sales_invoice_lines` carries `quantity`, `unit`, `rate_paise` and `gst_rate_bps` per line (migrations/050_sales_purchase_cycle.sql:63-68) and `get_sales_invoice_pdf` (:333-353) already SELECTs `*` from that table \u2014 the per-line rate is fetched and then dropped. I rendered a 5% invoice through `build_invoice_pdf` (single line, gst_rate_bps=500, cgst=sgst=2,500.00): the PDF text reads \"CGST @ 9% 2,500.00 / SGST @ 9% 2,500.00\". GUARDS lens finds nothing that prevents or bounds it: no CHECK constraint pins gst_rate_bps to 1800 (050:68 is a DEFAULT only; 176_service_catalogue.sql:36-37 explicitly allows 0\u201310000 bps), the frontend offers a free `GST_RATES` dropdown per line (apps/web/lib/invoices/shared, used by the sales/purchase editors), and the backend computes per line at whatever bps arrives (routers/sales_invoices.py:170-192, 717-742). The only test touching these rows, tests/test_invoice_pdf_line_items.py:80-91, asserts `\"CGST @ 9%\" in text` on an 18% fixture \u2014 it pins the hardcode rather than guarding against it; tests/test_invoice_pdf.py only covers `_compute_tax_splits` amounts. The path is live and customer-facing: routers/sales_invoices.py:1869-1892 (download), the same PDF is attached on send/resend (:1895+), and routers/portal_data.py:52 serves it into the customer portal. Only mitigation found is that mock mode returns 501, so this is a live-deployment-only defect. The frontend cite is accurate (apps/web/components/invoices/InvoiceEditor.tsx:304, 607-611 \u2014 `uniformRate` is non-null only when every line shares a rate, otherwise the label is bare \"CGST\"/\"SGST\"/\"IGST\").",
   "corrected_severity": "high",
   "what_is_actually_true": "The claim stands as written: the PDF prints a constant 9/9/18 rate label beside amounts computed from the real per-line rates, so any invoice not at 18% prints a document whose stated rate and stated tax do not reconcile, and a mixed-rate invoice cannot be represented at all (there is no rate-wise summary \u2014 one CGST row, one SGST row, one IGST row). Quantity, unit and per-line rate/tax are likewise fetched and discarded. Nothing else in the stack catches it \u2014 no constraint, no trigger, no test, no frontend gate; the one existing test cements the hardcoded label by asserting it on an 18% fixture. I lowered critical to high only because no stored money, GL posting or return figure is wrong \u2014 amounts throughout are correct integer paise and the GSTR-1 path reads `gst_rate_bps` (services/gst_return_service.py:297), so the defect is confined to the rendered document, and to invoices at any rate other than 18%. For a firm invoicing only its own 998211 professional services at 18% (the legacy `get_invoice_pdf` path) the output happens to be right; for client sales invoices at 0/5/12/28% it is wrong every time, including on the copy emailed to the customer and shown in the customer portal."
  }
 ],
 "wf_db2b59db-277": []
}