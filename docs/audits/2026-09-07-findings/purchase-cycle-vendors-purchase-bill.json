{
 "subsystem": "Purchase cycle \u2014 vendors, purchase bills, purchase credit/debit notes, purchase payments, TDS on purchase (resident + \u00a7195), ITC capture and reversal, vendor statements, MSME tracker, expense capture / document intelligence for bills",
 "what_exists": "Backend. `routers/purchase_bills.py` (1930 lines) is the centre: `_compute_bill_lines_and_totals` computes per-line GST in integer paise (full tax then split CGST/SGST so it equals the IGST a supply would attract), aggregates \u00a717(5) blocked credit into header `ineligible_itc_*_paise`, converts to base INR via `domain/currency/document_currency`, then resolves TDS. TDS routes by RESIDENCY first: `domain/tds/residency.py` says the 194-series charges only sums paid \"to a resident\", so a non-resident vendor goes to `domain/tds/section_195.py` (chargeability \u2192 nature \u2192 \u00a790(2) treaty \u2192 \u00a7206AA/Rule 37BC \u2192 surcharge + cess), and a resident goes to `domain/tds/tds_computer.resolve_tds` over the FY-versioned `domain/tds/section_rates.py`. Endpoints: list, create, `POST /bulk` (pre-fetched vendor/client cache, dedup, per-row errors), get, `GET /{id}/document-url` (signed URL minted on demand), delete (draft-only hard delete), PATCH (draft = full recompute; received = `_SOFT_BILL_UPDATE_FIELDS` only), `POST /{id}/receive` (journal via the one kernel, status rollback on posting failure, `_sync_tds_register`, inventory stock-in), `POST /{id}/cancel` (append-only kernel reversal + register removal + stock reversal), and `POST /from-document`.\n\n`routers/purchase_payments.py` records single-bill or unlinked vendor payments with a compare-and-swap claim on `paid_paise` against `(net_payable + credit_note_paise) \u2212 paid \u2212 debited`, plus reversal. `services/purchase_payment_service.py` adds multi-bill allocation and a realized-FX foreign path, reached today only from the bank match queue. `routers/debit_notes.py` (client \u2192 vendor, reduces payable) and `routers/purchase_credit_notes.py` (vendor undercharge, increases payable) both CAS-apply to the bill and post through the kernel with rollback. `services/vendor_statement_service.py` builds the ledger and AP ageing off the generated `outstanding_paise` column (migration 278). `services/tds_register_service.py` writes `tds_deductions` at the moment of credit and returns named `statutory_gaps`. `services/itc_reversal_service.py` + `domain/gst/itc_reversal.py` report Rule 37 180-day reversals; `services/itc_register_service.py` classifies posted journals as 4(B)(2)/4(D)(1) and refuses a figure the ledger does not support. `services/treaty_rate_service.py` reads `dtaa_treaty_rates` (migration 310). `routers/document_intelligence_v1.py` extracts an invoice (Groq for PDF text, Gemini for images), stores the original, and refuses to fabricate.\n\nFrontend. `apps/web/app/clients/[id]/purchases/page.tsx` (3143 lines) carries five tabs \u2014 Bills, Vendors, Payments, Debit Notes, Credit Notes \u2014 reading `purchase_bills`, `vendors`, `purchase_payments`, `debit_notes`, `purchase_credit_notes` directly over PostgREST and writing through the API. `components/purchases/PurchaseBillEditor.tsx` handles create/edit, RCM, multi-currency and AI extraction. Separately `app/accounting/msme-tracker/page.tsx` keeps a manual `msme_payments` table, and `app/accounting/suppliers/page.tsx` maintains a `suppliers` table that nothing in the purchase cycle reads.\n\nTables: `vendors`, `purchase_bills`, `purchase_bill_lines`, `purchase_payments`, `purchase_payment_allocations`, `debit_notes`, `debit_note_allocations`, `purchase_credit_notes`, `tds_deductions`, `itc_reversal_register`, `dtaa_treaty_rates`, plus the orphans `suppliers`, `msme_vendors`, `msme_payments`.",
 "maturity": {
  "grade": "C",
  "rationale": "The plumbing is genuinely good \u2014 one posting kernel with rollback on every path, integer paise throughout, compare-and-swap on every settlement counter, keyset pagination on every report, a duplicate-bill guard at both the API and the index, and a \u00a7195/non-resident engine that is better than anything in the Indian SME market. But three things a CA touches every single day are wrong or absent. (1) The resident TDS thresholds are modelled per-payment when the statute says per-FY-aggregate, so \u00a7194J/\u00a7194H/\u00a7194A silently withhold ZERO on real aggregates, and \u00a7194C withholds only on the crossing bill instead of the cumulative \u2014 both under-deductions that carry a 30% \u00a740(a)(ia) disallowance, and a test pins the \u00a7194C behaviour as correct. (2) \u00a717(5) blocked credit is accepted by the API and used by the GSTR-3B computer but is unreachable from any screen, and the journal debits GST Input for it anyway, so the ledger carries a permanent phantom asset. (3) The GSTR-2A/2B reconciliation \u2014 the reason most Indian CAs buy a purchase module at all \u2014 asks the CA to export the purchase register the platform already holds to CSV and re-upload it, does the matching in TypeScript, quotes the repealed 105% Rule 36(4) cushion, and writes nothing back. The grade is C rather than D because none of this is architectural: the kernel, the register and the domain modules are the right shapes, and every defect above is a bounded fix inside them.",
  "would_a_ca_buy_this_module_alone": "Not yet \u2014 the AP ledger and the foreign-vendor withholding would impress a CA, but a purchase module that under-deducts \u00a7194J TDS on aggregates, cannot flag a \u00a717(5) blocked credit, and makes them re-upload their own purchase register to match GSTR-2B is not one they can run a client on."
 },
 "strengths": [
  "The residency-first TDS routing is the best thing in this subsystem and I have seen nothing like it in Tally, Busy or Zoho Books. `domain/tds/residency.py:106-140` transcribes the charging words of each section ('to a resident') rather than inferring, and `routers/purchase_bills.py:530-556` therefore routes a non-resident vendor to \u00a7195 as a different CHARGING SECTION \u2014 no threshold, rate by nature of income, surcharge and cess \u2014 instead of just relabelling the return form. Market tools treat 26Q vs 27Q as a dropdown.",
  "`domain/tds/section_195.py:126-260` asks chargeability BEFORE rate (GE India Technology Centre v. CIT) and REFUSES rather than guessing in exactly three places \u2014 no nature recorded, business profits with no no-PE declaration, and a TRC on file with no treaty rate recorded. `services/treaty_rate_service.py:60-111` makes the DTAA position a per-(country, nature) firm fact rather than a per-vendor number. This is the correct posture and it is unusual.",
  "Every purchase-side accounting event goes through the one kernel with a compensating rollback. `routers/purchase_bills.py:1552-1578` flips the bill to received, posts, and rolls the status BACK to draft if `journal_for_purchase_bill` raises \u2014 while re-raising a genuine HTTPException so a locked-FY refusal is not collapsed into 'please try again'. `routers/debit_notes.py:500-522` and `services/purchase_payment_service.py:66-116` do the same for their paths.",
  "Lost-update prevention is real, not decorative. `routers/purchase_payments.py:124-175` retries a compare-and-swap on `paid_paise` (`.eq(\"paid_paise\", raw_paid)`) rather than read-check-write, and `routers/debit_notes.py:459-490` does the same on `debited_paise`. `_rollback_bill_claim` (line 177) decrements by its OWN amount rather than restoring a snapshot, so a concurrent payment is not clobbered.",
  "Duplicate supplier invoices are stopped twice and explained once. Migration 313 puts a partial unique index on `(client_id, vendor_id, lower(btrim(bill_no)))` excluding cancelled and soft-deleted rows, and `routers/purchase_bills.py:207-247` re-implements the same predicate purely so the CA gets a sentence naming the existing bill instead of a 23505.",
  "`domain/gst/itc_reversal.py:96-145` gets the two hardest bits of Rule 37 right \u2014 proportionate reversal per tax head with CEILING division (documented as deliberate, because under-reversal carries \u00a750 interest and over-reversal is re-availed under Rule 37(4)), and `reversal_period` returning the period IMMEDIATELY FOLLOWING expiry, which is the usual thing to get wrong.",
  "`services/itc_register_service.py:139-158` refuses to register a 4(B)(2) reversal larger than the GST Input credit actually moved on the journal behind it \u2014 so a return figure can never exceed what the ledger supports, and the books-vs-ledger comparator stays independent of the register.",
  "Every report in this subsystem obeys the pagination rule. `services/vendor_statement_service.py:37-59`, `services/itc_reversal_service.py:44-70` and `services/itc_register_service.py:50-64` all keyset-walk rather than accepting PostgREST's silent 1000-row truncation, and migration 278's generated `outstanding_paise` column moves the AP-ageing filter into SQL.",
  "Client-assignment scope is enforced on every backend purchase endpoint. `routers/purchase_bills.py:56-100` resolves the owning client from the bill id and returns the SAME 404 for 'not yours' and 'does not exist' so the status code is not an id oracle, and `_assert_batch_scope` (line 96) checks every distinct client in a bulk payload before any row is written.",
  "`services/tds_register_service.py:150-165` records a NIL \u00a7195 withholding as a real row with a `non_deduction_reason`, while correctly NOT recording a below-threshold resident bill \u2014 27Q reports non-deduction, 26Q reports deductions, and the docstring says the asymmetry is the statute's."
 ],
 "findings": [
  {
   "id": "PUR-01",
   "kind": "bug",
   "severity": "critical",
   "title": "\u00a7194J/\u00a7194H/\u00a7194A/\u00a7194D/\u00a7194G thresholds are applied per payment when the statute applies them to the FY aggregate \u2014 a professional paid \u20b930,000 four times has zero TDS deducted",
   "evidence": "domain/tds/section_rates.py:104-149 gives every section a `single_threshold_paise` and sets `aggregate_threshold_paise` only on 194C. domain/tds/tds_computer.py:305-310: `applies = taxable_paise > rule.single_threshold_paise or (rule.aggregate_threshold_paise is not None and fy_total > rule.aggregate_threshold_paise)` \u2014 for 194J the second clause is dead. I ran it: four \u20b930,000 bills under 194J withhold 0 paise against \u20b912,000 required; three \u20b915,000 bills under 194H withhold 0 against \u20b9900 required. \u00a7194J(1) proviso reads 'if such sum or, as the case may be, the aggregate of the sums credited or paid ... during the financial year does not exceed fifty thousand rupees'; \u00a7194H, \u00a7194A, \u00a7194D and \u00a7194G carry the same aggregate wording. The module docstring at section_rates.py:39-45 claims the simplifications 'over-flag rather than silently under-deduct' \u2014 for these sections the opposite is true.",
   "ca_experience": "A client pays their auditor \u20b930,000 a quarter (\u20b91,20,000 for the year). Every bill posts with tds_paise = 0, nothing reaches tds_deductions, 26Q reports nothing, and the CA discovers it when the assessing officer disallows 30% of \u20b91,20,000 under \u00a740(a)(ia) and levies \u00a7201(1A) interest at 1%/1.5% a month. Same for rent split across landlords, commission, and interest to non-banks. This is the single most common TDS section a CA firm's SME clients hit.",
   "market_standard": "TallyPrime tracks a running FY aggregate per party per nature-of-payment and deducts the moment the exemption limit is crossed; ClearTDS and Winman TDS do the same. No Indian product treats the \u00a7194J limit as per-invoice.",
   "confidence": "high",
   "suggested_fix": "Add `aggregate_threshold_paise` to 194J/194H/194A/194D/194G/193/194 (equal to the single threshold, since the statute states one annual limit), and change the ONE section that genuinely is per-payment \u2014 194I, \u20b950,000 per month or part after FA 2025 \u2014 to say so in its own comment. `_resolve_bill_resident_tds` already computes `fy_prior` correctly, so only the registry and the `applies` test move.",
   "effort": "days"
  },
  {
   "id": "PUR-02",
   "kind": "bug",
   "severity": "critical",
   "title": "When the \u00a7194C \u20b91L aggregate is crossed, TDS is deducted only on the crossing bill, not on the cumulative sums \u2014 and a test pins the wrong number as correct",
   "evidence": "domain/tds/tds_computer.py:313-317 computes `tds = taxable_paise * rate_bps // 10000` \u2014 always on THIS bill's taxable only, never on `fy_total`. I ran five \u20b925,000 bills to a company contractor: total withheld 50000 paise (\u20b9500) against \u20b92,500 required on the \u20b91,25,000 aggregate. \u00a7194C(5) proviso: 'where the aggregate of the amounts ... credited or paid ... during the financial year exceeds one lakh rupees, the person responsible for paying such sums shall be liable to deduct income-tax under this section' \u2014 the liability attaches to the aggregate of such sums, not to the marginal one. tests/test_tds_bill_engine.py:59-70 asserts `b5[\"tds_paise\"] == 500_00`, so the wrong behaviour is locked in by a passing test.",
   "ca_experience": "A transporter or job-worker billing \u20b925,000 a month: the CA sees \u20b9500 deducted on the fifth bill and nothing before it. The correct figure is \u20b92,500 on the fifth bill (catching up the earlier four). The shortfall disallows 30% of the whole \u20b91,25,000 under \u00a740(a)(ia) and attracts \u00a7201(1A) interest.",
   "market_standard": "TallyPrime's TDS deducts on the cumulative value at the moment the exemption limit is crossed \u2014 the standard 'threshold crossed, deduct on aggregate' behaviour every Indian package implements.",
   "confidence": "high",
   "suggested_fix": "When `applies` is true only because the aggregate clause fired, compute TDS on `fy_total` less TDS already deducted in the FY under that section for that payee, rather than on the current bill alone. The FY-prior figure is already fetched at routers/purchase_bills.py:317-332; it needs a companion sum of prior `tds_paise`. Rewrite tests/test_tds_bill_engine.py:59-70 as the negative control.",
   "effort": "days"
  },
  {
   "id": "PUR-03",
   "kind": "bug",
   "severity": "high",
   "title": "Surcharge and 4% cess are added on top of a DTAA treaty rate, over-deducting on every foreign payment where a treaty applies",
   "evidence": "domain/tds/section_195.py:222-256: after the \u00a790(2) branch sets `rate_bps = treaty_rate_bps` and `basis = \"treaty\"` (lines 218-220), surcharge and cess are applied unconditionally \u2014 `sur_pct = _surcharge_percent(...)`, `cess = (base_tax + surcharge) * rates.cess_percent // 100`. I ran a \u20b91 crore royalty at a 10% recorded treaty rate: a foreign company withholds 10.40%, a non-corporate payee withholds 11.44% (\u20b91,44,000 over-deducted). The module explicitly documents and defends its conservative choice for the \u00a7206AA floor at lines 244-252 but says nothing about the treaty branch, and tests/test_section_195_rates.py has no test combining a treaty rate with surcharge or cess.",
   "ca_experience": "The CA records a Singapore FTS or royalty treaty rate of 10% read off the agreement, and the bill withholds 10.4% or 11.44%. The vendor is short-paid, 27Q reports a rate that does not match the treaty article, and the vendor can only recover the excess by filing an Indian return. On a large remittance this is a conversation with the client's overseas supplier.",
   "market_standard": "The settled and mainstream Indian practice \u2014 supported by a consistent line of ITAT decisions (e.g. DIC Asia Pacific Pte Ltd v ADIT) \u2014 is that a DTAA rate is a ceiling on tax as defined in Article 2, which includes surcharge and cess, so neither is added on top. Winman TDS and ClearTDS cap at the treaty rate.",
   "confidence": "medium",
   "suggested_fix": "When `basis == \"treaty\"`, do not add surcharge or cess, and say so in the docstring beside the \u00a7206AA paragraph that already explains the opposite choice. If the owner wants to keep the conservative behaviour, make it an explicit recorded decision with a test naming it, because right now it is neither documented nor pinned.",
   "effort": "hours"
  },
  {
   "id": "PUR-04",
   "kind": "data_integrity",
   "severity": "high",
   "title": "\u00a717(5) blocked credit is still debited to GST Input in the journal, leaving a permanent phantom asset and a permanent GSTR-3B books-vs-ledger mismatch",
   "evidence": "services/phase2_journal_service.py:656-676 debits `gst_input_id` for the FULL `bill[\"cgst_paise\"]`/`sgst_paise`/`igst_paise` with no reference to `ineligible_itc_*_paise`, and the expense debits at lines 640-654 are the taxable amount only. Meanwhile domain/gst/gstr3b_computer.py:663-666 computes `book_igst = sum(p.igst_paise - p.ineligible_igst_paise ...)`, and services/gst_return_service.py:828-830 compares that against the GL `gst_input` movement. Nothing anywhere reverses the ineligible portion out of GST Input.",
   "ca_experience": "The client's trial balance carries an Input GST asset that will never be claimed and never clears \u2014 a hotel bill, a club membership, a works-contract invoice \u2014 while the expense head is understated by the same amount, so profit is overstated. The GSTR-3B books-vs-ledger reconciliation on the same screen reports an unexplained difference every month equal to the blocked GST, and the CA cannot make it agree.",
   "market_standard": "TallyPrime posts blocked credit to the expense/purchase ledger at voucher entry (its 'Ineligible for input credit' classification), so the GST Input ledger only ever holds claimable credit. Zoho Books does the same via its ITC-ineligible flag.",
   "confidence": "high",
   "suggested_fix": "In `journal_for_purchase_bill`, debit GST Input for `cgst_paise - ineligible_itc_cgst_paise` (and the same for SGST/IGST) and add the ineligible remainder to the expense debit \u2014 that is what \u00a717(5) means economically. Add a parity test asserting the GL gst_input movement equals `books_itc` for a bill with a blocked line.",
   "effort": "days"
  },
  {
   "id": "PUR-05",
   "kind": "gap",
   "severity": "high",
   "title": "\u00a717(5) blocked credit cannot be set from any screen \u2014 the API field, the DB column and the return computation all exist and are unreachable",
   "evidence": "models/invoices.py:222-246 accepts `itc_eligible` and `blocked_credit_reason` per line; migration 240 adds the columns; routers/purchase_bills.py:466-478 aggregates them; domain/gst/gstr3b_computer.py:668-671 files them in 4(B)(1). A grep for `itc_eligible` and `blocked_credit` across all of apps/web returns ZERO hits, and components/purchases/PurchaseBillEditor.tsx:475-490 builds the line payload with no such key.",
   "ca_experience": "GSTR-3B Table 4(B)(1) and 4(D)(1) are permanently nil for every client, so a client with any blocked credit over-claims ITC every month. The only workaround is to leave the GST off the bill entirely, which then misstates the payable to the vendor and breaks the 2B tie-up.",
   "market_standard": "TallyPrime asks 'Is ineligible for input credit?' with a \u00a717(5) reason at voucher entry; Zoho Books has a per-line ITC-eligibility dropdown; ClearTax's purchase register carries an ineligibility column. It is a first-class field everywhere.",
   "confidence": "high",
   "suggested_fix": "Add a per-line 'ITC eligible' toggle plus a \u00a717(5) reason picker to PurchaseBillEditor (the reason codes are listed in migration 240's column comment), and show the blocked total on the bill view drawer. Backend needs no change.",
   "effort": "days"
  },
  {
   "id": "PUR-06",
   "kind": "bug",
   "severity": "high",
   "title": "The vendor's TDS rate is collected on a form, shown in a list column, and never used by the computation",
   "evidence": "The vendor form at apps/web/app/clients/[id]/purchases/page.tsx:1827 has a 'TDS Rate %' input; line 1216 persists `tds_rate_bps`; line 1378-1379 renders it as a 'Rate' column. On the backend `_resolve_bill_resident_tds` (routers/purchase_bills.py:284-345) never reads `vendor.get(\"tds_rate_bps\")` \u2014 it calls `TDSComputer().resolve_tds`, which takes the rate from the FY registry. `grep tds_rate_bps routers/purchase_bills.py` shows only the COMPUTED rate being written back. The one place the constant is even named, `_TDS_DEFAULT_BPS` at routers/purchase_bills.py:30-37 ('Default rates when vendor master tds_rate_bps is 0'), has no readers at all.",
   "ca_experience": "A CA with a \u00a7197 lower-deduction certificate for 1% types 1 into the vendor's TDS Rate, sees '1.0%' in the vendor list, and every bill deducts 2% or 10%. They only find out when the vendor complains or the certificate limit is reconciled at year end. The UI actively confirms a setting that does nothing.",
   "market_standard": "In TallyPrime the party's TDS rate (including a lower/nil \u00a7197 rate with certificate number and validity) is what the voucher deducts at. A rate field that is displayed and ignored does not exist in any comparable product.",
   "confidence": "high",
   "suggested_fix": "Either make the vendor rate an override the engine honours (with a bound so it can never exceed the section rate silently), or remove the input and the list column and replace them with a read-only display of the section rate the registry will apply. The first is the honest fix and is the natural home for PUR-07.",
   "effort": "days"
  },
  {
   "id": "PUR-07",
   "kind": "gap",
   "severity": "high",
   "title": "No \u00a7197 lower-deduction or nil-deduction certificate anywhere \u2014 no certificate number, rate, threshold amount or validity period",
   "evidence": "The `vendors` table (migration 049, extended by 201/303/308/309/311) has no certificate column of any kind. `domain/tds/tds_computer.py:38-39` declares `is_lower_deduction` and `lower_deduction_cert` on `TDSDeducteeRecord` and nothing anywhere sets them (grep for `is_lower_deduction` returns only that declaration). `resolve_tds` (line 271) takes no certificate parameter. A grep for `197` across domain/, services/ and routers/ returns only migration/comment noise.",
   "ca_experience": "A transport contractor or an advertising agency with a \u00a7197 certificate at 0.5% cannot be modelled. The CA must either turn TDS off on the vendor (so no register row, no 26Q deductee line, no challan) or accept over-deduction at the section rate. 26Q's own deductee annexure has a lower-deduction flag and certificate-number field that can never be populated, so the FVU file is wrong for those payees.",
   "market_standard": "TallyPrime, ClearTDS, Winman TDS and Saral TDS all hold a \u00a7197 certificate per deductee: certificate number, section, rate, the amount up to which it applies, and validity dates, with the rate reverting to the section rate once the certificate limit is exhausted.",
   "confidence": "high",
   "suggested_fix": "Add `tds_certificate_no`, `tds_certificate_rate_bps`, `tds_certificate_limit_paise`, `tds_certificate_valid_from/to` to vendors; pass them into `resolve_tds` so the certificate rate applies to bills dated in the validity window up to the limit and the section rate applies beyond it; carry the flag and number onto the `tds_deductions` row for 26Q.",
   "effort": "weeks"
  },
  {
   "id": "PUR-08",
   "kind": "bug",
   "severity": "high",
   "title": "A purchase bill can be created and received into a period whose GSTR-3B has already been filed \u2014 the filed-return lock that guards sales invoices is missing on both paths",
   "evidence": "routers/sales_invoices.py:819-826 calls `period_lock_service.assert_open(...)` on invoice CREATE, right after the FY check. `_create_purchase_bill_core` (routers/purchase_bills.py:757-762) calls only `period_validation_service.validate_posting_date_cached` \u2014 no `assert_open`. `receive_purchase_bill` (routers/purchase_bills.py:1533-1536) likewise calls only `validate_posting_date`. The lock IS imported and used in `update_purchase_bill` at lines 1345-1347, so the omission is on create and receive specifically. `services/phase2_journal_service.py` has no `assert_open`/`period_lock_reason` reference, so the kernel does not catch it either. tests/test_documents_locked_by_filed_return.py covers only the purchase-bill EDIT path (line 189).",
   "ca_experience": "A late supplier invoice for March arrives in June. The CA books it dated March and receives it. The GSTR-3B for March was filed in April with its own ITC figure. The March books now claim credit the filed return never did, permanently. Nothing warns them, and the books-vs-ledger reconciliation the product prides itself on will disagree for that month forever. The equivalent sales invoice is refused with a sentence naming the filing date.",
   "market_standard": "TallyPrime blocks entry into a GST-return-locked period and prompts to move the voucher to the current period; ClearTax's books flag out-of-period ITC. A recipient who genuinely wants that credit takes it in the current 3B under \u00a716(4), which is exactly what the refusal message should say.",
   "confidence": "high",
   "suggested_fix": "Call `period_lock_service.assert_open(db, firm_id, client_id, bill_date)` in `_create_purchase_bill_core` (mirroring sales_invoices.py:824) and in `receive_purchase_bill` before the status flip. Extend tests/test_documents_locked_by_filed_return.py with a create case and a receive case.",
   "effort": "hours"
  },
  {
   "id": "PUR-09",
   "kind": "bug",
   "severity": "high",
   "title": "The Rule 37 report ignores debit notes and purchase credit notes, over-reversing ITC on any bill settled by a purchase return",
   "evidence": "services/itc_reversal_service.py:57-59 selects only `total_paise, paid_paise, tds_paise, cgst_paise, sgst_paise, igst_paise` \u2014 not `debited_paise` or `credit_note_paise`, and not the generated `outstanding_paise` column that migration 278 added precisely to net them. `domain/gst/itc_reversal.py:73-79`: `unpaid = total_paise - (paid + tds)`. Every other AP path in the codebase uses the full formula: routers/purchase_payments.py:150-156, routers/debit_notes.py:471-476 and services/vendor_statement_service.py:305-310 all compute `(net_payable + credit_note_paise) \u2212 paid \u2212 debited`.",
   "ca_experience": "A \u20b91,18,000 bill where \u20b959,000 of goods were returned on a debit note and the balance was never paid shows as \u20b91,18,000 unpaid at 180 days and reverses 100% of the \u20b918,000 ITC \u2014 but \u20b99,000 of that was already reversed by the debit note's own journal. The CA reverses \u20b99,000 too much in GSTR-3B Table 4(B)(2), understating credit for the month, and the compensating re-availment is not tracked.",
   "market_standard": "ClearTax's and Cygnet's Rule 37 / 180-day reports run off the net outstanding in the purchase ledger, which already reflects debit and credit notes.",
   "confidence": "high",
   "suggested_fix": "Add `debited_paise, credit_note_paise` to the select and pass them into `reversal_for_bill`, or read `outstanding_paise` directly. The proportion should also be taken against the CURRENT value of the supply, not the original `total_paise`. Add a case to tests/test_rule37_itc_reversal.py with a debit-noted bill as the negative control.",
   "effort": "hours"
  },
  {
   "id": "PUR-10",
   "kind": "gap",
   "severity": "high",
   "title": "No TDS is deducted or recorded on a vendor payment \u2014 advances to contractors withhold nothing, and purchase_payments has no tds_paise column at all",
   "evidence": "routers/purchase_payments.py:3 and :358 both assert 'TDS already deducted at the purchase bill stage, so payment is net'. `create_purchase_payment` (line 348) and `create_payment_core` (services/purchase_payment_service.py:120) contain no TDS logic \u2014 grep for `tds` across both files returns only those two comment lines. `purchase_payments` (migration 050:313, plus 148/159/161/215/226) has no `tds_paise` column, while the AR mirror `receipts` gained one in migration 077:27. `services/tds_register_service.py` is only ever called from `receive_purchase_bill` and `cancel_purchase_bill`. Migration 050:318 explicitly comments that `purchase_bill_id` is nullable 'to support advance payments before bill receipt'.",
   "ca_experience": "A client pays a \u20b95,00,000 mobilisation advance to a contractor in March; the bill arrives in May. \u00a7194C(3) requires deduction at credit OR payment, whichever is EARLIER \u2014 so \u20b910,000 was due to be deposited by 7 April. The product withholds nothing, creates no tds_deductions row, no challan obligation and no 26Q deductee line for Q4. Interest under \u00a7201(1A) runs from the payment date. Advances to vendors are routine in construction, manufacturing and any retainer arrangement.",
   "market_standard": "TallyPrime deducts TDS on an advance payment voucher and adjusts it against the bill when the bill is later booked; ClearTDS and Saral both model advance-payment deductions as first-class 26Q rows.",
   "confidence": "high",
   "suggested_fix": "Add `tds_paise`/`tds_section`/`tds_rate_bps` to purchase_payments; when a payment has no bill allocation (or exceeds the allocated bills), run the same `_resolve_bill_resident_tds` / \u00a7195 path over the unallocated amount, post the TDS credit in `journal_for_purchase_payment`, and call `sync_for_bill`'s equivalent for payments so the register carries the row. The adjustment against a later bill needs its own rule so the same sum is not deducted twice.",
   "effort": "weeks"
  },
  {
   "id": "PUR-11",
   "kind": "missing_feature",
   "severity": "high",
   "title": "The GSTR-2A/2B reconciliation asks the CA to export and re-upload the purchase register the platform already holds, matches in TypeScript, and writes nothing back",
   "evidence": "apps/web/app/gst/reconciliation/page.tsx:514 holds `booksRows` in React state populated only from a CSV/XLSX upload (`setBooksRows(rows)` at line 555) \u2014 the file never reads `purchase_bills`. The whole matching algorithm is `reconcile()` at lines 225-292, in the browser, keyed on `supplier_gstin|invoice_number` with a 3-paise tolerance. The result is rendered and Excel-exported (line 344) and never persisted. `gstr2a_records` (migration 028) has no `purchase_bill_id` column, so there is no place to store a per-invoice match even if one were computed. This also violates the house rule 'Zero business logic in the frontend'.",
   "ca_experience": "The one purchase-side task a CA does every single month \u2014 'which of my client's bills has the supplier not filed, and how much ITC must I hold back' \u2014 requires them to export the purchase register from this product, open Excel, download 2B from the portal, upload both back into this product, look at a screen, and then remember the answer, because nothing is written against the bill. Reopening the screen next month starts from zero. There is no supplier-wise defaulter list and no way to see, from the Purchases tab, that a specific bill is unmatched.",
   "market_standard": "This is the core of the ITC products CAs actually pay for. ClearTax GST, Cygnet, IRIS Sapphire and TaxGenie all pull 2A/2B, match invoice-by-invoice against the books with fuzzy invoice-number handling, persist a status per invoice, and produce a supplier-wise 'not filed' follow-up list. TallyPrime 5.0 downloads GSTR-2B and reconciles inside the ledger.",
   "confidence": "high",
   "suggested_fix": "Move `reconcile()` to a backend service that reads `purchase_bills` for the period, add `gstr2a_records.purchase_bill_id` plus a match status, persist the result, and surface the status as a column on the Purchases \u2192 Bills tab and as a supplier-wise defaulter list. Keep the CSV upload for the 2B side only.",
   "effort": "weeks"
  },
  {
   "id": "PUR-12",
   "kind": "bug",
   "severity": "high",
   "title": "The reconciliation screen tells the CA that ITC is restricted to 105% of GSTR-2A \u2014 a cushion repealed on 1 January 2022 \u2014 while the backend correctly uses 100%",
   "evidence": "apps/web/app/gst/reconciliation/page.tsx states '105% of eligible ITC' at lines 7, 217, 628 and 894, including in a prominent callout box ('CGST Rule 36(4): ITC is restricted to 105% of eligible credit appearing in GSTR-2A/2B'). The backend disagrees: domain/gst/gstr3b_computer.py:540-542 sets `_RULE_36_4_NUMERATOR = 100` with the comment 'as amended w.e.f. 1 Jan 2022 ... no provisional buffer'. Rule 36(4) was substituted by Notification 40/2021-CT (29-12-2021) with \u00a716(2)(aa) from 01-01-2022, removing the percentage entirely.",
   "ca_experience": "The CA reads a statutory instruction on screen and believes they may claim 5% more ITC than appears in 2B. Acting on it over-claims credit, attracting \u00a750 interest and a \u00a773/74 demand. It is worse than a missing feature: it is the product asserting repealed law with a section citation.",
   "market_standard": "Every current Indian GST product states the post-2022 position \u2014 ITC only to the extent reflected in GSTR-2B, no cushion.",
   "confidence": "high",
   "suggested_fix": "Replace all four strings with the \u00a716(2)(aa) / Rule 36(4)-as-substituted position and cite Notification 40/2021-CT. Worth grepping for other '105' / '110' / '120' Rule 36(4) survivals across apps/web at the same time.",
   "effort": "hours"
  },
  {
   "id": "PUR-13",
   "kind": "bug",
   "severity": "high",
   "title": "\u00a7194Q deducts 0.1% on the whole purchase value instead of on the value exceeding \u20b950 lakh, and has no FY aggregate at all",
   "evidence": "domain/tds/section_rates.py:141: `\"194Q\": TDSSectionRule(50_00_000_00, 10, 10)` \u2014 a single threshold with `aggregate_threshold_paise=None`. domain/tds/tds_computer.py:315 then computes `tds = taxable_paise * rate_bps // 10000` on the full amount. I ran a single \u20b960,00,000 bill: 600000 paise (\u20b96,000) withheld, against the statutory \u20b91,000 (0.1% of the \u20b910,00,000 exceeding \u20b950L). And because there is no aggregate, one hundred \u20b91,00,000 bills totalling \u20b91 crore from the same seller withhold nothing at all. \u00a7194Q(1) charges 0.1% 'of such sum exceeding fifty lakh rupees' where the value 'or aggregate of such value' in the year exceeds \u20b950 lakh.",
   "ca_experience": "Two opposite wrong numbers for the same client. A trading SME buying in many small lots crosses \u20b950 lakh with a supplier and deducts nothing all year \u2014 \u00a740(a)(ia) disallowance on the excess. A client with one large bill deducts six times what is due and the supplier is short-paid. \u00a7194Q applies to any buyer with turnover above \u20b910 crore, which is most of the SMEs a CA firm serves.",
   "market_standard": "TallyPrime tracks 194Q per party cumulatively and deducts on the incremental value above \u20b950 lakh, and also handles the \u00a7194Q vs \u00a7206C(1H) precedence (if the buyer deducts under 194Q, the seller does not collect under 206C(1H)).",
   "confidence": "high",
   "suggested_fix": "Give 194Q an excess-only computation: `tds = max(0, fy_total - 50L) * rate - tds_already_deducted_in_fy`, with the aggregate as the trigger. Note that \u00a7206C(1H) TCS is separately unimplemented \u2014 section_rates.py:143-149 says so explicitly for 206C \u2014 so the precedence rule cannot be modelled until TCS exists.",
   "effort": "days"
  },
  {
   "id": "PUR-14",
   "kind": "gap",
   "severity": "high",
   "title": "The TDS register's statutory gaps are computed on every receive and never reach a screen \u2014 the exact failure the module was written to fix",
   "evidence": "routers/purchase_bills.py:1594-1596 and 1631 return `{**updated_bill, \"tds_register\": _tds_sync}`, where `_tds_sync` carries `statutory_gaps`, `gap_details`, `vendor_name` and a `synced: False` failure signal (services/tds_register_service.py:193-233). The frontend at apps/web/app/clients/[id]/purchases/page.tsx:382-384 discards `result.data` entirely and shows 'Purchase bill received'. A grep for `statutory_gaps` across apps/web finds it only in the PAYROLL page (app/clients/[id]/payroll/page.tsx:574-591), which even has a regression test enforcing the wiring (scripts/payroll-gaps-reach-the-screen.test.ts). tds_register_service.py:44-49 and 224-227 say explicitly that a gap existing only in a log is the failure being fixed.",
   "ca_experience": "Five gap codes are computed and thrown away on every received bill: `vendor_residency_not_classified` (the deduction was filed on an assumption that the vendor is resident), `27q_identifiers_missing`, `form_15ca_not_recorded` (money left the country without Rule 37BB paperwork), `no_pe_declaration_undated` and `195_rates_unverified`. And when the register write FAILS, the CA is told 'Purchase bill received' while the deduction sits in the books with no challan and no 26Q row. All of it is invisible.",
   "market_standard": "Not a market comparison \u2014 this is the product's own established pattern (payroll) not being applied to the purchase side.",
   "confidence": "high",
   "suggested_fix": "Read `res.data.tds_register` in the receive handler and render `gap_details` the way the payroll page renders `statutory_gaps`; treat `synced: false` as a warning banner, not a success. Add the mirror of scripts/payroll-gaps-reach-the-screen.test.ts.",
   "effort": "hours"
  },
  {
   "id": "PUR-15",
   "kind": "missing_feature",
   "severity": "high",
   "title": "The MSME \u00a743B(h) tracker is a manually re-keyed side table with the whole statutory rule computed in TypeScript, disconnected from purchase bills and from vendors.msme_status",
   "evidence": "apps/web/app/accounting/msme-tracker/page.tsx:59-88 implements the entire \u00a743B(h) test in the browser \u2014 `due_days = agreement_type === \"written\" ? 45 : 15`, `addDays`, and a `status`/`disallowed_paise` decision \u2014 and reads/writes `msme_payments` directly over PostgREST (lines 156, 263). It never touches `purchase_bills`, `vendors` or `purchase_payments`. Separately, `vendors.msme_status` (migration 303:186) IS modelled, feeds `public.schedule_iii_ageing`, and is settable only from the Schedule III ageing report (app/clients/[id]/reports/ageing/page.tsx:441). There is no backend \u00a743B(h) code at all \u2014 grep for `43B` across apps/api returns only migration comments. There is also no MSMED \u00a716 interest computation anywhere. A third dead table, `msme_vendors` (migration 014:393), has zero readers.",
   "ca_experience": "Every MSME supplier invoice must be typed in a second time by hand, with the amount, date and agreement type re-entered, and it does not reconcile to the bills already in the system. The disallowance figure is computed in the browser and is not available to the income-tax computation. Vendors classified micro/small for the Schedule III note are invisible to the tracker and vice versa. No interest under MSMED \u00a716 (three times the RBI bank rate, compounded monthly) is computed, and \u00a743B(h) is stricter than the code models: MSMED \u00a715 caps the agreed period at 45 days but the agreed period may be SHORTER, so a written agreement saying 30 days is treated as 45 here and a genuine disallowance is missed.",
   "market_standard": "TallyPrime 4.1+ ships MSME tracking off the actual party ledger with an ageing report against the \u00a715 due date; Zoho Books flags MSME vendors on the bill; ClearTax's 43B(h) report is generated from the purchase register, not re-keyed.",
   "confidence": "high",
   "suggested_fix": "Delete the manual table and derive \u00a743B(h) on the backend from `purchase_bills` + `purchase_payments` for vendors whose `msme_status` is micro or small, with the agreed credit period read from `purchase_bills.credit_days` (already snapshotted) capped at 45 days, defaulting to 15 where no agreement is recorded. Report the year-of-accrual disallowance and the \u00a716 interest, and surface the vendor's MSME classification on the vendor master where the CA already is.",
   "effort": "weeks"
  },
  {
   "id": "PUR-16",
   "kind": "glitch",
   "severity": "high",
   "title": "A second, orphaned vendor master at /accounting/suppliers that no purchase path reads",
   "evidence": "apps/web/app/accounting/suppliers/page.tsx:151, 225, 227, 243 read and write the `suppliers` table (migration 030:2), which carries its own `supplier_name`, `gstin`, `pan`, `tds_section`, `tds_rate_percent`, `credit_limit_paise` and `payment_terms_days`. Grepping `suppliers` across apps/api finds it only in RLS-policy test allowlists (tests/test_direct_write_tables_are_role_guarded.py:45, tests/test_sql_role_tiers_match_python.py:60) \u2014 no router, service or domain module reads it. The purchase cycle uses `vendors` (migration 049) exclusively. The page is reachable from navigation (lib/workspace/knownRoutes.generated.ts:23).",
   "ca_experience": "A CA finds 'Suppliers' in the accounting menu, sets up their client's whole supplier master there with TDS sections and credit terms, then goes to Purchases \u2192 Bills and finds the vendor dropdown empty. The work is silently lost and there is no migration path. It also creates a second place a TDS section can be recorded and disagree with the one that matters.",
   "market_standard": "Not applicable \u2014 no product ships two disconnected supplier masters. This is a leftover from an earlier schema (migration 030 predates 049).",
   "confidence": "high",
   "suggested_fix": "Remove the route and the page, or make it a redirect to the client's Purchases \u2192 Vendors tab. If any production data exists in `suppliers`, write a one-off backfill into `vendors` first. Same treatment for the `msme_vendors` table, which has no readers at all.",
   "effort": "hours"
  },
  {
   "id": "PUR-17",
   "kind": "bug",
   "severity": "medium",
   "title": "POST /purchase-bills/from-document bypasses the whole compute engine and cannot succeed for a vendor that is not already in the master",
   "evidence": "routers/purchase_bills.py:1773-1937 builds `bill_draft` by hand: it never calls `_compute_bill_lines_and_totals`, so there is no interstate determination (`is_interstate` is left at its default), no per-line CGST/SGST/IGST split (lines are inserted with all three hard-coded to 0 at lines 1919-1921 while the header carries the extracted GST), no `total_gst_paise`, no credit terms, no currency columns, no `itc_eligible`, no TDS (line 1802: `\"tds_paise\": 0`) and no `period_validation_service` call. `vendor_id` is left None when no match is found (line 1789), but `purchase_bills.vendor_id` is `UUID NOT NULL REFERENCES vendors(id)` (migration 050:233) and no later migration relaxes it \u2014 so the insert at line 1913 raises and the CA gets a generic `document_failure_detail` message. No frontend calls this endpoint (the editor at components/purchases/PurchaseBillEditor.tsx:416 uses document-intelligence-v1 and then the ordinary POST /), so it is a second, divergent write path that is currently unreachable but live on the API.",
   "ca_experience": "If it is ever wired up: an invoice from a supplier not yet in the master \u2014 the common case for expense capture \u2014 fails with an opaque error instead of 'create the vendor first'. When it does succeed the bill's header GST does not tie to its lines, TDS is silently zero for a 194J vendor, and `total_gst_paise` reads 0 to every report that uses it. If `taxable + GST != total` (which nothing validates), `_create_journal`'s balance assertion refuses the receive.",
   "market_standard": "Zoho Books' Autoscan and Dext create the vendor inline from the extracted GSTIN/name when no match is found, rather than failing.",
   "confidence": "high",
   "suggested_fix": "Either delete the endpoint (the editor path already does the right thing) or route it through `_create_purchase_bill_core` with an explicit 422 'No vendor matches this GSTIN/name \u2014 create the vendor first' when the match fails.",
   "effort": "days"
  },
  {
   "id": "PUR-18",
   "kind": "gap",
   "severity": "medium",
   "title": "Import of goods has no path \u2014 no Bill of Entry, so the IGST paid to customs cannot be recorded and GSTR-3B 4(A)(1)/(2) are hard-coded nil",
   "evidence": "A grep for `bill of entry`, `bill_of_entry`, `BoE` and `import of goods` across apps/api finds nothing but a comment in migration 309 and a `customs` value in the exchange-rate-type enum (domain/currency/rate_types.py:12). domain/gst/gstr3b_computer.py:293-299 emits `(\"IMPG\", 0, 0, 0, 0)` and `(\"IMPS\", 0, 0, 0, 0)` unconditionally, with the docstring at line 280 conceding 'nothing upstream distinguishes an import ... from any other purchase yet'.",
   "ca_experience": "For an importing client the IGST paid on the Bill of Entry \u2014 often the largest single ITC item of the month \u2014 cannot be booked at all. Putting it on the vendor's bill overstates Trade Payables by the IGST (the supplier was never charged it); leaving it off loses the credit. GSTR-3B Table 4(A)(1) files nil against a 2B that shows the BoE, and the CA must file outside the product.",
   "market_standard": "TallyPrime has a dedicated Bill of Entry / import purchase voucher class; Zoho Books India has an 'Import' transaction type that splits the customs IGST from the supplier invoice; ClearTax reconciles BoE data from ICEGATE into 4(A)(1).",
   "confidence": "high",
   "suggested_fix": "A Bill of Entry document type carrying BoE number, port code, date and the customs IGST + cess, posting Dr GST Input / Cr Bank (or Cr Customs Duty Payable) without touching the vendor's AP, and feeding `IMPG`. Import of services is the \u00a7195/RCM combination that already half-exists and needs the `IMPS` row wired.",
   "effort": "weeks"
  },
  {
   "id": "PUR-19",
   "kind": "gap",
   "severity": "medium",
   "title": "Reverse-charge bills produce no self-invoice (\u00a731(3)(f)) and no payment voucher (\u00a731(3)(g))",
   "evidence": "`is_reverse_charge` is accepted (models/invoices.py:309), drives the payable exclusion (routers/purchase_bills.py:499-505) and the self-assessed output credit in the journal (services/phase2_journal_service.py:705-718). But a grep for `self_invoice`, `self-invoice`, `31(3)(f)` and `payment voucher` across apps/api and apps/web finds only an unrelated numbering comment in migration 159. No document is generated or numbered for an RCM inward supply.",
   "ca_experience": "Where the RCM supply comes from an unregistered supplier, \u00a731(3)(f) requires the recipient to issue a self-invoice \u2014 within 30 days of receipt since Rule 47A (01-11-2024) \u2014 and \u00a731(3)(g) requires a payment voucher. Neither exists, so the client has an RCM liability discharged in 3B with no invoice behind it. In a departmental audit the ITC on that RCM is challenged for want of the document Rule 36(1)(b) requires.",
   "market_standard": "TallyPrime auto-generates the RCM self-invoice with its own voucher series; Zoho Books India issues a 'Bill of Supply / self-invoice' for reverse-charge purchases from unregistered vendors.",
   "confidence": "high",
   "suggested_fix": "Generate a numbered self-invoice document (own series, own numbering under services/numbering.py) when a bill is received with `is_reverse_charge` and the vendor has no GSTIN, and a payment voucher when the RCM vendor is paid. A print/PDF is enough; nothing needs to be filed.",
   "effort": "weeks"
  },
  {
   "id": "PUR-20",
   "kind": "gap",
   "severity": "medium",
   "title": "GST compensation cess cannot be recorded on a purchase, so the cess ITC is lost for every client dealing in cess goods",
   "evidence": "`purchase_bills` has `ineligible_itc_cess_paise` (migration 240:41) but no `cess_paise` column at all \u2014 grep of the migrations confirms it. `purchase_bill_lines` has no cess rate or amount. services/gst_return_service.py:684 reads `int(b.get(\"cess_paise\") or 0)` on a purchase bill, which is therefore always 0, and domain/gst/gstr3b_computer.py:690 sets `result.itc_cess = book_cess` from that. `_compute_line_gst` (routers/purchase_bills.py:133-155) computes only CGST/SGST/IGST.",
   "ca_experience": "A client buying aerated drinks, tobacco, coal, or motor vehicles pays compensation cess on every inward invoice and it can be recorded nowhere. GSTR-3B Table 4(A) cess is nil, the credit is never claimed, and the trade payable to the vendor is understated by the cess, so the vendor ledger will not agree with the supplier's statement.",
   "market_standard": "Cess is a standard rate field on the purchase line in TallyPrime, Busy, Marg and Zoho Books India, and flows to 3B Table 4 cess automatically.",
   "confidence": "high",
   "suggested_fix": "Add `cess_rate_bps` and `cess_paise` to purchase_bill_lines and `cess_paise` to purchase_bills, compute it alongside the other heads in `_compute_line_gst`, include it in the payable and the journal, and expose a cess column on the bill editor. The GST computer and the ineligible-cess column are already waiting for it.",
   "effort": "days"
  },
  {
   "id": "PUR-21",
   "kind": "gap",
   "severity": "medium",
   "title": "AI-extracted invoice figures are never arithmetically validated and are never reconciled against what the CA saves",
   "evidence": "routers/document_intelligence_v1.py:204-234 (`_parse_extraction_json`) coerces the paise fields to int and returns them with no check that `taxable + cgst + sgst + igst == total`, and no check that the line items sum to the header. `_estimate_confidence` (line 268) counts populated fields only \u2014 it is field presence, not accuracy. On the frontend, components/purchases/PurchaseBillEditor.tsx:441-455 uses ONLY `ex.line_items` to build the editor lines and discards the extracted `total_paise`/`taxable_amount_paise` entirely, so nothing tells the CA whether the lines they are about to save add up to the invoice total the model read.",
   "ca_experience": "A model that misreads one digit of one line, or drops a line entirely from a multi-page invoice, produces a bill that looks plausible and is short. Nothing on the screen compares the reconstructed total against the invoice total, and 'confidence: high' is displayed for an extraction whose arithmetic does not close. If the model returns a total but no line items, the CA gets an empty bill with a filled header.",
   "market_standard": "Dext, Zoho Books Autoscan and Vyapar all show the original document side by side with the extracted fields and flag when the line total does not equal the header total before allowing a save.",
   "confidence": "high",
   "suggested_fix": "Validate the arithmetic server-side in `_parse_extraction_json` and return an explicit `arithmetic_ok` flag plus the extracted total; in the editor, show the extracted invoice total against the running line total and block the save (or warn hard) when they differ. Also show the stored `document_url` beside the form so the CA can check against the original.",
   "effort": "days"
  },
  {
   "id": "PUR-22",
   "kind": "missing_feature",
   "severity": "medium",
   "title": "One payment settling several bills is not reachable from the Purchases screen \u2014 the multi-bill allocation engine exists but only the bank match queue can use it",
   "evidence": "`services/purchase_payment_service.create_payment_core` implements full multi-bill allocation with `unallocated_paise` and per-bill CAS (lines 120-330). Its only caller is `services/bank_posting_service.py:1044`. The Payments tab posts to `POST /api/purchase-payments` (apps/web/app/clients/[id]/purchases/page.tsx:2033) and `components/purchases/PurchaseBillViewDrawer.tsx:465` does too \u2014 both hit `routers/purchase_payments.create_purchase_payment`, which takes a single optional `purchase_bill_id` (line 373). The form (page.tsx:1907-1912) has one `billId` state.",
   "ca_experience": "A client makes one NEFT of \u20b94,50,000 settling six supplier bills. The CA either records six payments with six fabricated references and six journal entries against one bank line, or records one unallocated payment which then sits as an advance and leaves all six bills showing as outstanding in the AP ageing.",
   "market_standard": "In TallyPrime a payment voucher shows the party's pending bills for Agst Ref selection; Zoho Books' vendor payment lets you tick multiple bills and shows the unapplied remainder as an advance. This is table stakes.",
   "confidence": "high",
   "suggested_fix": "Point the Payments tab's form at the multi-bill engine \u2014 list the vendor's open bills with allocation boxes, an auto-apply-oldest-first button and a visible unallocated remainder. The backend is done; this is UI plus an endpoint that reaches `create_payment_core`.",
   "effort": "days"
  },
  {
   "id": "PUR-23",
   "kind": "bug",
   "severity": "medium",
   "title": "Issuing a debit note (purchase return) does not resync the TDS register, so 26Q keeps reporting the pre-return payment amount",
   "evidence": "routers/debit_notes.py:418-560 (`issue_debit_note`) applies the note to the bill's `debited_paise`, posts the journal and updates the note \u2014 it never calls `_sync_tds_register` or `services.tds_register_service.sync_for_bill`. The same is true of `issue_purchase_credit_note` (routers/purchase_credit_notes.py:419-565), which INCREASES the payable. The register row written at receive time carries `payment_amount_paise = bill.taxable_amount_paise` (services/tds_register_service.py:200) and is never revisited.",
   "ca_experience": "A \u20b92,00,000 contractor bill has \u20b980,000 returned on a debit note. The 26Q deductee row still says \u20b92,00,000 was credited to that PAN, and the TDS shown is on the full amount. The vendor's 26AS shows income they did not receive, and the reconciliation between the TDS register and the purchase ledger will not close at year end.",
   "market_standard": "TallyPrime adjusts the TDS on a purchase return against the party's deduction so the 26Q figures track the net credit.",
   "confidence": "high",
   "suggested_fix": "Call `sync_for_bill` after a debit note or purchase credit note is issued against a bill, with the bill's post-note taxable amount. Note this also raises a genuine statutory question the owner should decide: whether the TDS itself is adjusted or only the reported gross.",
   "effort": "days"
  },
  {
   "id": "PUR-24",
   "kind": "gap",
   "severity": "medium",
   "title": "AP ageing lists only bills, so unallocated vendor advances are invisible and the ageing total does not equal the vendor ledger balance",
   "evidence": "`VendorStatementService.ap_aging` (services/vendor_statement_service.py:287-346) reads only `purchase_bills` with `outstanding_paise > 0`; `purchase_payments.unallocated_paise` (migration 226:36) is never read there, and no negative/advance row is emitted. `build_statement` (line 71) DOES include every payment, so the vendor statement's closing balance and the AP ageing total legitimately differ whenever an advance exists.",
   "ca_experience": "A \u20b93,00,000 advance to a supplier makes the vendor statement show a \u20b93,00,000 debit while the AP ageing shows the full gross bills with nothing netted. The CA cannot tie the ageing report to the Trade Payables control account, which is exactly the tie-up the Schedule III payables note needs.",
   "market_standard": "Tally's Bills Payable report shows 'On Account' amounts as a separate line; Zoho Books shows unapplied vendor advances in the payables ageing summary.",
   "confidence": "high",
   "suggested_fix": "Add an 'advances / on account' section to `ap_aging` sourced from `purchase_payments.unallocated_paise` (net of reversed payments) and report the reconciling total, so the ageing agrees with the AP control account.",
   "effort": "days"
  },
  {
   "id": "PUR-25",
   "kind": "missing_feature",
   "severity": "medium",
   "title": "No purchase orders, goods receipt notes or three-way matching",
   "evidence": "A grep for `purchase_order`, `purchase order`, `goods receipt` and `grn` across all of apps/api and apps/web returns nothing. `purchase_bills` has no PO reference column and the bill editor has no PO lookup.",
   "ca_experience": "For a trading or manufacturing client the CA cannot answer 'was this bill authorised, and did the goods actually arrive at the quantity billed'. Every bill is entered on trust. It also blocks the commitment/open-PO figure a year-end review would want.",
   "market_standard": "TallyPrime, Busy and Marg all ship Purchase Order \u2192 Receipt Note \u2192 Purchase Bill with quantity and rate matching; Zoho Books has PO \u2192 Bill conversion with a match indicator. For an SME's own staff using the platform this is a standard expectation.",
   "confidence": "high",
   "suggested_fix": "If the target user is the CA rather than the client's purchase clerk, this may be deliberately out of scope \u2014 it is worth an explicit owner decision rather than a build. If built, a lightweight PO with a bill-conversion path and a quantity-variance flag covers most of the value.",
   "effort": "months"
  },
  {
   "id": "PUR-26",
   "kind": "missing_feature",
   "severity": "medium",
   "title": "No recurring purchase bills, though recurring sales invoices are fully built",
   "evidence": "migration 107 creates `recurring_invoice_templates`, `recurring_invoice_template_lines` and `recurring_invoice_runs`; `services/recurring_invoice_service.py` and `routers/recurring_invoices.py` drive them. Grepping both files for `purchase` returns nothing, and there is no equivalent table or service for purchase bills.",
   "ca_experience": "Monthly rent (\u00a7194I), a retainer to a consultant (\u00a7194J), electricity and telecom bills, and software subscriptions all have to be keyed by hand every month for every client. These are exactly the bills where TDS applies and where a missed month causes a threshold and a challan to be missed too.",
   "market_standard": "Zoho Books has recurring bills; TallyPrime has voucher templates and repeat entry; Busy has recurring vouchers.",
   "confidence": "high",
   "suggested_fix": "Mirror the recurring-invoice tables and service for purchase bills, creating DRAFTS (never auto-receiving, per the house rule) so a human still posts them.",
   "effort": "days"
  },
  {
   "id": "PUR-27",
   "kind": "missing_feature",
   "severity": "medium",
   "title": "No expense claim or petty cash module \u2014 every small expense needs a vendor and a purchase bill",
   "evidence": "A grep for `expense claim`, `expense_claim`, `petty cash` and `reimburse` across apps/api and apps/web finds only payroll reimbursement components (models/payroll.py:501) and one comment in components/banking/EntryDetailModal.tsx:514 describing the bank-line route for 'an expense with no bill on file'. `_create_purchase_bill_core` requires `vendor_id` (routers/purchase_bills.py:614) and `purchase_bills.vendor_id` is NOT NULL.",
   "ca_experience": "Conveyance, staff welfare, small cash purchases and employee reimbursements all have to be entered either as bills against a dummy 'Cash Purchases' vendor, or through a bank/cash entry with no document attached. There is no way to capture a stack of receipts, no per-employee claim, and no approval trail on them.",
   "market_standard": "Zoho Books has Expenses with receipt attachment, mileage and employee reimbursement; Zoho Expense and Keka do full claim workflows; Tally handles it as payment vouchers but with a proper cash-book. The banking module here does cover the bank-line case, so this is a partial gap rather than a total one.",
   "confidence": "high",
   "suggested_fix": "A lightweight expense document (payee optional, receipt attachment, expense account, GST optional) posting through the same kernel would cover it. Worth checking with the owner whether the banking module's voucher path is considered sufficient before building.",
   "effort": "weeks"
  },
  {
   "id": "PUR-28",
   "kind": "security",
   "severity": "medium",
   "title": "Client-assignment scope is enforced on every purchase API endpoint and on none of the Purchases page's direct PostgREST reads",
   "evidence": "The backend enforces it thoroughly \u2014 routers/purchase_bills.py:56-100 (`_bill_owner`/`_assert_bill_scope`), :96 (`_assert_batch_scope`), routers/purchase_payments.py:232 (`_assert_payment_scope`). The frontend reads `purchase_bills`, `vendors`, `purchase_payments`, `debit_notes` and `purchase_credit_notes` straight over PostgREST scoped only by `client_id` (apps/web/app/clients/[id]/purchases/page.tsx:335-353, 1976-1980, 2317-2331), where the only check is the firm-level RLS policy `firm_id = get_my_firm_id()` (migration 050:268). Migrations 260/261 added role-aware WRITE policies, not assignment-aware SELECT ones.",
   "ca_experience": "An Executive or Reviewer assigned to three clients can type another client's id into the URL and read that client's entire purchase ledger, vendor master with PANs, and payment history. The API would refuse the same request. For a firm with confidentiality walls between engagement teams this is a real exposure.",
   "market_standard": "Jamku and Zoho Practice scope client visibility to the assigned team. Note this is the codebase-wide second data path CLAUDE.md documents rather than something specific to this subsystem \u2014 I am recording it here because vendor PANs and supplier pricing are among the more sensitive things it exposes.",
   "confidence": "high",
   "suggested_fix": "Either add assignment-aware RLS SELECT policies for the purchase tables (mirroring the 260/261 write policies), or route these five reads through the API where `assert_client_access` already runs. The first scales better across the ~320 direct reads.",
   "effort": "weeks"
  },
  {
   "id": "PUR-29",
   "kind": "gap",
   "severity": "medium",
   "title": "Reverse charge and \u00a717(5) eligibility have no test coverage on the purchase-bill compute path at all",
   "evidence": "`grep -rn is_reverse_charge tests/*.py` matches ten GST-side test files and none of the purchase ones \u2014 not tests/test_e2e_purchase_cycle.py, tests/test_phase2_purchase_cycle.py, tests/test_purchase_bill_line_extras.py or tests/test_tds_bill_engine.py. `grep -rn itc_eligible tests/` matches only tests/test_schema_guard.py, tests/test_r239_gst_computation_gaps.py and a schema fixture \u2014 never `_compute_bill_lines_and_totals`. So the RCM branch at routers/purchase_bills.py:499-505 (which changes what the vendor is owed) and the ineligible-ITC accumulation at :466-478 are both untested where they are computed.",
   "ca_experience": "Not a live wrong number today, but the RCM branch is the one that decides whether Trade Payables includes GST the vendor never charged. A regression there overstates every RCM client's payables and their bank reconciliation, and nothing in CI would catch it.",
   "market_standard": "Not applicable \u2014 this is an internal coverage gap measured against the codebase's own standard (CLAUDE.md: 'Every financial calculation must have a corresponding unit test').",
   "confidence": "high",
   "suggested_fix": "Add tests asserting: an RCM bill's `total_paise` and `net_payable_paise` exclude GST while `cgst/sgst/igst` are still populated; the receive journal balances with the output-liability legs; and a bill with one blocked line produces the right `ineligible_itc_*_paise` header totals.",
   "effort": "hours"
  },
  {
   "id": "PUR-30",
   "kind": "fine_tune",
   "severity": "low",
   "title": "A dead _TDS_DEFAULT_BPS table at the top of purchase_bills.py carries a \u00a7194H rate that is 2.5x the live one",
   "evidence": "routers/purchase_bills.py:29-37 defines `_TDS_DEFAULT_BPS` with `\"194H\": 500` (5%) and `\"194J\": 1000`, captioned 'Default rates when vendor master tds_rate_bps is 0'. `grep -n _TDS_DEFAULT_BPS routers/*.py services/*.py` finds only the definition \u2014 it has no readers. The live registry (domain/tds/section_rates.py:129) has 194H at 200 bps (2%) after the Finance (No. 2) Act 2024 cut.",
   "ca_experience": "No wrong number reaches a CA, but the first thing a developer reads in the purchase-bill router is a rate table that is both wrong and unused, and the comment implies a vendor-rate fallback that PUR-06 shows does not exist.",
   "market_standard": "Not applicable \u2014 code hygiene.",
   "confidence": "high",
   "suggested_fix": "Delete the constant.",
   "effort": "hours"
  },
  {
   "id": "PUR-31",
   "kind": "glitch",
   "severity": "low",
   "title": "Vendor payment numbering is count+1, so a deleted or compensated payment guarantees a collision the CA has to retry by hand",
   "evidence": "`_next_payment_seq` (routers/purchase_payments.py:218-230 and the identical copy at services/purchase_payment_service.py:52-64) returns `(resp.count or 0) + 1` over rows matching `VPMT-{fy}-%`. Migration 159:143 puts `UNIQUE (firm_id, payment_no)` on the table, and `_compensate_failed_settlement` (services/purchase_payment_service.py:105) DELETEs the payment row on a settlement failure. `_is_unique_violation` then surfaces a 409 'A payment numbering collision was detected; please retry.'",
   "ca_experience": "After any failed-and-compensated payment, the next payment attempt collides and the CA sees a 409 they must click through. Two CAs recording payments at the same moment hit it too. It is recoverable but it is a self-inflicted error the CA has to understand.",
   "market_standard": "Tally and Zoho use a monotonic sequence that never reuses a number, so a voided voucher leaves a gap rather than causing a collision.",
   "confidence": "high",
   "suggested_fix": "Take the max existing suffix rather than the count, or move numbering into services/numbering.py with a DB sequence, as the sales side's document numbering already contemplates.",
   "effort": "hours"
  },
  {
   "id": "PUR-32",
   "kind": "fine_tune",
   "severity": "low",
   "title": "Duplicate-bill detection is exact-match on vendor plus bill number only, so an OCR typo or a duplicated vendor record defeats it",
   "evidence": "`_duplicate_bill_id` (routers/purchase_bills.py:207-247) and migration 313's index both key on `(client_id, vendor_id, lower(btrim(bill_no)))`. Nothing compares amount or date. And `create_vendor`'s duplicate guard (routers/vendors.py:266-277) only matches on GSTIN or PAN, so two vendor rows for the same unregistered supplier are freely creatable \u2014 after which the same invoice can be booked twice under two vendor ids and neither guard fires.",
   "ca_experience": "The same supplier invoice entered as 'INV-001' and 'INV 001', or scanned twice with one character misread, is booked twice: the expenditure is double-counted, the ITC is claimed twice under \u00a716, and the deductee is filed twice on 26Q \u2014 precisely the harm the guard's own message at line 250 describes.",
   "market_standard": "Dext and Zoho Books flag a possible duplicate on same-vendor + same-amount + same-date regardless of the reference; Tally warns on a repeated supplier invoice number per party per year.",
   "confidence": "high",
   "suggested_fix": "Add a soft warning (not a refusal) when a live bill exists for the same vendor with the same `bill_date` and `total_paise` but a different number, and tighten the vendor duplicate guard to include a normalised-name match for vendors with no GSTIN or PAN.",
   "effort": "days"
  }
 ],
 "test_coverage_notes": "What is tested, and well: the \u00a7195 engine has 165 passing tests across tests/test_section_195_rates.py and tests/test_vendor_residency_and_27q.py covering chargeability, the treaty comparison, the two surcharge ladders, the capital-gains cap, cess on tax-plus-surcharge, and every refusal path. tests/test_no_duplicate_purchase_bill.py holds the API guard and migration 313's index together. tests/test_rule37_itc_reversal.py covers proportionate reversal, the ceiling rounding and the reversal period. tests/test_vendor_payment_concurrency.py, tests/test_purchase_payment_allocate.py and tests/test_r227_draft_bill_payment_guard.py cover the CAS and the draft-bill guard. tests/test_e2e_purchase_cycle.py walks a full lifecycle including cross-firm refusals. tests/test_itc_reversal_register.py covers the ledger-support refusal.\n\nTwo tests assert WRONG statutory behaviour, and both should be rewritten as negative controls for PUR-01 and PUR-02. tests/test_tds_bill_engine.py:59-70 (`test_h5_194c_aggregate_threshold_across_bills`) asserts `b5[\"tds_paise\"] == 500_00` \u2014 TDS on the crossing bill alone \u2014 which under \u00a7194C(5) should be \u20b92,500 on the \u20b91,25,000 aggregate. tests/test_tds_bill_engine.py:43-47 (`test_h5_below_threshold_no_tds`) is correct for one \u20b940,000 \u00a7194J bill in isolation, but it is the only \u00a7194J threshold test and it therefore pins a model that has no annual aggregate at all, which is where the statute puts the limit.\n\nWhat has no coverage where it is computed: `is_reverse_charge` never appears in any purchase-cycle test file (grep across tests/*.py matches only GST-side files), so the branch that decides whether Trade Payables includes GST the vendor never charged is untested. `itc_eligible` never appears in a test of `_compute_bill_lines_and_totals` \u2014 only in the schema guard and the GST computer's own tests. There is no test at all for `POST /purchase-bills/from-document` (grep finds it only in tests/test_r237_ar_ap_hardening.py, which is about ageing). No test asserts that the TDS register's `statutory_gaps` reach the frontend \u2014 the payroll equivalent has one (apps/web/scripts/payroll-gaps-reach-the-screen.test.ts) and could be copied. tests/test_documents_locked_by_filed_return.py covers the purchase-bill EDIT path only (line 189), not create and not receive, which is exactly where the lock is missing. \u00a7194Q has no test anywhere. And nothing pins the GL `gst_input` movement against the GSTR-3B `books_itc` for a bill with a blocked line \u2014 that parity test would have caught PUR-04 the day it was written.",
 "open_questions_for_owner": [
  "PUR-01 and PUR-02 are wrong money on a live path and everything else in this list is downstream of them. Do you want the FY-aggregate rewrite of the resident TDS thresholds treated as a hotfix ahead of the other findings, and do you want the two tests that currently pin the wrong \u00a7194C number rewritten as the negative control in the same commit?",
  "On PUR-03 (surcharge and cess added on top of a DTAA rate): the module already makes and documents a conservative over-deduction choice for the \u00a7206AA floor. Is the treaty behaviour the same deliberate choice \u2014 in which case it needs a docstring and a test naming it \u2014 or is it an oversight that should be capped at the treaty rate the way most Indian practice does?",
  "PUR-11 (2A/2B invoice-level matching against the purchase bills the product already holds) is the largest single piece of work here and, in my reading of the market, the strongest reason a CA would buy a purchase module at all. Is that the next big build, or is the GST subsystem's owner already scoping it \u2014 I stopped at the purchase-side boundary and did not audit the GST workspace.",
  "PUR-10 asks whether TDS on advance payments is in scope. It needs a `tds_paise` column on `purchase_payments`, a deduction at the payment event, and \u2014 the hard part \u2014 a rule for adjusting it against the bill when the bill arrives so the same sum is not deducted twice. Do you want the full credit-or-payment-whichever-earlier model, or is 'bills only' an accepted limitation you would rather state on screen than build?",
  "PUR-15: the MSME tracker is a manual side-table that duplicates `vendors.msme_status`, and `msme_vendors` and `suppliers` are two further dead tables with live UI or none. Do you want these consolidated onto `vendors` + `purchase_bills` in one pass, and is there production data in `suppliers` or `msme_payments` that needs a backfill first?",
  "PUR-25 (purchase orders / GRN / three-way match) and PUR-27 (expense claims / petty cash) are the two places where 'replace Tally' and 'a tool for the CA, not the client's purchase clerk' point in different directions. Which user are you building the purchase cycle for \u2014 the CA doing write-up work from a stack of invoices, or the SME's own staff entering transactions?",
  "PUR-28 is the codebase-wide direct-PostgREST scoping gap, and CLAUDE.md already records it. The purchase tables carry vendor PANs and supplier pricing, which is on the sensitive end of what it exposes. Is assignment-aware RLS on the SELECT side already on someone's roadmap, or should I have raised it as a subsystem-specific item?",
  "Several findings here (PUR-05 \u00a717(5), PUR-14 TDS gaps, PUR-22 multi-bill allocation) are backend capabilities with no screen. Is there a systematic way you want these tracked \u2014 a test that asserts every backend field a return depends on is reachable from a form \u2014 or should each be picked up individually?"
 ]
}